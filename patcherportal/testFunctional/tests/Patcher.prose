@testray.main.component.name = "Support QA"
Feature: Patcher UAT
    Testing the Patcher UAT instance.

  Teardown:
    * I go to the Patcher homepage
    * I teardown the tickets with ticketIds "${staticRequestIdList}" from zendesk.liferay.com
    * I tear down the Patcher build for the ticket "${staticTicketNumber}" on the account "${staticAccountCode}"
    * I tear down the Patcher build for the ticket "${staticTicketNumber}" on the account "${staticAccountCode}"
    * I tear down the Patcher build for the ticket "${staticRequestIdList}" on the account "${staticAccountCode}"
    * I tear down the Patcher fix containing the content "${staticFixContent1}, ${staticFixContent3}" and type "Generated"
    * I tear down the Patcher fix containing the content "${staticFixContent1}, ${staticFixContent3}" and type "Generated Private Public"
    * I tear down the Patcher fix containing the content "${staticFixContent1}, ${staticFixContent3}" and type "Patch"
    * I tear down the Patcher fix containing the content "${staticFixContent}" and type "Rebase"
    * I tear down the Patcher fix containing the content "${staticFixContent1}" and type "Rebase"
    * I tear down the Patcher fix containing the content "${staticFixContent2}" and type "Rebase"
    * I tear down the Patcher fix containing the content "${staticFixContent3}" and type "Rebase"
    * I tear down the Patcher fix containing the content "${staticFixContent4}" and type "Rebase"
    * I tear down the Patcher fix containing the content "${staticFixContent5}" and type "Rebase"
    * I tear down the Patcher fix containing the content "${staticFixContent6}" and type "Rebase"
    * I tear down the Patcher fix containing the content "${staticFixContent7}" and type "Rebase"
    * I tear down the Patcher fix containing the content "${staticFixContent8}" and type "Rebase"
    * I tear down the Patcher fix containing the content "${staticFixContent9}" and type "Rebase"
    * I tear down the Patcher fix containing the content "${staticFixContent10}" and type "Rebase"
    * I tear down the Patcher fix containing the content "${staticFixContent11}" and type "Rebase"
    * I tear down the Patcher fix containing the content "${staticFixContent12}" and type "Rebase"
    * I tear down the Patcher fix containing the content "${staticFixContent}" and type "Workaround"
    * I tear down the Patcher fix containing the content "${staticFixContent1}" and type "Workaround"
    * I tear down the Patcher fix containing the content "${staticFixContent2}" and type "Workaround"
    * I tear down the Patcher fix containing the content "${staticFixContent3}" and type "Workaround"
    * I tear down the Patcher fix containing the content "${staticFixContent4}" and type "Workaround"
    * I tear down the Patcher fix containing the content "${staticFixContent5}" and type "Workaround"
    * I tear down the Patcher fix containing the content "${staticFixContent6}" and type "Workaround"
    * I tear down the Patcher fix containing the content "${staticFixContent7}" and type "Workaround"
    * I tear down the Patcher fix containing the content "${staticFixContent8}" and type "Workaround"
    * I tear down the Patcher fix containing the content "${staticFixContent9}" and type "Workaround"
    * I tear down the Patcher fix containing the content "${staticFixContent10}" and type "Workaround"
    * I tear down the Patcher fix containing the content "${staticFixContent11}" and type "Workaround"
    * I tear down the Patcher fix containing the content "${staticFixContent12}" and type "Workaround"
    * I tear down the Patcher fix containing the content "${staticFixContent}" and type "Patch"
    * I tear down the Patcher fix containing the content "${staticFixContent1}" and type "Patch"
    * I tear down the Patcher fix containing the content "${staticFixContent2}" and type "Patch"
    * I tear down the Patcher fix containing the content "${staticFixContent3}" and type "Patch"
    * I tear down the Patcher fix containing the content "${staticFixContent4}" and type "Patch"
    * I tear down the Patcher fix containing the content "${staticFixContent5}" and type "Patch"
    * I tear down the Patcher fix containing the content "${staticFixContent6}" and type "Patch"
    * I tear down the Patcher fix containing the content "${staticFixContent7}" and type "Patch"
    * I tear down the Patcher fix containing the content "${staticFixContent8}" and type "Patch"
    * I tear down the Patcher fix containing the content "${staticFixContent9}" and type "Patch"
    * I tear down the Patcher fix containing the content "${staticFixContent10}" and type "Patch"
    * I tear down the Patcher fix containing the content "${staticFixContent11}" and type "Patch"
    * I tear down the Patcher fix containing the content "${staticFixContent12}" and type "Patch"

  Scenario: Create a 72 build
    @priority = "5"
    As a TSE, I should be able to create a 7.1 build containing private child builds
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I generate random fix content for the third fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.2                                                         |
      | Project Version | fix-pack-base-7210                                              |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | LPP-34242                                                       |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.2                                                         |
      | Project Version | fix-pack-base-7210                                              |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | LPS-108761_base                                                 |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.2                                                         |
      | Project Version | fix-pack-base-7210                                              |
      | Content         | ${staticFixContent3}                                            |
      | Branch Name     | fpf1-168475534                                                  |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1} |
      | Version         | 1.0                  |
      | Product         | DXP 7.2              |
      | Project Version | fix-pack-base-7210   |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2} |
      | Version         | 1.0                  |
      | Product         | DXP 7.2              |
      | Project Version | fix-pack-base-7210   |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    And I should be able to view a Patcher fix containing the content "${staticFixContent3}" and type "Patch" with the following values
      | Content         | ${staticFixContent3} |
      | Version         | 1.0                  |
      | Product         | DXP 7.2              |
      | Project Version | fix-pack-base-7210   |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent3}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.2                    |
      | Project Version | fix-pack-base-7210         |
      | Git Hash        | bcb7100e49                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.2                    |
      | Project Version | fix-pack-base-7210         |
      | Git Hash        | d5ffd78e62                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent3}" and type "Patch" with the following values
      | Content         | ${staticFixContent3}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.2                    |
      | Project Version | fix-pack-base-7210         |
      | Git Hash        | 17d30328d3                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.2                                                        |
      | Project Version | fix-pack-base-7210                                             |
      | Tickets List    | ${staticFixContent1},${staticFixContent2},${staticFixContent3} |
      | Account Code    | STVINTRA                                                       |
      | Support Ticket  | ${staticTicketNumber}                                          |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-base-7210      |
      | Content         | 0 Fix Packs + 3 Tickets |
      | Patcher Status  | Merging                 |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Version         | 1.0                                                              |
      | Name            | ${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3} |
      | Project Version | fix-pack-base-7210                                               |
      | Patcher Status  | Merging Only                                                     |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent1}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | bcb7100e49           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent2}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | d5ffd78e62           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent3}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Name     | ${staticFixContent3} |
      | Version  | 1.0                  |
      | Git Hash | 17d30328d3           |
      | Status   | Complete             |
      | Type     | Patch                |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-base-7210             |
      | Content         | 0 Fix Packs + 3 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Version         | 1.0                                                              |
      | Name            | ${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3} |
      | Project Version | fix-pack-base-7210                                               |
      | Patcher Status  | Complete Merging Only                                            |
      | Jenkins         | fixpack-builder-agent(uat)                                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat)                                   |

  Scenario: Create a 71 build with public child build and change the status to ready for release
    @priority = "3"
    As a TSE, I should be able to create and release a 7.1 build that contains a public child build
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I generate random fix content for the third fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.1                                                         |
      | Project Version | fix-pack-dxp-7-7110                                             |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | LPS-91660-DXP-7                                                 |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.1                                                         |
      | Project Version | fix-pack-dxp-7-7110                                             |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | LPS-108761_DXP-7                                                |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.1                                                         |
      | Project Version | fix-pack-dxp-6-7110                                             |
      | Content         | ${staticFixContent3}                                            |
      | Branch Name     | LPS-113733-fix-pack-dxp-6-7110                                  |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1} |
      | Version         | 1.0                  |
      | Product         | DXP 7.1              |
      | Project Version | fix-pack-dxp-7-7110  |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2} |
      | Version         | 1.0                  |
      | Product         | DXP 7.1              |
      | Project Version | fix-pack-dxp-7-7110  |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    And I should be able to view a Patcher fix containing the content "${staticFixContent3}" and type "Patch" with the following values
      | Content         | ${staticFixContent3} |
      | Version         | 1.0                  |
      | Product         | DXP 7.1              |
      | Project Version | fix-pack-dxp-6-7110  |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent3}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.1                    |
      | Project Version | fix-pack-dxp-7-7110        |
      | Git Hash        | 1916d4cf71                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.1                    |
      | Project Version | fix-pack-dxp-7-7110        |
      | Git Hash        | d94a1b18e1                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent3}" and type "Patch" with the following values
      | Content         | ${staticFixContent3}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.1                    |
      | Project Version | fix-pack-dxp-6-7110        |
      | Git Hash        | 0d085a8a96                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I create a request as a user with the email address "no-reply@intuitive.com.broken" with the following values
      | Product           | prd_liferay_dxp_7_1                              |
      | Component         | activation_key_project_administration            |
      | System Status     | production_functioning_with_limited_capabilities |
      | Subject           | Patcher Request Subject                          |
      | Description       | Patcher Request Description                      |
      | Organization Name | Intuitive Surgical-EE-IT                         |
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.1                                                        |
      | Project Version | fix-pack-dxp-7-7110                                            |
      | Tickets List    | ${staticFixContent1},${staticFixContent2},${staticFixContent3} |
      | Account Code    | INTUITIVE                                                      |
      | Support Ticket  | ${staticRequestIdList}                                         |
    And I set the account code "INTUITIVE" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticRequestIdList}" on the account "INTUITIVE" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticRequestIdList}  |
      | Version         | 1.0                     |
      | Project Version | fix-pack-dxp-7-7110     |
      | Content         | 0 Fix Packs + 3 Tickets |
      | Patcher Status  | Rebasing                |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}" for the ticket "${staticRequestIdList}" on the account "INTUITIVE" with the following values
      | Version         | 1.0                                                              |
      | Name            | ${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3} |
      | Project Version | fix-pack-dxp-7-7110                                              |
      | Patcher Status  | Rebasing Merging Only                                            |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent1}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}" for the ticket "${staticRequestIdList}" on the account "INTUITIVE" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | 1916d4cf71           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent2}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}" for the ticket "${staticRequestIdList}" on the account "INTUITIVE" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | d94a1b18e1           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent3}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}" for the ticket "${staticRequestIdList}" on the account "INTUITIVE" with the following values
      | Name    | ${staticFixContent3} |
      | Version | 1.0                  |
      | Status  | Complete             |
      | Type    | Rebase               |
    When I check the Patcher build for the ticket "${staticRequestIdList}" on the account "INTUITIVE" for rebase completion
    And I check the Patcher build for the ticket "${staticRequestIdList}" on the account "INTUITIVE" for merge completion
    And I check the Patcher build for the ticket "${staticRequestIdList}" on the account "INTUITIVE" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticRequestIdList}" on the account "INTUITIVE" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticRequestIdList}         |
      | Version         | 1.0                            |
      | Project Version | fix-pack-dxp-7-7110            |
      | Content         | 0 Fix Packs + 3 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}" for the ticket "${staticRequestIdList}" on the account "INTUITIVE" with the following values
      | Version         | 1.0                                                              |
      | Name            | ${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3} |
      | Project Version | fix-pack-dxp-7-7110                                              |
      | Patcher Status  | Complete Merging Only                                            |
      | Jenkins         | fixpack-builder-hotfix-7x(uat)                                   |
      | Jenkins         | fixpack-builder-agent(uat)                                       |
    When I change a Patcher build status to ready for release for the ticket "${staticRequestIdList}" on the account "INTUITIVE"
    Then the ticket with the ticketId "${staticRequestIdList}" should not have the comment containing the text "liferay-hotfix-"
    And the ticket with the ticketId "${staticRequestIdList}" should not have the comment containing the text "-7110.zip"
    And I should be able to view a Patcher build for the ticket "${staticRequestIdList}" on the account "INTUITIVE" with the following values
      | Type            | Official                       |
      | Support Ticket  |${staticRequestIdList}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-dxp-7-7110            |
      | Content         | 0 Fix Packs + 3 Tickets        |
      | Patcher Status  | Ready for Release              |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Create a public MP build
    @priority = "4"
    As a TSE, I should be able to create a public MP build
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I generate random fix content for the third fix
    And I generate random fix content for the fourth fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0 Liferay Knowledge Base                                  |
      | Project Version | marketplace-knowledge-base-3.0.2-7010                           |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | LPS-87103                                                       |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0 Liferay Knowledge Base                                  |
      | Project Version | marketplace-knowledge-base-3.0.2-7010                           |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | LPP-32185-hotfix                                                |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
      | Workaround      | true                                                            |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0 Liferay Knowledge Base                                  |
      | Project Version | marketplace-knowledge-base-3.0.2-7010                           |
      | Content         | ${staticFixContent3}                                            |
      | Branch Name     | LPS-91303-marketplace-knowledge-base-3.0.2-7010                 |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0 Liferay Knowledge Base                                  |
      | Project Version | marketplace-knowledge-base-3.0.2-7010                           |
      | Content         | ${staticFixContent4}                                            |
      | Branch Name     | LPS-84130                                                       |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Workaround" for add completion
    And I check the Patcher fix containing the content "${staticFixContent3}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent4}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1}                  |
      | Version         | 1.0                                   |
      | Product         | DXP 7.0 Liferay Knowledge Base        |
      | Project Version | marketplace-knowledge-base-3.0.2-7010 |
      | Git Hash        | 2a6c623d8f                            |
      | Patcher Status  | Complete                              |
      | Type            | Patch                                 |
      | Jenkins         | fixpack-builder-agent(uat)            |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Workaround" with the following values
      | Content         | ${staticFixContent2}                  |
      | Version         | 1.0                                   |
      | Product         | DXP 7.0 Liferay Knowledge Base        |
      | Project Version | marketplace-knowledge-base-3.0.2-7010 |
      | Git Hash        | 6e6cc55708                            |
      | Patcher Status  | Complete                              |
      | Type            | Workaround                            |
      | Jenkins         | fixpack-builder-agent(uat)            |
    And I should be able to view a Patcher fix containing the content "${staticFixContent3}" and type "Patch" with the following values
      | Content         | ${staticFixContent3}                  |
      | Version         | 1.0                                   |
      | Product         | DXP 7.0 Liferay Knowledge Base        |
      | Project Version | marketplace-knowledge-base-3.0.2-7010 |
      | Git Hash        | 2a607f48e1                            |
      | Patcher Status  | Complete                              |
      | Type            | Patch                                 |
      | Jenkins         | fixpack-builder-agent(uat)            |
    And I should be able to view a Patcher fix containing the content "${staticFixContent4}" and type "Patch" with the following values
      | Content         | ${staticFixContent4}                  |
      | Version         | 1.0                                   |
      | Product         | DXP 7.0 Liferay Knowledge Base        |
      | Project Version | marketplace-knowledge-base-3.0.2-7010 |
      | Git Hash        | b68b00864c                            |
      | Patcher Status  | Complete                              |
      | Type            | Patch                                 |
      | Jenkins         | fixpack-builder-agent(uat)            |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.0 Liferay Knowledge Base                                                      |
      | Project Version | marketplace-knowledge-base-3.0.2-7010                                               |
      | Tickets List    | ${staticFixContent1},${staticFixContent2},${staticFixContent3},${staticFixContent4} |
      | Account Code    | INDIANIOCLCM                                                                        |
      | Support Ticket  | ${staticTicketNumber}                                                               |
    And I set the account code "INDIANIOCLCM" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INDIANIOCLCM" with the following values
      | Type            | Official                              |
      | Support Ticket  | ${staticTicketNumber}                 |
      | Version         | 1.0                                   |
      | Project Version | marketplace-knowledge-base-3.0.2-7010 |
      | Content         | 0 Fix Packs + 4 Tickets               |
      | Patcher Status  | Merging                               |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}, ${staticFixContent4}" for the ticket "${staticTicketNumber}" on the account "INDIANIOCLCM" with the following values
      | Version         | 1.0                                                                                    |
      | Name            | ${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}, ${staticFixContent4} |
      | Project Version | marketplace-knowledge-base-3.0.2-7010                                                  |
      | Patcher Status  | Merging Only                                                                           |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent1}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}, ${staticFixContent4}" for the ticket "${staticTicketNumber}" on the account "INDIANIOCLCM" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | 2a6c623d8f           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent2}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}, ${staticFixContent4}" for the ticket "${staticTicketNumber}" on the account "INDIANIOCLCM" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | 6e6cc55708           |
      | Status   | Complete             |
      | Type     | Workaround           |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent3}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}, ${staticFixContent4}" for the ticket "${staticTicketNumber}" on the account "INDIANIOCLCM" with the following values
      | Name     | ${staticFixContent3} |
      | Version  | 1.0                  |
      | Git Hash | 2a607f48e1           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent4}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}, ${staticFixContent4}" for the ticket "${staticTicketNumber}" on the account "INDIANIOCLCM" with the following values
      | Name     | ${staticFixContent4} |
      | Version  | 1.0                  |
      | Git Hash | b68b00864c           |
      | Status   | Complete             |
      | Type     | Patch                |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "INDIANIOCLCM" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "INDIANIOCLCM" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INDIANIOCLCM" with the following values
      | Type            | Official                              |
      | Support Ticket  | ${staticTicketNumber}                 |
      | Version         | 1.0                                   |
      | Project Version | marketplace-knowledge-base-3.0.2-7010 |
      | Content         | 0 Fix Packs + 4 Tickets               |
      | Patcher Status  | Complete                              |
      | Jenkins         | fixpack-builder-hotfix-7x(uat)        |
      | Jenkins         | fixpack-builder-agent(uat)            |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}, ${staticFixContent4}" for the ticket "${staticTicketNumber}" on the account "INDIANIOCLCM" with the following values
      | Version         | 1.0                                                                                    |
      | Name            | ${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}, ${staticFixContent4} |
      | Project Version | marketplace-knowledge-base-3.0.2-7010                                                  |
      | Patcher Status  | Complete Merging Only                                                                  |
      | Jenkins         | fixpack-builder-hotfix-7x(uat)                                                         |
      | Jenkins         | fixpack-builder-agent(uat)                                                             |

  Scenario: Create a public MP build with public and private child builds
    @priority = "4"
    As a TSE, I should be able to create a public MP build with public and private child builds
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0 Liferay SAML 2.0 Provider                               |
      | Project Version | marketplace-saml-3.1.1-7010-private                             |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | LPS-88302-snapshot3                                             |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
      | Workaround      | true                                                            |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0 Liferay SAML 2.0 Provider                               |
      | Project Version | marketplace-saml-3.1.1-7010                                     |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | LPS-107323-de86                                                 |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Workaround" with the following values
      | Content         | ${staticFixContent1}                |
      | Version         | 1.0                                 |
      | Product         | DXP 7.0 Liferay SAML 2.0 Provider   |
      | Project Version | marketplace-saml-3.1.1-7010-private |
      | Patcher Status  | Adding                              |
      | Type            | Workaround                          |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2}              |
      | Version         | 1.0                               |
      | Product         | DXP 7.0 Liferay SAML 2.0 Provider |
      | Project Version | marketplace-saml-3.1.1-7010       |
      | Patcher Status  | Adding                            |
      | Type            | Patch                             |
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Workaround" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Workaround" with the following values
      | Content         | ${staticFixContent1}                |
      | Version         | 1.0                                 |
      | Product         | DXP 7.0 Liferay SAML 2.0 Provider   |
      | Project Version | marketplace-saml-3.1.1-7010-private |
      | Git Hash        | 8f13e49f9f                          |
      | Patcher Status  | Complete                            |
      | Type            | Workaround                          |
      | Jenkins         | fixpack-builder-agent(uat)          |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2}              |
      | Version         | 1.0                               |
      | Product         | DXP 7.0 Liferay SAML 2.0 Provider |
      | Project Version | marketplace-saml-3.1.1-7010       |
      | Git Hash        | 240c93340f                        |
      | Patcher Status  | Complete                          |
      | Type            | Patch                             |
      | Jenkins         | fixpack-builder-agent(uat)        |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.0 Liferay SAML 2.0 Provider         |
      | Project Version | marketplace-saml-3.1.1-7010               |
      | Tickets List    | ${staticFixContent1},${staticFixContent2} |
      | Account Code    | HITACHIGP                                 |
      | Support Ticket  | ${staticTicketNumber}                     |
    And I set the account code "HITACHIGP" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                    |
      | Support Ticket  | ${staticTicketNumber}       |
      | Version         | 1.0                         |
      | Project Version | marketplace-saml-3.1.1-7010 |
      | Content         | 0 Fix Packs + 2 Tickets     |
      | Patcher Status  | Merging                     |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}" for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Version         | 1.0                                 |
      | Name            | ${staticFixContent1}                |
      | Project Version | marketplace-saml-3.1.1-7010-private |
      | Patcher Status  | Merging Only                        |
    And I should be able to view a Patcher child build with the name "${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Version         | 1.0                         |
      | Name            | ${staticFixContent2}        |
      | Project Version | marketplace-saml-3.1.1-7010 |
      | Patcher Status  | Merging Only                |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent1}" for the child build with the name "${staticFixContent1}" for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | 8f13e49f9f           |
      | Status   | Complete             |
      | Type     | Workaround           |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent2}" for the child build with the name "${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | 240c93340f           |
      | Status   | Complete             |
      | Type     | Patch                |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | marketplace-saml-3.1.1-7010    |
      | Content         | 0 Fix Packs + 2 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}" for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Version         | 1.0                                 |
      | Name            | ${staticFixContent1}                |
      | Project Version | marketplace-saml-3.1.1-7010-private |
      | Patcher Status  | Complete Merging Only               |
      | Jenkins         | fixpack-builder-hotfix-7x(uat)      |
      | Jenkins         | fixpack-builder-agent(uat)          |
    And I should be able to view a Patcher child build with the name "${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Version         | 1.0                         |
      | Name            | ${staticFixContent2}        |
      | Project Version | marketplace-saml-3.1.1-7010 |
      | Patcher Status  | Complete Merging Only       |
      | Jenkins         | fixpack-builder-agent(uat)  |

  Scenario: Create a 71 build with public and private child builds
    @priority = "3"
    As a TSE, I should be able to create a 7.1 build containing both public and private child builds
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I generate random fix content for the third fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.1                                                         |
      | Project Version | fix-pack-dxp-8-7110-private                                     |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | fix-pack-LPP-33279-dxp-8-private                                |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.1                                                         |
      | Project Version | fix-pack-dxp-4-7110                                             |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | LPS-89633-dxp-4                                                 |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
      | Workaround      | true                                                            |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.1                                                         |
      | Project Version | fix-pack-dxp-8-7110                                             |
      | Content         | ${staticFixContent3}                                            |
      | Branch Name     | fpf2-168556409                                                  |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1}        |
      | Version         | 1.0                         |
      | Product         | DXP 7.1                     |
      | Project Version | fix-pack-dxp-8-7110-private |
      | Patcher Status  | Adding                      |
      | Type            | Patch                       |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Workaround" with the following values
      | Content         | ${staticFixContent2} |
      | Version         | 1.0                  |
      | Product         | DXP 7.1              |
      | Project Version | fix-pack-dxp-4-7110  |
      | Patcher Status  | Adding               |
      | Type            | Workaround           |
    And I should be able to view a Patcher fix containing the content "${staticFixContent3}" and type "Patch" with the following values
      | Content         | ${staticFixContent3} |
      | Version         | 1.0                  |
      | Product         | DXP 7.1              |
      | Project Version | fix-pack-dxp-8-7110  |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Workaround" for add completion
    When I check the Patcher fix containing the content "${staticFixContent3}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1}        |
      | Version         | 1.0                         |
      | Product         | DXP 7.1                     |
      | Project Version | fix-pack-dxp-8-7110-private |
      | Git Hash        | 0e7e9a2cd4                  |
      | Patcher Status  | Complete                    |
      | Type            | Patch                       |
      | Jenkins         | fixpack-builder-agent(uat)  |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Workaround" with the following values
      | Content         | ${staticFixContent2}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.1                    |
      | Project Version | fix-pack-dxp-4-7110        |
      | Git Hash        | ba56d593d1                 |
      | Patcher Status  | Complete                   |
      | Type            | Workaround                 |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent3}" and type "Patch" with the following values
      | Content         | ${staticFixContent3}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.1                    |
      | Project Version | fix-pack-dxp-8-7110        |
      | Git Hash        | 36bf6aaf63                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.1                                                        |
      | Project Version | fix-pack-dxp-8-7110                                            |
      | Tickets List    | ${staticFixContent3},${staticFixContent2},${staticFixContent1} |
      | Account Code    | DOEWAIKON                                                      |
      | Support Ticket  | ${staticTicketNumber}                                          |
    And I set the account code "DOEWAIKON" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "DOEWAIKON" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-dxp-8-7110     |
      | Content         | 0 Fix Packs + 3 Tickets |
      | Patcher Status  | Rebasing                |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}" for the ticket "${staticTicketNumber}" on the account "DOEWAIKON" with the following values
      | Version         | 1.0                         |
      | Name            | ${staticFixContent1}        |
      | Project Version | fix-pack-dxp-8-7110-private |
      | Patcher Status  | Merging Only                |
    And I should be able to view a Patcher child build with the name "${staticFixContent2}, ${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "DOEWAIKON" with the following values
      | Version         | 1.0                                        |
      | Name            | ${staticFixContent2}, ${staticFixContent3} |
      | Project Version | fix-pack-dxp-8-7110                        |
      | Patcher Status  | Rebasing Merging Only                      |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent1}" for the child build with the name "${staticFixContent1}" for the ticket "${staticTicketNumber}" on the account "DOEWAIKON" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | 0e7e9a2cd4           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent2}" for the child build with the name "${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "DOEWAIKON" with the following values
      | Name    | ${staticFixContent2} |
      | Version | 1.0                  |
      | Status  | Complete             |
      | Type    | Rebase               |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent3}" for the child build with the name "${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "DOEWAIKON" with the following values
      | Name     | ${staticFixContent3} |
      | Version  | 1.0                  |
      | Git Hash | 36bf6aaf63           |
      | Status   | Complete             |
      | Type     | Patch                |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "DOEWAIKON" for rebase completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "DOEWAIKON" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "DOEWAIKON" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "DOEWAIKON" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-dxp-8-7110            |
      | Content         | 0 Fix Packs + 3 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}" for the ticket "${staticTicketNumber}" on the account "DOEWAIKON" with the following values
      | Version         | 1.0                            |
      | Name            | ${staticFixContent1}           |
      | Project Version | fix-pack-dxp-8-7110-private    |
      | Patcher Status  | Complete Merging Only          |
      | Jenkins         | fixpack-builder-agent(uat)     |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
    And I should be able to view a Patcher child build with the name "${staticFixContent2}, ${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "DOEWAIKON" with the following values
      | Version         | 1.0                                        |
      | Name            | ${staticFixContent2}, ${staticFixContent3} |
      | Project Version | fix-pack-dxp-8-7110                        |
      | Patcher Status  | Complete Merging Only                      |
      | Jenkins         | fixpack-builder-agent(uat)                 |

  Scenario: Create a 71 build with a private child build
    @priority = "3"
    As a TSE, I should be able to create a 7.1 build containing private child builds
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.1                                                         |
      | Project Version | fix-pack-dxp-8-7110-private                                     |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | fix-pack-LPP-33279-dxp-8-private                                |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.1                                                         |
      | Project Version | fix-pack-dxp-8-7110                                             |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | fpf2-168556409                                                  |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1}        |
      | Version         | 1.0                         |
      | Product         | DXP 7.1                     |
      | Project Version | fix-pack-dxp-8-7110-private |
      | Patcher Status  | Adding                      |
      | Type            | Patch                       |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.1                    |
      | Project Version | fix-pack-dxp-8-7110        |
      | Patcher Status  | Adding                     |
      | Type            | Patch                      |
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1}        |
      | Version         | 1.0                         |
      | Product         | DXP 7.1                     |
      | Project Version | fix-pack-dxp-8-7110-private |
      | Git Hash        | 0e7e9a2cd4                  |
      | Patcher Status  | Complete                    |
      | Type            | Patch                       |
      | Jenkins         | fixpack-builder-agent(uat)  |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.1                    |
      | Project Version | fix-pack-dxp-8-7110        |
      | Git Hash        | 36bf6aaf63                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.1                                   |
      | Project Version | fix-pack-dxp-8-7110-private               |
      | Tickets List    | ${staticFixContent2},${staticFixContent1} |
      | Account Code    | DOEWAIKON                                 |
      | Support Ticket  | ${staticTicketNumber}                     |
    And I set the account code "DOEWAIKON" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "DOEWAIKON" with the following values
      | Type            | Official                    |
      | Support Ticket  | ${staticTicketNumber}       |
      | Version         | 1.0                         |
      | Project Version | fix-pack-dxp-8-7110-private |
      | Content         | 0 Fix Packs + 2 Tickets     |
      | Patcher Status  | Merging                     |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}" for the ticket "${staticTicketNumber}" on the account "DOEWAIKON" with the following values
      | Version         | 1.0                         |
      | Name            | ${staticFixContent1}        |
      | Project Version | fix-pack-dxp-8-7110-private |
      | Patcher Status  | Merging Only                |
    And I should be able to view a Patcher child build with the name "${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "DOEWAIKON" with the following values
      | Version         | 1.0                  |
      | Name            | ${staticFixContent2} |
      | Project Version | fix-pack-dxp-8-7110  |
      | Patcher Status  | Merging Only         |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent1}" for the child build with the name "${staticFixContent1}" for the ticket "${staticTicketNumber}" on the account "DOEWAIKON" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | 0e7e9a2cd4           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent2}" for the child build with the name "${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "DOEWAIKON" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | 36bf6aaf63           |
      | Status   | Complete             |
      | Type     | Patch                |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "DOEWAIKON" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "DOEWAIKON" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "DOEWAIKON" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-dxp-8-7110-private    |
      | Content         | 0 Fix Packs + 2 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}" for the ticket "${staticTicketNumber}" on the account "DOEWAIKON" with the following values
      | Version         | 1.0                            |
      | Name            | ${staticFixContent1}           |
      | Project Version | fix-pack-dxp-8-7110-private    |
      | Patcher Status  | Complete Merging Only          |
      | Jenkins         | fixpack-builder-agent(uat)     |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
    And I should be able to view a Patcher child build with the name "${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "DOEWAIKON" with the following values
      | Version         | 1.0                            |
      | Name            | ${staticFixContent2}           |
      | Project Version | fix-pack-dxp-8-7110            |
      | Patcher Status  | Complete Merging Only          |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Create a 71 build with a rebased child build
    @priority = "3"
    As a TSE, I should be able to create a 7.1 build containing a rebased child build
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I generate random fix content for the third fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.1                                                         |
      | Project Version | fix-pack-dxp-4-7110                                             |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | LPS-89633-dxp-4                                                 |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
      | Workaround      | true                                                            |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.1                                                         |
      | Project Version | fix-pack-dxp-8-7110                                             |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | LPP-36528                                                       |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.1                                                         |
      | Project Version | fix-pack-dxp-8-7110                                             |
      | Content         | ${staticFixContent3}                                            |
      | Branch Name     | fpf2-168556409                                                  |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Workaround" with the following values
      | Content         | ${staticFixContent1} |
      | Version         | 1.0                  |
      | Product         | DXP 7.1              |
      | Project Version | fix-pack-dxp-4-7110  |
      | Patcher Status  | Adding               |
      | Type            | Workaround           |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2} |
      | Version         | 1.0                  |
      | Product         | DXP 7.1              |
      | Project Version | fix-pack-dxp-8-7110  |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    And I should be able to view a Patcher fix containing the content "${staticFixContent3}" and type "Patch" with the following values
      | Content         | ${staticFixContent3} |
      | Version         | 1.0                  |
      | Product         | DXP 7.1              |
      | Project Version | fix-pack-dxp-8-7110  |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Workaround" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent3}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Workaround" with the following values
      | Content         | ${staticFixContent1}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.1                    |
      | Project Version | fix-pack-dxp-4-7110        |
      | Git Hash        | ba56d593d1                 |
      | Patcher Status  | Complete                   |
      | Type            | Workaround                 |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.1                    |
      | Project Version | fix-pack-dxp-8-7110        |
      | Git Hash        | 384c814b09                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent3}" and type "Patch" with the following values
      | Content         | ${staticFixContent3}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.1                    |
      | Project Version | fix-pack-dxp-8-7110        |
      | Git Hash        | 36bf6aaf63                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.1                                                        |
      | Project Version | fix-pack-dxp-8-7110                                            |
      | Tickets List    | ${staticFixContent1},${staticFixContent2},${staticFixContent3} |
      | Account Code    | DOEWAIKON                                                      |
      | Support Ticket  | ${staticTicketNumber}                                          |
    And I set the account code "DOEWAIKON" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "DOEWAIKON" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-dxp-8-7110     |
      | Content         | 0 Fix Packs + 3 Tickets |
      | Patcher Status  | Rebasing                |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "DOEWAIKON" with the following values
      | Version         | 1.0                                                              |
      | Name            | ${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3} |
      | Project Version | fix-pack-dxp-8-7110                                              |
      | Patcher Status  | Rebasing Merging Only                                            |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent1}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "DOEWAIKON" with the following values
      | Name    | ${staticFixContent1} |
      | Version | 1.0                  |
      | Type    | Rebase               |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent2}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "DOEWAIKON" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | 384c814b09           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent3}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "DOEWAIKON" with the following values
      | Name     | ${staticFixContent3} |
      | Version  | 1.0                  |
      | Git Hash | 36bf6aaf63           |
      | Status   | Complete             |
      | Type     | Patch                |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "DOEWAIKON" for rebase completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "DOEWAIKON" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "DOEWAIKON" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "DOEWAIKON" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-dxp-8-7110            |
      | Content         | 0 Fix Packs + 3 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "DOEWAIKON" with the following values
      | Version         | 1.0                                                              |
      | Name            | ${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3} |
      | Project Version | fix-pack-dxp-8-7110                                              |
      | Patcher Status  | Complete Merging Only                                            |
      | Jenkins         | fixpack-builder-agent(uat)                                       |

  Scenario: Create a 70 build with a de 81 child build
    @priority = "3"
    As a TSE, I should be able to create a 7.0 build containing a de-81 child build
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0                                                         |
      | Project Version | fix-pack-de-81-7010                                             |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | LPS-107154-LPS-107323-de-81-7010                                |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0                                                         |
      | Project Version | fix-pack-de-81-7010                                             |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | LRCI-941-de-81                                                  |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1} |
      | Version         | 1.0                  |
      | Product         | DXP 7.0              |
      | Project Version | fix-pack-de-81-7010  |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2} |
      | Version         | 1.0                  |
      | Product         | DXP 7.0              |
      | Project Version | fix-pack-de-81-7010  |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.0                    |
      | Project Version | fix-pack-de-81-7010        |
      | Git Hash        | c1fb2ee79c                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.0                    |
      | Project Version | fix-pack-de-81-7010        |
      | Git Hash        | 773ae19b83                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.0                                   |
      | Project Version | fix-pack-de-81-7010                       |
      | Tickets List    | ${staticFixContent1},${staticFixContent2} |
      | Account Code    | SOPRASGASXPO                              |
      | Support Ticket  | ${staticTicketNumber}                     |
    And I set the account code "SOPRASGASXPO" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "SOPRASGASXPO" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-de-81-7010     |
      | Content         | 0 Fix Packs + 2 Tickets |
      | Patcher Status  | Merging                 |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}, ${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "SOPRASGASXPO" with the following values
      | Version         | 1.0                                        |
      | Name            | ${staticFixContent1}, ${staticFixContent2} |
      | Project Version | fix-pack-de-81-7010                        |
      | Patcher Status  | Merging Only                               |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent1}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "SOPRASGASXPO" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | c1fb2ee79c           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent2}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "SOPRASGASXPO" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | 773ae19b83           |
      | Status   | Complete             |
      | Type     | Patch                |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "SOPRASGASXPO" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "SOPRASGASXPO" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "SOPRASGASXPO" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-de-81-7010            |
      | Content         | 0 Fix Packs + 2 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}, ${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "SOPRASGASXPO" with the following values
      | Version         | 1.0                                        |
      | Name            | ${staticFixContent1}, ${staticFixContent2} |
      | Project Version | fix-pack-de-81-7010                        |
      | Patcher Status  | Complete Merging Only                      |
      | Jenkins         | fixpack-builder-agent(uat)                 |

  Scenario: Create a 70 build with a public child build
    @priority = "3"
    As a TSE, I should be able to create and release a 7.0 build that contains a public child build
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0                                                         |
      | Project Version | fix-pack-de-76-7010                                             |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | LPP-33319                                                       |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
      | Workaround      | true                                                            |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Workaround" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.0             |
      | Project Version | fix-pack-de-76-7010 |
      | Patcher Status  | Adding              |
      | Type            | Workaround          |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Workaround" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Workaround" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.0                    |
      | Project Version | fix-pack-de-76-7010        |
      | Git Hash        | bfa79123d7                 |
      | Patcher Status  | Complete                   |
      | Type            | Workaround                 |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.0               |
      | Project Version | fix-pack-de-76-7010   |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | HITACHIGP             |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "HITACHIGP" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-de-76-7010     |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Merging                 |
    And I should be able to view a Patcher child build with the name "${staticFixContent}" for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Version         | 1.0                 |
      | Name            | ${staticFixContent} |
      | Project Version | fix-pack-de-76-7010 |
      | Patcher Status  | Merging Only        |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent}" for the child build with the name "${staticFixContent}" for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Name     | ${staticFixContent} |
      | Version  | 1.0                 |
      | Git Hash | bfa79123d7          |
      | Status   | Complete            |
      | Type     | Workaround          |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-de-76-7010            |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |
    And I should be able to view a Patcher child build with the name "${staticFixContent}" for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Version         | 1.0                            |
      | Name            | ${staticFixContent}            |
      | Project Version | fix-pack-de-76-7010            |
      | Patcher Status  | Complete Merging Only          |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Create an elasticsearch MP build with patch fix
    @priority = "4"
    As a TSE, I should be able to create an elastic search MP build with a patch fix
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0 Liferay Connector to Elasticsearch 6                    |
      | Project Version | marketplace-portal-search-elasticsearch6-1.0.1-7010             |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | LPP-39246-marketplace-portal-search-elasticsearch6-1.0.1-7010-2 |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}                                 |
      | Version         | 1.0                                                 |
      | Product         | DXP 7.0 Liferay Connector to Elasticsearch 6        |
      | Project Version | marketplace-portal-search-elasticsearch6-1.0.1-7010 |
      | Patcher Status  | Adding                                              |
      | Type            | Patch                                               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}                                 |
      | Version         | 1.0                                                 |
      | Product         | DXP 7.0 Liferay Connector to Elasticsearch 6        |
      | Project Version | marketplace-portal-search-elasticsearch6-1.0.1-7010 |
      | Git Hash        | 3bf35cfb52                                          |
      | Patcher Status  | Complete                                            |
      | Type            | Patch                                               |
      | Jenkins         | fixpack-builder-agent(uat)                          |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.0 Liferay Connector to Elasticsearch 6        |
      | Project Version | marketplace-portal-search-elasticsearch6-1.0.1-7010 |
      | Tickets List    | ${staticFixContent}                                 |
      | Account Code    | FEDBANK                                             |
      | Support Ticket  | ${staticTicketNumber}                               |
    And I set the account code "FEDBANK" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "FEDBANK" with the following values
      | Type            | Official                                            |
      | Support Ticket  | ${staticTicketNumber}                               |
      | Version         | 1.0                                                 |
      | Project Version | marketplace-portal-search-elasticsearch6-1.0.1-7010 |
      | Content         | 0 Fix Packs + 1 Tickets                             |
      | Patcher Status  | Merging                                             |
    And I should be able to view a Patcher child build with the name "${staticFixContent}" for the ticket "${staticTicketNumber}" on the account "FEDBANK" with the following values
      | Version         | 1.0                                                 |
      | Name            | ${staticFixContent}                                 |
      | Project Version | marketplace-portal-search-elasticsearch6-1.0.1-7010 |
      | Patcher Status  | Merging Only                                        |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent}" for the child build with the name "${staticFixContent}" for the ticket "${staticTicketNumber}" on the account "FEDBANK" with the following values
      | Name     | ${staticFixContent} |
      | Version  | 1.0                 |
      | Git Hash | 3bf35cfb52          |
      | Status   | Complete            |
      | Type     | Patch               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "FEDBANK" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "FEDBANK" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "FEDBANK" with the following values
      | Type            | Official                                            |
      | Support Ticket  | ${staticTicketNumber}                               |
      | Version         | 1.0                                                 |
      | Project Version | marketplace-portal-search-elasticsearch6-1.0.1-7010 |
      | Content         | 0 Fix Packs + 1 Tickets                             |
      | Patcher Status  | Complete                                            |
      | Jenkins         | fixpack-builder-hotfix-7x(uat)                      |
      | Jenkins         | fixpack-builder-agent(uat)                          |
    And I should be able to view a Patcher child build with the name "${staticFixContent}" for the ticket "${staticTicketNumber}" on the account "FEDBANK" with the following values
      | Version         | 1.0                                                 |
      | Name            | ${staticFixContent}                                 |
      | Project Version | marketplace-portal-search-elasticsearch6-1.0.1-7010 |
      | Patcher Status  | Complete Merging Only                               |
      | Jenkins         | fixpack-builder-hotfix-7x(uat)                      |
      | Jenkins         | fixpack-builder-agent(uat)                          |

  Scenario: Create an elasticsearch MP build with workaround fix
    @priority = "4"
    As a TSE, I should be able to create an elastic search MP build with a workaround fix
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0 Liferay Connector to Elasticsearch 6                    |
      | Project Version | marketplace-portal-search-elasticsearch6-1.0.1-7010             |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | elastic101Marketplace                                           |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
      | Workaround      | true                                                            |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0 Liferay Connector to Elasticsearch 6                    |
      | Project Version | marketplace-portal-search-elasticsearch6-1.0.1-7010             |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | LPP-39246-marketplace-portal-search-elasticsearch6-1.0.1-7010-2 |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Workaround" with the following values
      | Content         | ${staticFixContent1}                                |
      | Version         | 1.0                                                 |
      | Product         | DXP 7.0 Liferay Connector to Elasticsearch 6        |
      | Project Version | marketplace-portal-search-elasticsearch6-1.0.1-7010 |
      | Patcher Status  | Adding                                              |
      | Type            | Workaround                                          |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2}                                |
      | Version         | 1.0                                                 |
      | Product         | DXP 7.0 Liferay Connector to Elasticsearch 6        |
      | Project Version | marketplace-portal-search-elasticsearch6-1.0.1-7010 |
      | Patcher Status  | Adding                                              |
      | Type            | Patch                                               |
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Workaround" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Workaround" with the following values
      | Content         | ${staticFixContent1}                                |
      | Version         | 1.0                                                 |
      | Product         | DXP 7.0 Liferay Connector to Elasticsearch 6        |
      | Project Version | marketplace-portal-search-elasticsearch6-1.0.1-7010 |
      | Git Hash        | d758ca3550                                          |
      | Patcher Status  | Complete                                            |
      | Type            | Workaround                                          |
      | Jenkins         | fixpack-builder-agent(uat)                          |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2}                                |
      | Version         | 1.0                                                 |
      | Product         | DXP 7.0 Liferay Connector to Elasticsearch 6        |
      | Project Version | marketplace-portal-search-elasticsearch6-1.0.1-7010 |
      | Git Hash        | 3bf35cfb52                                          |
      | Patcher Status  | Complete                                            |
      | Type            | Patch                                               |
      | Jenkins         | fixpack-builder-agent(uat)                          |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.0 Liferay Connector to Elasticsearch 6        |
      | Project Version | marketplace-portal-search-elasticsearch6-1.0.1-7010 |
      | Tickets List    | ${staticFixContent1},${staticFixContent2}           |
      | Account Code    | OPAPSAOPAPGR                                        |
      | Support Ticket  | ${staticTicketNumber}                               |
    And I set the account code "OPAPSAOPAPGR" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "OPAPSAOPAPGR" with the following values
      | Type            | Official                                            |
      | Support Ticket  | ${staticTicketNumber}                               |
      | Version         | 1.0                                                 |
      | Project Version | marketplace-portal-search-elasticsearch6-1.0.1-7010 |
      | Content         | 0 Fix Packs + 2 Tickets                             |
      | Patcher Status  | Merging                                             |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}, ${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "OPAPSAOPAPGR" with the following values
      | Version         | 1.0                                                 |
      | Name            | ${staticFixContent1}, ${staticFixContent2}          |
      | Project Version | marketplace-portal-search-elasticsearch6-1.0.1-7010 |
      | Patcher Status  | Merging Only                                        |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent1}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "OPAPSAOPAPGR" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | d758ca3550           |
      | Status   | Complete             |
      | Type     | Workaround           |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent2}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "OPAPSAOPAPGR" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | 3bf35cfb52           |
      | Status   | Complete             |
      | Type     | Patch                |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "OPAPSAOPAPGR" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "OPAPSAOPAPGR" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "OPAPSAOPAPGR" with the following values
      | Type            | Official                                            |
      | Support Ticket  | ${staticTicketNumber}                               |
      | Version         | 1.0                                                 |
      | Project Version | marketplace-portal-search-elasticsearch6-1.0.1-7010 |
      | Content         | 0 Fix Packs + 2 Tickets                             |
      | Patcher Status  | Complete                                            |
      | Jenkins         | fixpack-builder-hotfix-7x(uat)                      |
      | Jenkins         | fixpack-builder-agent(uat)                          |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}, ${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "OPAPSAOPAPGR" with the following values
      | Version         | 1.0                                                 |
      | Name            | ${staticFixContent1}, ${staticFixContent2}          |
      | Project Version | marketplace-portal-search-elasticsearch6-1.0.1-7010 |
      | Patcher Status  | Complete Merging Only                               |
      | Jenkins         | fixpack-builder-hotfix-7x(uat)                      |
      | Jenkins         | fixpack-builder-agent(uat)                          |

  Scenario: Create a sync connector 411 MP build
    @priority = "3"
    As a TSE, I should be able to create a sync connector 4.1.1 MP build
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0 Liferay Sync Connector                                  |
      | Project Version | marketplace-sync-4.1.1-7010                                     |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | SYNC-1802-marketplace-sync-4.1.1-7010                           |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}            |
      | Version         | 1.0                            |
      | Product         | DXP 7.0 Liferay Sync Connector |
      | Project Version | marketplace-sync-4.1.1-7010    |
      | Patcher Status  | Adding                         |
      | Type            | Patch                          |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}            |
      | Version         | 1.0                            |
      | Product         | DXP 7.0 Liferay Sync Connector |
      | Project Version | marketplace-sync-4.1.1-7010    |
      | Git Hash        | 1cb1f8d8f4                     |
      | Patcher Status  | Complete                       |
      | Type            | Patch                          |
      | Jenkins         | fixpack-builder-agent(uat)     |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.0 Liferay Sync Connector |
      | Project Version | marketplace-sync-4.1.1-7010    |
      | Tickets List    | ${staticFixContent}            |
      | Account Code    | INDIAPIPPBDX                   |
      | Support Ticket  | ${staticTicketNumber}          |
    And I set the account code "INDIAPIPPBDX" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INDIAPIPPBDX" with the following values
      | Type            | Official                    |
      | Support Ticket  | ${staticTicketNumber}       |
      | Version         | 1.0                         |
      | Project Version | marketplace-sync-4.1.1-7010 |
      | Content         | 0 Fix Packs + 1 Tickets     |
      | Patcher Status  | Compiling                   |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "INDIAPIPPBDX" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INDIAPIPPBDX" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | marketplace-sync-4.1.1-7010    |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Create a sync connector 414 MP build
    @priority = "3"
    As a TSE, I should be able to create a sync connector 4.1.4 MP build
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0 Liferay Sync Connector                                  |
      | Project Version | marketplace-sync-4.1.4-7010                                     |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | SYNC-1802-marketplace-sync-4.1.4-7010                           |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0 Liferay Sync Connector                                  |
      | Project Version | marketplace-sync-4.1.4-7010                                     |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | LPS-84130-marketplace-sync                                      |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1}           |
      | Version         | 1.0                            |
      | Product         | DXP 7.0 Liferay Sync Connector |
      | Project Version | marketplace-sync-4.1.4-7010    |
      | Patcher Status  | Adding                         |
      | Type            | Patch                          |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2}           |
      | Version         | 1.0                            |
      | Product         | DXP 7.0 Liferay Sync Connector |
      | Project Version | marketplace-sync-4.1.4-7010    |
      | Patcher Status  | Adding                         |
      | Type            | Patch                          |
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1}           |
      | Version         | 1.0                            |
      | Product         | DXP 7.0 Liferay Sync Connector |
      | Project Version | marketplace-sync-4.1.4-7010    |
      | Git Hash        | a013b02289                     |
      | Patcher Status  | Complete                       |
      | Type            | Patch                          |
      | Jenkins         | fixpack-builder-agent(uat)     |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2}           |
      | Version         | 1.0                            |
      | Product         | DXP 7.0 Liferay Sync Connector |
      | Project Version | marketplace-sync-4.1.4-7010    |
      | Git Hash        | 39080e86ec                     |
      | Patcher Status  | Complete                       |
      | Type            | Patch                          |
      | Jenkins         | fixpack-builder-agent(uat)     |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.0 Liferay Sync Connector            |
      | Project Version | marketplace-sync-4.1.4-7010               |
      | Tickets List    | ${staticFixContent1},${staticFixContent2} |
      | Account Code    | INDIAPIPPBDX                              |
      | Support Ticket  | ${staticTicketNumber}                     |
    And I set the account code "INDIAPIPPBDX" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INDIAPIPPBDX" with the following values
      | Type            | Official                    |
      | Support Ticket  | ${staticTicketNumber}       |
      | Version         | 1.0                         |
      | Project Version | marketplace-sync-4.1.4-7010 |
      | Content         | 0 Fix Packs + 2 Tickets     |
      | Patcher Status  | Merging                     |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}, ${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "INDIAPIPPBDX" with the following values
      | Version         | 1.0                                        |
      | Name            | ${staticFixContent1}, ${staticFixContent2} |
      | Project Version | marketplace-sync-4.1.4-7010                |
      | Patcher Status  | Merging Only                               |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent1}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "INDIAPIPPBDX" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | a013b02289           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent2}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "INDIAPIPPBDX" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | 39080e86ec           |
      | Status   | Complete             |
      | Type     | Patch                |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "INDIAPIPPBDX" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "INDIAPIPPBDX" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INDIAPIPPBDX" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | marketplace-sync-4.1.4-7010    |
      | Content         | 0 Fix Packs + 2 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}, ${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "INDIAPIPPBDX" with the following values
      | Version         | 1.0                                        |
      | Name            | ${staticFixContent1}, ${staticFixContent2} |
      | Project Version | marketplace-sync-4.1.4-7010                |
      | Patcher Status  | Complete Merging Only                      |
      | Jenkins         | fixpack-builder-hotfix-7x(uat)             |
      | Jenkins         | fixpack-builder-agent(uat)                 |

  Scenario: Resolve a build conflict
    @priority = "5"
    As a TSE, I should be able to resolve a build conflict
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I generate random fix content for the third fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0                                                         |
      | Project Version | fix-pack-de-73-7010                                             |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | LPP-33650-0708-1                                                |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0                                                         |
      | Project Version | fix-pack-de-73-7010                                             |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | LPP-33650-0708-2                                                |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0                                                         |
      | Project Version | fix-pack-de-73-7010                                             |
      | Content         | ${staticFixContent3}                                            |
      | Branch Name     | LPP-33650-0708-3                                                |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1} |
      | Version         | 1.0                  |
      | Product         | DXP 7.0              |
      | Project Version | fix-pack-de-73-7010  |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2} |
      | Version         | 1.0                  |
      | Product         | DXP 7.0              |
      | Project Version | fix-pack-de-73-7010  |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    And I should be able to view a Patcher fix containing the content "${staticFixContent3}" and type "Patch" with the following values
      | Content         | ${staticFixContent3} |
      | Version         | 1.0                  |
      | Product         | DXP 7.0              |
      | Project Version | fix-pack-de-73-7010  |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent3}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.0                    |
      | Project Version | fix-pack-de-73-7010        |
      | Git Hash        | 6713a5f64f                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.0                    |
      | Project Version | fix-pack-de-73-7010        |
      | Git Hash        | a7fe608d38                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent3}" and type "Patch" with the following values
      | Content         | ${staticFixContent3}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.0                    |
      | Project Version | fix-pack-de-73-7010        |
      | Git Hash        | ed67e97e0a                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.0                                   |
      | Project Version | fix-pack-de-73-7010                       |
      | Tickets List    | ${staticFixContent1},${staticFixContent3} |
      | Account Code    | EXCELLUS                                  |
      | Support Ticket  | ${staticTicketNumber}                     |
    And I set the account code "EXCELLUS" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "EXCELLUS" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-de-73-7010     |
      | Content         | 0 Fix Packs + 2 Tickets |
      | Patcher Status  | Merging                 |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "EXCELLUS" for merge completion
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "EXCELLUS" with the following values
      | Type            | Official                   |
      | Support Ticket  | ${staticTicketNumber}      |
      | Version         | 1.0                        |
      | Project Version | fix-pack-de-73-7010        |
      | Content         | 0 Fix Packs + 2 Tickets    |
      | Patcher Status  | Conflict                   |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent1}, ${staticFixContent3}" for the child build with the name "${staticFixContent1}, ${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "EXCELLUS" with the following values
      | Name    | ${staticFixContent1}, ${staticFixContent3} |
      | Version | 1.0                                        |
      | Status  | Conflict                                   |
      | Type    | Generated                                  |
    When I edit a Patcher child build fix with the name "${staticFixContent1}, ${staticFixContent3}" for the child build with the name "${staticFixContent1}, ${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "EXCELLUS" to have the following values
      | Branch Name | LPP-33650-0708-2                                                |
      | Github URL  | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "EXCELLUS" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-de-73-7010     |
      | Content         | 0 Fix Packs + 2 Tickets |
      | Patcher Status  | Merging                 |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "EXCELLUS" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "EXCELLUS" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "EXCELLUS" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-de-73-7010            |
      | Content         | 0 Fix Packs + 2 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}, ${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "EXCELLUS" with the following values
      | Version         | 1.0                                        |
      | Name            | ${staticFixContent1}, ${staticFixContent3} |
      | Project Version | fix-pack-de-73-7010                        |
      | Patcher Status  | Complete Merging Only                      |
      | Jenkins         | fixpack-builder-hotfix-7x(uat)             |
      | Jenkins         | fixpack-builder-agent(uat)                 |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent1}, ${staticFixContent3}" for the child build with the name "${staticFixContent1}, ${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "EXCELLUS" with the following values
      | Name     | ${staticFixContent1}, ${staticFixContent3} |
      | Version  | 1.0                                        |
      | Git Hash | a7fe608d38                                 |
      | Status   | Complete                                   |
      | Type     | Patch                                      |

  Scenario: Resolve a rebase conflict
    @priority = "5"
    As a TSE, I should be able to resolve a rebase conflict
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.1                                                         |
      | Project Version | fix-pack-dxp-9-7110                                             |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | LPS-92793                                                       |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
      | Workaround      | true                                                            |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.1                                                         |
      | Project Version | fix-pack-dxp-11-7110                                            |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | fpf4-168556409                                                  |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Workaround" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Workaround" with the following values
      | Content         | ${staticFixContent1}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.1                    |
      | Project Version | fix-pack-dxp-9-7110        |
      | Git Hash        | d38c60582d                 |
      | Patcher Status  | Complete                   |
      | Type            | Workaround                 |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.1                    |
      | Project Version | fix-pack-dxp-11-7110       |
      | Git Hash        | 05fd6fa414                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.1                                   |
      | Project Version | fix-pack-dxp-11-7110                      |
      | Tickets List    | ${staticFixContent1},${staticFixContent2} |
      | Account Code    | FEDBANK                                   |
      | Support Ticket  | ${staticTicketNumber}                     |
    And I set the account code "FEDBANK" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "FEDBANK" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-dxp-11-7110    |
      | Content         | 0 Fix Packs + 2 Tickets |
      | Patcher Status  | Rebasing                |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "FEDBANK" for rebase completion
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "FEDBANK" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-dxp-11-7110    |
      | Content         | 0 Fix Packs + 2 Tickets |
      | Patcher Status  | Rebase Conflict         |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}, ${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "FEDBANK" with the following values
      | Version         | 1.0                                        |
      | Name            | ${staticFixContent1}, ${staticFixContent2} |
      | Project Version | fix-pack-dxp-11-7110                       |
      | Patcher Status  | Rebase Conflict Merging Only               |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent1}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "FEDBANK" with the following values
      | Name    | ${staticFixContent1} |
      | Version | 1.0                  |
      | Status  | Rebase Conflict      |
      | Type    | Rebase               |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent2}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "FEDBANK" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | 05fd6fa414           |
      | Status   | Complete             |
      | Type     | Patch                |
    When I update a Patcher fix containing the content "${staticFixContent1}" and type "Rebase" with the following values
      | Branch Name | fpf4-168556409                                                  |
      | Github URL  | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "FEDBANK" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-dxp-11-7110    |
      | Content         | 0 Fix Packs + 2 Tickets |
      | Patcher Status  | Merging                 |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "FEDBANK" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "FEDBANK" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "FEDBANK" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-dxp-11-7110           |
      | Content         | 0 Fix Packs + 2 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}, ${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "FEDBANK" with the following values
      | Version         | 1.0                                        |
      | Name            | ${staticFixContent1}, ${staticFixContent2} |
      | Project Version | fix-pack-dxp-11-7110                       |
      | Patcher Status  | Complete Merging Only                      |
      | Jenkins         | fixpack-builder-hotfix-7x(uat)             |
      | Jenkins         | fixpack-builder-agent(uat)                 |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent1}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "FEDBANK" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | 05fd6fa414           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent2}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "FEDBANK" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | 05fd6fa414           |
      | Status   | Complete             |
      | Type     | Patch                |

  Scenario: Create an OAuth MP Build
    @priority = "4"
    As a TSE, I should be able to create an OAuth MP build
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I generate random fix content for the third fix
    And I generate random fix content for the fourth fix
    And I generate random fix content for the fifth fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.1 Liferay Plugin for OAuth 2.0                            |
      | Project Version | marketplace-oauth2-provider-1.1.0-7110                          |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | OAUTH2-242-1.1.0                                                |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
      | Workaround      | true                                                            |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.1 Liferay Plugin for OAuth 2.0                            |
      | Project Version | marketplace-oauth2-provider-1.1.0-7110                          |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | LPS-108761_OAuth2-1.1.0-7110                                    |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.1 Liferay Plugin for OAuth 2.0                            |
      | Project Version | marketplace-oauth2-provider-1.1.0-7110                          |
      | Content         | ${staticFixContent3}                                            |
      | Branch Name     | fpf7-168554010-excl-lps-93651                                   |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.1 Liferay Plugin for OAuth 2.0                            |
      | Project Version | marketplace-oauth2-provider-1.1.0-7110                          |
      | Content         | ${staticFixContent4}                                            |
      | Branch Name     | LPS-84130-marketplace-oauth2-provider                           |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.1 Liferay Plugin for OAuth 2.0                            |
      | Project Version | marketplace-oauth2-provider-1.1.0-7110                          |
      | Content         | ${staticFixContent5}                                            |
      | Branch Name     | LRP-1310-LPS-88176                                              |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Workaround" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent3}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent4}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent5}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Workaround" with the following values
      | Content         | ${staticFixContent1}                   |
      | Version         | 1.0                                    |
      | Product         | DXP 7.1 Liferay Plugin for OAuth 2.0   |
      | Project Version | marketplace-oauth2-provider-1.1.0-7110 |
      | Git Hash        | 7cef2d0365                             |
      | Patcher Status  | Complete                               |
      | Type            | Workaround                             |
      | Jenkins         | fixpack-builder-agent(uat)             |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2}                   |
      | Version         | 1.0                                    |
      | Product         | DXP 7.1 Liferay Plugin for OAuth 2.0   |
      | Project Version | marketplace-oauth2-provider-1.1.0-7110 |
      | Git Hash        | 5560f1ad78                             |
      | Patcher Status  | Complete                               |
      | Type            | Patch                                  |
      | Jenkins         | fixpack-builder-agent(uat)             |
    And I should be able to view a Patcher fix containing the content "${staticFixContent3}" and type "Patch" with the following values
      | Content         | ${staticFixContent3}                   |
      | Version         | 1.0                                    |
      | Product         | DXP 7.1 Liferay Plugin for OAuth 2.0   |
      | Project Version | marketplace-oauth2-provider-1.1.0-7110 |
      | Git Hash        | 7fd4e38539                             |
      | Patcher Status  | Complete                               |
      | Type            | Patch                                  |
      | Jenkins         | fixpack-builder-agent(uat)             |
    And I should be able to view a Patcher fix containing the content "${staticFixContent4}" and type "Patch" with the following values
      | Content         | ${staticFixContent4}                   |
      | Version         | 1.0                                    |
      | Product         | DXP 7.1 Liferay Plugin for OAuth 2.0   |
      | Project Version | marketplace-oauth2-provider-1.1.0-7110 |
      | Git Hash        | b4273809c2                             |
      | Patcher Status  | Complete                               |
      | Type            | Patch                                  |
      | Jenkins         | fixpack-builder-agent(uat)             |
    And I should be able to view a Patcher fix containing the content "${staticFixContent5}" and type "Patch" with the following values
      | Content         | ${staticFixContent5}                   |
      | Version         | 1.0                                    |
      | Product         | DXP 7.1 Liferay Plugin for OAuth 2.0   |
      | Project Version | marketplace-oauth2-provider-1.1.0-7110 |
      | Git Hash        | 80ff686162                             |
      | Patcher Status  | Complete                               |
      | Type            | Patch                                  |
      | Jenkins         | fixpack-builder-agent(uat)             |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.1 Liferay Plugin for OAuth 2.0                                                                     |
      | Project Version | marketplace-oauth2-provider-1.1.0-7110                                                                   |
      | Tickets List    | ${staticFixContent1},${staticFixContent2},${staticFixContent3},${staticFixContent4},${staticFixContent5} |
      | Account Code    | LRJAPANBASICACT                                                                                          |
      | Support Ticket  | ${staticTicketNumber}                                                                                    |
    And I set the account code "LRJAPANBASICACT" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "LRJAPANBASICACT" with the following values
      | Type            | Official                               |
      | Support Ticket  | ${staticTicketNumber}                  |
      | Version         | 1.0                                    |
      | Project Version | marketplace-oauth2-provider-1.1.0-7110 |
      | Content         | 0 Fix Packs + 5 Tickets                |
      | Patcher Status  | Merging                                |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}, ${staticFixContent4}, ${staticFixContent5}" for the ticket "${staticTicketNumber}" on the account "LRJAPANBASICACT" with the following values
      | Version         | 1.0                                                                                                          |
      | Name            | ${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}, ${staticFixContent4}, ${staticFixContent5} |
      | Project Version | marketplace-oauth2-provider-1.1.0-7110                                                                       |
      | Patcher Status  | Merging Only                                                                                                 |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent1}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}, ${staticFixContent4}, ${staticFixContent5}" for the ticket "${staticTicketNumber}" on the account "LRJAPANBASICACT" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | 7cef2d0365           |
      | Status   | Complete             |
      | Type     | Workaround           |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent2}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}, ${staticFixContent4}, ${staticFixContent5}" for the ticket "${staticTicketNumber}" on the account "LRJAPANBASICACT" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | 5560f1ad78           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent3}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}, ${staticFixContent4}, ${staticFixContent5}" for the ticket "${staticTicketNumber}" on the account "LRJAPANBASICACT" with the following values
      | Name     | ${staticFixContent3} |
      | Version  | 1.0                  |
      | Git Hash | 7fd4e38539           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent4}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}, ${staticFixContent4}, ${staticFixContent5}" for the ticket "${staticTicketNumber}" on the account "LRJAPANBASICACT" with the following values
      | Name     | ${staticFixContent4} |
      | Version  | 1.0                  |
      | Git Hash | b4273809c2           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent5}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}, ${staticFixContent4}, ${staticFixContent5}" for the ticket "${staticTicketNumber}" on the account "LRJAPANBASICACT" with the following values
      | Name     | ${staticFixContent5} |
      | Version  | 1.0                  |
      | Git Hash | 80ff686162           |
      | Status   | Complete             |
      | Type     | Patch                |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "LRJAPANBASICACT" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "LRJAPANBASICACT" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "LRJAPANBASICACT" with the following values
      | Type            | Official                               |
      | Support Ticket  | ${staticTicketNumber}                  |
      | Version         | 1.0                                    |
      | Project Version | marketplace-oauth2-provider-1.1.0-7110 |
      | Content         | 0 Fix Packs + 5 Tickets                |
      | Patcher Status  | Complete                               |
      | Jenkins         | fixpack-builder-hotfix-7x(uat)         |
      | Jenkins         | fixpack-builder-agent(uat)             |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}, ${staticFixContent4}, ${staticFixContent5}" for the ticket "${staticTicketNumber}" on the account "LRJAPANBASICACT" with the following values
      | Version         | 1.0                                                                                                          |
      | Name            | ${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}, ${staticFixContent4}, ${staticFixContent5} |
      | Project Version | marketplace-oauth2-provider-1.1.0-7110                                                                       |
      | Patcher Status  | Complete Merging Only                                                                                        |
      | Jenkins         | fixpack-builder-agent(uat)                                                                                   |
      | Jenkins         | fixpack-builder-hotfix-7x(uat)                                                                               |

  Scenario: Create a 70 build below de 28
    @priority = "2"
    As a TSE, I should be able to create a 7.0 build below de-28
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0                                                         |
      | Project Version | fix-pack-de-16-7010                                             |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | LPS-72839-fp16                                                  |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
      | Workaround      | true                                                            |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Workaround" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.0             |
      | Project Version | fix-pack-de-16-7010 |
      | Patcher Status  | Adding              |
      | Type            | Workaround          |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Workaround" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Workaround" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.0                    |
      | Project Version | fix-pack-de-16-7010        |
      | Git Hash        | 3e8270bfc7                 |
      | Patcher Status  | Complete                   |
      | Type            | Workaround                 |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.0               |
      | Project Version | fix-pack-de-16-7010   |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | ADIFINTERNET          |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "ADIFINTERNET" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "ADIFINTERNET" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-de-16-7010     |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    And I should be able to view a Patcher build fix with the name "${staticFixContent}" for the ticket "${staticTicketNumber}" on the account "ADIFINTERNET" with the following values
      | Name     | ${staticFixContent} |
      | Version  | 1.0                 |
      | Git Hash | 3e8270bfc7          |
      | Status   | Complete            |
      | Type     | Workaround          |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "ADIFINTERNET" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "ADIFINTERNET" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-de-16-7010            |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Create a 62 build
    @priority = "2"
    As a TSE, I should be able to create a 6.2 build
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | Portal 6.x                                                      |
      | Project Version | 6.2.10 SP15                                                     |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | LPP-31496-1                                                     |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
      | Workaround      | true                                                            |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Workaround" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | Portal 6.x          |
      | Project Version | 6.2.10 SP15         |
      | Patcher Status  | Adding              |
      | Type            | Workaround          |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Workaround" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Workaround" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | Portal 6.x                 |
      | Project Version | 6.2.10 SP15                |
      | Git Hash        | 1b0a697c9a                 |
      | Patcher Status  | Complete                   |
      | Type            | Workaround                 |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | Portal 6.x            |
      | Project Version | 6.2.10 SP15           |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | FEDBANK               |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "FEDBANK" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "FEDBANK" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 6.2.10 SP15             |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    And I should be able to view a Patcher build fix with the name "${staticFixContent}" for the ticket "${staticTicketNumber}" on the account "FEDBANK" with the following values
      | Name     | ${staticFixContent} |
      | Version  | 1.0                 |
      | Git Hash | 1b0a697c9a          |
      | Status   | Complete            |
      | Type     | Workaround          |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "FEDBANK" for compile completion using the "fixpack-builder-dist(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "FEDBANK" with the following values
      | Type            | Official                   |
      | Support Ticket  | ${staticTicketNumber}      |
      | Version         | 1.0                        |
      | Project Version | 6.2.10 SP15                |
      | Content         | 0 Fix Packs + 1 Tickets    |
      | Patcher Status  | Complete                   |
      | Jenkins         | fixpack-builder-dist(uat)  |
      | Jenkins         | fixpack-builder-agent(uat) |

  Scenario: Create a 71 security build
    @priority = "4"
    As a TSE, I should be able to create and release a 7.1 security build
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I generate random fix content for the third fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.1                                                         |
      | Project Version | fix-pack-dxp-11-7110                                            |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | LPS-108761-dxp-11                                               |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.1                                                         |
      | Project Version | fix-pack-dxp-11-7110                                            |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | fix-pack-LPS-87748-dxp-11                                       |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.1                                                         |
      | Project Version | fix-pack-dxp-11-7110                                            |
      | Content         | ${staticFixContent3}                                            |
      | Branch Name     | fpf4-168556409                                                  |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1} |
      | Version         | 1.0                  |
      | Product         | DXP 7.1              |
      | Project Version | fix-pack-dxp-11-7110 |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2} |
      | Version         | 1.0                  |
      | Product         | DXP 7.1              |
      | Project Version | fix-pack-dxp-11-7110 |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    And I should be able to view a Patcher fix containing the content "${staticFixContent3}" and type "Patch" with the following values
      | Content         | ${staticFixContent3} |
      | Version         | 1.0                  |
      | Product         | DXP 7.1              |
      | Project Version | fix-pack-dxp-11-7110 |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent3}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.1                    |
      | Project Version | fix-pack-dxp-11-7110       |
      | Git Hash        | 80db310885                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.1                    |
      | Project Version | fix-pack-dxp-11-7110       |
      | Git Hash        | 0a16e755de                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent3}" and type "Patch" with the following values
      | Content         | ${staticFixContent3}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.1                    |
      | Project Version | fix-pack-dxp-11-7110       |
      | Git Hash        | 05fd6fa414                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.1                                                        |
      | Project Version | fix-pack-dxp-11-7110                                           |
      | Tickets List    | ${staticFixContent1},${staticFixContent2},${staticFixContent3} |
      | Account Code    | LIFERAYSECURITY                                                |
      | Support Ticket  | ${staticTicketNumber}                                          |
    And I set the account code "LIFERAYSECURITY" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-dxp-11-7110    |
      | Content         | 0 Fix Packs + 3 Tickets |
      | Patcher Status  | Merging                 |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" with the following values
      | Version         | 1.0                                                              |
      | Name            | ${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3} |
      | Project Version | fix-pack-dxp-11-7110                                             |
      | Patcher Status  | Merging Only                                                     |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent1}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | 80db310885           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent2}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | 0a16e755de           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent3}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" with the following values
      | Name     | ${staticFixContent3} |
      | Version  | 1.0                  |
      | Git Hash | 05fd6fa414           |
      | Status   | Complete             |
      | Type     | Patch                |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" with the following values
      | Type            | Official                                       |
      | Support Ticket  | ${staticTicketNumber}                          |
      | Version         | 1.0                                            |
      | Project Version | fix-pack-dxp-11-7110                           |
      | Content         | 0 Fix Packs + 3 Tickets                        |
      | Patcher Status  | Complete                                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat)                 |
      | Jenkins         | fixpack-builder-agent(uat)                     |
      | Hotfix          | liferay-security-dxp-11-${staticTicketNumber}- |
      | Hotfix          | -7110                                          |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" with the following values
      | Version         | 1.0                                                              |
      | Name            | ${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3} |
      | Project Version | fix-pack-dxp-11-7110                                             |
      | Patcher Status  | Complete Merging Only                                            |
      | Jenkins         | fixpack-builder-hotfix-7x(uat)                                   |
      | Jenkins         | fixpack-builder-agent(uat)                                       |

  Scenario: Create a subsequent 71 security build
    @priority = "4"
    As a TSE, I should be able to create and release a subsequent 7.1 security build
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I generate random fix content for the third fix
    And I generate random fix content for the fourth fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.1                                                         |
      | Project Version | fix-pack-dxp-11-7110                                            |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | LPS-108761-dxp-11                                               |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.1                                                         |
      | Project Version | fix-pack-dxp-11-7110                                            |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | fix-pack-LPS-87748-dxp-11                                       |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.1                                                         |
      | Project Version | fix-pack-dxp-11-7110                                            |
      | Content         | ${staticFixContent3}                                            |
      | Branch Name     | LPS-100523-dxp11                                                |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.1                                                         |
      | Project Version | fix-pack-dxp-11-7110                                            |
      | Content         | ${staticFixContent4}                                            |
      | Branch Name     | fpf4-168556409                                                  |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent3}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent4}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.1                    |
      | Project Version | fix-pack-dxp-11-7110       |
      | Git Hash        | 80db310885                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.1                    |
      | Project Version | fix-pack-dxp-11-7110       |
      | Git Hash        | 0a16e755de                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent3}" and type "Patch" with the following values
      | Content         | ${staticFixContent3}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.1                    |
      | Project Version | fix-pack-dxp-11-7110       |
      | Git Hash        | 7c16a2bca0                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent4}" and type "Patch" with the following values
      | Content         | ${staticFixContent4}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.1                    |
      | Project Version | fix-pack-dxp-11-7110       |
      | Git Hash        | 05fd6fa414                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.1                                                        |
      | Project Version | fix-pack-dxp-11-7110                                           |
      | Tickets List    | ${staticFixContent1},${staticFixContent2},${staticFixContent4} |
      | Account Code    | LIFERAYSECURITY                                                |
      | Support Ticket  | ${staticTicketNumber}                                          |
    And I set the account code "LIFERAYSECURITY" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-dxp-11-7110    |
      | Content         | 0 Fix Packs + 3 Tickets |
      | Patcher Status  | Merging                 |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent4}" for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" with the following values
      | Version         | 1.0                                                              |
      | Name            | ${staticFixContent1}, ${staticFixContent2}, ${staticFixContent4} |
      | Project Version | fix-pack-dxp-11-7110                                             |
      | Patcher Status  | Merging Only                                                     |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent1}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent4}" for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | 80db310885           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent2}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent4}" for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | 0a16e755de           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent4}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent4}" for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" with the following values
      | Name     | ${staticFixContent4} |
      | Version  | 1.0                  |
      | Git Hash | 05fd6fa414           |
      | Status   | Complete             |
      | Type     | Patch                |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" with the following values
      | Type            | Official                                       |
      | Support Ticket  | ${staticTicketNumber}                          |
      | Version         | 1.0                                            |
      | Project Version | fix-pack-dxp-11-7110                           |
      | Content         | 0 Fix Packs + 3 Tickets                        |
      | Patcher Status  | Complete                                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat)                 |
      | Jenkins         | fixpack-builder-agent(uat)                     |
      | Hotfix          | liferay-security-dxp-11-${staticTicketNumber}- |
      | Hotfix          | -7110                                          |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent4}" for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" with the following values
      | Version         | 1.0                                                              |
      | Name            | ${staticFixContent1}, ${staticFixContent2}, ${staticFixContent4} |
      | Project Version | fix-pack-dxp-11-7110                                             |
      | Patcher Status  | Complete Merging Only                                            |
      | Jenkins         | fixpack-builder-hotfix-7x(uat)                                   |
      | Jenkins         | fixpack-builder-agent(uat)                                       |
    When I get the security hotfix counter value for a build for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" that is built on the version "dxp-11" on the root "7110"
    And I increase the security hotfix counter value "${staticHotfixCounterValue}" by one
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.1                                                                             |
      | Project Version | fix-pack-dxp-11-7110                                                                |
      | Tickets List    | ${staticFixContent1},${staticFixContent2},${staticFixContent3},${staticFixContent4} |
      | Account Code    | LIFERAYSECURITY                                                                     |
      | Support Ticket  | ${staticTicketNumber}                                                               |
    And I set the account code "LIFERAYSECURITY" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-dxp-11-7110    |
      | Content         | 0 Fix Packs + 4 Tickets |
      | Patcher Status  | Merging                 |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}, ${staticFixContent4}" for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" with the following values
      | Version         | 1.0                                                                                    |
      | Name            | ${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}, ${staticFixContent4} |
      | Project Version | fix-pack-dxp-11-7110                                                                   |
      | Patcher Status  | Merging Only                                                                           |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent1}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}, ${staticFixContent4}" for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | 80db310885           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent2}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}, ${staticFixContent4}" for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | 0a16e755de           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent3}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}, ${staticFixContent4}" for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" with the following values
      | Name     | ${staticFixContent3} |
      | Version  | 1.0                  |
      | Git Hash | 7c16a2bca0           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent4}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}, ${staticFixContent4}" for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" with the following values
      | Name     | ${staticFixContent4} |
      | Version  | 1.0                  |
      | Git Hash | 05fd6fa414           |
      | Status   | Complete             |
      | Type     | Patch                |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" with the following values
      | Type            | Official                                       |
      | Support Ticket  | ${staticTicketNumber}                          |
      | Version         | 1.0                                            |
      | Project Version | fix-pack-dxp-11-7110                           |
      | Content         | 0 Fix Packs + 4 Tickets                        |
      | Patcher Status  | Complete                                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat)                 |
      | Jenkins         | fixpack-builder-agent(uat)                     |
      | Hotfix          | liferay-security-dxp-11-${staticTicketNumber}- |
      | Hotfix          | -7110                                          |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}, ${staticFixContent4}" for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" with the following values
      | Version         | 1.0                                                                                    |
      | Name            | ${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}, ${staticFixContent4} |
      | Project Version | fix-pack-dxp-11-7110                                                                   |
      | Patcher Status  | Complete Merging Only                                                                  |
      | Jenkins         | fixpack-builder-hotfix-7x(uat)                                                         |
      | Jenkins         | fixpack-builder-agent(uat)                                                             |
    And the security hotfix counter value for a build for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" that is built on the version "dxp-11" on the root "7110" should be "${staticUpdatedHotfixCounterValue}"

  Scenario: Create and release a 70 security build
    @priority = "3"
    As a TSE, I should be able to create and release a 7.0 security build
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0                                                         |
      | Project Version | fix-pack-de-10-7010                                             |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | fix-pack-LPP-34875-DE10                                         |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.0             |
      | Project Version | fix-pack-de-10-7010 |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.0                    |
      | Project Version | fix-pack-de-10-7010        |
      | Git Hash        | bcb6febe29                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.0               |
      | Project Version | fix-pack-de-10-7010   |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | LIFERAYSECURITY       |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "LIFERAYSECURITY" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-de-10-7010     |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    And I should be able to view a Patcher build fix with the name "${staticFixContent}" for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" with the following values
      | Name     | ${staticFixContent} |
      | Version  | 1.0                 |
      | Git Hash | bcb6febe29          |
      | Status   | Complete            |
      | Type     | Patch               |
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" with the following values
      | Type            | Official                                      |
      | Support Ticket  | ${staticTicketNumber}                         |
      | Version         | 1.0                                           |
      | Project Version | fix-pack-de-10-7010                           |
      | Content         | 0 Fix Packs + 1 Tickets                       |
      | Patcher Status  | Complete                                      |
      | Jenkins         | fixpack-builder-hotfix-7x(uat)                |
      | Jenkins         | fixpack-builder-agent(uat)                    |
      | Hotfix          | liferay-security-de-10-${staticTicketNumber}- |
      | Hotfix          | -7010                                         |

  Scenario: Create build with relative path in build binaries cache dir
    @priority = "4"
    As a TSE, I should be able to create a public MP build
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I generate random fix content for the third fix
    And I generate random fix content for the fourth fix
    And I generate random fix content for the fifth fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.2                                                         |
      | Project Version | fix-pack-dxp-3-7210                                             |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | fix-pack-LPS-105565-dxp-3                                       |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.2                                                         |
      | Project Version | fix-pack-dxp-4-7210                                             |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | LPS-108761-72dxp4                                               |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
      | Workaround      | true                                                            |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.2                                                         |
      | Project Version | fix-pack-dxp-3-7210                                             |
      | Content         | ${staticFixContent3}                                            |
      | Branch Name     | LPS-107240-dxp3-72x                                             |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
      | Workaround      | true                                                            |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.2                                                         |
      | Project Version | fix-pack-dxp-3-7210                                             |
      | Content         | ${staticFixContent4}                                            |
      | Branch Name     | LPS-113730-LPS-113733-dxp-3-2                                   |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.2                                                         |
      | Project Version | fix-pack-dxp-3-7210                                             |
      | Content         | ${staticFixContent5}                                            |
      | Branch Name     | LPS-142141-LPS-134572-72dxp3                                    |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Workaround" for add completion
    And I check the Patcher fix containing the content "${staticFixContent3}" and type "Workaround" for add completion
    And I check the Patcher fix containing the content "${staticFixContent4}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent5}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.2                    |
      | Project Version | fix-pack-dxp-3-7210        |
      | Git Hash        | 3f9709ffb6                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Workaround" with the following values
      | Content         | ${staticFixContent2}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.2                    |
      | Project Version | fix-pack-dxp-4-7210        |
      | Git Hash        | 41ee2a35f5                 |
      | Patcher Status  | Complete                   |
      | Type            | Workaround                 |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent3}" and type "Workaround" with the following values
      | Content         | ${staticFixContent3}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.2                    |
      | Project Version | fix-pack-dxp-3-7210        |
      | Git Hash        | 3463fcb5a3                 |
      | Patcher Status  | Complete                   |
      | Type            | Workaround                 |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent4}" and type "Patch" with the following values
      | Content         | ${staticFixContent4}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.2                    |
      | Project Version | fix-pack-dxp-3-7210        |
      | Git Hash        | 91cbf794e4                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent5}" and type "Patch" with the following values
      | Content         | ${staticFixContent5}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.2                    |
      | Project Version | fix-pack-dxp-3-7210        |
      | Git Hash        | 7f47bb43a7                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.2                                                                                                  |
      | Project Version | fix-pack-dxp-3-7210                                                                                      |
      | Tickets List    | ${staticFixContent1},${staticFixContent2},${staticFixContent3},${staticFixContent4},${staticFixContent5} |
      | Account Code    | ENDURANCE                                                                                                |
      | Support Ticket  | ${staticTicketNumber}                                                                                    |
    And I set the account code "ENDURANCE" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "ENDURANCE" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-dxp-3-7210     |
      | Content         | 0 Fix Packs + 5 Tickets |
      | Patcher Status  | Rebasing                |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "ENDURANCE" for rebase completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "ENDURANCE" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "ENDURANCE" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "ENDURANCE" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-dxp-3-7210            |
      | Content         | 0 Fix Packs + 5 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Create a 70 build below de 13
    @priority = "2"
    As a TSE, I should be able to create a 7.0 build below de-13
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I generate random fix content for the third fix
    And I generate random fix content for the fourth fix
    And I generate random fix content for the fifth fix
    And I generate random fix content for the sixth fix
    And I generate random fix content for the seventh fix
    And I generate random fix content for the eighth fix
    And I generate random fix content for the ninth fix
    And I generate random fix content for the tenth fix
    And I generate random fix content for the eleventh fix
    And I generate random fix content for the twelfth fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0                                                         |
      | Project Version | fix-pack-de-13-7010                                             |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | LPE-16497_LPS-78517_de13                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0                                                         |
      | Project Version | fix-pack-de-13-7010                                             |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | fix-pack-LPS-107154-LPS-107323-DE13                             |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0                                                         |
      | Project Version | fix-pack-de-13-7010                                             |
      | Content         | ${staticFixContent3}                                            |
      | Branch Name     | LPS-70667_7010_de13                                             |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
      | Workaround      | true                                                            |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0                                                         |
      | Project Version | fix-pack-de-13-7010                                             |
      | Content         | ${staticFixContent4}                                            |
      | Branch Name     | fix-LPS-71278-de13-7010                                         |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0                                                         |
      | Project Version | fix-pack-de-13-7010                                             |
      | Content         | ${staticFixContent5}                                            |
      | Branch Name     | LPS-71888-de13                                                  |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0                                                         |
      | Project Version | fix-pack-de-13-7010                                             |
      | Content         | ${staticFixContent6}                                            |
      | Branch Name     | LPS-71944-de13                                                  |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0                                                         |
      | Project Version | fix-pack-de-13-7010                                             |
      | Content         | ${staticFixContent7}                                            |
      | Branch Name     | LPS-73083_SDH_fix_de13_7010                                     |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
      | Workaround      | true                                                            |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0                                                         |
      | Project Version | fix-pack-de-13-7010                                             |
      | Content         | ${staticFixContent8}                                            |
      | Branch Name     | LPS-74128-de-13                                                 |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0                                                         |
      | Project Version | fix-pack-de-13-7010                                             |
      | Content         | ${staticFixContent9}                                            |
      | Branch Name     | LPS-74298-de-13                                                 |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0                                                         |
      | Project Version | fix-pack-de-13-7010                                             |
      | Content         | ${staticFixContent10}                                           |
      | Branch Name     | LPS-76691-de-13                                                 |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
      | Workaround      | true                                                            |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0                                                         |
      | Project Version | fix-pack-de-10-7010                                             |
      | Content         | ${staticFixContent11}                                           |
      | Branch Name     | LPE-16814-de10                                                  |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0                                                         |
      | Project Version | fix-pack-de-13-7010                                             |
      | Content         | ${staticFixContent12}                                           |
      | Branch Name     | LPSQA-93497-fix-pack-de-13-7010                                 |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent3}" and type "Workaround" for add completion
    And I check the Patcher fix containing the content "${staticFixContent4}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent5}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent6}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent7}" and type "Workaround" for add completion
    And I check the Patcher fix containing the content "${staticFixContent8}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent9}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent10}" and type "Workaround" for add completion
    And I check the Patcher fix containing the content "${staticFixContent11}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent12}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content        | ${staticFixContent1}       |
      | Version        | 1.0                        |
      | Product        | DXP 7.0                    |
      | Git Hash       | 9a0da626f1                 |
      | Patcher Status | Complete                   |
      | Type           | Patch                      |
      | Jenkins        | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content        | ${staticFixContent2}       |
      | Version        | 1.0                        |
      | Product        | DXP 7.0                    |
      | Git Hash       | e640ea9727                 |
      | Patcher Status | Complete                   |
      | Type           | Patch                      |
      | Jenkins        | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent3}" and type "Workaround" with the following values
      | Content        | ${staticFixContent3}       |
      | Version        | 1.0                        |
      | Product        | DXP 7.0                    |
      | Git Hash       | 7cd5fef531                 |
      | Patcher Status | Complete                   |
      | Type           | Workaround                 |
      | Jenkins        | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent4}" and type "Patch" with the following values
      | Content        | ${staticFixContent4}       |
      | Version        | 1.0                        |
      | Product        | DXP 7.0                    |
      | Git Hash       | e2cd93c545                 |
      | Patcher Status | Complete                   |
      | Type           | Patch                      |
      | Jenkins        | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent5}" and type "Patch" with the following values
      | Content        | ${staticFixContent5}       |
      | Version        | 1.0                        |
      | Product        | DXP 7.0                    |
      | Git Hash       | 571241e769                 |
      | Patcher Status | Complete                   |
      | Type           | Patch                      |
      | Jenkins        | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent6}" and type "Patch" with the following values
      | Content        | ${staticFixContent6}       |
      | Version        | 1.0                        |
      | Product        | DXP 7.0                    |
      | Git Hash       | 2e3c7a8281                 |
      | Patcher Status | Complete                   |
      | Type           | Patch                      |
      | Jenkins        | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent7}" and type "Workaround" with the following values
      | Content        | ${staticFixContent7}       |
      | Version        | 1.0                        |
      | Product        | DXP 7.0                    |
      | Git Hash       | 14dea47ecc                 |
      | Patcher Status | Complete                   |
      | Type           | Workaround                 |
      | Jenkins        | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent8}" and type "Patch" with the following values
      | Content        | ${staticFixContent8}       |
      | Version        | 1.0                        |
      | Product        | DXP 7.0                    |
      | Git Hash       | 2b2f400d07                 |
      | Patcher Status | Complete                   |
      | Type           | Patch                      |
      | Jenkins        | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent9}" and type "Patch" with the following values
      | Content        | ${staticFixContent9}       |
      | Version        | 1.0                        |
      | Product        | DXP 7.0                    |
      | Git Hash       | c2adfe7225                 |
      | Patcher Status | Complete                   |
      | Type           | Patch                      |
      | Jenkins        | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent10}" and type "Workaround" with the following values
      | Content        | ${staticFixContent10}      |
      | Version        | 1.0                        |
      | Product        | DXP 7.0                    |
      | Git Hash       | 7087832d0f                 |
      | Patcher Status | Complete                   |
      | Type           | Workaround                 |
      | Jenkins        | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent11}" and type "Patch" with the following values
      | Content        | ${staticFixContent11}      |
      | Version        | 1.0                        |
      | Product        | DXP 7.0                    |
      | Git Hash       | ea0924c3e4                 |
      | Patcher Status | Complete                   |
      | Type           | Patch                      |
      | Jenkins        | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent12}" and type "Patch" with the following values
      | Content        | ${staticFixContent12}      |
      | Version        | 1.0                        |
      | Product        | DXP 7.0                    |
      | Git Hash       | 14d83ff0e1                 |
      | Patcher Status | Complete                   |
      | Type           | Patch                      |
      | Jenkins        | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.0                                                                                                                                                                                                                                                        |
      | Project Version | fix-pack-de-13-7010                                                                                                                                                                                                                                            |
      | Tickets List    | ${staticFixContent1},${staticFixContent2},${staticFixContent3},${staticFixContent4},${staticFixContent5},${staticFixContent6},${staticFixContent7},${staticFixContent8},${staticFixContent9},${staticFixContent10},${staticFixContent11},${staticFixContent12} |
      | Account Code    | SOGEINOIPA                                                                                                                                                                                                                                                     |
      | Support Ticket  | ${staticTicketNumber}                                                                                                                                                                                                                                          |
    And I set the account code "SOGEINOIPA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "SOGEINOIPA" with the following values
      | Type            | Official                 |
      | Support Ticket  | ${staticTicketNumber}    |
      | Version         | 1.0                      |
      | Project Version | fix-pack-de-13-7010      |
      | Content         | 0 Fix Packs + 12 Tickets |
      | Patcher Status  | Rebasing                 |
    And I should be able to view a Patcher build fix with the name "${staticFixContent1}" for the ticket "${staticTicketNumber}" on the account "SOGEINOIPA" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | 9a0da626f1           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher build fix with the name "${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "SOGEINOIPA" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | e640ea9727           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher build fix with the name "${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "SOGEINOIPA" with the following values
      | Name     | ${staticFixContent3} |
      | Version  | 1.0                  |
      | Git Hash | 7cd5fef531           |
      | Status   | Complete             |
      | Type     | Workaround           |
    And I should be able to view a Patcher build fix with the name "${staticFixContent4}" for the ticket "${staticTicketNumber}" on the account "SOGEINOIPA" with the following values
      | Name     | ${staticFixContent4} |
      | Version  | 1.0                  |
      | Git Hash | e2cd93c545           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher build fix with the name "${staticFixContent5}" for the ticket "${staticTicketNumber}" on the account "SOGEINOIPA" with the following values
      | Name     | ${staticFixContent5} |
      | Version  | 1.0                  |
      | Git Hash | 571241e769           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher build fix with the name "${staticFixContent6}" for the ticket "${staticTicketNumber}" on the account "SOGEINOIPA" with the following values
      | Name     | ${staticFixContent6} |
      | Version  | 1.0                  |
      | Git Hash | 2e3c7a8281           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher build fix with the name "${staticFixContent7}" for the ticket "${staticTicketNumber}" on the account "SOGEINOIPA" with the following values
      | Name     | ${staticFixContent7} |
      | Version  | 1.0                  |
      | Git Hash | 14dea47ecc           |
      | Status   | Complete             |
      | Type     | Workaround           |
    And I should be able to view a Patcher build fix with the name "${staticFixContent8}" for the ticket "${staticTicketNumber}" on the account "SOGEINOIPA" with the following values
      | Name     | ${staticFixContent8} |
      | Version  | 1.0                  |
      | Git Hash | 2b2f400d07           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher build fix with the name "${staticFixContent9}" for the ticket "${staticTicketNumber}" on the account "SOGEINOIPA" with the following values
      | Name     | ${staticFixContent9} |
      | Version  | 1.0                  |
      | Git Hash | c2adfe7225           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher build fix with the name "${staticFixContent10}" for the ticket "${staticTicketNumber}" on the account "SOGEINOIPA" with the following values
      | Name     | ${staticFixContent10} |
      | Version  | 1.0                   |
      | Git Hash | 7087832d0f            |
      | Status   | Complete              |
      | Type     | Workaround            |
    And I should be able to view a Patcher build fix with the name "${staticFixContent11}" for the ticket "${staticTicketNumber}" on the account "SOGEINOIPA" with the following values
      | Name    | ${staticFixContent11} |
      | Version | 1.0                   |
      | Status  | Complete              |
      | Type    | Rebase                |
    And I should be able to view a Patcher build fix with the name "${staticFixContent12}" for the ticket "${staticTicketNumber}" on the account "SOGEINOIPA" with the following values
      | Name     | ${staticFixContent12} |
      | Version  | 1.0                   |
      | Git Hash | 14d83ff0e1            |
      | Status   | Complete              |
      | Type     | Patch                 |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "SOGEINOIPA" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "SOGEINOIPA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "SOGEINOIPA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-de-13-7010            |
      | Content         | 0 Fix Packs + 12 Tickets       |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Create a 70 build below de 46
    @priority = "2"
    As a TSE, I should be able to create a 7.0 build below de-46
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I generate random fix content for the third fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0                                                         |
      | Project Version | fix-pack-de-46-7010                                             |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | LPS-107323-de46                                                 |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0                                                         |
      | Project Version | fix-pack-de-46-7010                                             |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | LPS-110198-de46                                                 |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0                                                         |
      | Project Version | fix-pack-de-46-7010                                             |
      | Content         | ${staticFixContent3}                                            |
      | Branch Name     | LPS-80517-de46                                                  |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1} |
      | Version         | 1.0                  |
      | Product         | DXP 7.0              |
      | Project Version | fix-pack-de-46-7010  |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2} |
      | Version         | 1.0                  |
      | Product         | DXP 7.0              |
      | Project Version | fix-pack-de-46-7010  |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    And I should be able to view a Patcher fix containing the content "${staticFixContent3}" and type "Patch" with the following values
      | Content         | ${staticFixContent3} |
      | Version         | 1.0                  |
      | Product         | DXP 7.0              |
      | Project Version | fix-pack-de-46-7010  |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent3}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content        | ${staticFixContent1}       |
      | Version        | 1.0                        |
      | Product        | DXP 7.0                    |
      | Git Hash       | 75786e320d                 |
      | Patcher Status | Complete                   |
      | Type           | Patch                      |
      | Jenkins        | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content        | ${staticFixContent2}       |
      | Version        | 1.0                        |
      | Product        | DXP 7.0                    |
      | Git Hash       | 1ca98e3ed4                 |
      | Patcher Status | Complete                   |
      | Type           | Patch                      |
      | Jenkins        | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent3}" and type "Patch" with the following values
      | Content        | ${staticFixContent3}       |
      | Version        | 1.0                        |
      | Product        | DXP 7.0                    |
      | Git Hash       | 11dccef149                 |
      | Patcher Status | Complete                   |
      | Type           | Patch                      |
      | Jenkins        | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.0                                                        |
      | Project Version | fix-pack-de-46-7010                                            |
      | Tickets List    | ${staticFixContent1},${staticFixContent2},${staticFixContent3} |
      | Account Code    | BSESRAHTTPWW                                                   |
      | Support Ticket  | ${staticTicketNumber}                                          |
    And I set the account code "BSESRAHTTPWW" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "BSESRAHTTPWW" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-de-46-7010     |
      | Content         | 0 Fix Packs + 3 Tickets |
      | Patcher Status  | Merging                 |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "BSESRAHTTPWW" with the following values
      | Version         | 1.0                                                              |
      | Name            | ${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3} |
      | Project Version | fix-pack-de-46-7010                                              |
      | Patcher Status  | Merging Only                                                     |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent1}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "BSESRAHTTPWW" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | 75786e320d           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent2}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "BSESRAHTTPWW" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | 1ca98e3ed4           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent3}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "BSESRAHTTPWW" with the following values
      | Name     | ${staticFixContent3} |
      | Version  | 1.0                  |
      | Git Hash | 11dccef149           |
      | Status   | Complete             |
      | Type     | Patch                |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "BSESRAHTTPWW" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "BSESRAHTTPWW" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "BSESRAHTTPWW" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-de-46-7010            |
      | Content         | 0 Fix Packs + 3 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "BSESRAHTTPWW" with the following values
      | Version         | 1.0                                                              |
      | Name            | ${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3} |
      | Project Version | fix-pack-de-46-7010                                              |
      | Patcher Status  | Complete Merging Only                                            |
      | Jenkins         | fixpack-builder-hotfix-7x(uat)                                   |
      | Jenkins         | fixpack-builder-agent(uat)                                       |

  Scenario: Create DXP 72 Liferay Connector to Elasticsearch 7 build below marketplace portal search elasticsearch7 300 7210
    @priority = "4"
    As a TSE, I should be able to create DXP 7.2 Liferay Connector to Elasticsearch 7 build below marketplace-portal-search-elasticsearch7-3.0.0-7210
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I generate random fix content for the third fix
    And I generate random fix content for the fourth fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.2 Liferay Connector to Elasticsearch 7                    |
      | Project Version | marketplace-portal-search-elasticsearch7-3.0.0-7210             |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | LPS-111545-es7-3.0.0-7210                                       |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.2 Liferay Connector to Elasticsearch 7                    |
      | Project Version | marketplace-portal-search-elasticsearch7-3.0.0-7210             |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | LPS-111545-es7-3.0.0-7210                                       |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.2 Liferay Connector to Elasticsearch 7                    |
      | Project Version | marketplace-portal-search-elasticsearch7-3.0.0-7210             |
      | Content         | ${staticFixContent3}                                            |
      | Branch Name     | fpf72-168554010                                                 |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.2 Liferay Connector to Elasticsearch 7                    |
      | Project Version | marketplace-portal-search-elasticsearch7-3.0.0-7210             |
      | Content         | ${staticFixContent4}                                            |
      | Branch Name     | LPS-142141-LPS-134572-elasticsearch7-3.0.0-7210                 |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent3}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent4}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1}                                |
      | Version         | 1.0                                                 |
      | Product         | DXP 7.2 Liferay Connector to Elasticsearch 7        |
      | Project Version | marketplace-portal-search-elasticsearch7-3.0.0-7210 |
      | Git Hash        | 5396ba4c87                                          |
      | Patcher Status  | Complete                                            |
      | Type            | Patch                                               |
      | Jenkins         | fixpack-builder-agent(uat)                          |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2}                                |
      | Version         | 1.0                                                 |
      | Product         | DXP 7.2 Liferay Connector to Elasticsearch 7        |
      | Project Version | marketplace-portal-search-elasticsearch7-3.0.0-7210 |
      | Git Hash        | 5396ba4c87                                          |
      | Patcher Status  | Complete                                            |
      | Type            | Patch                                               |
      | Jenkins         | fixpack-builder-agent(uat)                          |
    And I should be able to view a Patcher fix containing the content "${staticFixContent3}" and type "Patch" with the following values
      | Content         | ${staticFixContent3}                                |
      | Version         | 1.0                                                 |
      | Product         | DXP 7.2 Liferay Connector to Elasticsearch 7        |
      | Project Version | marketplace-portal-search-elasticsearch7-3.0.0-7210 |
      | Git Hash        | 5ec37bee2d                                          |
      | Patcher Status  | Complete                                            |
      | Type            | Patch                                               |
      | Jenkins         | fixpack-builder-agent(uat)                          |
    And I should be able to view a Patcher fix containing the content "${staticFixContent4}" and type "Patch" with the following values
      | Content         | ${staticFixContent4}                                |
      | Version         | 1.0                                                 |
      | Product         | DXP 7.2 Liferay Connector to Elasticsearch 7        |
      | Project Version | marketplace-portal-search-elasticsearch7-3.0.0-7210 |
      | Git Hash        | da5c5ab1a4                                          |
      | Patcher Status  | Complete                                            |
      | Type            | Patch                                               |
      | Jenkins         | fixpack-builder-agent(uat)                          |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.2 Liferay Connector to Elasticsearch 7                                        |
      | Project Version | marketplace-portal-search-elasticsearch7-3.0.0-7210                                 |
      | Tickets List    | ${staticFixContent1},${staticFixContent2},${staticFixContent3},${staticFixContent4} |
      | Account Code    | INTUITIVE                                                                           |
      | Support Ticket  | ${staticTicketNumber}                                                               |
    And I set the account code "INTUITIVE" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                                            |
      | Support Ticket  | ${staticTicketNumber}                               |
      | Version         | 1.0                                                 |
      | Project Version | marketplace-portal-search-elasticsearch7-3.0.0-7210 |
      | Content         | 0 Fix Packs + 4 Tickets                             |
      | Patcher Status  | Merging                                             |
    And I should be able to view a Patcher build fix with the name "${staticFixContent1}" for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | 5396ba4c87           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher build fix with the name "${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | 5396ba4c87           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher build fix with the name "${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Name     | ${staticFixContent3} |
      | Version  | 1.0                  |
      | Git Hash | 5ec37bee2d           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher build fix with the name "${staticFixContent4}" for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Name     | ${staticFixContent4} |
      | Version  | 1.0                  |
      | Git Hash | da5c5ab1a4           |
      | Status   | Complete             |
      | Type     | Patch                |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                                            |
      | Support Ticket  | ${staticTicketNumber}                               |
      | Version         | 1.0                                                 |
      | Project Version | marketplace-portal-search-elasticsearch7-3.0.0-7210 |
      | Content         | 0 Fix Packs + 4 Tickets                             |
      | Patcher Status  | Complete                                            |
      | Jenkins         | fixpack-builder-hotfix-7x(uat)                      |
      | Jenkins         | fixpack-builder-agent(uat)                          |

  Scenario: Create a DXP 71 Liferay Commerce build below marketplace commerce 211 7110
    @priority = "4"
    As a TSE, I should be able to create a DXP 7.1 Liferay Commerce build below marketplace-commerce-2.1.1-7110
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I generate random fix content for the third fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.1 Liferay Commerce                                        |
      | Project Version | marketplace-commerce-2.1.1-7110                                 |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | COMMERCE-4107-hotfix-for-jp-demo                                |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.1 Liferay Commerce                                        |
      | Project Version | marketplace-commerce-2.1.1-7110                                 |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | LPS-94011-commerce211                                           |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.1 Liferay Commerce                                        |
      | Project Version | marketplace-commerce-2.1.1-7110                                 |
      | Content         | ${staticFixContent3}                                            |
      | Branch Name     | LPS-113730                                                      |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1}            |
      | Version         | 1.0                             |
      | Product         | DXP 7.1 Liferay Commerce        |
      | Project Version | marketplace-commerce-2.1.1-7110 |
      | Patcher Status  | Adding                          |
      | Type            | Patch                           |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2}            |
      | Version         | 1.0                             |
      | Product         | DXP 7.1 Liferay Commerce        |
      | Project Version | marketplace-commerce-2.1.1-7110 |
      | Patcher Status  | Adding                          |
      | Type            | Patch                           |
    And I should be able to view a Patcher fix containing the content "${staticFixContent3}" and type "Patch" with the following values
      | Content         | ${staticFixContent3}            |
      | Version         | 1.0                             |
      | Product         | DXP 7.1 Liferay Commerce        |
      | Project Version | marketplace-commerce-2.1.1-7110 |
      | Patcher Status  | Adding                          |
      | Type            | Patch                           |
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Patch" for add completion
    When I check the Patcher fix containing the content "${staticFixContent3}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1}            |
      | Version         | 1.0                             |
      | Product         | DXP 7.1 Liferay Commerce        |
      | Project Version | marketplace-commerce-2.1.1-7110 |
      | Git Hash        | 0b15e03442                      |
      | Patcher Status  | Complete                        |
      | Type            | Patch                           |
      | Jenkins         | fixpack-builder-agent(uat)      |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2}            |
      | Version         | 1.0                             |
      | Product         | DXP 7.1 Liferay Commerce        |
      | Project Version | marketplace-commerce-2.1.1-7110 |
      | Git Hash        | 210ccfe649                      |
      | Patcher Status  | Complete                        |
      | Type            | Patch                           |
      | Jenkins         | fixpack-builder-agent(uat)      |
    And I should be able to view a Patcher fix containing the content "${staticFixContent3}" and type "Patch" with the following values
      | Content         | ${staticFixContent3}            |
      | Version         | 1.0                             |
      | Product         | DXP 7.1 Liferay Commerce        |
      | Project Version | marketplace-commerce-2.1.1-7110 |
      | Git Hash        | 7a8d003fc6                      |
      | Patcher Status  | Complete                        |
      | Type            | Patch                           |
      | Jenkins         | fixpack-builder-agent(uat)      |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.1 Liferay Commerce                                       |
      | Project Version | marketplace-commerce-2.1.1-7110                                |
      | Tickets List    | ${staticFixContent1},${staticFixContent2},${staticFixContent3} |
      | Account Code    | LPSPJP                                                         |
      | Support Ticket  | ${staticTicketNumber}                                          |
    And I set the account code "LPSPJP" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "LPSPJP" with the following values
      | Type            | Official                        |
      | Support Ticket  | ${staticTicketNumber}           |
      | Version         | 1.0                             |
      | Project Version | marketplace-commerce-2.1.1-7110 |
      | Content         | 0 Fix Packs + 3 Tickets         |
      | Patcher Status  | Merging                         |
    And I should be able to view a Patcher build fix with the name "${staticFixContent1}" for the ticket "${staticTicketNumber}" on the account "LPSPJP" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | 0b15e03442           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher build fix with the name "${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "LPSPJP" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | 210ccfe649           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher build fix with the name "${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "LPSPJP" with the following values
      | Name     | ${staticFixContent3} |
      | Version  | 1.0                  |
      | Git Hash | 7a8d003fc6           |
      | Status   | Complete             |
      | Type     | Patch                |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "LPSPJP" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "LPSPJP" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "LPSPJP" with the following values
      | Type            | Official                        |
      | Support Ticket  | ${staticTicketNumber}           |
      | Version         | 1.0                             |
      | Project Version | marketplace-commerce-2.1.1-7110 |
      | Content         | 0 Fix Packs + 3 Tickets         |
      | Patcher Status  | Complete                        |
      | Jenkins         | fixpack-builder-hotfix-7x(uat)  |
      | Jenkins         | fixpack-builder-agent(uat)      |

  Scenario: Create a 73 build
    @priority = "5"
    As a TSE, I should be able to create a 7.3 build
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I generate random fix content for the third fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-base-7310                                              |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | LRP-824-DXP7.3                                                  |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-base-7310                                              |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | RELEASE-1000                                                    |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-base-7310                                              |
      | Content         | ${staticFixContent3}                                            |
      | Branch Name     | LPS-134572-LPS-142141-LRP-1615                                  |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent3}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-base-7310         |
      | Git Hash        | 86392e297c                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-base-7310         |
      | Git Hash        | c1e7b18cd1                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent3}" and type "Patch" with the following values
      | Content         | ${staticFixContent3}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-base-7310         |
      | Git Hash        | 67acfe7a4d                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.3                                                        |
      | Project Version | fix-pack-base-7310                                             |
      | Tickets List    | ${staticFixContent1},${staticFixContent2},${staticFixContent3} |
      | Account Code    | STVINTRA                                                       |
      | Support Ticket  | ${staticTicketNumber}                                          |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-base-7310      |
      | Content         | 0 Fix Packs + 3 Tickets |
      | Patcher Status  | Merging                 |
    And I should be able to view a Patcher build fix with the name "${staticFixContent1}" for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | 86392e297c           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher build fix with the name "${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | c1e7b18cd1           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher build fix with the name "${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Name     | ${staticFixContent3} |
      | Version  | 1.0                  |
      | Git Hash | 67acfe7a4d           |
      | Status   | Complete             |
      | Type     | Patch                |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-base-7310             |
      | Content         | 0 Fix Packs + 3 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Create a 73 commerce build
    @priority = "5"
    As a TSE, I should be able to create a 7.3 commerce build
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I generate random fix content for the third fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-base-7310                                              |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | LRP-934-commerce-7310                                           |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-base-7310                                              |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | RELEASE-1000                                                    |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-base-7310                                              |
      | Content         | ${staticFixContent3}                                            |
      | Branch Name     | LPS-134572-LPS-142141-LRP-1615                                  |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent3}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-base-7310         |
      | Git Hash        | 0c40ce0ae9                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-base-7310         |
      | Git Hash        | c1e7b18cd1                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent3}" and type "Patch" with the following values
      | Content         | ${staticFixContent3}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-base-7310         |
      | Git Hash        | 67acfe7a4d                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.3                                                        |
      | Project Version | fix-pack-base-7310                                             |
      | Tickets List    | ${staticFixContent1},${staticFixContent2},${staticFixContent3} |
      | Account Code    | LPSPJP                                                         |
      | Support Ticket  | ${staticTicketNumber}                                          |
    And I set the account code "LPSPJP" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "LPSPJP" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-base-7310      |
      | Content         | 0 Fix Packs + 3 Tickets |
      | Patcher Status  | Merging                 |
    And I should be able to view a Patcher build fix with the name "${staticFixContent1}" for the ticket "${staticTicketNumber}" on the account "LPSPJP" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | 0c40ce0ae9           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher build fix with the name "${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "LPSPJP" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | c1e7b18cd1           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher build fix with the name "${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "LPSPJP" with the following values
      | Name     | ${staticFixContent3} |
      | Version  | 1.0                  |
      | Git Hash | 67acfe7a4d           |
      | Status   | Complete             |
      | Type     | Patch                |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "LPSPJP" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "LPSPJP" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "LPSPJP" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-base-7310             |
      | Content         | 0 Fix Packs + 3 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Create a modified scss file in 73 base build
     @priority = "5"
     As a TSE, I should be able to create a modified scss file in 7.3 base build
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I generate random fix content for the third fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-base-7310                                              |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | LRP-1134                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-base-7310                                              |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | RELEASE-1000                                                    |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-base-7310                                              |
      | Content         | ${staticFixContent3}                                            |
      | Branch Name     | LPS-134572-LPS-142141-LRP-1615                                  |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent3}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-base-7310         |
      | Git Hash        | 1b1ff95535                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-base-7310         |
      | Git Hash        | c1e7b18cd1                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent3}" and type "Patch" with the following values
      | Content         | ${staticFixContent3}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-base-7310         |
      | Git Hash        | 67acfe7a4d                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.3                                                        |
      | Project Version | fix-pack-base-7310                                             |
      | Tickets List    | ${staticFixContent1},${staticFixContent2},${staticFixContent3} |
      | Account Code    | INTUITIVE                                                      |
      | Support Ticket  | ${staticTicketNumber}                                          |
    And I set the account code "INTUITIVE" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-base-7310      |
      | Content         | 0 Fix Packs + 3 Tickets |
      | Patcher Status  | Merging                 |
    And I should be able to view a Patcher build fix with the name "${staticFixContent1}" for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | 1b1ff95535           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher build fix with the name "${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | c1e7b18cd1           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher build fix with the name "${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Name     | ${staticFixContent3} |
      | Version  | 1.0                  |
      | Git Hash | 67acfe7a4d           |
      | Status   | Complete             |
      | Type     | Patch                |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-base-7310             |
      | Content         | 0 Fix Packs + 3 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Create a 73 Liferay Connector to SAML 2 build
    @priority = "5"
    As a TSE, I should be able to create a 7.3 Liferay Connector to SAML 2.0 build
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I generate random fix content for the third fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3 Liferay Connector to SAML 2.0                           |
      | Project Version | marketplace-saml-6.0.0-7310                                     |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | LRP-1103                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
      | Workaround      | true                                                            |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3 Liferay Connector to SAML 2.0                           |
      | Project Version | marketplace-saml-6.0.0-7310                                     |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | saml-RELEASE-1000                                               |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3 Liferay Connector to SAML 2.0                           |
      | Project Version | marketplace-saml-6.0.0-7310                                     |
      | Content         | ${staticFixContent3}                                            |
      | Branch Name     | LPS-142141-saml-6.0.0-7310                                      |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    And I check the Patcher fix containing the content "${staticFixContent1}" and type "Workaround" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent3}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Workaround" with the following values
      | Content         | ${staticFixContent1}                  |
      | Version         | 1.0                                   |
      | Product         | DXP 7.3 Liferay Connector to SAML 2.0 |
      | Project Version | marketplace-saml-6.0.0-7310           |
      | Git Hash        | 91adb37566                            |
      | Patcher Status  | Complete                              |
      | Type            | Workaround                            |
      | Jenkins         | fixpack-builder-agent(uat)            |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2}                  |
      | Version         | 1.0                                   |
      | Product         | DXP 7.3 Liferay Connector to SAML 2.0 |
      | Project Version | marketplace-saml-6.0.0-7310           |
      | Git Hash        | 2236bb4b26                            |
      | Patcher Status  | Complete                              |
      | Type            | Patch                                 |
      | Jenkins         | fixpack-builder-agent(uat)            |
    And I should be able to view a Patcher fix containing the content "${staticFixContent3}" and type "Patch" with the following values
      | Content         | ${staticFixContent3}                  |
      | Version         | 1.0                                   |
      | Product         | DXP 7.3 Liferay Connector to SAML 2.0 |
      | Project Version | marketplace-saml-6.0.0-7310           |
      | Git Hash        | 4314034e59                            |
      | Patcher Status  | Complete                              |
      | Type            | Patch                                 |
      | Jenkins         | fixpack-builder-agent(uat)            |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.3 Liferay Connector to SAML 2.0                          |
      | Project Version | marketplace-saml-6.0.0-7310                                    |
      | Tickets List    | ${staticFixContent1},${staticFixContent2},${staticFixContent3} |
      | Account Code    | HITACHIGP                                                      |
      | Support Ticket  | ${staticTicketNumber}                                          |
    And I set the account code "HITACHIGP" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                    |
      | Support Ticket  | ${staticTicketNumber}       |
      | Version         | 1.0                         |
      | Project Version | marketplace-saml-6.0.0-7310 |
      | Content         | 0 Fix Packs + 3 Tickets     |
      | Patcher Status  | Merging                     |
    And I should be able to view a Patcher build fix with the name "${staticFixContent1}" for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | 91adb37566           |
      | Status   | Complete             |
      | Type     | Workaround           |
    And I should be able to view a Patcher build fix with the name "${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | 2236bb4b26           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher build fix with the name "${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Name     | ${staticFixContent3} |
      | Version  | 1.0                  |
      | Git Hash | 4314034e59           |
      | Status   | Complete             |
      | Type     | Patch                |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | marketplace-saml-6.0.0-7310    |
      | Content         | 0 Fix Packs + 3 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Create a 70 Liferay Adaptive Media build
    @priority = "2"
    As a TSE, I should be able to create a 7.0 Liferay Adaptive Media build
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I generate random fix content for the third fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0 Liferay Adaptive Media                                  |
      | Project Version | marketplace-adaptive-media-1.0.1-7010                           |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | LPS-110595-AM-1.0.1                                             |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0 Liferay Adaptive Media                                  |
      | Project Version | marketplace-adaptive-media-1.0.1-7010                           |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | QALPS-107154                                                    |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0 Liferay Adaptive Media                                  |
      | Project Version | marketplace-adaptive-media-1.0.1-7010                           |
      | Content         | ${staticFixContent3}                                            |
      | Branch Name     | LPS-84130-marketplace-adaptive-media                            |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent3}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1}                  |
      | Version         | 1.0                                   |
      | Product         | DXP 7.0 Liferay Adaptive Media        |
      | Project Version | marketplace-adaptive-media-1.0.1-7010 |
      | Git Hash        | 1515bc4ddc                            |
      | Patcher Status  | Complete                              |
      | Type            | Patch                                 |
      | Jenkins         | fixpack-builder-agent(uat)            |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2}                  |
      | Version         | 1.0                                   |
      | Product         | DXP 7.0 Liferay Adaptive Media        |
      | Project Version | marketplace-adaptive-media-1.0.1-7010 |
      | Git Hash        | 602c7ad49e                            |
      | Patcher Status  | Complete                              |
      | Type            | Patch                                 |
      | Jenkins         | fixpack-builder-agent(uat)            |
    And I should be able to view a Patcher fix containing the content "${staticFixContent3}" and type "Patch" with the following values
      | Content         | ${staticFixContent3}                  |
      | Version         | 1.0                                   |
      | Product         | DXP 7.0 Liferay Adaptive Media        |
      | Project Version | marketplace-adaptive-media-1.0.1-7010 |
      | Git Hash        | c0953e7f41                            |
      | Patcher Status  | Complete                              |
      | Type            | Patch                                 |
      | Jenkins         | fixpack-builder-agent(uat)            |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.0 Liferay Adaptive Media                                 |
      | Project Version | marketplace-adaptive-media-1.0.1-7010                          |
      | Tickets List    | ${staticFixContent1},${staticFixContent2},${staticFixContent3} |
      | Account Code    | HITACHIGP                                                      |
      | Support Ticket  | ${staticTicketNumber}                                          |
    And I set the account code "HITACHIGP" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                              |
      | Support Ticket  | ${staticTicketNumber}                 |
      | Version         | 1.0                                   |
      | Project Version | marketplace-adaptive-media-1.0.1-7010 |
      | Content         | 0 Fix Packs + 3 Tickets               |
      | Patcher Status  | Merging                               |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Version         | 1.0                                                              |
      | Name            | ${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3} |
      | Project Version | marketplace-adaptive-media-1.0.1-7010                            |
      | Patcher Status  | Merging Only                                                     |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent1}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | 1515bc4ddc           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent2}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | 602c7ad49e           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent3}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Name     | ${staticFixContent3} |
      | Version  | 1.0                  |
      | Git Hash | c0953e7f41           |
      | Status   | Complete             |
      | Type     | Patch                |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                              |
      | Support Ticket  | ${staticTicketNumber}                 |
      | Version         | 1.0                                   |
      | Project Version | marketplace-adaptive-media-1.0.1-7010 |
      | Content         | 0 Fix Packs + 3 Tickets               |
      | Patcher Status  | Complete                              |
      | Jenkins         | fixpack-builder-hotfix-7x(uat)        |
      | Jenkins         | fixpack-builder-agent(uat)            |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Version         | 1.0                                                              |
      | Name            | ${staticFixContent1}, ${staticFixContent2}, ${staticFixContent3} |
      | Project Version | marketplace-adaptive-media-1.0.1-7010                            |
      | Patcher Status  | Complete Merging Only                                            |
      | Jenkins         | fixpack-builder-agent(uat)                                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat)                                   |

  Scenario: Create a 72 Liferay Connector to Solr 7 build
    @priority = "3"
    As a TSE, I should be able to create a 7.2 Liferay Connector to Solr 7 build
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.2 Liferay Connector to Solr 7                             |
      | Project Version | marketplace-portal-search-solr7-2.1.0-7210                      |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | LPP-40183-solr7-2.1.0-7210-hotfix                               |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.2 Liferay Connector to Solr 7                             |
      | Project Version | marketplace-portal-search-solr7-2.1.0-7210                      |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | LPS-142141-LPS-134572-search-solr7-2.1.0-7210                   |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1}                       |
      | Version         | 1.0                                        |
      | Product         | DXP 7.2 Liferay Connector to Solr 7        |
      | Project Version | marketplace-portal-search-solr7-2.1.0-7210 |
      | Patcher Status  | Adding                                     |
      | Type            | Patch                                      |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2}                       |
      | Version         | 1.0                                        |
      | Product         | DXP 7.2 Liferay Connector to Solr 7        |
      | Project Version | marketplace-portal-search-solr7-2.1.0-7210 |
      | Patcher Status  | Adding                                     |
      | Type            | Patch                                      |
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1}                       |
      | Version         | 1.0                                        |
      | Product         | DXP 7.2 Liferay Connector to Solr 7        |
      | Project Version | marketplace-portal-search-solr7-2.1.0-7210 |
      | Git Hash        | d453cb0094                                 |
      | Patcher Status  | Complete                                   |
      | Type            | Patch                                      |
      | Jenkins         | fixpack-builder-agent(uat)                 |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2}                       |
      | Version         | 1.0                                        |
      | Product         | DXP 7.2 Liferay Connector to Solr 7        |
      | Project Version | marketplace-portal-search-solr7-2.1.0-7210 |
      | Git Hash        | 28370e65a7                                 |
      | Patcher Status  | Complete                                   |
      | Type            | Patch                                      |
      | Jenkins         | fixpack-builder-agent(uat)                 |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.2 Liferay Connector to Solr 7        |
      | Project Version | marketplace-portal-search-solr7-2.1.0-7210 |
      | Tickets List    | ${staticFixContent1},${staticFixContent2}  |
      | Account Code    | LIFERAYSECURITY                            |
      | Support Ticket  | ${staticTicketNumber}                      |
    And I set the account code "LIFERAYSECURITY" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" with the following values
      | Type            | Official                                   |
      | Support Ticket  | ${staticTicketNumber}                      |
      | Version         | 1.0                                        |
      | Project Version | marketplace-portal-search-solr7-2.1.0-7210 |
      | Content         | 0 Fix Packs + 2 Tickets                    |
      | Patcher Status  | Merging                                    |
    And I should be able to view a Patcher build fix with the name "${staticFixContent1}" for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | d453cb0094           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher build fix with the name "${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | 28370e65a7           |
      | Status   | Complete             |
      | Type     | Patch                |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" with the following values
      | Type            | Official                                   |
      | Support Ticket  | ${staticTicketNumber}                      |
      | Version         | 1.0                                        |
      | Project Version | marketplace-portal-search-solr7-2.1.0-7210 |
      | Content         | 0 Fix Packs + 2 Tickets                    |
      | Patcher Status  | Complete                                   |
      | Jenkins         | fixpack-builder-hotfix-7x(uat)             |
      | Jenkins         | fixpack-builder-agent(uat)                 |

  Scenario: Create a 72 fix pack base build
    @priority = "4"
    As a TSE, I should be able to create a 7.2 fix pack base build
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.2                                                         |
      | Project Version | fix-pack-base-7210                                              |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | LPP-40352                                                       |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.2             |
      | Project Version | fix-pack-base-7210  |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.2                    |
      | Project Version | fix-pack-base-7210         |
      | Git Hash        | be19bd0596                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.2               |
      | Project Version | fix-pack-base-7210    |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | HITACHIGP             |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "HITACHIGP" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-base-7210      |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Merging                 |
    And I should be able to view a Patcher child build with the name "${staticFixContent}" for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Version         | 1.0                 |
      | Name            | ${staticFixContent} |
      | Project Version | fix-pack-base-7210  |
      | Patcher Status  | Merging Only        |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent}" for the child build with the name "${staticFixContent}" for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Name     | ${staticFixContent} |
      | Version  | 1.0                 |
      | Git Hash | be19bd0596          |
      | Status   | Complete            |
      | Type     | Patch               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-base-7210             |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |
    And I should be able to view a Patcher child build with the name "${staticFixContent}" for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Version         | 1.0                            |
      | Name            | ${staticFixContent}            |
      | Project Version | fix-pack-base-7210             |
      | Patcher Status  | Complete Merging Only          |
      | Jenkins         | fixpack-builder-agent(uat)     |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |

  Scenario: Create a 73 marketplace saml 6 build
    @priority = "5"
    As a TSE, I should be able to create a 7.3 marketplace saml 6 build
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I generate random fix content for the third fix
    And I generate random fix content for the fourth fix
    And I generate random fix content for the fifth fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3 Liferay Connector to SAML 2.0                           |
      | Project Version | marketplace-saml-6.0.0-7310                                     |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | LPS-123650-SAML-6                                               |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3 Liferay Connector to SAML 2.0                           |
      | Project Version | marketplace-saml-6.0.0-7310                                     |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | LPS-127005-marketplace-saml-6.0.0-7310                          |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
      | Workaround      | true                                                            |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3 Liferay Connector to SAML 2.0                           |
      | Project Version | marketplace-saml-6.0.0-7310                                     |
      | Content         | ${staticFixContent3}                                            |
      | Branch Name     | LPS-94011-saml-6.0.0-7310                                       |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
      | Workaround      | true                                                            |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3 Liferay Connector to SAML 2.0                           |
      | Project Version | marketplace-saml-6.0.0-7310                                     |
      | Content         | ${staticFixContent4}                                            |
      | Branch Name     | saml-RELEASE-1000                                               |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3 Liferay Connector to SAML 2.0                           |
      | Project Version | marketplace-saml-6.0.0-7310                                     |
      | Content         | ${staticFixContent5}                                            |
      | Branch Name     | LPS-142141-saml-6.0.0-7310                                      |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    And I check the Patcher fix containing the content "${staticFixContent1}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Workaround" for add completion
    And I check the Patcher fix containing the content "${staticFixContent3}" and type "Workaround" for add completion
    And I check the Patcher fix containing the content "${staticFixContent4}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent5}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1}                  |
      | Version         | 1.0                                   |
      | Product         | DXP 7.3 Liferay Connector to SAML 2.0 |
      | Project Version | marketplace-saml-6.0.0-7310           |
      | Git Hash        | 7d1b6e8595                            |
      | Patcher Status  | Complete                              |
      | Type            | Patch                                 |
      | Jenkins         | fixpack-builder-agent(uat)            |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Workaround" with the following values
      | Content         | ${staticFixContent2}                  |
      | Version         | 1.0                                   |
      | Product         | DXP 7.3 Liferay Connector to SAML 2.0 |
      | Project Version | marketplace-saml-6.0.0-7310           |
      | Git Hash        | 7a4424cba3                            |
      | Patcher Status  | Complete                              |
      | Type            | Workaround                            |
      | Jenkins         | fixpack-builder-agent(uat)            |
    And I should be able to view a Patcher fix containing the content "${staticFixContent3}" and type "Workaround" with the following values
      | Content         | ${staticFixContent3}                  |
      | Version         | 1.0                                   |
      | Product         | DXP 7.3 Liferay Connector to SAML 2.0 |
      | Project Version | marketplace-saml-6.0.0-7310           |
      | Git Hash        | 07f194b8f5                            |
      | Patcher Status  | Complete                              |
      | Type            | Workaround                            |
      | Jenkins         | fixpack-builder-agent(uat)            |
    And I should be able to view a Patcher fix containing the content "${staticFixContent4}" and type "Patch" with the following values
      | Content         | ${staticFixContent4}                  |
      | Version         | 1.0                                   |
      | Product         | DXP 7.3 Liferay Connector to SAML 2.0 |
      | Project Version | marketplace-saml-6.0.0-7310           |
      | Git Hash        | 2236bb4b26                            |
      | Patcher Status  | Complete                              |
      | Type            | Patch                                 |
      | Jenkins         | fixpack-builder-agent(uat)            |
    And I should be able to view a Patcher fix containing the content "${staticFixContent5}" and type "Patch" with the following values
      | Content         | ${staticFixContent5}                  |
      | Version         | 1.0                                   |
      | Product         | DXP 7.3 Liferay Connector to SAML 2.0 |
      | Project Version | marketplace-saml-6.0.0-7310           |
      | Git Hash        | 4314034e59                            |
      | Patcher Status  | Complete                              |
      | Type            | Patch                                 |
      | Jenkins         | fixpack-builder-agent(uat)            |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.3 Liferay Connector to SAML 2.0                                                                    |
      | Project Version | marketplace-saml-6.0.0-7310                                                                              |
      | Tickets List    | ${staticFixContent1},${staticFixContent2},${staticFixContent3},${staticFixContent4},${staticFixContent5} |
      | Account Code    | SOGEINOIPA                                                                                               |
      | Support Ticket  | ${staticTicketNumber}                                                                                    |
    And I set the account code "SOGEINOIPA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "SOGEINOIPA" with the following values
      | Type            | Official                    |
      | Support Ticket  | ${staticTicketNumber}       |
      | Version         | 1.0                         |
      | Project Version | marketplace-saml-6.0.0-7310 |
      | Content         | 0 Fix Packs + 5 Tickets     |
      | Patcher Status  | Merging                     |
    And I should be able to view a Patcher build fix with the name "${staticFixContent1}" for the ticket "${staticTicketNumber}" on the account "SOGEINOIPA" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | 7d1b6e8595           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher build fix with the name "${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "SOGEINOIPA" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | 7a4424cba3           |
      | Status   | Complete             |
      | Type     | Workaround           |
    And I should be able to view a Patcher build fix with the name "${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "SOGEINOIPA" with the following values
      | Name     | ${staticFixContent3} |
      | Version  | 1.0                  |
      | Git Hash | 07f194b8f5           |
      | Status   | Complete             |
      | Type     | Workaround           |
    And I should be able to view a Patcher build fix with the name "${staticFixContent4}" for the ticket "${staticTicketNumber}" on the account "SOGEINOIPA" with the following values
      | Name     | ${staticFixContent4} |
      | Version  | 1.0                  |
      | Git Hash | 2236bb4b26           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher build fix with the name "${staticFixContent5}" for the ticket "${staticTicketNumber}" on the account "SOGEINOIPA" with the following values
      | Name     | ${staticFixContent5} |
      | Version  | 1.0                  |
      | Git Hash | 4314034e59           |
      | Status   | Complete             |
      | Type     | Patch                |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "SOGEINOIPA" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "SOGEINOIPA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "SOGEINOIPA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | marketplace-saml-6.0.0-7310    |
      | Content         | 0 Fix Packs + 5 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Create a 70 fix pack de 31 build
    @priority = "2"
    As a TSE, I should be able to create a 7.0 fix pack de 31 build
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0                                                         |
      | Project Version | fix-pack-de-31-7010                                             |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | LPS-87074-70fp31                                                |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0                                                         |
      | Project Version | fix-pack-de-31-7010                                             |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | LPS-107154-LPS-107323                                           |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1} |
      | Version         | 1.0                  |
      | Product         | DXP 7.0              |
      | Project Version | fix-pack-de-31-7010  |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2} |
      | Version         | 1.0                  |
      | Product         | DXP 7.0              |
      | Project Version | fix-pack-de-31-7010  |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.0                    |
      | Project Version | fix-pack-de-31-7010        |
      | Git Hash        | 6bb7f4287f                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.0                    |
      | Project Version | fix-pack-de-31-7010        |
      | Git Hash        | ded0b9a6fa                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.0                                   |
      | Project Version | fix-pack-de-31-7010                       |
      | Tickets List    | ${staticFixContent1},${staticFixContent2} |
      | Account Code    | HITACHIGP                                 |
      | Support Ticket  | ${staticTicketNumber}                     |
    And I set the account code "HITACHIGP" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-de-31-7010     |
      | Content         | 0 Fix Packs + 2 Tickets |
      | Patcher Status  | Merging                 |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}, ${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Version         | 1.0                                        |
      | Name            | ${staticFixContent1}, ${staticFixContent2} |
      | Project Version | fix-pack-de-31-7010                        |
      | Patcher Status  | Merging Only                               |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent1}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | 6bb7f4287f           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent2}" for the child build with the name "${staticFixContent1}, ${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | ded0b9a6fa           |
      | Status   | Complete             |
      | Type     | Patch                |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-de-31-7010            |
      | Content         | 0 Fix Packs + 2 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}, ${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Version         | 1.0                                        |
      | Name            | ${staticFixContent1}, ${staticFixContent2} |
      | Project Version | fix-pack-de-31-7010                        |
      | Patcher Status  | Complete Merging Only                      |
      | Jenkins         | fixpack-builder-agent(uat)                 |
      | Jenkins         | fixpack-builder-hotfix-7x(uat)             |

  Scenario: Create a 73 fix pack dxp 1 build
    @priority = "5"
    As a TSE, I should be able to create a 7.3 fix pack dxp 1 build
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I generate random fix content for the third fix
    And I generate random fix content for the fourth fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-dxp-1-7310                                             |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | 7310fp01_LPS-94011                                              |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
      | Workaround      | true                                                            |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-dxp-1-7310                                             |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | 7310FP01                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
      | Workaround      | true                                                            |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-dxp-1-7310                                             |
      | Content         | ${staticFixContent3}                                            |
      | Branch Name     | LPS-127040_dxp-1                                                |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-dxp-1-7310                                             |
      | Content         | ${staticFixContent4}                                            |
      | Branch Name     | LRP-1615_dxp-1-7310                                             |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Workaround" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Workaround" for add completion
    And I check the Patcher fix containing the content "${staticFixContent3}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent4}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Workaround" with the following values
      | Content         | ${staticFixContent1}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-dxp-1-7310        |
      | Git Hash        | 439fdf4bb4                 |
      | Patcher Status  | Complete                   |
      | Type            | Workaround                 |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Workaround" with the following values
      | Content         | ${staticFixContent2}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-dxp-1-7310        |
      | Git Hash        | 638bb33d50                 |
      | Patcher Status  | Complete                   |
      | Type            | Workaround                 |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent3}" and type "Patch" with the following values
      | Content         | ${staticFixContent3}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-dxp-1-7310        |
      | Git Hash        | 418aa161c0                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent4}" and type "Patch" with the following values
      | Content         | ${staticFixContent4}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-dxp-1-7310        |
      | Git Hash        | 7de99a3239                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.3                                                                             |
      | Project Version | fix-pack-dxp-1-7310                                                                 |
      | Tickets List    | ${staticFixContent1},${staticFixContent2},${staticFixContent3},${staticFixContent4} |
      | Account Code    | SOGEINOIPA                                                                          |
      | Support Ticket  | ${staticTicketNumber}                                                               |
    And I set the account code "SOGEINOIPA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "SOGEINOIPA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-dxp-1-7310     |
      | Content         | 0 Fix Packs + 4 Tickets |
      | Patcher Status  | Merging                 |
    And I should be able to view a Patcher build fix with the name "${staticFixContent1}" for the ticket "${staticTicketNumber}" on the account "SOGEINOIPA" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | 439fdf4bb4           |
      | Status   | Complete             |
      | Type     | Workaround           |
    And I should be able to view a Patcher build fix with the name "${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "SOGEINOIPA" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | 638bb33d50           |
      | Status   | Complete             |
      | Type     | Workaround           |
    And I should be able to view a Patcher build fix with the name "${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "SOGEINOIPA" with the following values
      | Name     | ${staticFixContent3} |
      | Version  | 1.0                  |
      | Git Hash | 418aa161c0           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher build fix with the name "${staticFixContent4}" for the ticket "${staticTicketNumber}" on the account "SOGEINOIPA" with the following values
      | Name     | ${staticFixContent4} |
      | Version  | 1.0                  |
      | Git Hash | 7de99a3239           |
      | Status   | Complete             |
      | Type     | Patch                |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "SOGEINOIPA" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "SOGEINOIPA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "SOGEINOIPA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-dxp-1-7310            |
      | Content         | 0 Fix Packs + 4 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Create 73 fix pack dxp 1 fixes and merge on fix pack dxp 1 project version
    @priority = "5"
    As a TSE, I should be able to create 73 pack dxp 1 fixes and merge on fix pack dxp 1 project version
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-dxp-1-7310                                             |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | LRPQA-1134                                                      |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-dxp-1-7310                                             |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | LPP-40716                                                       |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
      | Workaround      | true                                                            |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1} |
      | Version         | 1.0                  |
      | Product         | DXP 7.3              |
      | Project Version | fix-pack-dxp-1-7310  |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Workaround" with the following values
      | Content         | ${staticFixContent2} |
      | Version         | 1.0                  |
      | Product         | DXP 7.3              |
      | Project Version | fix-pack-dxp-1-7310  |
      | Patcher Status  | Adding               |
      | Type            | Workaround           |
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Workaround" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-dxp-1-7310        |
      | Git Hash        | 9d3ecc1783                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Workaround" with the following values
      | Content         | ${staticFixContent2}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-dxp-1-7310        |
      | Git Hash        | 318eff8258                 |
      | Patcher Status  | Complete                   |
      | Type            | Workaround                 |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.3                                   |
      | Project Version | fix-pack-dxp-1-7310                       |
      | Tickets List    | ${staticFixContent1},${staticFixContent2} |
      | Account Code    | STVINTRA                                  |
      | Support Ticket  | ${staticTicketNumber}                     |
      | Merge Only      | true                                      |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-dxp-1-7310     |
      | Content         | 0 Fix Packs + 2 Tickets |
      | Patcher Status  | Merging Only            |
    And I should be able to view a Patcher build fix with the name "${staticFixContent1}" for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | 9d3ecc1783           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher build fix with the name "${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | 318eff8258           |
      | Status   | Complete             |
      | Type     | Workaround           |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" and "fixpack-builder-agent(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                   |
      | Support Ticket  | ${staticTicketNumber}      |
      | Version         | 1.0                        |
      | Project Version | fix-pack-dxp-1-7310        |
      | Content         | 0 Fix Packs + 2 Tickets    |
      | Patcher Status  | Complete Merging Only      |
      | Jenkins         | fixpack-builder-agent(uat) |

  Scenario: Create 73 pack pack base fixes and merge on fix pack base project version
    @priority = "5"
    As a TSE, I should be able to Create 73 pack pack base fixes and merge on fix pack base project version
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-base-7310                                              |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | LPS-129394_BASE                                                 |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-base-7310                                              |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | LRP-1134                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1} |
      | Version         | 1.0                  |
      | Product         | DXP 7.3              |
      | Project Version | fix-pack-base-7310   |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2} |
      | Version         | 1.0                  |
      | Product         | DXP 7.3              |
      | Project Version | fix-pack-base-7310   |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-base-7310         |
      | Git Hash        | 28a7bf5868                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-base-7310         |
      | Git Hash        | 1b1ff95535                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.3                                   |
      | Project Version | fix-pack-base-7310                        |
      | Tickets List    | ${staticFixContent1},${staticFixContent2} |
      | Account Code    | STVINTRA                                  |
      | Support Ticket  | ${staticTicketNumber}                     |
      | Merge Only      | true                                      |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-base-7310      |
      | Content         | 0 Fix Packs + 2 Tickets |
      | Patcher Status  | Merging Only            |
    And I should be able to view a Patcher build fix with the name "${staticFixContent1}" for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | 28a7bf5868           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher build fix with the name "${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | 1b1ff95535           |
      | Status   | Complete             |
      | Type     | Patch                |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" and "fixpack-builder-agent(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                   |
      | Support Ticket  | ${staticTicketNumber}      |
      | Version         | 1.0                        |
      | Project Version | fix-pack-base-7310         |
      | Content         | 0 Fix Packs + 2 Tickets    |
      | Patcher Status  | Complete Merging Only      |
      | Jenkins         | fixpack-builder-agent(uat) |

  Scenario: Check that LPS 99047 populate Troubleshooting ticket suggestions for fix pack dxp 1 7210 builds
    @priority = "5"
    As a TSE, I should be able to check that LPS 99047 populate Troubleshooting ticket suggestions for fix pack dxp 1 7210 builds
    Given I log into the Patcher homepage as the admin user
    And I check build for new account with Troubleshooting tickets with the following values
      | Product                            | DXP 7.2                         |
      | Project Version                    | fix-pack-dxp-1-7210             |
      | Troubleshooting Ticket Suggestions | LPS-99047,LPS-113730,LPS-113733 |

  Scenario: Check that LPS 85987 LPS 88314 and LPS 93497 populate Troubleshooting ticket suggestions for fix pack dxp 1 7110 builds
    @priority = "5"
    As a TSE, I should be able to check that LPS 85987 LPS 88314 and LPS 93497 populate Troubleshooting ticket suggestions for fix pack dxp 1 7110 builds
    Given I log into the Patcher homepage as the admin user
    And I check build for new account with Troubleshooting tickets with the following values
      | Product                            | DXP 7.1                                             |
      | Project Version                    | fix-pack-dxp-1-7110                                 |
      | Troubleshooting Ticket Suggestions | LPS-85987,LPS-88314,LPS-93497,LPS-113730,LPS-113733 |

  Scenario: Check that LPS 85987 LPS 88314 and LPS 93497 populate Troubleshooting ticket suggestions for fix pack dxp 2 7110 builds
    @priority = "5"
    As a TSE, I should be able to check that LPS 85987 LPS 88314 and LPS 93497 populate Troubleshooting ticket suggestions for fix pack dxp 2 7110 builds
    Given I log into the Patcher homepage as the admin user
    And I check build for new account with Troubleshooting tickets with the following values
      | Product                            | DXP 7.1                                             |
      | Project Version                    | fix-pack-dxp-2-7110                                 |
      | Troubleshooting Ticket Suggestions | LPS-85987,LPS-88314,LPS-93497,LPS-113730,LPS-113733 |

  Scenario: Check that LPS 88314 LPS 93497 LPS 92441 and LPS 94011 populate Troubleshooting ticket suggestions for fix pack dxp 3 7110 builds
    @priority = "5"
    As a TSE, I should be able to check that LPS 88314 LPS 93497 LPS 92441 and LPS 94011 populate Troubleshooting ticket suggestions for fix pack dxp 3 7110 builds
    Given I log into the Patcher homepage as the admin user
    And I check build for new account with Troubleshooting tickets with the following values
      | Product                            | DXP 7.1                                                       |
      | Project Version                    | fix-pack-dxp-3-7110                                           |
      | Troubleshooting Ticket Suggestions | LPS-88314,LPS-93497,LPS-92441,LPS-94011,LPS-113730,LPS-113733 |

  Scenario: Check that LPS 88314 LPS 93497 LPS 92441 and LPS 94011 populate Troubleshooting ticket suggestions for fix pack dxp 4 7110 builds
    @priority = "5"
    As a TSE, I should be able to check that LPS 88314 LPS 93497 LPS 92441 and LPS 94011 populate Troubleshooting ticket suggestions for fix pack dxp 4 7110 builds
    Given I log into the Patcher homepage as the admin user
    And I check build for new account with Troubleshooting tickets with the following values
      | Product                            | DXP 7.1                                                       |
      | Project Version                    | fix-pack-dxp-4-7110                                           |
      | Troubleshooting Ticket Suggestions | LPS-88314,LPS-93497,LPS-92441,LPS-94011,LPS-113730,LPS-113733 |

  Scenario: Check that LPS 93497 LPS 92441 and LPS 94011 populate Troubleshooting ticket suggestions for fix pack dxp 5 7110 builds
    @priority = "5"
    As a TSE, I should be able to check that LPS 93497 LPS 92441 and LPS 94011 populate Troubleshooting ticket suggestions for fix pack dxp 5 7110 builds
    Given I log into the Patcher homepage as the admin user
    And I check build for new account with Troubleshooting tickets with the following values
      | Product                            | DXP 7.1                                             |
      | Project Version                    | fix-pack-dxp-5-7110                                 |
      | Troubleshooting Ticket Suggestions | LPS-93497,LPS-92441,LPS-94011,LPS-113730,LPS-113733 |

  Scenario: Check that LPS 93497 LPS 92441 and LPS 94011 populate Troubleshooting ticket suggestions for fix pack dxp 6 7110 builds
    @priority = "5"
    As a TSE, I should be able to check that LPS 93497 LPS 92441 and LPS 94011 populate Troubleshooting ticket suggestions for fix pack dxp 6 7110 builds
    Given I log into the Patcher homepage as the admin user
    And I check build for new account with Troubleshooting tickets with the following values
      | Product                            | DXP 7.1                                             |
      | Project Version                    | fix-pack-dxp-6-7110                                 |
      | Troubleshooting Ticket Suggestions | LPS-93497,LPS-92441,LPS-94011,LPS-113730,LPS-113733 |

  Scenario: Check that LPS 93497 LPS 92441 and LPS 94011 populate Troubleshooting ticket suggestions for fix pack dxp 7 7110 builds
    @priority = "5"
    As a TSE, I should be able to check that LPS 93497 LPS 92441 and LPS 94011 populate Troubleshooting ticket suggestions for fix pack dxp 7 7110 builds
    Given I log into the Patcher homepage as the admin user
    And I check build for new account with Troubleshooting tickets with the following values
      | Product                            | DXP 7.1                                             |
      | Project Version                    | fix-pack-dxp-7-7110                                 |
      | Troubleshooting Ticket Suggestions | LPS-93497,LPS-92441,LPS-94011,LPS-113730,LPS-113733 |

  Scenario: Check that LPS 93497 LPS92441 and LPS 94011 populate Troubleshooting ticket suggestions for fix pack dxp 8 7110 builds
    @priority = "5"
    As a TSE, I should be able to check that LPS 93497 LPS92441 and LPS 94011 populate Troubleshooting ticket suggestions for fix pack dxp 8 7110 builds
    Given I log into the Patcher homepage as the admin user
    And I check build for new account with Troubleshooting tickets with the following values
      | Product                            | DXP 7.1                                             |
      | Project Version                    | fix-pack-dxp-8-7110                                 |
      | Troubleshooting Ticket Suggestions | LPS-93497,LPS-92441,LPS-94011,LPS-113730,LPS-113733 |

  Scenario: Check that LPS 93497 LPS92441 and LPS 94011 populate Troubleshooting ticket suggestions for fix pack dxp 9 7110 builds
    @priority = "5"
    As a TSE, I should be able to check that LPS 93497 LPS92441 and LPS 94011 populate Troubleshooting ticket suggestions for fix pack dxp 9 7110 builds
    Given I log into the Patcher homepage as the admin user
    And I check build for new account with Troubleshooting tickets with the following values
      | Product                            | DXP 7.1                                             |
      | Project Version                    | fix-pack-dxp-9-7110                                 |
      | Troubleshooting Ticket Suggestions | LPS-93497,LPS-92441,LPS-94011,LPS-113730,LPS-113733 |

  Scenario: Check that LPS 93497 LPS92441 and LPS 94011 populate Troubleshooting ticket suggestions for fix pack dxp 10 7110 builds
    @priority = "5"
    As a TSE, I should be able to check that LPS 93497 LPS92441 and LPS 94011 populate Troubleshooting ticket suggestions for fix pack dxp 10 7110 builds
    Given I log into the Patcher homepage as the admin user
    And I check build for new account with Troubleshooting tickets with the following values
      | Product                            | DXP 7.1                                             |
      | Project Version                    | fix-pack-dxp-10-7110                                |
      | Troubleshooting Ticket Suggestions | LPS-93497,LPS-92441,LPS-94011,LPS-113730,LPS-113733 |

  Scenario: Check that LPS 93497 and LPS 94011 populate Troubleshooting ticket suggestions for fix pack dxp 11 7110 builds
    @priority = "5"
    As a TSE, I should be able to check that LPS 93497 and LPS 94011 populate Troubleshooting ticket suggestions for fix pack dxp 11 7110 builds
    Given I log into the Patcher homepage as the admin user
    And I check build for new account with Troubleshooting tickets with the following values
      | Product                            | DXP 7.1                                   |
      | Project Version                    | fix-pack-dxp-11-7110                      |
      | Troubleshooting Ticket Suggestions | LPS-93497,LPS-94011,LPS-113730,LPS-113733 |

  Scenario: Check that LPS 93497 and LPS 94011 populate Troubleshooting ticket suggestions for fix pack dxp 12 7110 builds
    @priority = "5"
    As a TSE, I should be able to check that LPS 93497 and LPS 94011 populate Troubleshooting ticket suggestions for fix pack dxp 12 7110 builds
    Given I log into the Patcher homepage as the admin user
    And I check build for new account with Troubleshooting tickets with the following values
      | Product                            | DXP 7.1                                   |
      | Project Version                    | fix-pack-dxp-12-7110                      |
      | Troubleshooting Ticket Suggestions | LPS-93497,LPS-94011,LPS-113730,LPS-113733 |

  Scenario: Check that LPE 16890 and LPS 94011 populate Troubleshooting ticket suggestions for fix pack dxp 13 7110 builds
    @priority = "5"
    As a TSE, I should be able to check that LPE 16890 and LPS 94011 populate Troubleshooting ticket suggestions for fix pack dxp 13 7110 builds
    Given I log into the Patcher homepage as the admin user
    And I check build for new account with Troubleshooting tickets with the following values
      | Product                            | DXP 7.1                                   |
      | Project Version                    | fix-pack-dxp-13-7110                      |
      | Troubleshooting Ticket Suggestions | LPE-16890,LPS-94011,LPS-113730,LPS-113733 |

  Scenario: Check that LPE 16890 and LPS 94011 populate Troubleshooting ticket suggestions for fix pack dxp 14 7110 builds
    @priority = "5"
    As a TSE, I should be able to check that LPS 93497 LPS92441 and LPS 94011 populate Troubleshooting ticket suggestions for fix pack dxp 14 7110 builds
    Given I log into the Patcher homepage as the admin user
    And I check build for new account with Troubleshooting tickets with the following values
      | Product                            | DXP 7.1                                   |
      | Project Version                    | fix-pack-dxp-14-7110                      |
      | Troubleshooting Ticket Suggestions | LPE-16890,LPS-94011,LPS-113730,LPS-113733 |

  Scenario: Check that LPS 85987 populate Troubleshooting ticket suggestions for fix pack de 28 7010 builds
    @priority = "5"
    As a TSE, I should be able to check that LPS 85987 populate Troubleshooting ticket suggestions for fix pack de 28 7010 builds
    Given I log into the Patcher homepage as the admin user
    And I check build for new account with Troubleshooting tickets with the following values
      | Product                            | DXP 7.0                         |
      | Project Version                    | fix-pack-de-28-7010             |
      | Tickets List                       | LPS-78037                       |
      | Troubleshooting Ticket Suggestions | LPS-85987,LPS-107154,LPS-107323 |

  Scenario: Check that LPS 85987 populate Troubleshooting ticket suggestions for fix pack de 27 7010 builds
    @priority = "5"
    As a TSE, I should be able to check that LPS 85987 populate Troubleshooting ticket suggestions for fix pack de 27 7010 builds
    Given I log into the Patcher homepage as the admin user
    And I check build for new account with Troubleshooting tickets with the following values
      | Product                            | DXP 7.0                         |
      | Project Version                    | fix-pack-de-27-7010             |
      | Tickets List                       | LPS-78037                       |
      | Troubleshooting Ticket Suggestions | LPS-85987,LPS-107154,LPS-107323 |

  Scenario: Check that LPS 85987 populate Troubleshooting ticket suggestions for fix pack de 1 7010 builds
    @priority = "5"
    As a TSE, I should be able to check that LPS 85987 populate Troubleshooting ticket suggestions for fix pack de 1 7010 builds
    Given I log into the Patcher homepage as the admin user
    And I check build for new account with Troubleshooting tickets with the following values
      | Product                            | DXP 7.0                         |
      | Project Version                    | fix-pack-de-1-7010              |
      | Tickets List                       | LPS-78037                       |
      | Troubleshooting Ticket Suggestions | LPS-85987,LPS-107154,LPS-107323 |

  Scenario: Check that LPS 85987 populate Troubleshooting ticket suggestions for fix pack de 10 7010 builds
    @priority = "5"
    As a TSE, I should be able to check that LPS 85987 populate Troubleshooting ticket suggestions for fix pack de 10 7010 builds
    Given I log into the Patcher homepage as the admin user
    And I check build for new account with Troubleshooting tickets with the following values
      | Product                            | DXP 7.0                         |
      | Project Version                    | fix-pack-de-10-7010             |
      | Tickets List                       | LPS-78037                       |
      | Troubleshooting Ticket Suggestions | LPS-85987,LPS-107154,LPS-107323 |

  Scenario: Check that LPS 85987 populate Troubleshooting ticket suggestions for fix pack de 20 7010 builds
    @priority = "5"
    As a TSE, I should be able to check that LPS 85987 populate Troubleshooting ticket suggestions for fix pack de 20 7010 builds
    Given I log into the Patcher homepage as the admin user
    And I check build for new account with Troubleshooting tickets with the following values
      | Product                            | DXP 7.0                         |
      | Project Version                    | fix-pack-de-20-7010             |
      | Tickets List                       | LPS-78037                       |
      | Troubleshooting Ticket Suggestions | LPS-85987,LPS-107154,LPS-107323 |

  Scenario: Check that LPS 85987 populate Troubleshooting ticket suggestions for fix pack de 35 7010 builds
    @priority = "5"
    As a TSE, I should be able to check that LPS 85987 populate Troubleshooting ticket suggestions for fix pack de 35 7010 builds
    Given I log into the Patcher homepage as the admin user
    And I check build for new account with Troubleshooting tickets with the following values
      | Product                            | DXP 7.0                         |
      | Project Version                    | fix-pack-de-35-7010             |
      | Tickets List                       | LPS-78037                       |
      | Troubleshooting Ticket Suggestions | LPS-85987,LPS-107154,LPS-107323 |

  Scenario: Check that LPS 85987 populate Troubleshooting ticket suggestions for fix pack de 45 7010 builds
    @priority = "5"
    As a TSE, I should be able to check that LPS 85987 populate Troubleshooting ticket suggestions for fix pack de 45 7010 builds
    Given I log into the Patcher homepage as the admin user
    And I check build for new account with Troubleshooting tickets with the following values
      | Product                            | DXP 7.0                         |
      | Project Version                    | fix-pack-de-45-7010             |
      | Tickets List                       | LPS-78037                       |
      | Troubleshooting Ticket Suggestions | LPS-85987,LPS-107154,LPS-107323 |

  Scenario: Check that LPS 85987 does not populate Troubleshooting ticket suggestions for fix pack de 46 7010 builds
    @priority = "5"
    As a TSE, I should not be able to check that LPS 85987 populate Troubleshooting ticket suggestions for fix pack de 46 7010 builds
    Given I log into the Patcher homepage as the admin user
    And I check build for new account with Troubleshooting tickets with the following values
      | Product                            | DXP 7.0               |
      | Project Version                    | fix-pack-de-46-7010   |
      | Tickets List                       | LPS-78037             |
      | Troubleshooting Ticket Suggestions | LPS-107154,LPS-107323 |

  Scenario: Check that LPS 107276 populate Troubleshooting ticket suggestions for fix pack de 88 7010 builds
    @priority = "5"
    As a TSE, I should be able to check that LPS 107276 populate Troubleshooting ticket suggestions for fix pack de 88 7010 builds
    Given I log into the Patcher homepage as the admin user
    And I check build for new account with Troubleshooting tickets with the following values
      | Product                            | DXP 7.0                          |
      | Project Version                    | fix-pack-de-88-7010              |
      | Tickets List                       | LPS-78037                        |
      | Troubleshooting Ticket Suggestions | LPS-107276,LPS-107154,LPS-107323 |

  Scenario: Check that LPS 107276 does not populate Troubleshooting ticket suggestions for fix pack de 87 7010 builds
    @priority = "5"
    As a TSE, I should not be able to check that LPS 107276 populate Troubleshooting ticket suggestions for fix pack de 87 7010 builds
    Given I log into the Patcher homepage as the admin user
    And I check build for new account with Troubleshooting tickets with the following values
      | Product                            | DXP 7.0               |
      | Project Version                    | fix-pack-de-87-7010   |
      | Tickets List                       | LPS-78037             |
      | Troubleshooting Ticket Suggestions | LPS-107154,LPS-107323 |

  Scenario: Check that no tickets populate Troubleshooting ticket suggestions for fix pack de 89 7010 builds
    @priority = "5"
    As a TSE, I should be able to check that no tickets populate Troubleshooting ticket suggestions for fix pack de 89 7010 builds
    Given I log into the Patcher homepage as the admin user
    And I check build for new account with Troubleshooting tickets with the following values
      | Product                            | DXP 7.0              |
      | Project Version                    | fix-pack-de-89-7010  |

  Scenario: Check for successful DXP 72 build creation
    @priority = "4"
    As a TSE, I should be able to check for successful DXP 72 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.2                                                         |
      | Project Version | fix-pack-base-7210                                              |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | fpf1-168475534                                                  |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1} |
      | Version         | 1.0                  |
      | Product         | DXP 7.2              |
      | Project Version | fix-pack-base-7210   |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.2                    |
      | Project Version | fix-pack-base-7210         |
      | Git Hash        | 17d30328d3                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product                            | DXP 7.2                         |
      | Project Version                    | fix-pack-base-7210              |
      | Tickets List                       | ${staticFixContent1}            |
      | Troubleshooting Ticket Suggestions | LPS-93497,LPS-113730,LPS-113733 |
      | Account Code                       | HITACHIGP                       |
      | Support Ticket                     | ${staticTicketNumber}           |
    And I set the account code "HITACHIGP" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-base-7210      |
      | Content         | 0 Fix Packs + 4 Tickets |
      | Patcher Status  | Merging                 |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}, LPS-113730, LPS-113733, LPS-93497" for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Version         | 1.0                                                     |
      | Name            | ${staticFixContent1}, LPS-113730, LPS-113733, LPS-93497 |
      | Project Version | fix-pack-base-7210                                      |
      | Patcher Status  | Merging Only                                            |
    And I should be able to view a Patcher child build fix with the name "LPS-93497" for the child build with the name "${staticFixContent1}, LPS-113730, LPS-113733, LPS-93497" for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Name     | LPS-93497  |
      | Version  | 1.0        |
      | Git Hash | c15920b890 |
      | Status   | Complete   |
      | Type     | Patch      |
    And I should be able to view a Patcher child build fix with the name "LPS-113730, LPS-113733" for the child build with the name "${staticFixContent1}, LPS-113730, LPS-113733, LPS-93497" for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Name     | LPS-113730, LPS-113733 |
      | Version  | 1.0                    |
      | Git Hash | e8dacbfae9             |
      | Status   | Complete               |
      | Type     | Patch                  |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent1}" for the child build with the name "${staticFixContent1}, LPS-113730, LPS-113733, LPS-93497" for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | 17d30328d3           |
      | Status   | Complete             |
      | Type     | Patch                |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-base-7210             |
      | Content         | 0 Fix Packs + 4 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}, LPS-113730, LPS-113733, LPS-93497" for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Version         | 1.0                                                     |
      | Name            | ${staticFixContent1}, LPS-113730, LPS-113733, LPS-93497 |
      | Project Version | fix-pack-base-7210                                      |
      | Patcher Status  | Complete Merging Only                                   |
      | Jenkins         | fixpack-builder-agent(uat)                              |
      | Jenkins         | fixpack-builder-hotfix-7x(uat)                          |

  Scenario: Check for successful DXP 71 build creation
    @priority = "4"
    As a TSE, I should be able to for successful DXP 71 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.1                                                         |
      | Project Version | fix-pack-base-7110                                              |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | LPS-89343_GA1                                                   |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
      | Workaround      | true                                                            |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Workaround" with the following values
      | Content         | ${staticFixContent1} |
      | Version         | 1.0                  |
      | Product         | DXP 7.1              |
      | Project Version | fix-pack-base-7110   |
      | Patcher Status  | Adding               |
      | Type            | Workaround           |
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Workaround" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Workaround" with the following values
      | Content         | ${staticFixContent1}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.1                    |
      | Project Version | fix-pack-base-7110         |
      | Git Hash        | 65281272e0                 |
      | Patcher Status  | Complete                   |
      | Type            | Workaround                 |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product                            | DXP 7.1                                             |
      | Project Version                    | fix-pack-base-7110                                  |
      | Tickets List                       | ${staticFixContent1}                                |
      | Troubleshooting Ticket Suggestions | LPS-85987,LPS-88314,LPS-93497,LPS-113730,LPS-113733 |
      | Account Code                       | STVINTRA                                            |
      | Support Ticket                     | ${staticTicketNumber}                               |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-base-7110      |
      | Content         | 0 Fix Packs + 6 Tickets |
      | Patcher Status  | Merging                 |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}, LPS-113730, LPS-113733, LPS-85987, LPS-88314, LPS-93497" for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Version         | 1.0                                                                           |
      | Name            | ${staticFixContent1}, LPS-113730, LPS-113733, LPS-85987, LPS-88314, LPS-93497 |
      | Project Version | fix-pack-base-7110                                                            |
      | Patcher Status  | Merging Only                                                                  |
    And I should be able to view a Patcher child build fix with the name "LPS-85987, LPS-88314, LPS-93497" for the child build with the name "${staticFixContent1}, LPS-113730, LPS-113733, LPS-85987, LPS-88314, LPS-93497" for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Name     | LPS-85987, LPS-88314, LPS-93497 |
      | Version  | 1.2                             |
      | Git Hash | 7e6ec8fbff                      |
      | Status   | Complete                        |
      | Type     | Patch                           |
    And I should be able to view a Patcher child build fix with the name "LPS-113730, LPS-113733" for the child build with the name "${staticFixContent1}, LPS-113730, LPS-113733, LPS-85987, LPS-88314, LPS-93497" for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Name     | LPS-113730, LPS-113733 |
      | Version  | 1.0                    |
      | Git Hash | f589f0ad31             |
      | Status   | Complete               |
      | Type     | Patch                  |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent1}" for the child build with the name "${staticFixContent1}, LPS-113730, LPS-113733, LPS-85987, LPS-88314, LPS-93497" for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | 65281272e0           |
      | Status   | Complete             |
      | Type     | Workaround           |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-base-7110             |
      | Content         | 0 Fix Packs + 6 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}, LPS-113730, LPS-113733, LPS-85987, LPS-88314, LPS-93497" for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Version         | 1.0                                                                           |
      | Name            | ${staticFixContent1}, LPS-113730, LPS-113733, LPS-85987, LPS-88314, LPS-93497 |
      | Project Version | fix-pack-base-7110                                                            |
      | Patcher Status  | Complete Merging Only                                                         |
      | Jenkins         | fixpack-builder-agent(uat)                                                    |
      | Jenkins         | fixpack-builder-hotfix-7x(uat)                                                |

  Scenario: Check for successful DXP 70 build creation
    @priority = "4"
    As a TSE, I should be able to for successful DXP 70 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.0                                                         |
      | Project Version | fix-pack-de-45-7010                                             |
      | Content         | ${staticFixContent1},LPS-107154,LPS-107323                      |
      | Branch Name     | LPE-17046                                                       |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1}, LPS-107154, LPS-107323 |
      | Version         | 1.0                                          |
      | Product         | DXP 7.0                                      |
      | Project Version | fix-pack-de-45-7010                          |
      | Patcher Status  | Adding                                       |
      | Type            | Patch                                        |
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1}, LPS-107154, LPS-107323 |
      | Version         | 1.0                                          |
      | Product         | DXP 7.0                                      |
      | Project Version | fix-pack-de-45-7010                          |
      | Git Hash        | fc21fefa42                                   |
      | Patcher Status  | Complete                                     |
      | Type            | Patch                                        |
      | Jenkins         | fixpack-builder-agent(uat)                   |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product                            | DXP 7.0               |
      | Project Version                    | fix-pack-de-45-7010   |
      | Tickets List                       | ${staticFixContent1}  |
      | Troubleshooting Ticket Suggestions | LPS-107154,LPS-107323 |
      | Account Code                       | SKODAINTRANET         |
      | Support Ticket                     | ${staticTicketNumber} |
    And I set the account code "SKODAINTRANET" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "SKODAINTRANET" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-de-45-7010     |
      | Content         | 0 Fix Packs + 3 Tickets |
      | Patcher Status  | Merging                 |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}, LPS-107154, LPS-107323" for the ticket "${staticTicketNumber}" on the account "SKODAINTRANET" with the following values
      | Version         | 1.0                                          |
      | Name            | ${staticFixContent1}, LPS-107154, LPS-107323 |
      | Project Version | fix-pack-de-45-7010                          |
      | Patcher Status  | Merging Only                                 |
    And I should be able to view a Patcher child build fix with the name "${staticFixContent1}, LPS-107154, LPS-107323" for the child build with the name "${staticFixContent1}" for the ticket "${staticTicketNumber}" on the account "SKODAINTRANET" with the following values
      | Name     | ${staticFixContent1}, LPS-107154, LPS-107323 |
      | Version  | 1.0                                          |
      | Git Hash | fc21fefa42                                   |
      | Status   | Complete                                     |
      | Type     | Patch                                        |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "SKODAINTRANET" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "SKODAINTRANET" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "SKODAINTRANET" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-de-45-7010            |
      | Content         | 0 Fix Packs + 3 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |
    And I should be able to view a Patcher child build with the name "${staticFixContent1}" for the ticket "${staticTicketNumber}, LPS-107154, LPS-107323" on the account "SKODAINTRANET" with the following values
      | Version         | 1.0                                          |
      | Name            | ${staticFixContent1}, LPS-107154, LPS-107323 |
      | Project Version | fix-pack-de-45-7010                          |
      | Patcher Status  | Complete                                     |
      | Jenkins         | fixpack-builder-agent(uat)                   |
      | Jenkins         | fixpack-builder-hotfix-7x(uat)               |

  Scenario: Check for successful DXP 73 build creation
    @priority = "4"
    As a TSE, I should be able to for successful DXP 73 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-base-7310                                              |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | LRP-1134                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-base-7310                                              |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | LPS-134572-LPS-142141-LRP-1615                                  |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1} |
      | Version         | 1.0                  |
      | Product         | DXP 7.3              |
      | Project Version | fix-pack-base-7310   |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2} |
      | Version         | 1.0                  |
      | Product         | DXP 7.3              |
      | Project Version | fix-pack-base-7310   |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-base-7310         |
      | Git Hash        | 1b1ff95535                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-base-7310         |
      | Git Hash        | 67acfe7a4d                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product                            | DXP 7.3                                   |
      | Project Version                    | fix-pack-base-7310                        |
      | Tickets List                       | ${staticFixContent1},${staticFixContent2} |
      | Troubleshooting Ticket Suggestions | LPS-121824,RELEASE-3097,RELEASE-3128      |
      | Account Code                       | INTUITIVE                                 |
      | Support Ticket                     | ${staticTicketNumber}                     |
    And I set the account code "INTUITIVE" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-base-7310      |
      | Content         | 0 Fix Packs + 5 Tickets |
      | Patcher Status  | Merging                 |
    And I should be able to view a Patcher build fix with the name "${staticFixContent1}" for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | 1b1ff95535           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher build fix with the name "${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | 67acfe7a4d           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher build fix with the name "RELEASE-3128" for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Name     | RELEASE-3128 |
      | Version  | 1.0          |
      | Git Hash | 0d5b82dd41   |
      | Status   | Complete     |
      | Type     | Patch        |
    And I should be able to view a Patcher build fix with the name "LPS-121824" for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Name     | LPS-121824 |
      | Version  | 1.1        |
      | Git Hash | 8f3feeb2e4 |
      | Status   | Complete   |
      | Type     | Patch      |
    And I should be able to view a Patcher build fix with the name "RELEASE-3097" for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Name     | RELEASE-3097 |
      | Version  | 1.1          |
      | Git Hash | a026064a73   |
      | Status   | Complete     |
      | Type     | Patch        |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-base-7310             |
      | Content         | 0 Fix Packs + 5 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Create a 73 fix pack dxp 2 build
    @priority = "5"
    As a TSE, I should be able to create a 73 fix pack dxp 2 build
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I generate random fix content for the third fix
    And I generate random fix content for the fourth fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-dxp-2-7310                                             |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | LPSQA-134777-dxp-2-7310                                         |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-dxp-2-7310                                             |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | LPS-94011                                                       |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
      | Workaround      | true                                                            |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-dxp-2-7310                                             |
      | Content         | ${staticFixContent3}                                            |
      | Branch Name     | LRP-1615-dxp2                                                   |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-dxp-2-7310                                             |
      | Content         | ${staticFixContent4}                                            |
      | Branch Name     | LRP-1603                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Workaround" for add completion
    And I check the Patcher fix containing the content "${staticFixContent3}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent4}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-dxp-2-7310        |
      | Git Hash        | d666e66427                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Workaround" with the following values
      | Content         | ${staticFixContent2}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-dxp-2-7310        |
      | Git Hash        | 6a060a5d07                 |
      | Patcher Status  | Complete                   |
      | Type            | Workaround                 |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent3}" and type "Patch" with the following values
      | Content         | ${staticFixContent3}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-dxp-2-7310        |
      | Git Hash        | c28c935fe0                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent4}" and type "Patch" with the following values
      | Content         | ${staticFixContent4}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-dxp-2-7310        |
      | Git Hash        | 17019bfe92                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.3                                                                             |
      | Project Version | fix-pack-dxp-2-7310                                                                 |
      | Tickets List    | ${staticFixContent1},${staticFixContent2},${staticFixContent3},${staticFixContent4} |
      | Account Code    | HITACHIGP                                                                           |
      | Support Ticket  | ${staticTicketNumber}                                                               |
    And I set the account code "HITACHIGP" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-dxp-2-7310     |
      | Content         | 0 Fix Packs + 4 Tickets |
      | Patcher Status  | Merging                 |
    And I should be able to view a Patcher build fix with the name "${staticFixContent1}" for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | d666e66427           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher build fix with the name "${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | 6a060a5d07           |
      | Status   | Complete             |
      | Type     | Workaround           |
    And I should be able to view a Patcher build fix with the name "${staticFixContent3}" for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Name     | ${staticFixContent3} |
      | Version  | 1.0                  |
      | Git Hash | c28c935fe0           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher build fix with the name "${staticFixContent4}" for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Name     | ${staticFixContent4} |
      | Version  | 1.0                  |
      | Git Hash | 17019bfe92           |
      | Status   | Complete             |
      | Type     | Patch                |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-dxp-2-7310            |
      | Content         | 0 Fix Packs + 4 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Create 73 fix pack dxp 2 fixes and merge on fix pack dxp 2 project version
    @priority = "5"
    As a TSE, I should be able to create 73 fix pack dxp 2 fixes and merge on fix pack dxp 2 project version
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-dxp-2-7310                                             |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | LPSQA-134777-dxp-2-7310                                         |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-dxp-2-7310                                             |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | LPS-94011                                                       |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
      | Workaround      | true                                                            |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1} |
      | Version         | 1.0                  |
      | Product         | DXP 7.3              |
      | Project Version | fix-pack-dxp-2-7310  |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Workaround" with the following values
      | Content         | ${staticFixContent2} |
      | Version         | 1.0                  |
      | Product         | DXP 7.3              |
      | Project Version | fix-pack-dxp-2-7310  |
      | Patcher Status  | Adding               |
      | Type            | Workaround           |
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Workaround" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-dxp-2-7310        |
      | Git Hash        | d666e66427                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Workaround" with the following values
      | Content         | ${staticFixContent2}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-dxp-2-7310        |
      | Git Hash        | 6a060a5d07                 |
      | Patcher Status  | Complete                   |
      | Type            | Workaround                 |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.3                                   |
      | Project Version | fix-pack-dxp-2-7310                       |
      | Tickets List    | ${staticFixContent1},${staticFixContent2} |
      | Account Code    | STVINTRA                                  |
      | Support Ticket  | ${staticTicketNumber}                     |
      | Merge Only      | true                                      |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-dxp-2-7310     |
      | Content         | 0 Fix Packs + 2 Tickets |
      | Patcher Status  | Merging Only            |
    And I should be able to view a Patcher build fix with the name "${staticFixContent1}" for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | d666e66427           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher build fix with the name "${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | 6a060a5d07           |
      | Status   | Complete             |
      | Type     | Workaround           |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" and "fixpack-builder-agent(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                   |
      | Support Ticket  | ${staticTicketNumber}      |
      | Version         | 1.0                        |
      | Project Version | fix-pack-dxp-2-7310        |
      | Content         | 0 Fix Packs + 2 Tickets    |
      | Patcher Status  | Complete Merging Only      |
      | Jenkins         | fixpack-builder-agent(uat) |

  Scenario: Create 73 fix pack dxp 1 and dxp 2 fixes and merge on fix pack dxp 2 project version
    @priority = "5"
    As a TSE, I should be able to create 73 fix pack dxp 1 and dxp 2 fixes and merge on fix pack dxp 2 project version
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-dxp-1-7310                                             |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | 7310fp01_LPS-94011                                              |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
      | Workaround      | true                                                            |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-dxp-2-7310                                             |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | LPS-94011                                                       |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
      | Workaround      | true                                                            |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Workaround" with the following values
      | Content         | ${staticFixContent1} |
      | Version         | 1.0                  |
      | Product         | DXP 7.3              |
      | Project Version | fix-pack-dxp-1-7310  |
      | Patcher Status  | Adding               |
      | Type            | Workaround           |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Workaround" with the following values
      | Content         | ${staticFixContent2} |
      | Version         | 1.0                  |
      | Product         | DXP 7.3              |
      | Project Version | fix-pack-dxp-2-7310  |
      | Patcher Status  | Adding               |
      | Type            | Workaround           |
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Workaround" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Workaround" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Workaround" with the following values
      | Content         | ${staticFixContent1}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-dxp-1-7310        |
      | Git Hash        | 439fdf4bb4                 |
      | Patcher Status  | Complete                   |
      | Type            | Workaround                 |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Workaround" with the following values
      | Content         | ${staticFixContent2}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-dxp-2-7310        |
      | Git Hash        | 6a060a5d07                 |
      | Patcher Status  | Complete                   |
      | Type            | Workaround                 |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.3                                   |
      | Project Version | fix-pack-dxp-2-7310                       |
      | Tickets List    | ${staticFixContent1},${staticFixContent2} |
      | Account Code    | STVINTRA                                  |
      | Support Ticket  | ${staticTicketNumber}                     |
      | Merge Only      | true                                      |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-dxp-2-7310     |
      | Content         | 0 Fix Packs + 2 Tickets |
      | Patcher Status  | Rebasing Merging Only   |
    And I should be able to view a Patcher build fix with the name "${staticFixContent1}" for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Status   | Rebasing             |
      | Type     | Rebase               |
    And I should be able to view a Patcher build fix with the name "${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | 6a060a5d07           |
      | Status   | Complete             |
      | Type     | Workaround           |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for rebase completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" and "fixpack-builder-agent(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                   |
      | Support Ticket  | ${staticTicketNumber}      |
      | Version         | 1.0                        |
      | Project Version | fix-pack-dxp-2-7310        |
      | Content         | 0 Fix Packs + 2 Tickets    |
      | Patcher Status  | Complete Merging Only      |
      | Jenkins         | fixpack-builder-agent(uat) |

  Scenario: Create a 73 Liferay SOAP Connector for SharePoint build
    @priority = "5"
    As a TSE, I should be able to create a 73 Liferay SOAP Connector for SharePoint build
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3 Liferay SOAP Connector for SharePoint                   |
      | Project Version | marketplace-sharepoint-soap-6.0.0-7310                          |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | LPS-135055                                                      |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3 Liferay SOAP Connector for SharePoint                   |
      | Project Version | marketplace-sharepoint-soap-6.0.0-7310                          |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | 142141-sharepoint-soap-6.0.0-7310                               |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1}                          |
      | Version         | 1.0                                           |
      | Product         | DXP 7.3 Liferay SOAP Connector for SharePoint |
      | Project Version | marketplace-sharepoint-soap-6.0.0-7310        |
      | Patcher Status  | Adding                                        |
      | Type            | Patch                                         |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2}                          |
      | Version         | 1.0                                           |
      | Product         | DXP 7.3 Liferay SOAP Connector for SharePoint |
      | Project Version | marketplace-sharepoint-soap-6.0.0-7310        |
      | Patcher Status  | Adding                                        |
      | Type            | Patch                                         |
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1}                          |
      | Version         | 1.0                                           |
      | Product         | DXP 7.3 Liferay SOAP Connector for SharePoint |
      | Project Version | marketplace-sharepoint-soap-6.0.0-7310        |
      | Git Hash        | 4b6cb18c33                                    |
      | Patcher Status  | Complete                                      |
      | Type            | Patch                                         |
      | Jenkins         | fixpack-builder-agent(uat)                    |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2}                          |
      | Version         | 1.0                                           |
      | Product         | DXP 7.3 Liferay SOAP Connector for SharePoint |
      | Project Version | marketplace-sharepoint-soap-6.0.0-7310        |
      | Git Hash        | 233fcc3fd0                                    |
      | Patcher Status  | Complete                                      |
      | Type            | Patch                                         |
      | Jenkins         | fixpack-builder-agent(uat)                    |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.3 Liferay SOAP Connector for SharePoint |
      | Project Version | marketplace-sharepoint-soap-6.0.0-7310        |
      | Tickets List    | ${staticFixContent1},${staticFixContent2}     |
      | Account Code    | LIFERAYSECURITY                               |
      | Support Ticket  | ${staticTicketNumber}                         |
    And I set the account code "LIFERAYSECURITY" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" with the following values
      | Type            | Official                               |
      | Support Ticket  | ${staticTicketNumber}                  |
      | Version         | 1.0                                    |
      | Project Version | marketplace-sharepoint-soap-6.0.0-7310 |
      | Content         | 0 Fix Packs + 2 Tickets                |
      | Patcher Status  | Merging                                |
    And I should be able to view a Patcher build fix with the name "${staticFixContent1}" for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | 4b6cb18c33           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher build fix with the name "${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | 233fcc3fd0           |
      | Status   | Complete             |
      | Type     | Patch                |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" with the following values
      | Type            | Official                               |
      | Support Ticket  | ${staticTicketNumber}                  |
      | Version         | 1.0                                    |
      | Project Version | marketplace-sharepoint-soap-6.0.0-7310 |
      | Content         | 0 Fix Packs + 2 Tickets                |
      | Patcher Status  | Complete                               |
      | Jenkins         | fixpack-builder-hotfix-7x(uat)         |
      | Jenkins         | fixpack-builder-agent(uat)             |

  Scenario: Check for successful DXP 73 dxp 1 creation
    @priority = "5"
    As a TSE, I should be able to for successful DXP 73 dxp 1 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-dxp-1-7310                                             |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | LPP-42156-dxp1                                                  |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
      | Workaround      | true                                                            |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-dxp-1-7310                                             |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | LRP-1615_dxp-1-7310                                             |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Workaround" with the following values
      | Content         | ${staticFixContent1} |
      | Version         | 1.0                  |
      | Product         | DXP 7.3              |
      | Project Version | fix-pack-dxp-1-7310  |
      | Patcher Status  | Adding               |
      | Type            | Workaround           |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2} |
      | Version         | 1.0                  |
      | Product         | DXP 7.3              |
      | Project Version | fix-pack-dxp-1-7310  |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Workaround" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Workaround" with the following values
      | Content         | ${staticFixContent1}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-dxp-1-7310        |
      | Git Hash        | 39c57fdcbb                 |
      | Patcher Status  | Complete                   |
      | Type            | Workaround                 |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-dxp-1-7310        |
      | Git Hash        | 7de99a3239                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.3                                   |
      | Project Version | fix-pack-dxp-1-7310                       |
      | Tickets List    | ${staticFixContent1},${staticFixContent2} |
      | Account Code    | HITACHIGP                                 |
      | Support Ticket  | ${staticTicketNumber}                     |
    And I set the account code "HITACHIGP" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-dxp-1-7310     |
      | Content         | 0 Fix Packs + 2 Tickets |
      | Patcher Status  | Merging                 |
    And I should be able to view a Patcher build fix with the name "${staticFixContent1}" for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | 39c57fdcbb           |
      | Status   | Complete             |
      | Type     | Workaround           |
    And I should be able to view a Patcher build fix with the name "${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | 7de99a3239           |
      | Status   | Complete             |
      | Type     | Patch                |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-dxp-1-7310            |
      | Content         | 0 Fix Packs + 2 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 fix pack base build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | fix-pack-base-7413                                              |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | LRP-1484                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | fix-pack-base-7413                                              |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | LPS-142141-base-7413                                            |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1} |
      | Version         | 1.0                  |
      | Product         | DXP 7.4              |
      | Project Version | fix-pack-base-7413   |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2} |
      | Version         | 1.0                  |
      | Product         | DXP 7.4              |
      | Project Version | fix-pack-base-7413   |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | fix-pack-base-7413         |
      | Git Hash        | 170bb12dc5                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | fix-pack-base-7413         |
      | Git Hash        | d78d442e76                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4                                   |
      | Project Version | fix-pack-base-7413                        |
      | Tickets List    | ${staticFixContent1},${staticFixContent2} |
      | Account Code    | LIFERAYSECURITY                           |
      | Support Ticket  | ${staticTicketNumber}                     |
    And I set the account code "LIFERAYSECURITY" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-base-7413      |
      | Content         | 0 Fix Packs + 2 Tickets |
      | Patcher Status  | Merging                 |
    And I should be able to view a Patcher build fix with the name "${staticFixContent1}" for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | 170bb12dc5           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher build fix with the name "${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | d78d442e76           |
      | Status   | Complete             |
      | Type     | Patch                |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "LIFERAYSECURITY" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-base-7413             |
      | Content         | 0 Fix Packs + 2 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 73 dxp 3 build creation
    @priority = "5"
    As a TSE, I should be able to for successful DXP 73 dxp 3 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content for the first fix
    And I generate random fix content for the second fix
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-dxp-3-7310                                             |
      | Content         | ${staticFixContent1}                                            |
      | Branch Name     | LPSQA-131967                                                    |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-dxp-3-7310                                             |
      | Content         | ${staticFixContent2}                                            |
      | Branch Name     | LRP-1615-fp-dxp-3-7310                                          |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1} |
      | Version         | 1.0                  |
      | Product         | DXP 7.3              |
      | Project Version | fix-pack-dxp-3-7310  |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2} |
      | Version         | 1.0                  |
      | Product         | DXP 7.3              |
      | Project Version | fix-pack-dxp-3-7310  |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    When I check the Patcher fix containing the content "${staticFixContent1}" and type "Patch" for add completion
    And I check the Patcher fix containing the content "${staticFixContent2}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent1}" and type "Patch" with the following values
      | Content         | ${staticFixContent1}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-dxp-3-7310        |
      | Git Hash        | 0cb58a699f                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    And I should be able to view a Patcher fix containing the content "${staticFixContent2}" and type "Patch" with the following values
      | Content         | ${staticFixContent2}       |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-dxp-3-7310        |
      | Git Hash        | 48e36c6a21                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.3                                   |
      | Project Version | fix-pack-dxp-3-7310                       |
      | Tickets List    | ${staticFixContent1},${staticFixContent2} |
      | Account Code    | HITACHIGP                                 |
      | Support Ticket  | ${staticTicketNumber}                     |
    And I set the account code "HITACHIGP" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-dxp-3-7310     |
      | Content         | 0 Fix Packs + 2 Tickets |
      | Patcher Status  | Merging                 |
    And I should be able to view a Patcher build fix with the name "${staticFixContent1}" for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Name     | ${staticFixContent1} |
      | Version  | 1.0                  |
      | Git Hash | 0cb58a699f           |
      | Status   | Complete             |
      | Type     | Patch                |
    And I should be able to view a Patcher build fix with the name "${staticFixContent2}" for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Name     | ${staticFixContent2} |
      | Version  | 1.0                  |
      | Git Hash | 48e36c6a21           |
      | Status   | Complete             |
      | Type     | Patch                |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for merge completion
    And I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-dxp-3-7310            |
      | Content         | 0 Fix Packs + 2 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 73 fix pack base creation without KotlinNullPointerException failure
    @priority = "5"
    As a TSE, I should be able to for successful DXP 73 fix pack base creation without KotlinNullPointerException failure
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-base-7310                                              |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | LRP-1605-fix                                                    |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.3             |
      | Project Version | fix-pack-base-7310  |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-base-7310         |
      | Git Hash        | 2bc4cd0894                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.3               |
      | Project Version | fix-pack-base-7310    |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-base-7310      |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-base-7310             |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u1 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u1 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u1                                                       |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u1                                                         |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u1           |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u1                  |
      | Git Hash        | 16b9bb66a1                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u1             |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | HITACHIGP             |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "HITACHIGP" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u1               |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u1                      |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u2 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u2 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u2                                                       |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u2                                                         |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u2           |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u2                  |
      | Git Hash        | cf98a4b95c                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u2             |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u2               |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u2                      |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u3 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u3 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u3                                                       |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u3                                                         |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u3           |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u3                  |
      | Git Hash        | 1a7f0948e7                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u3             |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | INTUITIVE             |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "INTUITIVE" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u3               |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u3                      |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u4 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u4 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u4                                                       |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u4                                                         |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u4           |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u4                  |
      | Git Hash        | 5b953ce96b                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u4             |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | HITACHIGP             |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "HITACHIGP" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u4               |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u4                      |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u5 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u5 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u5                                                       |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u5                                                         |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u5           |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u5                  |
      | Git Hash        | 6ee8f854d4                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u5             |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u5               |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u5                      |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u6 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u6 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u6                                                       |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u6                                                         |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u6           |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u6                  |
      | Git Hash        | 92cdf8e047                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u6             |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | INTUITIVE             |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "INTUITIVE" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u6               |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u6                      |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 71 dxp 25 build creation
    @priority = "5"
    As a TSE, I should be able to for successful DXP 71 dxp 25 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.1                                                         |
      | Project Version | fix-pack-dxp-25-7110                                            |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | LRP-1611                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}  |
      | Version         | 1.0                  |
      | Product         | DXP 7.1              |
      | Project Version | fix-pack-dxp-25-7110 |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.1                    |
      | Project Version | fix-pack-dxp-25-7110       |
      | Git Hash        | fde66af760                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.1               |
      | Project Version | fix-pack-dxp-25-7110  |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-dxp-25-7110    |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-dxp-25-7110           |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u7 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u7 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u7                                                       |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u7                                                         |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u7           |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u7                  |
      | Git Hash        | 9de7a51d8e                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u7             |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | INTUITIVE             |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "INTUITIVE" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u7               |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u7                      |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u8 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u8 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u8                                                       |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u8                                                         |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u8           |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u8                  |
      | Git Hash        | cf09256fe6                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u8             |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u8               |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u8                      |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u9 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u9 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u9                                                       |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u9                                                         |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u9           |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u9                  |
      | Git Hash        | 17a63fbe65                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u9             |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | HITACHIGP             |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "HITACHIGP" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u9               |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u9                      |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u10 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u10 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u10                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u10                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u10          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u10                 |
      | Git Hash        | 1ff2b6fc5b                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u10            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u10              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u10                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u15 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u15 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u15                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u15                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u15          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u15                 |
      | Git Hash        | 9c33c0e64f                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u15            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | INTUITIVE             |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "INTUITIVE" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u15              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u15                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u16 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u16 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u16                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u16                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u16          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u16                 |
      | Git Hash        | 49d40b0837                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u16            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | HITACHIGP             |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "HITACHIGP" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u16              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u16                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u17 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u17 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u17                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u17                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u17          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u17                 |
      | Git Hash        | d6fcfc9c5a                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u17            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | HITACHIGP             |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "HITACHIGP" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u17              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u17                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u18 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u18 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u18                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u18                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u18          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u18                 |
      | Git Hash        | 6517fd5bea                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u18            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | INTUITIVE             |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "INTUITIVE" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u18              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u18                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u19 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u19 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u19                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u19                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u19          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u19                 |
      | Git Hash        | f3b573ece2                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u19            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | HITACHIGP             |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "HITACHIGP" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u19              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u19                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u20 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u20 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u20                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u20                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u20          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u20                 |
      | Git Hash        | 4a0014a118                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u20            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | INTUITIVE             |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "INTUITIVE" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u20              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u20                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u21 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u21 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u21                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u21                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u21          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u21                 |
      | Git Hash        | e6f3de5ce9                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u21            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | HITACHIGP             |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "HITACHIGP" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u21              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u21                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u22 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u22 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u22                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u22                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u22          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u22                 |
      | Git Hash        | f648e8ddef                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u22            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | INTUITIVE             |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "INTUITIVE" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u22              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u22                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u23 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u23 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u23                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u23                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u23          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u23                 |
      | Git Hash        | f1d90fa1c3                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u23            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | HITACHIGP             |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "HITACHIGP" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u23              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u23                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u24 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u24 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u24                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u24                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u24          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u24                 |
      | Git Hash        | d534ca3cff                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u24            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u24              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u24                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 73 dxp 4 build creation
    @priority = "5"
    As a TSE, I should be able to for successful DXP 73 dxp4 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-dxp-4-7310                                             |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | LRP-1754                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.3             |
      | Project Version | fix-pack-dxp-4-7310 |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-dxp-4-7310        |
      | Git Hash        | 65b4c224fb                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.3               |
      | Project Version | fix-pack-dxp-4-7310   |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | INTUITIVE             |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "INTUITIVE" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-dxp-4-7310     |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-dxp-4-7310            |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u25 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u25 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u25                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u25                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u25          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u25                 |
      | Git Hash        | 24a2e1bf1d                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u25            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u25              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u25                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u26 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u26 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u26                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u26                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u26          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u26                 |
      | Git Hash        | b364b4484c                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u26            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | INTUITIVE             |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "INTUITIVE" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u26              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u26                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u27 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u27 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u27                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u27                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u27          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u27                 |
      | Git Hash        | e30ced3848                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u27            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u27              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u27                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u28 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u28 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u28                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u28                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u28          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u28                 |
      | Git Hash        | 33548eb8b9                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u28            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | INTUITIVE             |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "INTUITIVE" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u28              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u28                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u29 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u29 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u29                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u29                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u29          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u29                 |
      | Git Hash        | 0e7cab9507                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u29            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u29              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u29                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 73 dxp 5 build creation
    @priority = "5"
    As a TSE, I should be able to for successful DXP 73 dxp5 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-dxp-5-7310                                             |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | LPS-148530_dxp5                                                 |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.3             |
      | Project Version | fix-pack-dxp-5-7310 |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-dxp-5-7310        |
      | Git Hash        | 5663d8dd63                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.3               |
      | Project Version | fix-pack-dxp-5-7310   |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | INTUITIVE             |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "INTUITIVE" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-dxp-5-7310     |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-dxp-5-7310            |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 73 dxp 6 build creation
    @priority = "5"
    As a TSE, I should be able to for successful DXP 73 dxp6 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-dxp-6-7310                                             |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | LPS-110506_dxp6                                                 |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.3             |
      | Project Version | fix-pack-dxp-6-7310 |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-dxp-6-7310        |
      | Git Hash        | 4315dbdc3b                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.3               |
      | Project Version | fix-pack-dxp-6-7310   |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | HITACHIGP             |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "HITACHIGP" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-dxp-6-7310     |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-dxp-6-7310            |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u30 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u30 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u30                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u30                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u30          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u30                 |
      | Git Hash        | e38432a44e                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u30            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u30              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u30                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u31 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u31 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u31                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u31                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u31          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u31                 |
      | Git Hash        | 397d5e229c                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u31            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u31              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u31                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u32 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u32 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u32                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u32                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u32          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u32                 |
      | Git Hash        | 59946f86d0                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u32            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | INTUITIVE             |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "INTUITIVE" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u32              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u32                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 73 dxp 7 build creation
    @priority = "5"
    As a TSE, I should be able to for successful DXP 73 dxp7 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-dxp-7-7310                                             |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | LPS-157042-dxp-7-7310                                           |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.3             |
      | Project Version | fix-pack-dxp-7-7310 |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-dxp-7-7310        |
      | Git Hash        | f1bfe730b6                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.3               |
      | Project Version | fix-pack-dxp-7-7310   |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | HITACHIGP             |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "HITACHIGP" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-dxp-7-7310     |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-dxp-7-7310            |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u33 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u33 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u33                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u33                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u33          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u33                 |
      | Git Hash        | 33386c6aea                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u33            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | INTUITIVE             |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "INTUITIVE" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u33              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u33                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 72 dxp 15 build creation
    @priority = "4"
    As a TSE, I should be able to for successful DXP 72 dxp15 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.2                                                         |
      | Project Version | fix-pack-dxp-15-7210                                            |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | fix-pack-dxp-15-7210-LPS-158634                                 |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}  |
      | Version         | 1.0                  |
      | Product         | DXP 7.2              |
      | Project Version | fix-pack-dxp-15-7210 |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.2                    |
      | Project Version | fix-pack-dxp-15-7210       |
      | Git Hash        | 193b20f97e                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.2               |
      | Project Version | fix-pack-dxp-15-7210  |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | HITACHIGP             |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "HITACHIGP" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-dxp-15-7210    |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-dxp-15-7210           |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 72 dxp 16 build creation and manual release
    @priority = "4"
    As a TSE, I should be able to for successful DXP 72 dxp16 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.2                                                         |
      | Project Version | fix-pack-dxp-16-7210                                            |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | LPS-142380_fp16                                                 |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}  |
      | Version         | 1.0                  |
      | Product         | DXP 7.2              |
      | Project Version | fix-pack-dxp-16-7210 |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.2                    |
      | Project Version | fix-pack-dxp-16-7210       |
      | Git Hash        | 5734d2e558                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I create a request as a user with the email address "no-reply@intuitive.com.broken" with the following values
      | Product           | prd_liferay_dxp_7_2                              |
      | Component         | activation_key_project_administration            |
      | System Status     | production_functioning_with_limited_capabilities |
      | Subject           | Patcher Request Subject                          |
      | Description       | Patcher Request Description                      |
      | Organization Name | Intuitive Surgical-EE-IT                         |
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.2                |
      | Project Version | fix-pack-dxp-16-7210   |
      | Tickets List    | ${staticFixContent}    |
      | Account Code    | INTUITIVE              |
      | Support Ticket  | ${staticRequestIdList} |
    And I set the account code "INTUITIVE" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticRequestIdList}" on the account "INTUITIVE" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticRequestIdList}  |
      | Version         | 1.0                     |
      | Project Version | fix-pack-dxp-16-7210    |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticRequestIdList}" on the account "INTUITIVE" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticRequestIdList}" on the account "INTUITIVE" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticRequestIdList}         |
      | Version         | 1.0                            |
      | Project Version | fix-pack-dxp-16-7210           |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |
    When I release a Patcher build for the ticket "${staticRequestIdList}" on the account "INTUITIVE" manually
    Then the ticket with the ticketId "${staticRequestIdList}" should not have the comment containing the text "liferay-hotfix-"
    And the ticket with the ticketId "${staticRequestIdList}" should not have the comment containing the text "-7210.zip"
    And I should be able to view a Patcher build for the ticket "${staticRequestIdList}" on the account "INTUITIVE" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticRequestIdList}         |
      | Version         | 1.0                            |
      | Project Version | fix-pack-dxp-16-7210           |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Released                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 72 dxp 17 build creation and release to Help Center
    @priority = "4"
    As a TSE, I should be able to for successful DXP 72 dxp17 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.2                                                         |
      | Project Version | fix-pack-dxp-17-7210                                            |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | LPE-17216-to-LPS-146180-dxp-17-7210                             |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}  |
      | Version         | 1.0                  |
      | Product         | DXP 7.2              |
      | Project Version | fix-pack-dxp-17-7210 |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.2                    |
      | Project Version | fix-pack-dxp-17-7210       |
      | Git Hash        | b9daa08de6                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I create a request as a user with the email address "no-reply@intuitive.com.broken" with the following values
      | Product           | prd_liferay_dxp_7_2                              |
      | Component         | activation_key_project_administration            |
      | System Status     | production_functioning_with_limited_capabilities |
      | Subject           | Patcher Request Subject                          |
      | Description       | Patcher Request Description                      |
      | Organization Name | Intuitive Surgical-EE-IT                         |
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.2                |
      | Project Version | fix-pack-dxp-17-7210   |
      | Tickets List    | ${staticFixContent}    |
      | Account Code    | INTUITIVE              |
      | Support Ticket  | ${staticRequestIdList} |
    And I set the account code "INTUITIVE" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticRequestIdList}" on the account "INTUITIVE" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticRequestIdList}  |
      | Version         | 1.0                     |
      | Project Version | fix-pack-dxp-17-7210    |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticRequestIdList}" on the account "INTUITIVE" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticRequestIdList}" on the account "INTUITIVE" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticRequestIdList}         |
      | Version         | 1.0                            |
      | Project Version | fix-pack-dxp-17-7210           |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |
    When I release a Patcher build to Help Center for the ticket "${staticRequestIdList}" on the account "INTUITIVE"
    Then the ticket with the ticketId "${staticRequestIdList}" should have the comment containing the text "liferay-hotfix-"
    And the ticket with the ticketId "${staticRequestIdList}" should have the comment containing the text "-7210.zip"
    And I should be able to view a Patcher build for the ticket "${staticRequestIdList}" on the account "INTUITIVE" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticRequestIdList}         |
      | Version         | 1.0                            |
      | Project Version | fix-pack-dxp-17-7210           |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Released                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 72 dxp 18 build creation and change Patcher build status to ready for release and release manually
    @priority = "4"
    As a TSE, I should be able to for successful DXP 72 dxp18 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.2                                                         |
      | Project Version | fix-pack-dxp-18-7210                                            |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | LPS-123311_LPS-135731_fp18                                      |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}  |
      | Version         | 1.0                  |
      | Product         | DXP 7.2              |
      | Project Version | fix-pack-dxp-18-7210 |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.2                    |
      | Project Version | fix-pack-dxp-18-7210       |
      | Git Hash        | 4027686a4f                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I create a request as a user with the email address "no-reply@intuitive.com.broken" with the following values
      | Product           | prd_liferay_dxp_7_2                              |
      | Component         | activation_key_project_administration            |
      | System Status     | production_functioning_with_limited_capabilities |
      | Subject           | Patcher Request Subject                          |
      | Description       | Patcher Request Description                      |
      | Organization Name | Intuitive Surgical-EE-IT                         |
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.2                |
      | Project Version | fix-pack-dxp-18-7210   |
      | Tickets List    | ${staticFixContent}    |
      | Account Code    | INTUITIVE              |
      | Support Ticket  | ${staticRequestIdList} |
    And I set the account code "INTUITIVE" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticRequestIdList}" on the account "INTUITIVE" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticRequestIdList}  |
      | Version         | 1.0                     |
      | Project Version | fix-pack-dxp-18-7210    |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticRequestIdList}" on the account "INTUITIVE" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticRequestIdList}" on the account "INTUITIVE" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticRequestIdList}         |
      | Version         | 1.0                            |
      | Project Version | fix-pack-dxp-18-7210           |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |
    When I change a Patcher build status to ready for release for the ticket "${staticRequestIdList}" on the account "INTUITIVE"
    Then the ticket with the ticketId "${staticRequestIdList}" should not have the comment containing the text "liferay-hotfix-"
    And the ticket with the ticketId "${staticRequestIdList}" should not have the comment containing the text "-7110.zip"
    And I should be able to view a Patcher build for the ticket "${staticRequestIdList}" on the account "INTUITIVE" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticRequestIdList}         |
      | Version         | 1.0                            |
      | Project Version | fix-pack-dxp-18-7210           |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Ready for Release              |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |
    When I release a Patcher build for the ticket "${staticRequestIdList}" on the account "INTUITIVE" manually
    Then the ticket with the ticketId "${staticRequestIdList}" should not have the comment containing the text "liferay-hotfix-"
    And the ticket with the ticketId "${staticRequestIdList}" should not have the comment containing the text "-7110.zip"
    And I should be able to view a Patcher build for the ticket "${staticRequestIdList}" on the account "INTUITIVE" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticRequestIdList}         |
      | Version         | 1.0                            |
      | Project Version | fix-pack-dxp-18-7210           |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Released                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 72 dxp 19 build creation and change Patcher build status to ready for release and release to Help Center
    @priority = "4"
    As a TSE, I should be able to for successful DXP 72 dxp19 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.2                                                         |
      | Project Version | fix-pack-dxp-19-7210                                            |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | 72-dxp-19-test                                                  |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}  |
      | Version         | 1.0                  |
      | Product         | DXP 7.2              |
      | Project Version | fix-pack-dxp-19-7210 |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.2                    |
      | Project Version | fix-pack-dxp-19-7210       |
      | Git Hash        | c02b1275e2                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I create a request as a user with the email address "no-reply@intuitive.com.broken" with the following values
      | Product           | prd_liferay_dxp_7_2                              |
      | Component         | activation_key_project_administration            |
      | System Status     | production_functioning_with_limited_capabilities |
      | Subject           | Patcher Request Subject                          |
      | Description       | Patcher Request Description                      |
      | Organization Name | Intuitive Surgical-EE-IT                         |
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.2                |
      | Project Version | fix-pack-dxp-19-7210   |
      | Tickets List    | ${staticFixContent}    |
      | Account Code    | INTUITIVE              |
      | Support Ticket  | ${staticRequestIdList} |
    And I set the account code "INTUITIVE" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticRequestIdList}" on the account "INTUITIVE" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticRequestIdList}  |
      | Version         | 1.0                     |
      | Project Version | fix-pack-dxp-19-7210    |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticRequestIdList}" on the account "INTUITIVE" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticRequestIdList}" on the account "INTUITIVE" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticRequestIdList}         |
      | Version         | 1.0                            |
      | Project Version | fix-pack-dxp-19-7210           |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |
    When I change a Patcher build status to ready for release for the ticket "${staticRequestIdList}" on the account "INTUITIVE"
    Then the ticket with the ticketId "${staticRequestIdList}" should not have the comment containing the text "liferay-hotfix-"
    And the ticket with the ticketId "${staticRequestIdList}" should not have the comment containing the text "-7110.zip"
    And I should be able to view a Patcher build for the ticket "${staticRequestIdList}" on the account "INTUITIVE" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticRequestIdList}         |
      | Version         | 1.0                            |
      | Project Version | fix-pack-dxp-19-7210           |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Ready for Release              |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |
    When I release a Patcher build to Help Center for the ticket "${staticRequestIdList}" on the account "INTUITIVE"
    Then the ticket with the ticketId "${staticRequestIdList}" should have the comment containing the text "liferay-hotfix-"
    And the ticket with the ticketId "${staticRequestIdList}" should have the comment containing the text "-7210.zip"
    And I should be able to view a Patcher build for the ticket "${staticRequestIdList}" on the account "INTUITIVE" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticRequestIdList}         |
      | Version         | 1.0                            |
      | Project Version | fix-pack-dxp-19-7210           |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Released                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u34 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u34 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u34                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u34                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u34          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u34                 |
      | Git Hash        | 37eeb3371e                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u34            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | INTUITIVE             |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "INTUITIVE" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u34              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u34                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 73 dxp 8 build creation
    @priority = "5"
    As a TSE, I should be able to for successful DXP 73 dxp8 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-dxp-8-7310                                             |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | LPS-110506-dxp-8-7310                                           |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.3             |
      | Project Version | fix-pack-dxp-8-7310 |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-dxp-8-7310        |
      | Git Hash        | 06e62da19d                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.3               |
      | Project Version | fix-pack-dxp-8-7310   |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | HITACHIGP             |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "HITACHIGP" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-dxp-8-7310     |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-dxp-8-7310            |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u35 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u35 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u35                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u35                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u35          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u35                 |
      | Git Hash        | 0cca815870                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u35            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | INTUITIVE             |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "INTUITIVE" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u35              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u35                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u36 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u36 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u36                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u36                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u36          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u36                 |
      | Git Hash        | d1380a6420                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u36            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | INTUITIVE             |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "INTUITIVE" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u36              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u36                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 73 dxp 9 build creation
    @priority = "5"
    As a TSE, I should be able to for successful DXP 73 dxp9 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-dxp-9-7310                                             |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-fix-pack-dxp-9-7310                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.3             |
      | Project Version | fix-pack-dxp-9-7310 |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-dxp-9-7310        |
      | Git Hash        | 05f1ac1fa7                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.3               |
      | Project Version | fix-pack-dxp-9-7310   |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | HITACHIGP             |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "HITACHIGP" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-dxp-9-7310     |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-dxp-9-7310            |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u37 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u37 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u37                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u37                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u37          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u37                 |
      | Git Hash        | 25f4474c1f                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u37            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u37              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u37                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 73 dxp 10 build creation
    @priority = "5"
    As a TSE, I should be able to for successful DXP 73 dxp10 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-dxp-10-7310                                            |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-fix-pack-dxp-10-7310                                       |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}  |
      | Version         | 1.0                  |
      | Product         | DXP 7.3              |
      | Project Version | fix-pack-dxp-10-7310 |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-dxp-10-7310       |
      | Git Hash        | ffec9a1494                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.3               |
      | Project Version | fix-pack-dxp-10-7310  |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | HITACHIGP             |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "HITACHIGP" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-dxp-10-7310    |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-dxp-10-7310           |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u38 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u38 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u38                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u38                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u38          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u38                 |
      | Git Hash        | 66798ba35d                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u38            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u38              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u38                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u39 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u39 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u39                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u39                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u39          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u39                 |
      | Git Hash        | a1fb4d347c                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u39            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u39              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u39                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 73 dxp 11 build creation
    @priority = "5"
    As a TSE, I should be able to for successful DXP 73 dxp11 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-dxp-11-7310                                            |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-fix-pack-dxp-11-7310                                       |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}  |
      | Version         | 1.0                  |
      | Product         | DXP 7.3              |
      | Project Version | fix-pack-dxp-11-7310 |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-dxp-11-7310       |
      | Git Hash        | aff142ca0b                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.3               |
      | Project Version | fix-pack-dxp-11-7310  |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | HITACHIGP             |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "HITACHIGP" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-dxp-11-7310    |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-dxp-11-7310           |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u40 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u40 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u40                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u40                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u40          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u40                 |
      | Git Hash        | 2ef8e924fa                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u40            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u40              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u40                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u41 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u41 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u41                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u41                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u41          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u41                 |
      | Git Hash        | 591e9b0f6f                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u41            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u41              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u41                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 73 dxp 12 build creation
    @priority = "5"
    As a TSE, I should be able to for successful DXP 73 dxp12 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-dxp-12-7310                                            |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-dxp-12-7310                                                |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}  |
      | Version         | 1.0                  |
      | Product         | DXP 7.3              |
      | Project Version | fix-pack-dxp-12-7310 |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-dxp-12-7310       |
      | Git Hash        | 8fb91ad540                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.3               |
      | Project Version | fix-pack-dxp-12-7310  |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | HITACHIGP             |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "HITACHIGP" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-dxp-12-7310    |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-dxp-12-7310           |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u42 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u42 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u42                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u42                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u42          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u42                 |
      | Git Hash        | 36005704ab                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u42            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u42              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u42                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 73 dxp 13 build creation
    @priority = "5"
    As a TSE, I should be able to for successful DXP 73 dxp13 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-dxp-13-7310                                            |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-dxp-13-7310                                                |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}  |
      | Version         | 1.0                  |
      | Product         | DXP 7.3              |
      | Project Version | fix-pack-dxp-13-7310 |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-dxp-13-7310       |
      | Git Hash        | cd16b0f42f                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.3               |
      | Project Version | fix-pack-dxp-13-7310  |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-dxp-13-7310    |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-dxp-13-7310           |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u43 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u43 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u43                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u43                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u43          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u43                 |
      | Git Hash        | 4b7b10c8af                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u43            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u43              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u43                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 74 u44 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u44 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u44                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u44                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u44          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u44                 |
      | Git Hash        | 860e3b201d                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u44            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u44              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u44                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 72 dxp 20 build creation
    @priority = "4"
    As a TSE, I should be able to for successful DXP 72 dxp20 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.2                                                         |
      | Project Version | fix-pack-dxp-20-7210                                            |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | LPS-136413-72-dxp-20                                            |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}  |
      | Version         | 1.0                  |
      | Product         | DXP 7.2              |
      | Project Version | fix-pack-dxp-20-7210 |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.2                    |
      | Project Version | fix-pack-dxp-20-7210       |
      | Git Hash        | 4b1282db23                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.2               |
      | Project Version | fix-pack-dxp-20-7210  |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | HITACHIGP             |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "HITACHIGP" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-dxp-20-7210    |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-dxp-20-7210           |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |
  
  Scenario: Check for successful DXP 74 u45 build creation
    @ignore = "true"
    As a TSE, I should be able to for successful DXP 74 u45 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                        |
      | Project Version | 7.4.13-u45                                                     |
      | Content         | ${staticFixContent}                                            |
      | Branch Name     | test-u45                                                       |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u45          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u45                 |
      | Git Hash        | 10bf0c9543                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u45            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u45              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u45                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 73 dxp 14 build creation
    @priority = "5"
    As a TSE, I should be able to for successful DXP 73 dxp14 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-dxp-14-7310                                            |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-dxp-14-7310                                                |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}  |
      | Version         | 1.0                  |
      | Product         | DXP 7.3              |
      | Project Version | fix-pack-dxp-14-7310 |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-dxp-14-7310       |
      | Git Hash        | 3efca8e9d0                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.3               |
      | Project Version | fix-pack-dxp-14-7310  |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-dxp-14-7310    |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-dxp-14-7310           |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

Scenario: Check for successful DXP 74 u46 build creation
    @ignore = "true"
    @priority = "5"
    As a TSE, I should be able to for successful DXP 74 u46 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u46                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u46                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u46          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u46                 |
      | Git Hash        | 39606316a1                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u46            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | INTUITIVE             |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "INTUITIVE" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u46              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "INTUITIVE" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u46                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

Scenario: Check for successful DXP 74 u47 build creation
    @ignore = "true"
    @priority = "5"
    As a TSE, I should be able to for successful DXP 74 u47 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u47                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u47                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u47          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u47                 |
      | Git Hash        | df837b44da                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u47            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u47              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u47                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

Scenario: Check for successful DXP 74 u48 build creation
    @ignore = "true"
    @priority = "5"
    As a TSE, I should be able to for successful DXP 74 u48 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u48                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u48                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u48          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u48                 |
      | Git Hash        | 590a6cf014                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u48            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u48              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u48                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

  Scenario: Check for successful DXP 73 dxp 15 build creation
    @priority = "5"
    As a TSE, I should be able to for successful DXP 73 dxp15 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.3                                                         |
      | Project Version | fix-pack-dxp-15-7310                                            |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-dxp-15-7310                                                |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}  |
      | Version         | 1.0                  |
      | Product         | DXP 7.3              |
      | Project Version | fix-pack-dxp-15-7310 |
      | Patcher Status  | Adding               |
      | Type            | Patch                |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.3                    |
      | Project Version | fix-pack-dxp-15-7310       |
      | Git Hash        | b30bbd612e                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.3               |
      | Project Version | fix-pack-dxp-15-7310  |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | fix-pack-dxp-15-7310    |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | fix-pack-dxp-15-7310           |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

Scenario: Check for successful DXP 74 u49 build creation
    @ignore = "true"
    @priority = "5"
    As a TSE, I should be able to for successful DXP 74 u49 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u49                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u49                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u49          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u49                 |
      | Git Hash        | 800427d57a                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u49            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u49              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u49                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

Scenario: Check for successful DXP 74 u50 build creation
    @ignore = "true"
    @priority = "5"
    As a TSE, I should be able to for successful DXP 74 u50 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u50                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u50                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u50          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u50                 |
      | Git Hash        | 92e84194eb                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u50            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u50              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u50                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

Scenario: Check for successful DXP 74 u51 build creation
    @ignore = "true"
    @priority = "5"
    As a TSE, I should be able to for successful DXP 74 u51 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u51                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u51                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u51          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u51                 |
      | Git Hash        | 40d2726f7d                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u51            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u51              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u51                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

Scenario: Check for successful DXP 74 u52 build creation
    @ignore = "true"
    @priority = "5"
    As a TSE, I should be able to for successful DXP 74 u52 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u52                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u52                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u52          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u52                 |
      | Git Hash        | d4ecda088f                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u52            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u52              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u52                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

Scenario: Check for successful DXP 74 u53 build creation
    @ignore = "true"
    @priority = "5"
    As a TSE, I should be able to for successful DXP 74 u53 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u53                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u53                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u53          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u53                 |
      | Git Hash        | b96ece8306                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u53            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u53              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u53                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

Scenario: Check for successful DXP 74 u54 build creation
    @ignore = "true"
    @priority = "5"
    As a TSE, I should be able to for successful DXP 74 u54 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u54                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u54                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u54          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u54                 |
      | Git Hash        | c11578493b                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u54            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u54              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u54                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

Scenario: Check for successful DXP 74 u55 build creation
    @ignore = "true"
    @priority = "5"
    As a TSE, I should be able to for successful DXP 74 u55 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u55                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u55                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u55          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u55                 |
      | Git Hash        | cb82e65877                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u55            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u55              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u55                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

Scenario: Check for successful DXP 74 u56 build creation
    @priority = "5"
    As a TSE, I should be able to for successful DXP 74 u56 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u56                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u56                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u56          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u56                 |
      | Git Hash        | f9bf817194                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u56            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | HITACHIGP             |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "HITACHIGP" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u56              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "HITACHIGP" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u56                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

Scenario: Check for successful DXP 74 u57 build creation
    @priority = "5"
    As a TSE, I should be able to for successful DXP 74 u57 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u57                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u57                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u57          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u57                 |
      | Git Hash        | 9a200e01f1                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u57            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u57              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u57                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

Scenario: Check for successful DXP 74 u58 build creation
    @priority = "5"
    As a TSE, I should be able to for successful DXP 74 u58 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u58                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u58                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u58          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u58                 |
      | Git Hash        | 4f9a1b0fd6                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u58            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u58              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u58                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

Scenario: Check for successful DXP 74 u59 build creation
    @priority = "5"
    As a TSE, I should be able to for successful DXP 74 u59 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u59                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u59                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u59          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u59                 |
      | Git Hash        | 82a4a42497                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u59            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u59              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u59                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

Scenario: Check for successful DXP 74 u60 build creation
    @priority = "5"
    As a TSE, I should be able to for successful DXP 74 u60 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u60                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u60                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u60          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u60                 |
      | Git Hash        | fb15e7f55c                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u60            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u60              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u60                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

Scenario: Check for successful DXP 74 u61 build creation
    @priority = "5"
    As a TSE, I should be able to for successful DXP 74 u61 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u61                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u61                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u61          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u61                 |
      | Git Hash        | 30258c31e1                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u61            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u61              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u61                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

Scenario: Check for successful DXP 74 u62 build creation
    @priority = "5"
    As a TSE, I should be able to for successful DXP 74 u62 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u62                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u62                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u62          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u62                 |
      | Git Hash        | 363ffb1d3e                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u62            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u62              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u62                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      
Scenario: Check for successful DXP 74 u63 build creation
    @priority = "5"
    As a TSE, I should be able to for successful DXP 74 u63 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u63                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u63                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u63          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u63                 |
      | Git Hash        | 76f089016c                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u63            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u63              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u63                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

Scenario: Check for successful DXP 74 u64 build creation
    @priority = "5"
    As a TSE, I should be able to for successful DXP 74 u64 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u64                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u64                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u64          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u64                 |
      | Git Hash        | 8331356a1e                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u64            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u64              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u64                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

Scenario: Check for successful DXP 74 u65 build creation
    @priority = "5"
    As a TSE, I should be able to for successful DXP 74 u65 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u65                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u65                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u65          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u65                 |
      | Git Hash        | 91581af448                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u65            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u65              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u65                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |

Scenario: Check for successful DXP 74 u67 build creation
    @priority = "5"
    As a TSE, I should be able to for successful DXP 74 u67 build creation
    Given I log into the Patcher homepage as the admin user
    And I generate random fix content
    And I create a Patcher fix with the following values
      | Product         | DXP 7.4                                                         |
      | Project Version | 7.4.13-u67                                                      |
      | Content         | ${staticFixContent}                                             |
      | Branch Name     | test-u67                                                        |
      | Github URL      | git@github.com:Liferay-Patcher-Test-Fixes/liferay-portal-ee.git |
    And I wait for "5" seconds
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent} |
      | Version         | 1.0                 |
      | Product         | DXP 7.4             |
      | Project Version | 7.4.13-u67          |
      | Patcher Status  | Adding              |
      | Type            | Patch               |
    When I check the Patcher fix containing the content "${staticFixContent}" and type "Patch" for add completion
    Then I should be able to view a Patcher fix containing the content "${staticFixContent}" and type "Patch" with the following values
      | Content         | ${staticFixContent}        |
      | Version         | 1.0                        |
      | Product         | DXP 7.4                    |
      | Project Version | 7.4.13-u67                 |
      | Git Hash        | 37be04b468                 |
      | Patcher Status  | Complete                   |
      | Type            | Patch                      |
      | Jenkins         | fixpack-builder-agent(uat) |
    When I generate a random ticket number
    And I create a Patcher build for a new account with the following values
      | Product         | DXP 7.4               |
      | Project Version | 7.4.13-u67            |
      | Tickets List    | ${staticFixContent}   |
      | Account Code    | STVINTRA              |
      | Support Ticket  | ${staticTicketNumber} |
    And I set the account code "STVINTRA" as a static variable
    And I wait for "5" seconds
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                |
      | Support Ticket  | ${staticTicketNumber}   |
      | Version         | 1.0                     |
      | Project Version | 7.4.13-u67              |
      | Content         | 0 Fix Packs + 1 Tickets |
      | Patcher Status  | Compiling               |
    When I check the Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" for compile completion using the "fixpack-builder-hotfix-7x(uat)" Jenkins job
    Then I should be able to view a Patcher build for the ticket "${staticTicketNumber}" on the account "STVINTRA" with the following values
      | Type            | Official                       |
      | Support Ticket  | ${staticTicketNumber}          |
      | Version         | 1.0                            |
      | Project Version | 7.4.13-u67                     |
      | Content         | 0 Fix Packs + 1 Tickets        |
      | Patcher Status  | Complete                       |
      | Jenkins         | fixpack-builder-hotfix-7x(uat) |
      | Jenkins         | fixpack-builder-agent(uat)     |