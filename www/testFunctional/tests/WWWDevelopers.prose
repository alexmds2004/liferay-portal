@testray.main.component.name = "Support QA"

Feature: WWW Developers
	Frontend testing for the www developers

Scenario: Developers Homepage Navigation
	End users should be able to view all links in the developers homepage.

	Given I go to the www page with the relative url "developers"
	Then there should be no errors on the page
    When I click on the "Community Site" card on the developers homepage
    Then there should be no errors on the page
	And the current url content should not contain the text "www-uat.liferay.com"
	When I go to the www page with the relative url "developers"
    And I click on the "Documentation" card on the developers homepage
    Then there should be no errors on the page
	And the current url content should not contain the text "www-uat.liferay.com"
    When I go to the www page with the relative url "developers"
    And I click on the "Community Downloads" card on the developers homepage
    Then there should be no errors on the page
	And the current url content should contain the text "www-uat.liferay.com"
    When I go to the www page with the relative url "developers"
    And I click on the "Marketplace" card on the developers homepage
    Then there should be no errors on the page
	And the current url content should not contain the text "www-uat.liferay.com"
    When I go to the www page with the relative url "developers"
    And I click on the "Customer Portal" card on the developers homepage
    Then there should be no errors on the page
	And the current url content should not contain the text "www-uat.liferay.com"

Scenario: View OG Tags in Developers page
	The developers page og tags should be specific values.

	Given I go to the www page with the relative url "developers"
	Then the current page og title tag should be "Liferay Developer Resources"
	And the current page og description tag should be "A collection of all the resources to develop with Liferay including the community, documentation, customer portal, downloads, and marketplace. Stay Nerdy"
	And the current page og url tag should be "https://www-uat.liferay.com/developers"
	And the current page og image tag should be "https://www-uat.liferay.com/documents/10182/0/open_graph_image"

Scenario: View Developers page hreflang tags
	All hreflang tags on the developers page should be specific values.

	Given I go to the www page with the relative url "developers"
	Then the hreflang tag for the relative language url "zh/developers" should be "zh-CN"
	And the hreflang tag for the relative language url "es/developers" should be "es-ES"
	And the hreflang tag for the relative language url "fr/developers" should be "fr-FR"
	And the hreflang tag for the relative language url "developers" should be "en-US"
	And the hreflang tag for the relative language url "pt/developers" should be "pt-BR"