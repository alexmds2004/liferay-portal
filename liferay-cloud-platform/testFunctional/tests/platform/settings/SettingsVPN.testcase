@component-name = "liferay-cloud-platform"
definition {

	var projectName = "poshitest";

	property testray.main.component.name = "liferay-cloud-platform";

	setUp {
		Utils.launchLCP();

		LoginPage.login();
	}

	tearDown {
		Utils.tearDownProjects();

		Topbar.logout();
	}

	@description = "Hovering over the question mark icons displays a tooltip about the related information"
	@priority = "3"
	test DisplayTooltips {
		task ("When: I access the VPN tab on the Settings page") {
			Utils.goToPage(page = "projects/projtest-dev/settings/vpn");
		}

		task ("Then: The user can see a tooltip by hovering the mouse over the question mark icons") {
			Default.MouseOver(
				key_infoID = "Name",
				locator1 = "Settings#QUESTION_MARK_ICON");

			Default.AssertTextEquals(
				locator1 = "Settings#QUESTION_MARK_TOOLTIP",
				value1 = "Name of the VPN");

			Default.MouseOver(
				key_infoID = "Protocol",
				locator1 = "Settings#QUESTION_MARK_ICON");

			Default.AssertTextEquals.assertPartialText(
				locator1 = "Settings#QUESTION_MARK_TOOLTIP",
				value1 = "VPN protocols are");

			Default.MouseOver(
				key_infoID = "Server",
				locator1 = "Settings#QUESTION_MARK_ICON");

			Default.AssertTextEquals.assertPartialText(
				locator1 = "Settings#QUESTION_MARK_TOOLTIP",
				value1 = "VPN server is");

			Default.MouseOver(
				key_infoID = "Routes",
				locator1 = "Settings#QUESTION_MARK_ICON");

			Default.AssertTextEquals.assertPartialText(
				locator1 = "Settings#QUESTION_MARK_TOOLTIP",
				value1 = "Port routes from");

			Default.MouseOver(
				key_infoID = "Status",
				locator1 = "Settings#QUESTION_MARK_ICON");

			Default.AssertTextEquals.assertPartialText(
				locator1 = "Settings#QUESTION_MARK_TOOLTIP",
				value1 = "Status of the connection");
		}
	}

	@description = "VPN tab displays a link to the corresponding documentation"
	@priority = "3"
	test LinkToDocumentation {
		task ("When: I access the VPN tab on the Settings page") {
			Utils.goToPage(page = "projects/projtest-infra/settings/vpn");
		}

		task ("And: I click the Learn More link") {
			Default.Click(locator1 = "Utils#LEARN_MORE_LINK");
		}

		task ("Then: The corresponding documentation page opens") {
			Default.SelectWindow(locator1 = "title=Connecting a VPN Server to Liferay Cloud — Liferay Learn");

			Default.AssertElementPresent(
				key_headerTitle = "Connecting a VPN Server to Liferay Cloud",
				locator1 = "Utils#HEADER_TITLE");
		}
	}

	@description = "Admin users see 'No VPN' and 'Create New VPN' button on settings page when no vpn was created"
	@priority = "5"
	test NoVPNInformationForAdminUser {
		task ("Given: The admin user is logged in When: User navigates to VPN tab on Settings page") {
			Utils.goToPage(page = "projects/projtest-infra/settings/vpn");
		}

		task ("Then: 'No VPN' message is displayed") {
			Default.AssertElementPresent(locator1 = "Settings#NO_VPN_MESSAGE");
		}

		task ("And: The 'Create New VPN' button is displayed") {
			Default.AssertElementPresent(
				key_btnName = "Create New VPN",
				locator1 = "Settings#CREATE_BUTTON");
		}
	}

	@description = "If the user has no privileges, the information 'No VPN' is displayed, as well as a message about the roles with this permission and a link to the team page"
	@priority = "5"
	test NoVPNInformationForGuestRoles {
		task ("Given: The user has no privileges and the project has no VPN") {
			Topbar.logout();

			LoginPage.loginAsGuest();
		}

		task ("When: User navigates to VPN tab on Settings page") {
			Utils.goToPage(page = "projects/projtest-infra/settings/vpn");
		}

		task ("Then: 'No VPN' message is displayed") {
			Default.AssertElementPresent(locator1 = "Settings#NO_VPN_MESSAGE");
		}

		task ("And: User can see a link to the team page") {
			Default.AssertElementPresent(
				key_linkText = "go to Team page",
				locator1 = "Utils#EXTERNAL_LINK");
		}

		task ("And: User's permission information is displayed") {
			Default.AssertTextEquals.assertPartialText(
				locator1 = "Utils#INFO_ALERT_MESSAGE",
				value1 = "Info: Only Admin and contributor users can create a VPN to this environment");
		}

		task ("And: The 'Create New VPN' button is not displayed") {
			Default.AssertElementNotPresent(
				key_btnName = "Create New VPN",
				locator1 = "Settings#CREATE_BUTTON");
		}
	}

	@description = "Clicking on VPN Name redirects to the View VPN page if user role has permissions to do so"
	@priority = "4"
	test VPNNameRedirects {
		task ("When: User clicks on the VPN name") {
			Utils.goToPage(page = "projects/projtest-dev/settings/vpn");

			Default.Click(
				key_itemName = "Projtest Connection",
				locator1 = "Utils#TABLE_ITEM");
		}

		task ("Then: Console redirects to View VPN page") {
			Default.AssertElementPresent(
				key_headerTitle = "Projtest Connection",
				locator1 = "Utils#HEADER_TITLE");
		}
	}

}