@component-name = "liferay-cloud-platform"
definition {

	property testray.main.component.name = "liferay-cloud-platform";

	static var staticProjectId = "";
	static var staticProjectIdInfra = "";
	static var staticProjectIdTearDowns = ListUtil.newList();

	setUp {
		Utils.launchLCP();

		LoginPage.login();

		var staticProjectId = ProjectsAPI.createProject();

		var staticProjectIdInfra = ProjectsAPI.createProject(
			environment = "true",
			projectId = "${staticProjectId}-infra");
	}

	tearDown {
		UtilsAPI.tearDownProjects(projectId = ${staticProjectIdTearDowns});

		Topbar.logout();
	}

	@description = "Project ID, Environment Type, Environment ID and Location information is displayed correctly according to the current environment"
	@priority = 4
	test DisplayEnvironmentInformation {
		task ("When: I access the Info tab on the Settings page") {
			Utils.goToPage(page = "projects/${staticProjectIdInfra}/settings/info");
		}

		task ("Then: I can see current environment information") {
			Default.AssertTextEquals(
				key_infoItem = "Project Name",
				locator1 = "Settings#ENVIRONMENT_SPECIFIC_INFORMATION",
				value1 = ${staticProjectId});

			Default.AssertTextEquals(
				key_infoItem = "Environment ID",
				locator1 = "Settings#ENVIRONMENT_SPECIFIC_INFORMATION",
				value1 = "INFRA");

			Default.AssertTextEquals(
				key_infoItem = "Environment Type",
				locator1 = "Settings#ENVIRONMENT_SPECIFIC_INFORMATION",
				value1 = "Non-Production");

			Default.AssertTextEquals(
				key_infoItem = "Location",
				locator1 = "Settings#ENVIRONMENT_SPECIFIC_INFORMATION",
				value1 = "Oregon, USA");
		}
	}

	@description = "Hovering over the map location and the question mark icon displays a tooltip"
	@priority = 3
	test DisplayTooltips {
		task ("When: I access the Info tab on the Settings page") {
			Utils.goToPage(page = "projects/${staticProjectIdInfra}/settings/info");
		}

		task ("Then: I can see a tooltip by hovering over each question mark icon") {
			Default.AssertElementPresent(
				key_cardTitle = "Environment Info",
				locator1 = "Settings#CARD_TITLE");

			Default.MouseOver(
				key_infoID = "Project Name",
				locator1 = "Settings#QUESTION_MARK_ICON");

			Default.AssertTextEquals.assertPartialText(
				locator1 = "Settings#QUESTION_MARK_TOOLTIP",
				value1 = "Project Name is");

			Default.MouseOver(
				key_infoID = "Environment ID",
				locator1 = "Settings#QUESTION_MARK_ICON");

			Default.AssertTextEquals.assertPartialText(
				locator1 = "Settings#QUESTION_MARK_TOOLTIP",
				value1 = "Environment ID is");

			Default.MouseOver(
				key_infoID = "Environment Type",
				locator1 = "Settings#QUESTION_MARK_ICON");

			Default.AssertTextEquals.assertPartialText(
				locator1 = "Settings#QUESTION_MARK_TOOLTIP",
				value1 = "Environment Type is");

			Default.MouseOver(
				key_infoID = "Location",
				locator1 = "Settings#QUESTION_MARK_ICON");

			Default.AssertTextEquals.assertPartialText(
				locator1 = "Settings#QUESTION_MARK_TOOLTIP",
				value1 = "Location specifies the area where");
		}

		task ("And: I can see a tooltip by hovering over the environment location map") {
			Default.MouseOver(locator1 = "Settings#ENVIRONMENT_LOCATION_MAP");

			Default.AssertTextEquals.assertPartialText(
				locator1 = "Settings#QUESTION_MARK_TOOLTIP",
				value1 = "${staticProjectIdInfra} is operating from Oregon, USA.");
		}
	}

	@description = "Clicking on the environment badge redirects to the Environment overview page"
	@priority = 4
	test EnvironmentBadgeRedirects {
		task ("When: User clicks on the environment badge in the Environment Info tab in Settings page") {
			Utils.goToPage(page = "projects/${staticProjectIdInfra}/settings/info");

			Default.Click(locator1 = "Settings#ENVIRONMENT_BADGE");
		}

		task ("Then: Console redirects to the environment's Overview page") {
			Default.AssertElementPresent(
				key_headerTitle = "Overview",
				locator1 = "Utils#HEADER_TITLE");

			Default.AssertTextEquals(
				locator1 = "Environment#ENVIRONMENTS_DROPDOWN",
				value1 = "infra");
		}
	}

	@description = "Environment info section displays a link to the corresponding documentation"
	@priority = 3
	test LinkToDocumentation {
		task ("When: I access the Environment Info tab on the Settings page") {
			Utils.goToPage(page = "projects/${staticProjectIdInfra}/settings/info");
		}

		task ("And: I click the Learn More link") {
			Default.Click(locator1 = "Utils#LEARN_MORE_LINK");
		}

		task ("Then: The corresponding documentation page opens") {
			Default.SelectWindow(locator1 = "title=Understanding Liferay Cloud Environments — Liferay Learn");

			Default.AssertElementPresent(
				key_headerTitle = "Understanding Liferay Cloud Environments",
				locator1 = "Utils#HEADER_TITLE");
		}
	}

}