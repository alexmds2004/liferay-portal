@component-name = "liferay-cloud-platform"
definition {

	property testray.main.component.name = "liferay-cloud-platform";

	setUp {
		Utils.launchLCP();
	}

	@description = "User can login and logout successfully with valid email and password"
	@priority = 5
	test CanLoginInConsole {
		task ("Given: I provide correct login credentials") {
			LoginPage.login();
		}

		task ("Then: Console redirects to Projects page") {
			Default.AssertElementPresent(
				key_headerTitle = "Projects",
				locator1 = "Utils#HEADER_TITLE");
		}
	}

	@description = "User can login and logout successfully with valid email and password"
	@priority = 5
	test CanLogoutInConsole {
		task ("Given: I log in") {
			LoginPage.login();
		}

		task ("When: I log out") {
			Topbar.logout();
		}

		task ("Then: Console redirects to Login page") {
			Default.AssertElementPresent(
				key_headerTitle = "Log Into Your Account",
				locator1 = "Utils#HEADER_TITLE");
		}
	}

}