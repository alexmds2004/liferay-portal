#@testray.main.component.name = "Support QA"
#@type = "zendesk.integration"

Feature: OSB to Zendesk Partner Integration
	Testing user partner updates in Zendesk caused by changes in OSB.

Setup:
	* I get all test passwords and admin emails
	* I generate a provisioning project name
	* I create a user with the following parameters
	| Screen Name   | osb.partner1                      |
	| Email Address | osb.partner1@lrpartner.com.broken |
	| First Name    | OSB                               |
	| Last Name     | Partner1                          |
	* I create a user with the following parameters
	| Screen Name   | osb.partner2                      |
	| Email Address | osb.partner2@lrpartner.com.broken |
	| First Name    | OSB                               |
	| Last Name     | Partner2                          |
	* I generate static start and end dates
	* I create an account on koroneiki with the following values
	| Name   | Osbpar - ${staticProjectName} | |
	| Code   | OSBPAROSBPAR                  | |
	| Status | Active                        | |
	* I create a product purchase for an account with the name "Osbpar - ${staticProjectName}" and the product "Service Partnership" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	* I assign the contact with the email address "osb.partner1@lrpartner.com.broken" on the account "Osbpar - ${staticProjectName}" to the following roles
	| Partner Member |
	* I assign the contact with the email address "osb.partner2@lrpartner.com.broken" on the account "Osbpar - ${staticProjectName}" to the following roles
	| Partner Manager |
	* I synchronize the account entitlement definition with the name "Partner"
	* I create an assigned team nested field for a team with the name "Osbpar - ${staticProjectName}" assigned to the account "Osbzen - ${staticProjectName}"

Teardown:
	* I get the ID of the default user belonging to the organization "Osbzen - ${staticProjectName}" from zendesk.liferay.com
	* I rename the first default user ID variable
	* I get the ID of the default user belonging to the organization "Osbpar - ${staticProjectName}" from zendesk.liferay.com
	* I rename the second default user ID variable
	* I tear down the projects with the following project codes
	| OSBPAROSBPAR |
	| OSBZENOSBZEN |
	* I tear down the accounts on koroneiki with the following names
	| Osbpar - ${staticProjectName} |
	| Osbzen - ${staticProjectName} |
	* I tear down the accounts on koroneiki with the following "Code" values
	| OSBPAROSBPAR |
	| OSBZENOSBZEN |
	* I synchronize the account entitlement definition with the name "Gold Subscription"
	* I tear down the users with the following email addresses from web.liferay.com
	| osb.partner1@lrpartner.com.broken |
	| osb.partner2@lrpartner.com.broken |
	* I tear down the users with the following email addresses from customer.liferay.com
	| osb.partner1@lrpartner.com.broken |
	| osb.partner2@lrpartner.com.broken |
	* I tear down the users with the following email addresses from koroneiki
	| osb.partner1@lrpartner.com.broken |
	| osb.partner2@lrpartner.com.broken |
	* I tear down the contacts on koroneiki with the following email addresses
	| osb.partner1@lrpartner.com.broken |
	| osb.partner2@lrpartner.com.broken |
	* I tear down the users with the following email addresses from okta
	| osb.partner1@lrpartner.com.broken |
	| osb.partner2@lrpartner.com.broken |
	* I obtain the IDs of the following user email addresses from zendesk.liferay.com
	| osb.partner1@lrpartner.com.broken |
	| osb.partner2@lrpartner.com.broken |
	* I teardown users with the following userIds "${staticUserIdList}" from zendesk.liferay.com
	* I obtain the external IDs of the following organizations from zendesk.liferay.com if still present
	| Osbpar - ${staticProjectName} |
	| Osbzen - ${staticProjectName} |
	* I teardown organizations with the following organizationExternalIds "${staticOrganizationExternalIdList}" from zendesk.liferay.com
	* I teardown the user with userId "${staticDefaultUserId1}" from zendesk.liferay.com
	* I teardown the user with userId "${staticDefaultUserId2}" from zendesk.liferay.com

Scenario: View new partner workers
	@ help.center.acceptance = "true"
	New partner workers who are added to Zendesk via OSB sync should be in their appropriate organization with their appropriate tags.

	Given I create an account on koroneiki with the following values
	| Assigned Team Nested Field | ${staticAssignedTeamNestedField} | |
	| Code                       | OSBZENOSBZEN                     | |
	| Language                   | English                          | |
	| Name                       | Osbzen - ${staticProjectName}    | |
	| Region                     | United States                    | |
	| Status                     | Active                           | |
	| Tier                       | T1                               | |
	And I assign the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" to the following roles
	| Partner |
	And I assign the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" to the following roles
	| First Line Support |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner1@lrpartner.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner2@lrpartner.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner1@lrpartner.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner2@lrpartner.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner1@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner1 [P]"
	And the user with the email address "osb.partner2@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner2 [P]"
	And the user with the email address "osb.partner1@lrpartner.com.broken" should be subscribed to the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner2@lrpartner.com.broken" should be subscribed to the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner1@lrpartner.com.broken" should have the "osb_kb" tag
	And the user with the email address "osb.partner2@lrpartner.com.broken" should have the "osb_kb" tag
	And the user with the email address "osb.partner1@lrpartner.com.broken" should have the "osb_partner" tag
	And the user with the email address "osb.partner2@lrpartner.com.broken" should have the "osb_partner" tag

Scenario: View new partner workers on fls only project
	New partner workers on fls only projects who are added to Zendesk via OSB sync should be in their appropriate organization with their appropriate tags.

	Given I create an account on koroneiki with the following values
	| Assigned Team Nested Field | ${staticAssignedTeamNestedField} | |
	| Code                       | OSBZENOSBZEN                     | |
	| Language                   | English                          | |
	| Name                       | Osbzen - ${staticProjectName}    | |
	| Region                     | United States                    | |
	| Status                     | Active                           | |
	| Tier                       | T1                               | |
	And I assign the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" to the following roles
	| First Line Support |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner1@lrpartner.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner2@lrpartner.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner1@lrpartner.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner2@lrpartner.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner1@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner1 [P]"
	And the user with the email address "osb.partner2@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner2 [P]"
	And the user with the email address "osb.partner1@lrpartner.com.broken" should be subscribed to the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner2@lrpartner.com.broken" should be subscribed to the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner1@lrpartner.com.broken" should have the "osb_kb" tag
	And the user with the email address "osb.partner2@lrpartner.com.broken" should have the "osb_kb" tag
	And the user with the email address "osb.partner1@lrpartner.com.broken" should have the "osb_partner" tag
	And the user with the email address "osb.partner2@lrpartner.com.broken" should have the "osb_partner" tag

Scenario: View new partner workers on non fls project
	New partner workers on non fls projects who are added to Zendesk via OSB sync should be in their appropriate organization with their appropriate tags.

	Given I create an account on koroneiki with the following values
	| Assigned Team Nested Field | ${staticAssignedTeamNestedField} | |
	| Code                       | OSBZENOSBZEN                     | |
	| Language                   | English                          | |
	| Name                       | Osbzen - ${staticProjectName}    | |
	| Region                     | United States                    | |
	| Status                     | Active                           | |
	| Tier                       | T1                               | |
	And I assign the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" to the following roles
	| Partner |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner1@lrpartner.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner2@lrpartner.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner1@lrpartner.com.broken" should not be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner2@lrpartner.com.broken" should not be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner1@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner1 [P]"
	And the user with the email address "osb.partner2@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner2 [P]"
	And the user with the email address "osb.partner1@lrpartner.com.broken" should not be subscribed to the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner2@lrpartner.com.broken" should not be subscribed to the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner1@lrpartner.com.broken" should have the "osb_kb" tag
	And the user with the email address "osb.partner2@lrpartner.com.broken" should have the "osb_kb" tag
	And the user with the email address "osb.partner1@lrpartner.com.broken" should not have the "osb_partner" tag
	And the user with the email address "osb.partner2@lrpartner.com.broken" should not have the "osb_partner" tag

Scenario: View resynced partner workers
	Partner workers who are resynced to Zendesk should be in their appropriate organization with their appropriate tags.

	Given I create an account on koroneiki with the following values
	| Assigned Team Nested Field | ${staticAssignedTeamNestedField} | |
	| Code                       | OSBZENOSBZEN                     | |
	| Language                   | English                          | |
	| Name                       | Osbzen - ${staticProjectName}    | |
	| Region                     | United States                    | |
	| Status                     | Active                           | |
	| Tier                       | T1                               | |
	And I assign the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" to the following roles
	| Partner |
	And I assign the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" to the following roles
	| First Line Support |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner1@lrpartner.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner2@lrpartner.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner1@lrpartner.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner2@lrpartner.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner1@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner1 [P]"
	And the user with the email address "osb.partner2@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner2 [P]"
	When I remove a user with the email address "osb.partner1@lrpartner.com.broken" from an organization with the name "Osbzen - ${staticProjectName}"
	And I remove a user with the email address "osb.partner2@lrpartner.com.broken" from an organization with the name "Osbzen - ${staticProjectName}"
	And I remove all tags from the user with the email address "osb.partner1@lrpartner.com.broken"
	And I remove all tags from the user with the email address "osb.partner2@lrpartner.com.broken"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "10" seconds
	And I get all test passwords and admin emails
	And I log into the support homepage as "${staticSupportAdminEmailAdress}" with password "${staticSupportAdminPassword}"
	And I go to the OSB admin main page
	And I sync the project with the code "OSBZENOSBZEN" to zendesk
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the user with the email address "osb.partner1@lrpartner.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner2@lrpartner.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner1@lrpartner.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner2@lrpartner.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner1@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner1 [P]"
	And the user with the email address "osb.partner2@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner2 [P]"
	And the user with the email address "osb.partner1@lrpartner.com.broken" should be subscribed to the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner2@lrpartner.com.broken" should be subscribed to the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner1@lrpartner.com.broken" should have the "osb_kb" tag
	And the user with the email address "osb.partner2@lrpartner.com.broken" should have the "osb_kb" tag
	And the user with the email address "osb.partner1@lrpartner.com.broken" should have the "osb_partner" tag
	And the user with the email address "osb.partner2@lrpartner.com.broken" should have the "osb_partner" tag

Scenario: View resynced partner workers via provisioning
	Partner workers who are resynced to Zendesk via provisioning should be in their appropriate organization with their appropriate tags.

	Given I create an account on koroneiki with the following values
	| Assigned Team Nested Field | ${staticAssignedTeamNestedField} | |
	| Code                       | OSBZENOSBZEN                     | |
	| Language                   | English                          | |
	| Name                       | Osbzen - ${staticProjectName}    | |
	| Region                     | United States                    | |
	| Status                     | Active                           | |
	| Tier                       | T1                               | |
	And I assign the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" to the following roles
	| Partner |
	And I assign the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" to the following roles
	| First Line Support |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner1@lrpartner.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner2@lrpartner.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner1@lrpartner.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner2@lrpartner.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner1@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner1 [P]"
	And the user with the email address "osb.partner2@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner2 [P]"
	When I remove a user with the email address "osb.partner1@lrpartner.com.broken" from an organization with the name "Osbzen - ${staticProjectName}"
	And I remove a user with the email address "osb.partner2@lrpartner.com.broken" from an organization with the name "Osbzen - ${staticProjectName}"
	And I remove all tags from the user with the email address "osb.partner1@lrpartner.com.broken"
	And I remove all tags from the user with the email address "osb.partner2@lrpartner.com.broken"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "10" seconds
	And I get all test passwords and admin emails
	And I log into the provisioning homepage as "${staticProvisioningAdminEmailAddress}" with password "${staticProvisioningAdminPassword}"
	And I go to the account index page
	And I search the accounts page using the keyword "Osbzen"
	And I click on the account with the name "Osbzen - ${staticProjectName}" on the provisioning index
	And I click on the Sync To Zendesk button on the account page
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the user with the email address "osb.partner1@lrpartner.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner2@lrpartner.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner1@lrpartner.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner2@lrpartner.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner1@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner1 [P]"
	And the user with the email address "osb.partner2@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner2 [P]"
	And the user with the email address "osb.partner1@lrpartner.com.broken" should be subscribed to the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner2@lrpartner.com.broken" should be subscribed to the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner1@lrpartner.com.broken" should have the "osb_kb" tag
	And the user with the email address "osb.partner2@lrpartner.com.broken" should have the "osb_kb" tag
	And the user with the email address "osb.partner1@lrpartner.com.broken" should have the "osb_partner" tag
	And the user with the email address "osb.partner2@lrpartner.com.broken" should have the "osb_partner" tag

Scenario: View expired project partner worker updates
	If a projects order becomes inactive in OSB, existing partner workers on that project should have their tags removed and also be removed from their organization in Zendesk.

	Given I create an account on koroneiki with the following values
	| Assigned Team Nested Field | ${staticAssignedTeamNestedField} | |
	| Code                       | OSBZENOSBZEN                     | |
	| Language                   | English                          | |
	| Name                       | Osbzen - ${staticProjectName}    | |
	| Region                     | United States                    | |
	| Status                     | Active                           | |
	| Tier                       | T1                               | |
	And I assign the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" to the following roles
	| Partner |
	And I assign the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" to the following roles
	| First Line Support |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner1@lrpartner.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner2@lrpartner.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner1@lrpartner.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner2@lrpartner.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner1@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner1 [P]"
	And the user with the email address "osb.partner2@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner2 [P]"
	When I generate past static start and end dates
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "Portal Production" to have the following values
	| End Date                | ${staticPastEndDate}         | |
	| Original End Date       | ${staticPastOriginalEndDate} | |
	| Perpetual               | false                        | |
	| Start Date              | ${staticPastStartDate}       | |
	| Status                  | Approved                     | |
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "Gold Subscription" to have the following values
	| End Date                | ${staticPastEndDate}         | |
	| Original End Date       | ${staticPastOriginalEndDate} | |
	| Perpetual               | false                        | |
	| Start Date              | ${staticPastStartDate}       | |
	| Status                  | Approved                     | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	Then the user with the email address "osb.partner1@lrpartner.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner2@lrpartner.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner1@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner1 [P]"
	And the user with the email address "osb.partner2@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner2 [P]"
	And the user with the email address "osb.partner1@lrpartner.com.broken" should be subscribed to the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner2@lrpartner.com.broken" should be subscribed to the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner1@lrpartner.com.broken" should have the "osb_kb" tag
	And the user with the email address "osb.partner2@lrpartner.com.broken" should have the "osb_kb" tag
	And the user with the email address "osb.partner1@lrpartner.com.broken" should have the "osb_partner" tag
	And the user with the email address "osb.partner2@lrpartner.com.broken" should have the "osb_partner" tag
#	When I update the user with the email address "osb.partner1@lrpartner.com.broken" to use the password "${staticZendeskAdminPassword}"
#	And I update the user with the email address "osb.partner2@lrpartner.com.broken" to use the password "${staticZendeskAdminPassword}"
#	And I log into zendesk as "osb.partner1@lrpartner.com.broken" with password "${staticZendeskAdminPassword}"
#	And I go to the Submit a ticket form
#	Then the "Organization" dropdown should not be present
#	When I click on the "Product" dropdown
#	Then the following options should not be visible
#	| Liferay Portal |
#	When I log out and log into zendesk as "osb.partner2@lrpartner.com.broken" with password "${staticZendeskAdminPassword}"
#	And I go to the Submit a ticket form
#	Then the "Organization" dropdown should not be present
#	When I click on the "Product" dropdown
#	Then the following options should not be visible
#	| Liferay Portal |

Scenario: View renewed project partner worker updates
	If a projects order becomes active from inactive in OSB, existing partner workers on that project should have their tags and also be part of their organization in Zendesk.

	Given I create an account on koroneiki with the following values
	| Assigned Team Nested Field | ${staticAssignedTeamNestedField} | |
	| Code                       | OSBZENOSBZEN                     | |
	| Language                   | English                          | |
	| Name                       | Osbzen - ${staticProjectName}    | |
	| Region                     | United States                    | |
	| Status                     | Active                           | |
	| Tier                       | T1                               | |
	And I assign the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" to the following roles
	| Partner |
	And I assign the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" to the following roles
	| First Line Support |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner1@lrpartner.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner2@lrpartner.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner1@lrpartner.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner2@lrpartner.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner1@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner1 [P]"
	And the user with the email address "osb.partner2@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner2 [P]"
	When I generate past static start and end dates
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "Portal Production" to have the following values
	| End Date                | ${staticPastEndDate}         | |
	| Original End Date       | ${staticPastOriginalEndDate} | |
	| Perpetual               | false                        | |
	| Start Date              | ${staticPastStartDate}       | |
	| Status                  | Approved                     | |
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "Gold Subscription" to have the following values
	| End Date                | ${staticPastEndDate}         | |
	| Original End Date       | ${staticPastOriginalEndDate} | |
	| Perpetual               | false                        | |
	| Start Date              | ${staticPastStartDate}       | |
	| Status                  | Approved                     | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "10" seconds
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "Portal Production" to have the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "Gold Subscription" to have the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner1@lrpartner.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner2@lrpartner.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner1@lrpartner.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner2@lrpartner.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner1@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner1 [P]"
	And the user with the email address "osb.partner2@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner2 [P]"
	And the user with the email address "osb.partner1@lrpartner.com.broken" should be subscribed to the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner2@lrpartner.com.broken" should be subscribed to the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner1@lrpartner.com.broken" should have the "osb_kb" tag
	And the user with the email address "osb.partner2@lrpartner.com.broken" should have the "osb_kb" tag
	And the user with the email address "osb.partner1@lrpartner.com.broken" should have the "osb_partner" tag
	And the user with the email address "osb.partner2@lrpartner.com.broken" should have the "osb_partner" tag

Scenario: View partner removal updates
	If a projects order becomes inactive in OSB, existing partner workers on that project should have their tags removed and also be removed from their organization in Zendesk.

	Given I create an account on koroneiki with the following values
	| Assigned Team Nested Field | ${staticAssignedTeamNestedField} | |
	| Code                       | OSBZENOSBZEN                     | |
	| Language                   | English                          | |
	| Name                       | Osbzen - ${staticProjectName}    | |
	| Region                     | United States                    | |
	| Status                     | Active                           | |
	| Tier                       | T1                               | |
	And I assign the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" to the following roles
	| Partner |
	And I assign the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" to the following roles
	| First Line Support |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner1@lrpartner.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner2@lrpartner.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner1@lrpartner.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner2@lrpartner.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner1@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner1 [P]"
	And the user with the email address "osb.partner2@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner2 [P]"
	When I remove the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" from the following roles
	| Partner |
	And I remove the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" from the following roles
	| First Line Support |
	Then the user with the email address "osb.partner1@lrpartner.com.broken" should not be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner2@lrpartner.com.broken" should not be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner1@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner1 [P]"
	And the user with the email address "osb.partner2@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner2 [P]"
	And the user with the email address "osb.partner1@lrpartner.com.broken" should not be subscribed to the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner2@lrpartner.com.broken" should not be subscribed to the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner1@lrpartner.com.broken" should have the "osb_kb" tag
	And the user with the email address "osb.partner2@lrpartner.com.broken" should have the "osb_kb" tag
	And the user with the email address "osb.partner1@lrpartner.com.broken" should not have the "osb_partner" tag
	And the user with the email address "osb.partner2@lrpartner.com.broken" should not have the "osb_partner" tag

Scenario: View partner first line support removal updates
	If a projects order becomes inactive in OSB, existing partner workers on that project should have their tags removed and also be removed from their organization in Zendesk.

	Given I create an account on koroneiki with the following values
	| Assigned Team Nested Field | ${staticAssignedTeamNestedField} | |
	| Code                       | OSBZENOSBZEN                     | |
	| Language                   | English                          | |
	| Name                       | Osbzen - ${staticProjectName}    | |
	| Region                     | United States                    | |
	| Status                     | Active                           | |
	| Tier                       | T1                               | |
	And I assign the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" to the following roles
	| Partner |
	And I assign the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" to the following roles
	| First Line Support |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner1@lrpartner.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner2@lrpartner.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner1@lrpartner.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner2@lrpartner.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner1@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner1 [P]"
	And the user with the email address "osb.partner2@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner2 [P]"
	When I remove the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" from the following roles
	| First Line Support |
	Then the user with the email address "osb.partner1@lrpartner.com.broken" should not be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner2@lrpartner.com.broken" should not be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner1@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner1 [P]"
	And the user with the email address "osb.partner2@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner2 [P]"
	And the user with the email address "osb.partner1@lrpartner.com.broken" should not be subscribed to the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner2@lrpartner.com.broken" should not be subscribed to the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner1@lrpartner.com.broken" should have the "osb_kb" tag
	And the user with the email address "osb.partner2@lrpartner.com.broken" should have the "osb_kb" tag
	And the user with the email address "osb.partner1@lrpartner.com.broken" should not have the "osb_partner" tag
	And the user with the email address "osb.partner2@lrpartner.com.broken" should not have the "osb_partner" tag

Scenario: View new partner workers without initial project
	New partner workers who are added to Zendesk via OSB sync but have a partner that does not belong to a project should be in their appropriate organization with their appropriate tags.

	Given I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T1                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner1@lrpartner.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner2@lrpartner.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner1@lrpartner.com.broken" should not be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner2@lrpartner.com.broken" should not be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner1@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner1 [P]"
	And the user with the email address "osb.partner2@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner2 [P]"
	And the user with the email address "osb.partner1@lrpartner.com.broken" should not be subscribed to the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner2@lrpartner.com.broken" should not be subscribed to the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner1@lrpartner.com.broken" should have the "osb_kb" tag
	And the user with the email address "osb.partner2@lrpartner.com.broken" should have the "osb_kb" tag
	And the user with the email address "osb.partner1@lrpartner.com.broken" should not have the "osb_partner" tag
	And the user with the email address "osb.partner2@lrpartner.com.broken" should not have the "osb_partner" tag

Scenario: View resynced partner workers without initial project via provisioning
	Resynced partner workers via provisioning who are added to Zendesk via OSB sync but have a partner that does not belong to a project should be in their appropriate organization with their appropriate tags.

	Given I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T1                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner1@lrpartner.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner2@lrpartner.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner1@lrpartner.com.broken" should not be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner2@lrpartner.com.broken" should not be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner1@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner1 [P]"
	And the user with the email address "osb.partner2@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner2 [P]"
	When I update the user with the email address "osb.partner1@lrpartner.com.broken" to have the name "OSB Partner1"
	And I update the user with the email address "osb.partner2@lrpartner.com.broken" to have the name "OSB Partner2"
	And I remove all tags from the user with the email address "osb.partner1@lrpartner.com.broken"
	And I remove all tags from the user with the email address "osb.partner2@lrpartner.com.broken"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "10" seconds
	And I get all test passwords and admin emails
	And I log into the provisioning homepage as "${staticProvisioningAdminEmailAddress}" with password "${staticProvisioningAdminPassword}"
	And I go to the account index page
	And I search the accounts page using the keyword "Osbpar"
	And I click on the account with the name "Osbpar - ${staticProjectName}" on the provisioning index
	And I click on the Sync To Zendesk button on the account page
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the user with the email address "osb.partner1@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner1 [P]"
	And the user with the email address "osb.partner2@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner2 [P]"
	And the user with the email address "osb.partner1@lrpartner.com.broken" should not be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner2@lrpartner.com.broken" should not be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner1@lrpartner.com.broken" should not be subscribed to the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner2@lrpartner.com.broken" should not be subscribed to the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner1@lrpartner.com.broken" should have the "osb_kb" tag
	And the user with the email address "osb.partner2@lrpartner.com.broken" should have the "osb_kb" tag
	And the user with the email address "osb.partner1@lrpartner.com.broken" should not have the "osb_partner" tag
	And the user with the email address "osb.partner2@lrpartner.com.broken" should not have the "osb_partner" tag

Scenario: View partner member removal updates
	If a member is removed from a partner tied to a project in OSB, the members tags should be removed and the member should also be removed from the organization in Zendesk.

	Given I create an account on koroneiki with the following values
	| Assigned Team Nested Field | ${staticAssignedTeamNestedField} | |
	| Code                       | OSBZENOSBZEN                     | |
	| Language                   | English                          | |
	| Name                       | Osbzen - ${staticProjectName}    | |
	| Region                     | United States                    | |
	| Status                     | Active                           | |
	| Tier                       | T1                               | |
	And I assign the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" to the following roles
	| Partner |
	And I assign the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" to the following roles
	| First Line Support |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner1@lrpartner.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner2@lrpartner.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.partner1@lrpartner.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner2@lrpartner.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner1@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner1 [P]"
	And the user with the email address "osb.partner2@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner2 [P]"
	When I remove customer contacts with the following email addresses from the account "Osbpar - ${staticProjectName}"
	| osb.partner2@lrpartner.com.broken |
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the user with the email address "osb.partner1@lrpartner.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner2@lrpartner.com.broken" should not be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner1@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner1 [P]"
	And the user with the email address "osb.partner2@lrpartner.com.broken" on zendesk.liferay.com should have the name "OSB Partner2"
	And the user with the email address "osb.partner1@lrpartner.com.broken" should be subscribed to the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner2@lrpartner.com.broken" should not be subscribed to the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.partner1@lrpartner.com.broken" should have the "osb_kb" tag
	And the user with the email address "osb.partner2@lrpartner.com.broken" should not have the "osb_kb" tag
	And the user with the email address "osb.partner1@lrpartner.com.broken" should have the "osb_partner" tag
	And the user with the email address "osb.partner2@lrpartner.com.broken" should not have the "osb_partner" tag