@testray.main.component.name = "Support QA"
@type = "zendesk.integration"

Feature: OSB to Zendesk Project Integration
	Testing project sync updates in Zendesk caused by changes in OSB.

Setup:
	* I get all test passwords and admin emails

Teardown:
	* I get the ID of the default user belonging to the organization "Osbzen - ${staticProjectName}" from zendesk.liferay.com
	* I rename the first default user ID variable
	* I get the ID of the default user belonging to the organization "Osbpar - ${staticProjectName}" from zendesk.liferay.com
	* I rename the second default user ID variable
	* I tear down the projects with the following project codes
	| OSBPAROSBPAR |
	| OSBZENOSBZEN |
	* I tear down the accounts on koroneiki with the following names
	| Osbpar - ${staticProjectName} |
	| Osbzen - ${staticProjectName} |
	* I tear down the accounts on koroneiki with the following "Code" values
	| OSBPAROSBPAR |
	| OSBZENOSBZEN |
	* I synchronize the account entitlement definition with the name "Platinum Subscription"
	* I synchronize the account entitlement definition with the name "Gold Subscription"
	* I synchronize the account entitlement definition with the name "Limited Subscription"
	* I synchronize the account entitlement definition with the name "Premium Subscription"
	* I obtain the external IDs of the following organizations from zendesk.liferay.com if still present
	| Osbpar - ${staticProjectName} |
	| Osbzen - ${staticProjectName} |
	* I teardown organizations with the following organizationExternalIds "${staticOrganizationExternalIdList}" from zendesk.liferay.com
	* I teardown the user with userId "${staticDefaultUserId1}" from zendesk.liferay.com
	* I teardown the user with userId "${staticDefaultUserId2}" from zendesk.liferay.com

Scenario: View new portal project
	New Portal projects that are added to Zendesk via OSB sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T4                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the organization "Osbzen - ${staticProjectName}" should have the tag "liferay_portal"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "gold"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "t1"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "closed"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Gold                          |
	| Status                     | Approved                      |
	| Support Region             | US                            |
	| Support Language           | English                       |
	| Tier                       | T4                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner First Line Support | false                         |
	And the support account ID of the project with the code "OSBZENOSBZEN" should match the organization external ID on zendesk.liferay.com
	When I get the organization ID of the organization "Osbzen - ${staticProjectName}" from zendesk.liferay.com
	Then the external link with the entity id "${staticOrganizationID}" and domain "zendesk" should be present on the account "Osbzen - ${staticProjectName}" with the following values
	| Domain      | zendesk                 |
	| Entity Id   | ${staticOrganizationID} |

Scenario: View resynced portal project
	Portal projects that are resynced to Zendesk via OSB sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T4                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	When I assign an organization "Osbzen - ${staticProjectName}" with the following tags while replacing existing tags
	| liferay_dxp |
	| t1          |
	| platinum    |
	| closed      |
	| japan       |
	| japanese    |
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "10" seconds
	And I log into the support homepage as "${staticSupportAdminEmailAdress}" with password "${staticSupportAdminPassword}"
	And I go to the OSB admin main page
	And I sync the project with the code "OSBZENOSBZEN" to zendesk
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the organization "Osbzen - ${staticProjectName}" should have the tag "liferay_portal"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "gold"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "t1"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "closed"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "japan"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "japanese"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Gold                          |
	| Status                     | Approved                      |
	| Support Region             | US                            |
	| Support Language           | English                       |
	| Tier                       | T4                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner First Line Support | false                         |
	And the support account ID of the project with the code "OSBZENOSBZEN" should match the organization external ID on zendesk.liferay.com

Scenario: View resynced deleted portal project
	Deleted portal projects that are resynced to Zendesk via OSB sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T4                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	When I get the organization ID of the organization "Osbzen - ${staticProjectName}" from zendesk.liferay.com
	And I teardown the organization with organizationId "${staticOrganizationID}" from zendesk.liferay.com
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "10" seconds
	And I log into the support homepage as "${staticSupportAdminEmailAdress}" with password "${staticSupportAdminPassword}"
	And I go to the OSB admin main page
	And I sync the project with the code "OSBZENOSBZEN" to zendesk
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the organization "Osbzen - ${staticProjectName}" should have the tag "liferay_portal"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "gold"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "t1"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "closed"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "japan"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "japanese"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street    |
	| Details                    | United States      |
	| Details                    | 123 Main Street    |
	| Details                    | California         |
	| SLA                        | Gold               |
	| Status                     | Approved           |
	| Support Region             | US                 |
	| Support Language           | English            |
	| Tier                       | T4                 |
	| Account Code               | OSBZENOSBZEN       |
	| Partner First Line Support | false              |
	And the support account ID of the project with the code "OSBZENOSBZEN" should match the organization external ID on zendesk.liferay.com

Scenario: View resynced portal project via provisioning
	Portal projects that are resynced to Zendesk via provisioning sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T4                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	When I assign an organization "Osbzen - ${staticProjectName}" with the following tags while replacing existing tags
	| liferay_dxp |
	| t1          |
	| platinum    |
	| closed      |
	| japan       |
	| japanese    |
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "10" seconds
	And I log into the provisioning homepage as "${staticProvisioningAdminEmailAddress}" with password "${staticProvisioningAdminPassword}"
	And I go to the account index page
	And I search the accounts page using the keyword "Osbzen"
	And I click on the account with the name "Osbzen - ${staticProjectName}" on the provisioning index
	And I click on the Sync To Zendesk button on the account page
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the organization "Osbzen - ${staticProjectName}" should have the tag "liferay_portal"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "gold"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "t1"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "closed"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "japan"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "japanese"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Gold                          |
	| Status                     | Approved                      |
	| Support Region             | US                            |
	| Support Language           | English                       |
	| Tier                       | T4                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner First Line Support | false                         |
	And the support account ID of the project with the code "OSBZENOSBZEN" should match the organization external ID on zendesk.liferay.com

Scenario: View resynced deleted portal project via provisioning
	Deleted portal projects that are resynced to Zendesk via provisioning sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T4                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	When I get the organization ID of the organization "Osbzen - ${staticProjectName}" from zendesk.liferay.com
	And I teardown the organization with organizationId "${staticOrganizationID}" from zendesk.liferay.com
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "10" seconds
	And I log into the provisioning homepage as "${staticProvisioningAdminEmailAddress}" with password "${staticProvisioningAdminPassword}"
	And I go to the account index page
	And I search the accounts page using the keyword "Osbzen"
	And I click on the account with the name "Osbzen - ${staticProjectName}" on the provisioning index
	And I click on the Sync To Zendesk button on the account page
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the organization "Osbzen - ${staticProjectName}" should have the tag "liferay_portal"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "gold"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "t1"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "closed"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "japan"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "japanese"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | 123 Main Street               |
	| Details                    | California                    |
	| SLA                        | Gold                          |
	| Status                     | Approved                      |
	| Support Region             | US                            |
	| Support Language           | English                       |
	| Tier                       | T4                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner First Line Support | false                         |
	And the support account ID of the project with the code "OSBZENOSBZEN" should match the organization external ID on zendesk.liferay.com

Scenario: View new dxp project
	@ help.center.acceptance = "true"
	New DXP projects that are added to Zendesk via OSB sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T1                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 7 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the organization "Osbzen - ${staticProjectName}" should have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t1"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_portal"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "gold"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "closed"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Platinum                      |
	| Status                     | Approved                      |
	| Support Region             | US                            |
	| Support Language           | English                       |
	| Tier                       | T1                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner First Line Support | false                         |
	And the support account ID of the project with the code "OSBZENOSBZEN" should match the organization external ID on zendesk.liferay.com
	When I get the organization ID of the organization "Osbzen - ${staticProjectName}" from zendesk.liferay.com
	Then the external link with the entity id "${staticOrganizationID}" and domain "zendesk" should be present on the account "Osbzen - ${staticProjectName}" with the following values
	| Domain      | zendesk                 |
	| Entity Id   | ${staticOrganizationID} |

Scenario: View new dxp project with no subscription
	New DXP projects that should not be added to Zendesk via OSB with no subscription.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T1                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 7 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	Then the support organization with the code "OSBZENOSBZEN" should not be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should not be present on zendesk.liferay.com

Scenario: View new dxp project with future platinum subscription
	New DXP projects that should not be added to Zendesk via OSB with future platinum subscription.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T1                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I generate future static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 7 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Korone - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticFutureEndDate}         | |
	| Original End Date       | ${staticFutureOriginalEndDate} | |
	| Perpetual               | false                          | |
	| Quantity                | 1                              | |
	| Start Date              | ${staticFutureStartDate}       | |
	| Status                  | Approved                       | |
	Then the support organization with the code "OSBZENOSBZEN" should not be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should not be present on zendesk.liferay.com

Scenario: View new future dxp project with future platinum subscription
	New future DXP projects that should not be added to Zendesk via OSB with future platinum subscription.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T1                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I generate future static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticFutureEndDate}         |   |
	| Original End Date       | ${staticFutureOriginalEndDate} |   |
	| Perpetual               | false                          |   |
	| Product Field and Value | sizing                         | 1 |
	| Product Field and Value | licenses                       | 1 |
	| Product Field and Value | version                        | 7 |
	| Quantity                | 1                              |   |
	| Start Date              | ${staticFutureStartDate}       |   |
	| Status                  | Approved                       |   |
	And I create a product purchase for an account with the name "Korone - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticFutureEndDate}         | |
	| Original End Date       | ${staticFutureOriginalEndDate} | |
	| Perpetual               | false                          | |
	| Quantity                | 1                              | |
	| Start Date              | ${staticFutureStartDate}       | |
	| Status                  | Approved                       | |
	Then the support organization with the code "OSBZENOSBZEN" should not be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should not be present on zendesk.liferay.com

Scenario: View new dxp project with past platinum subscription
	New DXP projects that should not be added to Zendesk via OSB with past platinum subscription.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T1                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I generate past static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}               |   |
	| Original End Date       | ${staticOriginalEndDate}       |   |
	| Perpetual               | false                          |   |
	| Product Field and Value | sizing                         | 1 |
	| Product Field and Value | licenses                       | 1 |
	| Product Field and Value | version                        | 7 |
	| Quantity                | 1                              |   |
	| Start Date              | ${staticStartDate}             |   |
	| Status                  | Approved                       |   |
	And I create a product purchase for an account with the name "Korone - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticPastEndDate}         | |
	| Original End Date       | ${staticPastOriginalEndDate} | |
	| Perpetual               | false                        | |
	| Quantity                | 1                            | |
	| Start Date              | ${staticPastStartDate}       | |
	| Status                  | Approved                     | |
	Then the support organization with the code "OSBZENOSBZEN" should not be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should not be present on zendesk.liferay.com

Scenario: View new past dxp project with past platinum subscription
	New past DXP projects that should not be added to Zendesk via OSB with past platinum subscription.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T1                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I generate past static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticPastEndDate}           |   |
	| Original End Date       | ${staticPastOriginalEndDate}   |   |
	| Perpetual               | false                          |   |
	| Product Field and Value | sizing                         | 1 |
	| Product Field and Value | licenses                       | 1 |
	| Product Field and Value | version                        | 7 |
	| Quantity                | 1                              |   |
	| Start Date              | ${staticPastStartDate}         |   |
	| Status                  | Approved                       |   |
	And I create a product purchase for an account with the name "Korone - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticPastEndDate}         | |
	| Original End Date       | ${staticPastOriginalEndDate} | |
	| Perpetual               | false                        | |
	| Quantity                | 1                            | |
	| Start Date              | ${staticPastStartDate}       | |
	| Status                  | Approved                     | |
	Then the support organization with the code "OSBZENOSBZEN" should not be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should not be present on zendesk.liferay.com

Scenario: View resynced dxp project
	DXP projects that are resynced to Zendesk via OSB sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T1                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 7 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	When I assign an organization "Osbzen - ${staticProjectName}" with the following tags while replacing existing tags
	| liferay_portal |
	| t4             | 
	| gold           |
	| closed         |
	| japan          |
	| japanese       |
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "10" seconds
	And I log into the support homepage as "${staticSupportAdminEmailAdress}" with password "${staticSupportAdminPassword}"
	And I go to the OSB admin main page
	And I sync the project with the code "OSBZENOSBZEN" to zendesk
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the organization "Osbzen - ${staticProjectName}" should have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t1"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_portal"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "gold"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "closed"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "japan"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "japanese"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Platinum                      |
	| Status                     | Approved                      |
	| Support Region             | US                            |
	| Support Language           | English                       |
	| Tier                       | T1                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner First Line Support | false                         |
	And the support account ID of the project with the code "OSBZENOSBZEN" should match the organization external ID on zendesk.liferay.com

Scenario: View resynced deleted dxp project
	Deleted DXP projects that are resynced to Zendesk via OSB sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T1                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 7 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	When I get the organization ID of the organization "Osbzen - ${staticProjectName}" from zendesk.liferay.com
	And I teardown the organization with organizationId "${staticOrganizationID}" from zendesk.liferay.com
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "10" seconds
	And I log into the support homepage as "${staticSupportAdminEmailAdress}" with password "${staticSupportAdminPassword}"
	And I go to the OSB admin main page
	And I sync the project with the code "OSBZENOSBZEN" to zendesk
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the organization "Osbzen - ${staticProjectName}" should have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t1"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_portal"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "gold"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "closed"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "japan"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "japanese"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Platinum                      |
	| Status                     | Approved                      |
	| Support Region             | US                            |
	| Support Language           | English                       |
	| Tier                       | T1                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner First Line Support | false                         |
	And the support account ID of the project with the code "OSBZENOSBZEN" should match the organization external ID on zendesk.liferay.com

Scenario: View resynced dxp project via provisioning
	DXP projects that are resynced to Zendesk via provisioning sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T1                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 7 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	When I assign an organization "Osbzen - ${staticProjectName}" with the following tags while replacing existing tags
	| liferay_portal |
	| t4             | 
	| gold           |
	| closed         |
	| japan          |
	| japanese       |
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "10" seconds
	And I log into the provisioning homepage as "${staticProvisioningAdminEmailAddress}" with password "${staticProvisioningAdminPassword}"
	And I go to the account index page
	And I search the accounts page using the keyword "Osbzen"
	And I click on the account with the name "Osbzen - ${staticProjectName}" on the provisioning index
	And I click on the Sync To Zendesk button on the account page
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the organization "Osbzen - ${staticProjectName}" should have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t1"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_portal"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "gold"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "closed"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "japan"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "japanese"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Platinum                      |
	| Status                     | Approved                      |
	| Support Region             | US                            |
	| Support Language           | English                       |
	| Tier                       | T1                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner First Line Support | false                         |
	And the support account ID of the project with the code "OSBZENOSBZEN" should match the organization external ID on zendesk.liferay.com

Scenario: View resynced deleted dxp project via provisioning
	Deleted DXP projects that are resynced to Zendesk via provisioning sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T1                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 7 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	When I get the organization ID of the organization "Osbzen - ${staticProjectName}" from zendesk.liferay.com
	And I teardown the organization with organizationId "${staticOrganizationID}" from zendesk.liferay.com
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "10" seconds
	And I log into the provisioning homepage as "${staticProvisioningAdminEmailAddress}" with password "${staticProvisioningAdminPassword}"
	And I go to the account index page
	And I search the accounts page using the keyword "Osbzen"
	And I click on the account with the name "Osbzen - ${staticProjectName}" on the provisioning index
	And I click on the Sync To Zendesk button on the account page
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the organization "Osbzen - ${staticProjectName}" should have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t1"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_portal"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "gold"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "closed"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "japan"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "japanese"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Platinum                      |
	| Status                     | Approved                      |
	| Support Region             | US                            |
	| Support Language           | English                       |
	| Tier                       | T1                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner First Line Support | false                         |
	And the support account ID of the project with the code "OSBZENOSBZEN" should match the organization external ID on zendesk.liferay.com

Scenario: View no limited project
	New limited subscription projects should not be added to Zendesk via OSB sync

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T1                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 7 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Limited Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Limited Subscription"
	Then the organization "Osbzen - ${staticProjectName}" should not be present on zendesk.liferay.com

Scenario: View new dxp and portal project
	New Portal and DXP projects that are added to Zendesk via OSB sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T1                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 7 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the organization "Osbzen - ${staticProjectName}" should have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "liferay_portal"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t1"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "gold"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "closed"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Platinum                      |
	| Status                     | Approved                      |
	| Support Region             | US                            |
	| Support Language           | English                       |
	| Tier                       | T1                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner First Line Support | false                         |

Scenario: View expired portal project
	Expired portal projects that are updated to Zendesk via OSB sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T4                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	When I generate past static start and end dates
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "Portal Production" to have the following values
	| End Date                | ${staticPastEndDate}         | |
	| Original End Date       | ${staticPastOriginalEndDate} | |
	| Perpetual               | false                        | |
	| Start Date              | ${staticPastStartDate}       | |
	| Status                  | Approved                     | |
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "Gold Subscription" to have the following values
	| End Date                | ${staticPastEndDate}         | |
	| Original End Date       | ${staticPastOriginalEndDate} | |
	| Perpetual               | false                        | |
	| Start Date              | ${staticPastStartDate}       | |
	| Status                  | Approved                     | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_portal"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "gold"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "expired"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Gold                          |
	| Status                     | Expired                       |
	| Support Region             | US                            |
	| Support Language           | English                       |
	| Tier                       | T4                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner First Line Support | false                         |

Scenario: View future portal project
	Future portal projects that are updated to Zendesk via OSB sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T4                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	When I generate future static start and end dates
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "Portal Production" to have the following values
	| End Date                | ${staticFutureEndDate}         |   |
	| Original End Date       | ${staticFutureOriginalEndDate} |   |
	| Perpetual               | false                          |   |
	| Product Field and Value | sizing                         | 1 |
	| Product Field and Value | licenses                       | 1 |
	| Product Field and Value | version                        | 6 |
	| Quantity                | 1                              |   |
	| Start Date              | ${staticFutureStartDate}       |   |
	| Status                  | Approved                       |   |
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "Gold Subscription" to have the following values
	| End Date                | ${staticFutureEndDate}         | |
	| Original End Date       | ${staticFutureOriginalEndDate} | |
	| Perpetual               | false                          | |
	| Quantity                | 1                              | |
	| Start Date              | ${staticFutureStartDate}       | |
	| Status                  | Approved                       | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	And I log into the provisioning homepage as "${staticProvisioningAdminEmailAddress}" with password "${staticProvisioningAdminPassword}"
	And I go to the account index page
	And I search the accounts page using the keyword "Osbzen"
	And I click on the account with the name "Osbzen - ${staticProjectName}" on the provisioning index
	And I click on the Sync To Zendesk button on the account page
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the organization "Osbzen - ${staticProjectName}" should have the tag "liferay_portal"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "gold"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "closed"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| Status                     | Closed                        |
	| Support Region             | US                            |
	| Support Language           | English                       |
	| Tier                       | T4                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner First Line Support | false                         |

Scenario: View renewed portal project
	Renewed portal projects that are updated to Zendesk via OSB sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T4                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	When I generate past static start and end dates
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "Portal Production" to have the following values
	| End Date                | ${staticPastEndDate}         | |
	| Original End Date       | ${staticPastOriginalEndDate} | |
	| Perpetual               | false                        | |
	| Start Date              | ${staticPastStartDate}       | |
	| Status                  | Approved                     | |
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "Gold Subscription" to have the following values
	| End Date                | ${staticPastEndDate}         | |
	| Original End Date       | ${staticPastOriginalEndDate} | |
	| Perpetual               | false                        | |
	| Start Date              | ${staticPastStartDate}       | |
	| Status                  | Approved                     | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "10" seconds
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "Portal Production" to have the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "Gold Subscription" to have the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	When I wait for "1" minutes
	Then the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the organization "Osbzen - ${staticProjectName}" should have the tag "liferay_portal"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "gold"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "t1"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "closed"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Gold                          |
	| Status                     | Approved                      |
	| Support Region             | US                            |
	| Support Language           | English                       |
	| Tier                       | T4                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner First Line Support | false                         |
	And the support account ID of the project with the code "OSBZENOSBZEN" should match the organization external ID on zendesk.liferay.com

Scenario: View expired dxp project
	Expired dxp projects that are updated to Zendesk via OSB sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T1                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 7 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	When I generate past static start and end dates
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "DXP Production" to have the following values
	| End Date                | ${staticPastEndDate}         | |
	| Original End Date       | ${staticPastOriginalEndDate} | |
	| Perpetual               | false                        | |
	| Start Date              | ${staticPastStartDate}       | |
	| Status                  | Approved                     | |
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "Platinum Subscription" to have the following values
	| End Date                | ${staticPastEndDate}         | |
	| Original End Date       | ${staticPastOriginalEndDate} | |
	| Perpetual               | false                        | |
	| Start Date              | ${staticPastStartDate}       | |
	| Status                  | Approved                     | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t1"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "expired"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Platinum                      |
	| Status                     | Expired                       |
	| Support Region             | US                            |
	| Support Language           | English                       |
	| Tier                       | T1                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner First Line Support | false                         |

Scenario: View renewed dxp project
	Renewed dxp projects that are updated to Zendesk via OSB sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T1                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 7 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	When I generate past static start and end dates
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "DXP Production" to have the following values
	| End Date                | ${staticPastEndDate}         | |
	| Original End Date       | ${staticPastOriginalEndDate} | |
	| Perpetual               | false                        | |
	| Start Date              | ${staticPastStartDate}       | |
	| Status                  | Approved                     | |
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "Platinum Subscription" to have the following values
	| End Date                | ${staticPastEndDate}         | |
	| Original End Date       | ${staticPastOriginalEndDate} | |
	| Perpetual               | false                        | |
	| Start Date              | ${staticPastStartDate}       | |
	| Status                  | Approved                     | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "10" seconds
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "DXP Production" to have the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "Platinum Subscription" to have the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the organization "Osbzen - ${staticProjectName}" should have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t1"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_portal"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "gold"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "closed"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Platinum                      |
	| Status                     | Approved                      |
	| Support Region             | US                            |
	| Support Language           | English                       |
	| Tier                       | T1                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner First Line Support | false                         |
	And the support account ID of the project with the code "OSBZENOSBZEN" should match the organization external ID on zendesk.liferay.com

Scenario: View portal project with partner
	New portal partner projects that are added to Zendesk via OSB sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I generate static start and end dates
	And I create an account on koroneiki with the following values
	| Name   | Osbpar - ${staticProjectName} | |
	| Code   | OSBPAROSBPAR                  | |
	| Status | Active                        | |
	And I create a product purchase for an account with the name "Osbpar - ${staticProjectName}" and the product "Service Partnership" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Partner"
	And I create an assigned team nested field for a team with the name "Osbpar - ${staticProjectName}" assigned to the account "Osbzen - ${staticProjectName}"
	And I create an account on koroneiki with the following values
	| Assigned Team Nested Field | ${staticAssignedTeamNestedField} | |
	| Code                       | OSBZENOSBZEN                     | |
	| Language                   | English                          | |
	| Name                       | Osbzen - ${staticProjectName}    | |
	| Region                     | United States                    | |
	| Status                     | Active                           | |
	| Tier                       | T4                               | |
	And I assign the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" to the following roles
	| Partner |
	And I assign the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" to the following roles
	| First Line Support |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the organization "Osbzen - ${staticProjectName}" should have the tag "liferay_portal"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "gold"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Gold                          |
	| Status                     | Approved                      |
	| Support Region             | US                            |
	| Support Language           | English                       |
	| Tier                       | T4                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner Organization       | Osbpar - ${staticProjectName} |
	| Partner First Line Support | true                          |

Scenario: View dxp project with partner
	New dxp partner projects that are added to Zendesk via OSB sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I generate static start and end dates
	And I create an account on koroneiki with the following values
	| Name   | Osbpar - ${staticProjectName} | |
	| Code   | OSBPAROSBPAR                  | |
	| Status | Active                        | |
	And I create a product purchase for an account with the name "Osbpar - ${staticProjectName}" and the product "Service Partnership" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Partner"
	And I create an assigned team nested field for a team with the name "Osbpar - ${staticProjectName}" assigned to the account "Osbzen - ${staticProjectName}"
	And I create an account on koroneiki with the following values
	| Assigned Team Nested Field | ${staticAssignedTeamNestedField} | |
	| Code                       | OSBZENOSBZEN                     | |
	| Language                   | English                          | |
	| Name                       | Osbzen - ${staticProjectName}    | |
	| Region                     | United States                    | |
	| Status                     | Active                           | |
	| Tier                       | T1                               | |
	And I assign the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" to the following roles
	| Partner |
	And I assign the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" to the following roles
	| First Line Support |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 7 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the organization "Osbzen - ${staticProjectName}" should have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t1"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Platinum                      |
	| Status                     | Approved                      |
	| Support Region             | US                            |
	| Support Language           | English                       |
	| Tier                       | T1                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner Organization       | Osbpar - ${staticProjectName} |
	| Partner First Line Support | true                          |

Scenario: View new dxp project BR
	New DXP projects in the BR region that are added to Zendesk via OSB sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | Portuguese                    | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | Brazil                        | |
	| Status   | Active                        | |
	| Tier     | T1                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 7 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the organization "Osbzen - ${staticProjectName}" should have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "brazil"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t1"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "portuguese"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_portal"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "gold"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "closed"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Platinum                      |
	| Status                     | Approved                      |
	| Support Region             | Brazil                        |
	| Support Language           | Portuguese                    |
	| Tier                       | T1                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner First Line Support | false                         |

Scenario: View new dxp project JP
	New DXP projects in the JP region that are added to Zendesk via OSB sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | Japanese                      | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | Japan                         | |
	| Status   | Active                        | |
	| Tier     | T1                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 7 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the organization "Osbzen - ${staticProjectName}" should have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "japan"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t1"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "japanese"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_portal"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "gold"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "closed"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Platinum                      |
	| Status                     | Approved                      |
	| Support Region             | Japan                         |
	| Support Language           | Japanese                      |
	| Tier                       | T1                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner First Line Support | false                         |

Scenario: View new dxp project HU
	New DXP projects in the HU region that are added to Zendesk via OSB sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | Hungary                       | |
	| Status   | Active                        | |
	| Tier     | T1                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 7 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the organization "Osbzen - ${staticProjectName}" should have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "hungary"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t1"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_portal"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "gold"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "closed"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Platinum                      |
	| Status                     | Approved                      |
	| Support Region             | Hungary                       |
	| Support Language           | English                       |
	| Tier                       | T1                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner First Line Support | false                         |

Scenario: View default end user
	Organizations added to Zendesk via OSB sync should have a default end user.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T1                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 7 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the default user on the organization "Osbzen - ${staticProjectName}" should be on zendesk.liferay.com
	And the default user should be in the organization "Osbzen - ${staticProjectName}"

Scenario: View portal project with partner removal
	New portal partner projects that are added to Zendesk via OSB sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I generate static start and end dates
	And I create an account on koroneiki with the following values
	| Name   | Osbpar - ${staticProjectName} | |
	| Code   | OSBPAROSBPAR                  | |
	| Status | Active                        | |
	And I create a product purchase for an account with the name "Osbpar - ${staticProjectName}" and the product "Service Partnership" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Partner"
	And I create an assigned team nested field for a team with the name "Osbpar - ${staticProjectName}" assigned to the account "Osbzen - ${staticProjectName}"
	And I create an account on koroneiki with the following values
	| Assigned Team Nested Field | ${staticAssignedTeamNestedField} | |
	| Code                       | OSBZENOSBZEN                     | |
	| Language                   | English                          | |
	| Name                       | Osbzen - ${staticProjectName}    | |
	| Region                     | United States                    | |
	| Status                     | Active                           | |
	| Tier                       | T4                               | |
	And I assign the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" to the following roles
	| Partner |
	And I assign the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" to the following roles
	| First Line Support |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	When I remove the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" from the following roles
	| Partner |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the organization "Osbzen - ${staticProjectName}" should have the tag "liferay_portal"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "gold"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Gold                          |
	| Status                     | Approved                      |
	| Support Region             | US                            |
	| Support Language           | English                       |
	| Tier                       | T4                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner Organization       | Osbpar - ${staticProjectName} |
	| Partner First Line Support | true                          |

Scenario: View dxp project with partner removal
	New dxp partner projects that are added to Zendesk via OSB sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I generate static start and end dates
	And I create an account on koroneiki with the following values
	| Name   | Osbpar - ${staticProjectName} | |
	| Code   | OSBPAROSBPAR                  | |
	| Status | Active                        | |
	And I create a product purchase for an account with the name "Osbpar - ${staticProjectName}" and the product "Service Partnership" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Partner"
	And I create an assigned team nested field for a team with the name "Osbpar - ${staticProjectName}" assigned to the account "Osbzen - ${staticProjectName}"
	And I create an account on koroneiki with the following values
	| Assigned Team Nested Field | ${staticAssignedTeamNestedField} | |
	| Code                       | OSBZENOSBZEN                     | |
	| Language                   | English                          | |
	| Name                       | Osbzen - ${staticProjectName}    | |
	| Region                     | United States                    | |
	| Status                     | Active                           | |
	| Tier                       | T1                               | |
	And I assign the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" to the following roles
	| Partner |
	And I assign the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" to the following roles
	| First Line Support |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 7 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	When I remove the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" from the following roles
	| Partner |
	Then the organization "Osbzen - ${staticProjectName}" should have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t1"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Platinum                      |
	| Status                     | Approved                      |
	| Support Region             | US                            |
	| Support Language           | English                       |
	| Tier                       | T1                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner Organization       | Osbpar - ${staticProjectName} |
	| Partner First Line Support | true                          |

Scenario: View portal project with partner first line support removal
	New portal partner projects that are added to Zendesk via OSB sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I generate static start and end dates
	And I create an account on koroneiki with the following values
	| Name   | Osbpar - ${staticProjectName} | |
	| Code   | OSBPAROSBPAR                  | |
	| Status | Active                        | |
	And I create a product purchase for an account with the name "Osbpar - ${staticProjectName}" and the product "Service Partnership" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Partner"
	And I create an assigned team nested field for a team with the name "Osbpar - ${staticProjectName}" assigned to the account "Osbzen - ${staticProjectName}"
	And I create an account on koroneiki with the following values
	| Assigned Team Nested Field | ${staticAssignedTeamNestedField} | |
	| Code                       | OSBZENOSBZEN                     | |
	| Language                   | English                          | |
	| Name                       | Osbzen - ${staticProjectName}    | |
	| Region                     | United States                    | |
	| Status                     | Active                           | |
	| Tier                       | T4                               | |
	And I assign the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" to the following roles
	| Partner |
	And I assign the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" to the following roles
	| First Line Support |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	When I remove the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" from the following roles
	| First Line Support |
	Then the organization "Osbzen - ${staticProjectName}" should have the tag "liferay_portal"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "gold"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Gold                          |
	| Status                     | Approved                      |
	| Support Region             | US                            |
	| Support Language           | English                       |
	| Tier                       | T4                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner Organization       | Osbpar - ${staticProjectName} |
	| Partner First Line Support | false                         |

Scenario: View dxp project with partner first line support removal
	New dxp partner projects that are added to Zendesk via OSB sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I generate static start and end dates
	And I create an account on koroneiki with the following values
	| Name   | Osbpar - ${staticProjectName} | |
	| Code   | OSBPAROSBPAR                  | |
	| Status | Active                        | |
	And I create a product purchase for an account with the name "Osbpar - ${staticProjectName}" and the product "Service Partnership" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Partner"
	And I create an assigned team nested field for a team with the name "Osbpar - ${staticProjectName}" assigned to the account "Osbzen - ${staticProjectName}"
	And I create an account on koroneiki with the following values
	| Assigned Team Nested Field | ${staticAssignedTeamNestedField} | |
	| Code                       | OSBZENOSBZEN                     | |
	| Language                   | English                          | |
	| Name                       | Osbzen - ${staticProjectName}    | |
	| Region                     | United States                    | |
	| Status                     | Active                           | |
	| Tier                       | T1                               | |
	And I assign the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" to the following roles
	| Partner |
	And I assign the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" to the following roles
	| First Line Support |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 7 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	When I remove the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" from the following roles
	| First Line Support |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the organization "Osbzen - ${staticProjectName}" should have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t1"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Platinum                      |
	| Status                     | Approved                      |
	| Support Region             | US                            |
	| Support Language           | English                       |
	| Tier                       | T1                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner Organization       | Osbpar - ${staticProjectName} |
	| Partner First Line Support | false                         |

Scenario: Update project
	@ help.center.acceptance = "true"
	Project updates should reflect in zendesk.liferay.com.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T4                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	When I create an account on koroneiki with the following values
	| Name   | Osbpar - ${staticProjectName} | |
	| Code   | OSBPAROSBPAR                  | |
	| Status | Active                        | |
	And I create a product purchase for an account with the name "Osbpar - ${staticProjectName}" and the product "Service Partnership" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Partner"
	And I create an assigned team nested field for a team with the name "Osbpar - ${staticProjectName}" assigned to the account "Osbzen - ${staticProjectName}"
	And I update a koroneiki account with the name "Osbzen - ${staticProjectName}" including assigned teams to have the following values
	| Assigned Team Nested Field | ${staticAssignedTeamNestedField} | |
	| Tier                       | T1                               | |
	| Region                     | Spain                            | |
	And I assign the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" to the following roles
	| Partner |
	And I assign the team with the name "Osbpar - ${staticProjectName}" on the account "Osbzen - ${staticProjectName}" to the following roles
	| First Line Support |
	And I update a postal address on koroneiki for the account "Osbzen - ${staticProjectName}" with the street address "123 Main Street" to have the following values
	| Country               | Canada          |
	| Locality              | Toronto         |
	| Region                | Ontario         |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | M4B1B3          |
	| Primary               | True            |
	| Street Address Line 1 | 456 Main Street |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 7 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	When I log into the provisioning homepage as "${staticProvisioningAdminEmailAddress}" with password "${staticProvisioningAdminPassword}"
	And I go to the account index page
	And I search the accounts page using the keyword "Osbzen"
	And I click on the account with the name "Osbzen - ${staticProjectName}" on the provisioning index
	And I click on the "Support" tab
	And I edit the following account fields and types to the following values under the account details tab
	| Support Language | Select | Spanish | |
	Then the organization "Osbzen - ${staticProjectName}" should have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "liferay_portal"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "spain"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t1"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "spanish"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "gold"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "closed"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 456 Main Street               |
	| Details                    | Canada                        |
	| Details                    | Ontario                       |
	| SLA                        | Platinum                      |
	| Status                     | Approved                      |
	| Support Region             | Spain                         |
	| Support Language           | Spanish                       |
	| Tier                       | T1                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner Organization       | Osbpar - ${staticProjectName} |
	| Partner First Line Support | true                          |
	And the support account ID of the project with the code "OSBZENOSBZEN" should match the organization external ID on zendesk.liferay.com

Scenario: Update expired project language
	Expired project language updates should not reflect in zendesk.liferay.com.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T4                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 7 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	When I generate past static start and end dates
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "DXP Production" to have the following values
	| End Date                | ${staticPastEndDate}         | |
	| Original End Date       | ${staticPastOriginalEndDate} | |
	| Perpetual               | false                        | |
	| Start Date              | ${staticPastStartDate}       | |
	| Status                  | Approved                     | |
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "Platinum Subscription" to have the following values
	| End Date                | ${staticPastEndDate}         | |
	| Original End Date       | ${staticPastOriginalEndDate} | |
	| Perpetual               | false                        | |
	| Quantity                | 1                            | |
	| Start Date              | ${staticPastStartDate}       | |
	| Status                  | Approved                     | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	Then the organization "Osbzen - ${staticProjectName}" should have the tag "expired"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "approved"
	When I log into the provisioning homepage as "${staticProvisioningAdminEmailAddress}" with password "${staticProvisioningAdminPassword}"
	And I go to the account index page
	And I search the accounts page using the keyword "Osbzen"
	And I click on the account with the name "Osbzen - ${staticProjectName}" on the provisioning index
	And I click on the "Support" tab
	And I edit the following account fields and types to the following values under the account details tab
	| Support Language | Select | Spanish | |
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "spanish"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "expired"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Platinum                      |
	| Status                     | Expired                       |
	| Support Region             | US                            |
	| Support Language           | English                       |
	| Tier                       | T4                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner First Line Support | false                         |
	And the support account ID of the project with the code "OSBZENOSBZEN" should match the organization external ID on zendesk.liferay.com

Scenario: Update future project language
	Future project language updates should not reflect in zendesk.liferay.com.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T4                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 7 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	When I generate future static start and end dates
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "DXP Production" to have the following values
	| End Date                | ${staticFutureEndDate}         | |
	| Original End Date       | ${staticFutureOriginalEndDate} | |
	| Perpetual               | false                          | |
	| Start Date              | ${staticFutureStartDate}       | |
	| Status                  | Approved                       | |
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "Platinum Subscription" to have the following values
	| End Date                | ${staticFutureEndDate}         | |
	| Original End Date       | ${staticFutureOriginalEndDate} | |
	| Perpetual               | false                          | |
	| Quantity                | 1                              | |
	| Start Date              | ${staticFutureStartDate}       | |
	| Status                  | Approved                       | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	And I log into the provisioning homepage as "${staticProvisioningAdminEmailAddress}" with password "${staticProvisioningAdminPassword}"
	And I go to the account index page
	And I search the accounts page using the keyword "Osbzen"
	And I click on the account with the name "Osbzen - ${staticProjectName}" on the provisioning index
	And I click on the Sync To Zendesk button on the account page
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	Then the organization "Osbzen - ${staticProjectName}" should have the tag "closed"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "approved"
	And I go to the account index page
	And I search the accounts page using the keyword "Osbzen"
	And I click on the account with the name "Osbzen - ${staticProjectName}" on the provisioning index
	And I click on the "Support" tab
	And I edit the following account fields and types to the following values under the account details tab
	| Support Language | Select | Spanish | |
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the organization "Osbzen - ${staticProjectName}" should have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "spanish"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "closed"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Platinum                      |
	| Status                     | Closed                        |
	| Support Region             | US                            |
	| Support Language           | Spanish                       |
	| Tier                       | T4                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner First Line Support | false                         |
	And the support account ID of the project with the code "OSBZENOSBZEN" should match the organization external ID on zendesk.liferay.com

Scenario: Update future project language before reactivation
	Future project languages updates should reflect in zendesk.liferay.com after reactivation

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T4                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 7 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	And I log into the provisioning homepage as "${staticProvisioningAdminEmailAddress}" with password "${staticProvisioningAdminPassword}"
	And I go to the account index page
	And I search the accounts page using the keyword "Osbzen"
	And I click on the account with the name "Osbzen - ${staticProjectName}" on the provisioning index
	And I click on the Sync To Zendesk button on the account page
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	When I generate future static start and end dates
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "DXP Production" to have the following values
	| End Date                | ${staticFutureEndDate}         | |
	| Original End Date       | ${staticFutureOriginalEndDate} | |
	| Perpetual               | false                          | |
	| Start Date              | ${staticFutureStartDate}       | |
	| Status                  | Approved                       | |
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "Platinum Subscription" to have the following values
	| End Date                | ${staticFutureEndDate}         | |
	| Original End Date       | ${staticFutureOriginalEndDate} | |
	| Perpetual               | false                          | |
	| Quantity                | 1                              | |
	| Start Date              | ${staticFutureStartDate}       | |
	| Status                  | Approved                       | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	And I go to the account index page
	And I search the accounts page using the keyword "Osbzen"
	And I click on the account with the name "Osbzen - ${staticProjectName}" on the provisioning index
	And I click on the Sync To Zendesk button on the account page
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should have the tag "closed"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "approved"
	And I go to the account index page
	And I search the accounts page using the keyword "Osbzen"
	And I click on the account with the name "Osbzen - ${staticProjectName}" on the provisioning index
	And I click on the "Support" tab
	And I edit the following account fields and types to the following values under the account details tab
	| Support Language | Select | Spanish | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	And I go to the account index page
	And I search the accounts page using the keyword "Osbzen"
	And I click on the account with the name "Osbzen - ${staticProjectName}" on the provisioning index
	And I click on the Sync To Zendesk button on the account page
	Then the organization "Osbzen - ${staticProjectName}" should not have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "spanish"
	When I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "DXP Production" to have the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "Platinum Subscription" to have the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	And I go to the account index page
	And I search the accounts page using the keyword "Osbzen"
	And I click on the account with the name "Osbzen - ${staticProjectName}" on the provisioning index
	And I click on the Sync To Zendesk button on the account page
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	Then the organization "Osbzen - ${staticProjectName}" should have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "spanish"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "closed"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Platinum                      |
	| Status                     | Approved                      |
	| Support Region             | US                            |
	| Support Language           | Spanish                       |
	| Tier                       | T4                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner First Line Support | false                         |
	And the support account ID of the project with the code "OSBZENOSBZEN" should match the organization external ID on zendesk.liferay.com

Scenario: Update expired project language before reactivation
	Expired project languages updates should reflect in zendesk.liferay.com after reactivation.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T4                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 7 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	When I generate past static start and end dates
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "DXP Production" to have the following values
	| End Date                | ${staticPastEndDate}         | |
	| Original End Date       | ${staticPastOriginalEndDate} | |
	| Perpetual               | false                        | |
	| Start Date              | ${staticPastStartDate}       | |
	| Status                  | Approved                     | |
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "Platinum Subscription" to have the following values
	| End Date                | ${staticPastEndDate}         | |
	| Original End Date       | ${staticPastOriginalEndDate} | |
	| Perpetual               | false                        | |
	| Start Date              | ${staticPastStartDate}       | |
	| Status                  | Approved                     | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the organization "Osbzen - ${staticProjectName}" should have the tag "expired"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "approved"
	When I log into the provisioning homepage as "${staticProvisioningAdminEmailAddress}" with password "${staticProvisioningAdminPassword}"
	And I go to the account index page
	And I search the accounts page using the keyword "Osbzen"
	And I click on the account with the name "Osbzen - ${staticProjectName}" on the provisioning index
	And I click on the "Support" tab
	And I edit the following account fields and types to the following values under the account details tab
	| Support Language | Select | Spanish | |
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "spanish"
	When I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "DXP Production" to have the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "Platinum Subscription" to have the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the organization "Osbzen - ${staticProjectName}" should have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "spanish"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "closed"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Platinum                      |
	| Status                     | Approved                      |
	| Support Region             | US                            |
	| Support Language           | Spanish                       |
	| Tier                       | T4                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner First Line Support | false                         |
	And the support account ID of the project with the code "OSBZENOSBZEN" should match the organization external ID on zendesk.liferay.com

Scenario: View new lxc sm project
	New LXC sm projects that are added to Zendesk via OSB sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T1                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "LXC SM Subscription HA Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 7 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Premium Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "DXP Cloud"
	And I synchronize the account entitlement definition with the name "Premium Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the organization "Osbzen - ${staticProjectName}" should have the tag "lxc_sm"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t1"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "premium"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_portal"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "gold"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "closed"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Premium                       |
	| Status                     | Approved                      |
	| Support Region             | US                            |
	| Support Language           | English                       |
	| Tier                       | T1                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner First Line Support | false                         |
	And the support account ID of the project with the code "OSBZENOSBZEN" should match the organization external ID on zendesk.liferay.com
	When I get the organization ID of the organization "Osbzen - ${staticProjectName}" from zendesk.liferay.com
	Then the external link with the entity id "${staticOrganizationID}" and domain "zendesk" should be present on the account "Osbzen - ${staticProjectName}" with the following values
	| Domain      | zendesk                 |
	| Entity Id   | ${staticOrganizationID} |

Scenario: View new lxc engage project
	New LXC engage projects that are added to Zendesk via OSB sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T1                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "LXC Subscription - Engage Site" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 7 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Premium Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Premium Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the organization "Osbzen - ${staticProjectName}" should have the tag "lxc_engage"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t1"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "premium"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_portal"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "gold"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "closed"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Premium                       |
	| Status                     | Approved                      |
	| Support Region             | US                            |
	| Support Language           | English                       |
	| Tier                       | T1                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner First Line Support | false                         |
	And the support account ID of the project with the code "OSBZENOSBZEN" should match the organization external ID on zendesk.liferay.com
	When I get the organization ID of the organization "Osbzen - ${staticProjectName}" from zendesk.liferay.com
	Then the external link with the entity id "${staticOrganizationID}" and domain "zendesk" should be present on the account "Osbzen - ${staticProjectName}" with the following values
	| Domain      | zendesk                 |
	| Entity Id   | ${staticOrganizationID} |

Scenario: View new lxc csp project
	New LXC csp projects that are added to Zendesk via OSB sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T1                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "LXC - CSP - Custom User Tier" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 7 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Premium Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Premium Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the organization "Osbzen - ${staticProjectName}" should have the tag "lxc_csp"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t1"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "premium"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_portal"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "gold"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "closed"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Premium                       |
	| Status                     | Approved                      |
	| Support Region             | US                            |
	| Support Language           | English                       |
	| Tier                       | T1                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner First Line Support | false                         |
	And the support account ID of the project with the code "OSBZENOSBZEN" should match the organization external ID on zendesk.liferay.com
	When I get the organization ID of the organization "Osbzen - ${staticProjectName}" from zendesk.liferay.com
	Then the external link with the entity id "${staticOrganizationID}" and domain "zendesk" should be present on the account "Osbzen - ${staticProjectName}" with the following values
	| Domain      | zendesk                 |
	| Entity Id   | ${staticOrganizationID} |

Scenario: View new lxc support project
	New LXC transact projects that are added to Zendesk via OSB sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T1                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "LXC Subscription - Support Site" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 7 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Premium Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the organization "Osbzen - ${staticProjectName}" should have the tag "lxc_support"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t1"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "premium"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_portal"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "gold"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "closed"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Premium                       |
	| Status                     | Approved                      |
	| Support Region             | US                            |
	| Support Language           | English                       |
	| Tier                       | T1                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner First Line Support | false                         |
	And the support account ID of the project with the code "OSBZENOSBZEN" should match the organization external ID on zendesk.liferay.com
	When I get the organization ID of the organization "Osbzen - ${staticProjectName}" from zendesk.liferay.com
	Then the external link with the entity id "${staticOrganizationID}" and domain "zendesk" should be present on the account "Osbzen - ${staticProjectName}" with the following values
	| Domain      | zendesk                 |
	| Entity Id   | ${staticOrganizationID} |

Scenario: View new lxc transact project
	New LXC transact projects that are added to Zendesk via OSB sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T1                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "LXC Subscription - Transact Site" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 7 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Premium Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Premium Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the organization "Osbzen - ${staticProjectName}" should have the tag "lxc_transact"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t1"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "premium"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_portal"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "gold"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "closed"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Premium                       |
	| Status                     | Approved                      |
	| Support Region             | US                            |
	| Support Language           | English                       |
	| Tier                       | T1                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner First Line Support | false                         |
	And the support account ID of the project with the code "OSBZENOSBZEN" should match the organization external ID on zendesk.liferay.com
	When I get the organization ID of the organization "Osbzen - ${staticProjectName}" from zendesk.liferay.com
	Then the external link with the entity id "${staticOrganizationID}" and domain "zendesk" should be present on the account "Osbzen - ${staticProjectName}" with the following values
	| Domain      | zendesk                 |
	| Entity Id   | ${staticOrganizationID} |

Scenario: View new managed services for LXC SM project
	New managed services for LXC SM projects that are added to Zendesk via OSB sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T1                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Managed Services for Liferay Experience Cloud - Self Managed" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 7 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Premium Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Premium Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the organization "Osbzen - ${staticProjectName}" should have the tag "managed_services_sm"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t1"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "premium"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_portal"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "gold"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "closed"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Premium                       |
	| Status                     | Approved                      |
	| Support Region             | US                            |
	| Support Language           | English                       |
	| Tier                       | T1                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner First Line Support | false                         |
	And the support account ID of the project with the code "OSBZENOSBZEN" should match the organization external ID on zendesk.liferay.com
	When I get the organization ID of the organization "Osbzen - ${staticProjectName}" from zendesk.liferay.com
	Then the external link with the entity id "${staticOrganizationID}" and domain "zendesk" should be present on the account "Osbzen - ${staticProjectName}" with the following values
	| Domain      | zendesk                 |
	| Entity Id   | ${staticOrganizationID} |

Scenario: View new managed services for Liferay DXP project
	New managed services for Liferay DXP projects that are added to Zendesk via OSB sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T1                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Managed Services for Liferay DXP - Self-Hosted" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 7 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Premium Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Premium Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the organization "Osbzen - ${staticProjectName}" should have the tag "managed_services_sh"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t1"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "premium"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_portal"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "gold"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "closed"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Premium                       |
	| Status                     | Approved                      |
	| Support Region             | US                            |
	| Support Language           | English                       |
	| Tier                       | T1                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner First Line Support | false                         |
	And the support account ID of the project with the code "OSBZENOSBZEN" should match the organization external ID on zendesk.liferay.com
	When I get the organization ID of the organization "Osbzen - ${staticProjectName}" from zendesk.liferay.com
	Then the external link with the entity id "${staticOrganizationID}" and domain "zendesk" should be present on the account "Osbzen - ${staticProjectName}" with the following values
	| Domain      | zendesk                 |
	| Entity Id   | ${staticOrganizationID} |

Scenario: View new lxc pro plan project
	New LXC Pro Plan projects that are added to Zendesk via OSB sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T1                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "LXC - Pro Plan" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 7 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Premium Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Premium Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the organization "Osbzen - ${staticProjectName}" should have the tag "lxc_pro"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t1"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "premium"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_portal"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "gold"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "closed"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Premium                       |
	| Status                     | Approved                      |
	| Support Region             | US                            |
	| Support Language           | English                       |
	| Tier                       | T1                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner First Line Support | false                         |
	And the support account ID of the project with the code "OSBZENOSBZEN" should match the organization external ID on zendesk.liferay.com
	When I get the organization ID of the organization "Osbzen - ${staticProjectName}" from zendesk.liferay.com
	Then the external link with the entity id "${staticOrganizationID}" and domain "zendesk" should be present on the account "Osbzen - ${staticProjectName}" with the following values
	| Domain      | zendesk                 |
	| Entity Id   | ${staticOrganizationID} |

Scenario: View new lxc business plan project
	New LXC Business Plan projects that are added to Zendesk via OSB sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T1                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "LXC - Business Plan" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 7 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Premium Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Premium Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the organization "Osbzen - ${staticProjectName}" should have the tag "lxc_business"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t1"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "premium"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_portal"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "gold"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "closed"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Premium                       |
	| Status                     | Approved                      |
	| Support Region             | US                            |
	| Support Language           | English                       |
	| Tier                       | T1                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner First Line Support | false                         |
	And the support account ID of the project with the code "OSBZENOSBZEN" should match the organization external ID on zendesk.liferay.com
	When I get the organization ID of the organization "Osbzen - ${staticProjectName}" from zendesk.liferay.com
	Then the external link with the entity id "${staticOrganizationID}" and domain "zendesk" should be present on the account "Osbzen - ${staticProjectName}" with the following values
	| Domain      | zendesk                 |
	| Entity Id   | ${staticOrganizationID} |
	
Scenario: View new lxc enterprise plan project
	New LXC LXC Enterprise Plan projects that are added to Zendesk via OSB sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T1                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "LXC - Enterprise Plan" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 7 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Premium Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Premium Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the organization "Osbzen - ${staticProjectName}" should have the tag "lxc_enterprise"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t1"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "premium"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_portal"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "gold"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "closed"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Premium                       |
	| Status                     | Approved                      |
	| Support Region             | US                            |
	| Support Language           | English                       |
	| Tier                       | T1                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner First Line Support | false                         |
	And the support account ID of the project with the code "OSBZENOSBZEN" should match the organization external ID on zendesk.liferay.com
	When I get the organization ID of the organization "Osbzen - ${staticProjectName}" from zendesk.liferay.com
	Then the external link with the entity id "${staticOrganizationID}" and domain "zendesk" should be present on the account "Osbzen - ${staticProjectName}" with the following values
	| Domain      | zendesk                 |
	| Entity Id   | ${staticOrganizationID} |

Scenario: View new dxp project with 71 extended premium support
	New DXP projects with 71 extended premium support purchases that are added to Zendesk via OSB sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T1                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 7 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Extended Premium Support - Liferay DXP 7.1" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the organization "Osbzen - ${staticProjectName}" should have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t1"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "7_1_eps"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_portal"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "gold"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "closed"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Platinum                      |
	| Status                     | Approved                      |
	| Support Region             | US                            |
	| Support Language           | English                       |
	| Tier                       | T1                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner First Line Support | false                         |
	And the support account ID of the project with the code "OSBZENOSBZEN" should match the organization external ID on zendesk.liferay.com
	When I get the organization ID of the organization "Osbzen - ${staticProjectName}" from zendesk.liferay.com
	Then the external link with the entity id "${staticOrganizationID}" and domain "zendesk" should be present on the account "Osbzen - ${staticProjectName}" with the following values
	| Domain      | zendesk                 |
	| Entity Id   | ${staticOrganizationID} |

Scenario: View new dxp project with 72 extended premium support
	New DXP projects with 72 extended premium support purchases that are added to Zendesk via OSB sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T1                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 7 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Extended Premium Support - Liferay DXP 7.2" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the organization "Osbzen - ${staticProjectName}" should have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t1"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "7_2_eps"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_portal"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "gold"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "closed"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Platinum                      |
	| Status                     | Approved                      |
	| Support Region             | US                            |
	| Support Language           | English                       |
	| Tier                       | T1                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner First Line Support | false                         |
	And the support account ID of the project with the code "OSBZENOSBZEN" should match the organization external ID on zendesk.liferay.com
	When I get the organization ID of the organization "Osbzen - ${staticProjectName}" from zendesk.liferay.com
	Then the external link with the entity id "${staticOrganizationID}" and domain "zendesk" should be present on the account "Osbzen - ${staticProjectName}" with the following values
	| Domain      | zendesk                 |
	| Entity Id   | ${staticOrganizationID} |

Scenario: View new dxp project with enterprise search ewsa
	New DXP projects with enterprise search ewsa purchases that are added to Zendesk via OSB sync should have the appropriate parameters and tags.

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T1                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 7 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Enterprise Search Enterprise-Wide Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	And I log into the provisioning homepage as "${staticProvisioningAdminEmailAddress}" with password "${staticProvisioningAdminPassword}"
	And I go to the account index page
	And I search the accounts page using the keyword "Osbzen"
	And I click on the account with the name "Osbzen - ${staticProjectName}" on the provisioning index
	And I click on the Sync To Zendesk button on the account page
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the organization "Osbzen - ${staticProjectName}" should have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t1"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "enterprise_search"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_portal"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "gold"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "closed"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Platinum                      |
	| Status                     | Approved                      |
	| Support Region             | US                            |
	| Support Language           | English                       |
	| Tier                       | T1                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner First Line Support | false                         |
	And the support account ID of the project with the code "OSBZENOSBZEN" should match the organization external ID on zendesk.liferay.com
	When I get the organization ID of the organization "Osbzen - ${staticProjectName}" from zendesk.liferay.com
	Then the external link with the entity id "${staticOrganizationID}" and domain "zendesk" should be present on the account "Osbzen - ${staticProjectName}" with the following values
	| Domain      | zendesk                 |
	| Entity Id   | ${staticOrganizationID} |

	Scenario: View external links on zendesk organization
	Testing the ability to create external links in Koroneiki and viewing the external links in the zendesk organization 

	Given I generate a provisioning project name
	And I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T1                            | |
	And I create a postal address for an account with the name "Osbzen - ${staticProjectName}" on koroneiki with the following values
	| Country               | United States   |
	| Locality              | Diamond Bar     |
	| Region                | California      |
	| Type                  | Business        |
	| Mailing               | True            |
	| Postal Code           | 91765           |
	| Primary               | True            |
	| Street Address Line 1 | 123 Main Street |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 7 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Enterprise Search Enterprise-Wide Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	Then the external link with the entity id "${staticOrganizationID}" and domain "zendesk" should be present on the account "Osbzen - ${staticProjectName}" with the following values
	| Domain      | zendesk                 |
	| Entity Id   | ${staticOrganizationID} |
	When I create an external link for the account "Osbzen - ${staticProjectName}" with the following values
	| Domain      | salesforce                  |
	| Entity Id   | ${staticDossieraAccountKey} |
	| Entity Name | account                     |
	And I create an external link for the account "Osbzen - ${staticProjectName}" with the following values
	| Domain      | dossiera                    |
	| Entity Id   | ${staticDossieraProjectKey} |
	| Entity Name | project                     |
	And I create an external link for the account "Osbzen - ${staticProjectName}" with the following values
	| Domain      | dossiera                  |
	| Entity Id   | ${staticDossieraAccountKey} |
	| Entity Name | account                     |
	And I create an external link for the account "Osbzen - ${staticProjectName}" with the following values
	| Domain      | analytics-cloud |
	| Entity Id   | 67890           |
	| Entity Name | group           |
	And I create an external link for the account "Osbzen - ${staticProjectName}" with the following values
	| Domain      | dxp-cloud          |
	| Entity Id   | 1z2x3c4v5b6n7m8a9s |
	| Entity Name | project            |
	And the external link with the entity id "${staticDossieraAccountKey}" and domain "salesforce" should be present on the account "Osbzen - ${staticProjectName}" with the following values
	| Domain      | salesforce                  |
	| Entity Id   | ${staticDossieraAccountKey} |
	| Entity Name | account                     |
	And the external link with the entity id "${staticDossieraProjectKey}" and domain "dossiera" should be present on the account "Osbzen - ${staticProjectName}" with the following values
	| Domain      | dossiera                    |
	| Entity Id   | ${staticDossieraProjectKey} |
	| Entity Name | project                     |
	And the external link with the entity id "${staticDossieraProjectKey}" and domain "dossiera" should be present on the account "Osbzen - ${staticProjectName}" with the following values
	| Domain      | dossiera                    |
	| Entity Id   | ${staticDossieraProjectKey} |
	| Entity Name | account                     |
	And the external link with the entity id "67890" and domain "analytics-cloud" should be present on the account "Osbzen - ${staticProjectName}" with the following values
	| Domain      | analytics-cloud |
	| Entity Id   | 67890           |
	| Entity Name | group           |
	And the external link with the entity id "1z2x3c4v5b6n7m8a9s" and domain "dxp-cloud" should be present on the account "Osbzen - ${staticProjectName}" with the following values
	| Domain      | dxp-cloud          |
	| Entity Id   | 1z2x3c4v5b6n7m8a9s |
	| Entity Name | project            |
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the organization "Osbzen - ${staticProjectName}" should have the tag "liferay_dxp"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "us"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "t1"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "platinum"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "english"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "approved"
	And the organization "Osbzen - ${staticProjectName}" should have the tag "enterprise_search"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "liferay_portal"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "gold"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "t4"
	And the organization "Osbzen - ${staticProjectName}" should not have the tag "closed"
	And the organization "Osbzen - ${staticProjectName}" should have the following parameters
	| Name                       | Osbzen - ${staticProjectName} |
	| Details                    | 123 Main Street               |
	| Details                    | United States                 |
	| Details                    | California                    |
	| SLA                        | Platinum                      |
	| Status                     | Approved                      |
	| Support Region             | US                            |
	| Support Language           | English                       |
	| Tier                       | T1                            |
	| Account Code               | OSBZENOSBZEN                  |
	| Partner First Line Support | false                         |
	| External Links             | workspace/67890               |
	| External Links             | projects/1z2x3c4v5b6n7m8a9s   |
	| External Links             | salesforce/view               |
	| External Links             | /dossiera/account/            |
	And the support account ID of the project with the code "OSBZENOSBZEN" should match the organization external ID on zendesk.liferay.com
	When I get the organization ID of the organization "Osbzen - ${staticProjectName}" from zendesk.liferay.com
	Then the external link with the entity id "${staticOrganizationID}" and domain "zendesk" should be present on the account "Osbzen - ${staticProjectName}" with the following values
	| Domain      | zendesk                 |
	| Entity Id   | ${staticOrganizationID} |