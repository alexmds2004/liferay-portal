#@testray.main.component.name = "Support QA"
#@type = "customer.portal"

Feature: Project Details Source Code Access
	Testing the Source Code Access feature for the Project Details portlet.

Setup:
	* I get all test passwords and admin emails
	* I generate a provisioning project name
	* I generate a provisioning company name
	* I create a user with the following parameters
	| Screen Name   | pjd.customer1                       |
	| Email Address | pjd.customer1@lrcustomer.com.broken |
	| First Name    | PJD                                 |
	| Last Name     | Customer1                           |
	* I create a user with the following parameters
	| Screen Name   | pjd.customer2                       |
	| Email Address | pjd.customer2@lrcustomer.com.broken |
	| First Name    | PJD                                 |
	| Last Name     | Customer2                           |
	* I create a user with the following parameters
	| Screen Name   | pjd.partner1                      |
	| Email Address | pjd.partner1@lrpartner.com.broken |
	| First Name    | PJD                               |
	| Last Name     | Partner1                          |
	* I create an account on koroneiki with the following values
	| Name   | Zenpar - ${staticCompanyName} | |
	| Code   | ZENPARZENPAR                  | |
	| Status | Active                        | |
	* I assign the contact with the email address "pjd.partner1@lrpartner.com.broken" on the account "Zenpar - ${staticCompanyName}" to the following roles
	| Partner Manager |
	* I generate static start and end dates
	* I create a product purchase for an account with the name "Zenpar - ${staticCompanyName}" and the product "Service Partnership" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	* I create an assigned team nested field for a team with the name "Zenpar - ${staticCompanyName}" assigned to the account "Pjdzen - ${staticProjectName}"
	* I create an account on koroneiki with the following values
	| Assigned Team Nested Field | ${staticAssignedTeamNestedField} | |
	| Code                       | PJDZENPJDZEN                     | |
	| Name                       | Pjdzen - ${staticProjectName}    | |
	| Region                     | United States                    | |
	| Status                     | Active                           | |
	| Tier                       | T1                               | |
	* I assign the team with the name "Zenpar - ${staticCompanyName}" on the account "Pjdzen - ${staticProjectName}" to the following roles
	| Partner |
	* I assign the team with the name "Zenpar - ${staticCompanyName}" on the account "Pjdzen - ${staticProjectName}" to the following roles
	| First Line Support |
	* I assign the contact with the email address "pjd.customer1@lrcustomer.com.broken" on the account "Pjdzen - ${staticProjectName}" to the following roles
	| Support Requester |
	* I assign the contact with the email address "pjd.customer2@lrcustomer.com.broken" on the account "Pjdzen - ${staticProjectName}" to the following roles
	| Support User |
	* I create an account on koroneiki with the following values
	| Code   | DUPZENDUPZEN                     | |
	| Name   | Dupzen - ${staticProjectName}    | |
	| Region | United States                    | |
	| Status | Active                           | |
	| Tier   | T1                               | |

Teardown:
	* I get the ID of the default user belonging to the organization "Pjdzen - ${staticProjectName}" from zendesk.liferay.com
	* I rename the first default user ID variable
	* I get the ID of the default user belonging to the organization "Dupzen - ${staticProjectName}" from zendesk.liferay.com
	* I rename the second default user ID variable
	* I get the ID of the default user belonging to the organization "Zenpar - ${staticCompanyName}" from zendesk.liferay.com
	* I rename the third default user ID variable
	* I tear down the projects with the following project codes
	| ZENPARZENPAR |
	| PJDZENPJDZEN |
	| DUPZENDUPZEN |
	* I tear down the accounts on koroneiki with the following names
	| Zenpar - ${staticCompanyName} |
	| Pjdzen - ${staticProjectName} |
	| Dupzen - ${staticProjectName} |
	* I tear down the accounts on koroneiki with the following "Code" values
	| ZENPARZENPAR |
	| PJDZENPJDZEN |
	| DUPZENDUPZEN |
	* I synchronize the account entitlement definition with the name "Platinum Subscription"
	* I tear down the users with the following email addresses from web.liferay.com
	| pjd.customer1@lrcustomer.com.broken |
	| pjd.customer2@lrcustomer.com.broken |
	| pjd.partner1@lrpartner.com.broken   |
	* I tear down the users with the following email addresses from customer.liferay.com
	| pjd.customer1@lrcustomer.com.broken |
	| pjd.customer2@lrcustomer.com.broken |
	| pjd.partner1@lrpartner.com.broken   |
	* I tear down the users with the following email addresses from koroneiki
	| pjd.customer1@lrcustomer.com.broken |
	| pjd.customer2@lrcustomer.com.broken |
	| pjd.partner1@lrpartner.com.broken   |
	* I tear down the contacts on koroneiki with the following email addresses
	| pjd.customer1@lrcustomer.com.broken |
	| pjd.customer2@lrcustomer.com.broken |
	| pjd.partner1@lrpartner.com.broken   |
	* I tear down the users with the following email addresses from okta
	| pjd.customer1@lrcustomer.com.broken |
	| pjd.customer2@lrcustomer.com.broken |
	| pjd.partner1@lrpartner.com.broken   |
	* I obtain the IDs of the following user email addresses from zendesk.liferay.com
	| pjd.customer1@lrcustomer.com.broken |
	| pjd.customer2@lrcustomer.com.broken |
	| pjd.partner1@lrpartner.com.broken   |
	* I teardown users with the following userIds "${staticUserIdList}" from zendesk.liferay.com
	* I obtain the external IDs of the following organizations from zendesk.liferay.com if still present
	| Zenpar - ${staticCompanyName} |
	| Pjdzen - ${staticProjectName} |
	| Dupzen - ${staticProjectName} |
	* I teardown organizations with the following organizationExternalIds "${staticOrganizationExternalIdList}" from zendesk.liferay.com
	* I teardown the user with userId "${staticDefaultUserId1}" from zendesk.liferay.com
	* I teardown the user with userId "${staticDefaultUserId2}" from zendesk.liferay.com
	* I teardown the user with userId "${staticDefaultUserId3}" from zendesk.liferay.com
	* I log out and log into the support homepage as "amos.fong@liferay.com.broken" with password "test"
	* I automatically have all non Source Code Access users removed from the GitHub repository 

Scenario: View source code access user
	@ help.center.acceptance = "true"
	New source code access users on an approved project should be able to gain access to the proper GitHub repository.

	Given I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	Then the support organization with the code "PJDZENPJDZEN" should be part of the support site
	When I log into the support homepage as "${staticSupportAdminEmailAdress}" with password "${staticSupportAdminPassword}"
	And I go to the Project Details page 
	And I search for the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the Source Code Access tab
	And I add a Source Code Access user with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	Then the Source Code Access user panel for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And the Source Code Access user details for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And I accept the GitHub repository invitation using the username "infoservicesqa" and the password "rejoicealways123"
	Then I should be able to view the is-support-integration repository using the username "infoservicesqa" and the password "rejoicealways123"

Scenario: View source code access user added to closed project
	New source code access users on a closed project should be able to gain access to the proper GitHub repository after project approval.

	Given I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	Then the support organization with the code "PJDZENPJDZEN" should be part of the support site
	When I update a koroneiki account with the name "Pjdzen - ${staticProjectName}" to have the following values
	| Status | Closed | |
	And I log into the support homepage as "${staticSupportAdminEmailAdress}" with password "${staticSupportAdminPassword}"
	And I go to the Project Details page 
	And I search for the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the Source Code Access tab
	And I add a Source Code Access user with the following values on a closed project
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	Then the Source Code Access user panel for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And the Source Code Access user details for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	When I wait for "30" seconds
	Then I should not be able to view the GitHub repository invitation using the username "infoservicesqa" and the password "rejoicealways123"
	And I should not be able to view the is-support-integration repository using the username "infoservicesqa" and the password "rejoicealways123"

Scenario: View source code access user added to expired project
	New source code access users on an expired project should not be able to gain access to the proper GitHub repository.

	Given I generate past static start and end dates
	And I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticPastEndDate}         | |
	| Original End Date       | ${staticPastOriginalEndDate} | |
	| Perpetual               | false                        | |
	| Quantity                | 1                            | |
	| Start Date              | ${staticPastStartDate}       | |
	| Status                  | Approved                     | |
	And I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticPastEndDate}         | |
	| Original End Date       | ${staticPastOriginalEndDate} | |
	| Perpetual               | false                        | |
	| Quantity                | 1                            | |
	| Start Date              | ${staticPastStartDate}       | |
	| Status                  | Approved                     | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	And I log into the support homepage as "${staticSupportAdminEmailAdress}" with password "${staticSupportAdminPassword}"
	And I go to the Project Details page 
	And I search for the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the Source Code Access tab
	And I add a Source Code Access user with the following values on a closed project
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	Then the Source Code Access user panel for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And the Source Code Access user details for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	Then I should not be able to view the GitHub repository invitation using the username "infoservicesqa" and the password "rejoicealways123"
	And I should not be able to view the is-support-integration repository using the username "infoservicesqa" and the password "rejoicealways123"

Scenario: Delete source code access user
	Source code access users on an approved project and deleted should not be able to gain access to the proper GitHub repository.

	Given I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	Then the support organization with the code "PJDZENPJDZEN" should be part of the support site
	And I log into the support homepage as "${staticSupportAdminEmailAdress}" with password "${staticSupportAdminPassword}"
	And I go to the Project Details page
	And I search for the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the Source Code Access tab
	And I add a Source Code Access user with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	Then the Source Code Access user panel for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And the Source Code Access user details for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And I accept the GitHub repository invitation using the username "infoservicesqa" and the password "rejoicealways123"
	And I go to the Project Details page 
	And I search for the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the Source Code Access tab
	And I delete the Source Code Access user with the email address "info.services.qa@gmail.com"
	Then I should not be able to view the is-support-integration repository using the username "infoservicesqa" and the password "rejoicealways123"

Scenario: Delete source code access user from closed project
	Source code access users on a closed project and deleted should not be able to gain access to the proper GitHub repository.

	Given I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	Then the support organization with the code "PJDZENPJDZEN" should be part of the support site
	When I update a koroneiki account with the name "Pjdzen - ${staticProjectName}" to have the following values
	| Status | Closed | |
	And I log into the support homepage as "${staticSupportAdminEmailAdress}" with password "${staticSupportAdminPassword}"
	And I go to the Project Details page
	And I search for the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the Source Code Access tab
	And I add a Source Code Access user with the following values on a closed project
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	Then the Source Code Access user panel for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And the Source Code Access user details for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And I delete the Source Code Access user with the email address "info.services.qa@gmail.com"
	Then I should not be able to view the GitHub repository invitation using the username "infoservicesqa" and the password "rejoicealways123"
	And I should not be able to view the is-support-integration repository using the username "infoservicesqa" and the password "rejoicealways123"

Scenario: View source code access user after project expiration
	New source code access users on an approved project and later expired project should not be able to gain access to the proper GitHub repository.

	Given I generate past static start and end dates
	And I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	Then the support organization with the code "PJDZENPJDZEN" should be part of the support site
	When I log into the support homepage as "${staticSupportAdminEmailAdress}" with password "${staticSupportAdminPassword}"
	And I go to the Project Details page 
	And I search for the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the Source Code Access tab
	And I add a Source Code Access user with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	Then the Source Code Access user panel for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And the Source Code Access user details for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And I accept the GitHub repository invitation using the username "infoservicesqa" and the password "rejoicealways123"
	And I update a product purchase on koroneiki made by the account "Pjdzen - ${staticProjectName}" for the product "DXP Production" to have the following values
	| End Date                | ${staticPastEndDate}         | |
	| Original End Date       | ${staticPastOriginalEndDate} | |
	| Perpetual               | false                        | |
	| Start Date              | ${staticPastStartDate}       | |
	| Status                  | Approved                     | |
	And I update a product purchase on koroneiki made by the account "Pjdzen - ${staticProjectName}" for the product "Platinum Subscription" to have the following values
	| End Date                | ${staticPastEndDate}         | |
	| Original End Date       | ${staticPastOriginalEndDate} | |
	| Perpetual               | false                        | |
	| Start Date              | ${staticPastStartDate}       | |
	| Status                  | Approved                     | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	# Necessary pause to allow synchronization to take place before the github repo is viewed again without permissions
	And I wait for "5" seconds
	Then I should not be able to view the is-support-integration repository using the username "infoservicesqa" and the password "rejoicealways123"

Scenario: View source code access user after project closure
	New source code access users on an approved project and later closed project should not be able to gain access to the proper GitHub repository.

	Given I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	Then the support organization with the code "PJDZENPJDZEN" should be part of the support site
	When I log into the support homepage as "${staticSupportAdminEmailAdress}" with password "${staticSupportAdminPassword}"
	And I go to the Project Details page 
	And I search for the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the Source Code Access tab
	And I add a Source Code Access user with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	Then the Source Code Access user panel for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And the Source Code Access user details for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And I accept the GitHub repository invitation using the username "infoservicesqa" and the password "rejoicealways123"
	And I update a koroneiki account with the name "Pjdzen - ${staticProjectName}" to have the following values
	| Status | Closed | |
	# Necessary pause to allow synchronization to take place before the github repo is viewed again without permissions
	And I wait for "5" seconds
	Then I should not be able to view the is-support-integration repository using the username "infoservicesqa" and the password "rejoicealways123"

Scenario: View source code access user after project approval
	New source code access users on a closed project that later gets approved should be able to gain access to the proper GitHub repository.

	Given I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	Then the support organization with the code "PJDZENPJDZEN" should be part of the support site
	When I update a koroneiki account with the name "Pjdzen - ${staticProjectName}" to have the following values
	| Status | Closed | |
	And I log into the support homepage as "${staticSupportAdminEmailAdress}" with password "${staticSupportAdminPassword}"
	And I go to the Project Details page
	And I search for the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the Source Code Access tab
	And I add a Source Code Access user with the following values on a closed project
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	Then the Source Code Access user panel for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And the Source Code Access user details for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	When I update a koroneiki account with the name "Pjdzen - ${staticProjectName}" to have the following values
	| Status | Active | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	And I accept the GitHub repository invitation using the username "infoservicesqa" and the password "rejoicealways123"
	Then I should be able to view the is-support-integration repository using the username "infoservicesqa" and the password "rejoicealways123"

Scenario: View source code access user added by team member
	New source code access users on an approved project added by a team member should be able to gain access to the proper GitHub repository.

	Given I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	And I synchronize the contact entitlement definition with the name "Customer"
	And I synchronize the contact entitlement definition with the name "Customer - DXP"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support user with the email address "pjd.customer1@lrcustomer.com.broken" should be synced to the support site
	And the support user with the email address "pjd.customer2@lrcustomer.com.broken" should be synced to the support site
	When I update the support password for user with the email address "pjd.customer1@lrcustomer.com.broken" to "${staticSupportUserTestPassword}"
	And I update the support password for user with the email address "pjd.customer2@lrcustomer.com.broken" to "${staticSupportUserTestPassword}"
	And I assign the support user with the email address "pjd.customer1@lrcustomer.com.broken" to the support group "Help Center"
	And I assign the support user with the email address "pjd.customer2@lrcustomer.com.broken" to the support group "Help Center"
	And I assign the support user with the email address "pjd.customer1@lrcustomer.com.broken" to the support role "Customer - DXP"
	And I assign the support user with the email address "pjd.customer2@lrcustomer.com.broken" to the support role "Customer - DXP"
	Then the support organization with the code "PJDZENPJDZEN" should be part of the support site
	When I log into the support homepage as "pjd.customer1@lrcustomer.com.broken" with password "${staticSupportUserTestPassword}" 
	And I go to the Project Details page 
	And I go to the Source Code Access tab
	And I add a Source Code Access user with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	Then the Source Code Access user panel for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And the Source Code Access user details for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And I accept the GitHub repository invitation using the username "infoservicesqa" and the password "rejoicealways123"
	Then I should be able to view the is-support-integration repository using the username "infoservicesqa" and the password "rejoicealways123"

Scenario: View source code access user added by team member after project expiration
	New source code access users on an approved project added by a team member should not be able to gain access to the proper GitHub repository after their project has expired.

	Given I generate past static start and end dates
	And I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	And I synchronize the contact entitlement definition with the name "Customer"
	And I synchronize the contact entitlement definition with the name "Customer - DXP"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support user with the email address "pjd.customer1@lrcustomer.com.broken" should be synced to the support site
	And the support user with the email address "pjd.customer2@lrcustomer.com.broken" should be synced to the support site
	When I update the support password for user with the email address "pjd.customer1@lrcustomer.com.broken" to "${staticSupportUserTestPassword}"
	And I update the support password for user with the email address "pjd.customer2@lrcustomer.com.broken" to "${staticSupportUserTestPassword}"
	And I assign the support user with the email address "pjd.customer1@lrcustomer.com.broken" to the support group "Help Center"
	And I assign the support user with the email address "pjd.customer2@lrcustomer.com.broken" to the support group "Help Center"
	And I assign the support user with the email address "pjd.customer1@lrcustomer.com.broken" to the support role "Customer - DXP"
	And I assign the support user with the email address "pjd.customer2@lrcustomer.com.broken" to the support role "Customer - DXP"
	Then the support organization with the code "PJDZENPJDZEN" should be part of the support site
	When I log into the support homepage as "pjd.customer1@lrcustomer.com.broken" with password "${staticSupportUserTestPassword}" 
	And I go to the Project Details page 
	And I go to the Source Code Access tab
	And I add a Source Code Access user with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	Then the Source Code Access user panel for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And the Source Code Access user details for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	When I wait for "30" seconds
	And I accept the GitHub repository invitation using the username "infoservicesqa" and the password "rejoicealways123"
	And I update a product purchase on koroneiki made by the account "Pjdzen - ${staticProjectName}" for the product "DXP Production" to have the following values
	| End Date                | ${staticPastEndDate}         | |
	| Original End Date       | ${staticPastOriginalEndDate} | |
	| Perpetual               | false                        | |
	| Start Date              | ${staticPastStartDate}       | |
	| Status                  | Approved                     | |
	And I update a product purchase on koroneiki made by the account "Pjdzen - ${staticProjectName}" for the product "Platinum Subscription" to have the following values
	| End Date                | ${staticPastEndDate}         | |
	| Original End Date       | ${staticPastOriginalEndDate} | |
	| Perpetual               | false                        | |
	| Start Date              | ${staticPastStartDate}       | |
	| Status                  | Approved                     | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	# Necessary pause to allow synchronization to take place before the github repo is viewed again without permissions
	And I wait for "5" seconds
	Then I should not be able to view the is-support-integration repository using the username "infoservicesqa" and the password "rejoicealways123"

Scenario: View source code access user removed by team member
	New source code access users on an approved project removed by a team member should not be able to gain access to the proper GitHub repository.

	Given I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	And I synchronize the contact entitlement definition with the name "Customer"
	And I synchronize the contact entitlement definition with the name "Customer - DXP"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support user with the email address "pjd.customer1@lrcustomer.com.broken" should be synced to the support site
	And the support user with the email address "pjd.customer2@lrcustomer.com.broken" should be synced to the support site
	When I update the support password for user with the email address "pjd.customer1@lrcustomer.com.broken" to "${staticSupportUserTestPassword}"
	And I update the support password for user with the email address "pjd.customer2@lrcustomer.com.broken" to "${staticSupportUserTestPassword}"
	And I assign the support user with the email address "pjd.customer1@lrcustomer.com.broken" to the support group "Help Center"
	And I assign the support user with the email address "pjd.customer2@lrcustomer.com.broken" to the support group "Help Center"
	And I assign the support user with the email address "pjd.customer1@lrcustomer.com.broken" to the support role "Customer - DXP"
	And I assign the support user with the email address "pjd.customer2@lrcustomer.com.broken" to the support role "Customer - DXP"
	Then the support organization with the code "PJDZENPJDZEN" should be part of the support site
	When I log into the support homepage as "pjd.customer1@lrcustomer.com.broken" with password "${staticSupportUserTestPassword}" 
	And I go to the Project Details page 
	And I go to the Source Code Access tab
	And I add a Source Code Access user with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	Then the Source Code Access user panel for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And the Source Code Access user details for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	When I wait for "30" seconds
	And I accept the GitHub repository invitation using the username "infoservicesqa" and the password "rejoicealways123"
	And I go to the Project Details page 
	And I go to the Source Code Access tab
	And I delete the Source Code Access user with the email address "info.services.qa@gmail.com"
	And I log out and log into the support homepage as "amos.fong@liferay.com.broken" with password "test" 
	Then I should not be able to view the is-support-integration repository using the username "infoservicesqa" and the password "rejoicealways123"

Scenario: View source code access user invitation acceptance after access removal
	Testing for new source code access users who only gain access after access removal via late invitation acceptance on an approved project should be able to gain access to the proper GitHub repository.

	Given I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	Then the support organization with the code "PJDZENPJDZEN" should be part of the support site
	When I log into the support homepage as "${staticSupportAdminEmailAdress}" with password "${staticSupportAdminPassword}"
	And I go to the Project Details page 
	And I search for the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the Source Code Access tab
	And I add a Source Code Access user with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	Then the Source Code Access user panel for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And the Source Code Access user details for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	When I delete the Source Code Access user with the email address "info.services.qa@gmail.com"
	And I accept the GitHub repository invitation using the username "infoservicesqa" and the password "rejoicealways123"
	Then I should be able to view the is-support-integration repository using the username "infoservicesqa" and the password "rejoicealways123"
	When I automatically have all non Source Code Access users removed from the GitHub repository 
	Then I should not be able to view the is-support-integration repository using the username "infoservicesqa" and the password "rejoicealways123"

Scenario: View source code access user invitation acceptance after project expiration
	Testing for new source code access users who only gain access after project expiration via late invitation acceptance on an approved project should be able to gain access to the proper GitHub repository.

	Given I generate past static start and end dates
	And I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	Then the support organization with the code "PJDZENPJDZEN" should be part of the support site
	When I log into the support homepage as "${staticSupportAdminEmailAdress}" with password "${staticSupportAdminPassword}"
	And I go to the Project Details page 
	And I search for the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the Source Code Access tab
	And I add a Source Code Access user with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	Then the Source Code Access user panel for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And the Source Code Access user details for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And I update a product purchase on koroneiki made by the account "Pjdzen - ${staticProjectName}" for the product "DXP Production" to have the following values
	| End Date                | ${staticPastEndDate}         | |
	| Original End Date       | ${staticPastOriginalEndDate} | |
	| Perpetual               | false                        | |
	| Start Date              | ${staticPastStartDate}       | |
	| Status                  | Approved                     | |
	And I update a product purchase on koroneiki made by the account "Pjdzen - ${staticProjectName}" for the product "Platinum Subscription" to have the following values
	| End Date                | ${staticPastEndDate}         | |
	| Original End Date       | ${staticPastOriginalEndDate} | |
	| Perpetual               | false                        | |
	| Start Date              | ${staticPastStartDate}       | |
	| Status                  | Approved                     | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	And I accept the GitHub repository invitation using the username "infoservicesqa" and the password "rejoicealways123"
	Then I should be able to view the is-support-integration repository using the username "infoservicesqa" and the password "rejoicealways123"
	When I automatically have all non Source Code Access users removed from the GitHub repository 
	Then I should not be able to view the is-support-integration repository using the username "infoservicesqa" and the password "rejoicealways123"

Scenario: View source code access user on multiple projects
	New source code access users on multiple approved projects should be able to gain access to the proper GitHub repository.

	Given I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | | 
	And I create a product purchase for an account with the name "Dupzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I create a product purchase for an account with the name "Dupzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	Then the support organization with the code "PJDZENPJDZEN" should be part of the support site
	And the support organization with the code "DUPZENDUPZEN" should be part of the support site
	When I log into the support homepage as "${staticSupportAdminEmailAdress}" with password "${staticSupportAdminPassword}"
	And I go to the Project Details page 
	And I search for the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the Source Code Access tab
	And I add a Source Code Access user with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	Then the Source Code Access user panel for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And the Source Code Access user details for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And I accept the GitHub repository invitation using the username "infoservicesqa" and the password "rejoicealways123"
	Then I should be able to view the is-support-integration repository using the username "infoservicesqa" and the password "rejoicealways123"
	When I go to the Project Details page 
	And I search for the project with the name "Dupzen - ${staticProjectName}"
	And I go to the project with the name "Dupzen - ${staticProjectName}"
	And I go to the Source Code Access tab
	And I add a Source Code Access user with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	Then the Source Code Access user panel for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And the Source Code Access user details for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	Then I should not be able to view the GitHub repository invitation using the username "infoservicesqa" and the password "rejoicealways123"
	And I should be able to view the is-support-integration repository using the username "infoservicesqa" and the password "rejoicealways123"

Scenario: View source code access user on multiple projects after removal from one project
	New source code access users on multiple approved projects should be able to gain access to the proper GitHub repository even if they are removed from one project.

	Given I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I create a product purchase for an account with the name "Dupzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I create a product purchase for an account with the name "Dupzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	Then the support organization with the code "PJDZENPJDZEN" should be part of the support site
	And the support organization with the code "DUPZENDUPZEN" should be part of the support site
	When I log into the support homepage as "${staticSupportAdminEmailAdress}" with password "${staticSupportAdminPassword}"
	And I go to the Project Details page 
	And I search for the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the Source Code Access tab
	And I add a Source Code Access user with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	Then the Source Code Access user panel for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And the Source Code Access user details for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And I accept the GitHub repository invitation using the username "infoservicesqa" and the password "rejoicealways123"
	And I go to the Project Details page 
	And I search for the project with the name "Dupzen - ${staticProjectName}"
	And I go to the project with the name "Dupzen - ${staticProjectName}"
	And I go to the Source Code Access tab
	And I add a Source Code Access user with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	Then the Source Code Access user panel for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And the Source Code Access user details for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	When I go to the Project Details page 
	And I search for the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the Source Code Access tab
	And I delete the Source Code Access user with the email address "info.services.qa@gmail.com"
	Then I should be able to view the is-support-integration repository using the username "infoservicesqa" and the password "rejoicealways123"

Scenario: View source code access user on multiple projects after one project expiration
	New source code access users on multiple approved projects should be able to gain access to the proper GitHub repository even if they are on one expired project.

	Given I generate past static start and end dates
	And I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | | 
	And I create a product purchase for an account with the name "Dupzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I create a product purchase for an account with the name "Dupzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "PJDZENPJDZEN" should be part of the support site
	And the support organization with the code "DUPZENDUPZEN" should be part of the support site
	When I log into the support homepage as "${staticSupportAdminEmailAdress}" with password "${staticSupportAdminPassword}"
	And I go to the Project Details page 
	And I search for the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the Source Code Access tab
	And I add a Source Code Access user with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	Then the Source Code Access user panel for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And the Source Code Access user details for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And I accept the GitHub repository invitation using the username "infoservicesqa" and the password "rejoicealways123"
	And I go to the Project Details page 
	And I search for the project with the name "Dupzen - ${staticProjectName}"
	And I go to the project with the name "Dupzen - ${staticProjectName}"
	And I go to the Source Code Access tab
	And I add a Source Code Access user with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	Then the Source Code Access user panel for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And the Source Code Access user details for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	When I update a product purchase on koroneiki made by the account "Pjdzen - ${staticProjectName}" for the product "DXP Production" to have the following values
	| End Date                | ${staticPastEndDate}         | |
	| Original End Date       | ${staticPastOriginalEndDate} | |
	| Perpetual               | false                        | |
	| Start Date              | ${staticPastStartDate}       | |
	| Status                  | Approved                     | |
	And I update a product purchase on koroneiki made by the account "Pjdzen - ${staticProjectName}" for the product "Platinum Subscription" to have the following values
	| End Date                | ${staticPastEndDate}         | |
	| Original End Date       | ${staticPastOriginalEndDate} | |
	| Perpetual               | false                        | |
	| Start Date              | ${staticPastStartDate}       | |
	| Status                  | Approved                     | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	Then I should be able to view the is-support-integration repository using the username "infoservicesqa" and the password "rejoicealways123"

Scenario: View source code access user after removal from multiple projects
	New source code access users on multiple approved projects should not be able to gain access to the proper GitHub repository if they are removed from all projects.

	Given I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | | 
	And I create a product purchase for an account with the name "Dupzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I create a product purchase for an account with the name "Dupzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "PJDZENPJDZEN" should be part of the support site
	And the support organization with the code "DUPZENDUPZEN" should be part of the support site
	When I log into the support homepage as "${staticSupportAdminEmailAdress}" with password "${staticSupportAdminPassword}"
	And I go to the Project Details page 
	And I search for the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the Source Code Access tab
	And I add a Source Code Access user with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	Then the Source Code Access user panel for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And the Source Code Access user details for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And I accept the GitHub repository invitation using the username "infoservicesqa" and the password "rejoicealways123"
	And I go to the Project Details page 
	And I search for the project with the name "Dupzen - ${staticProjectName}"
	And I go to the project with the name "Dupzen - ${staticProjectName}"
	And I go to the Source Code Access tab
	And I add a Source Code Access user with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	Then the Source Code Access user panel for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And the Source Code Access user details for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	When I go to the Project Details page 
	And I search for the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the Source Code Access tab
	And I delete the Source Code Access user with the email address "info.services.qa@gmail.com"
	And I go to the Project Details page 
	And I search for the project with the name "Dupzen - ${staticProjectName}"
	And I go to the project with the name "Dupzen - ${staticProjectName}"
	And I go to the Source Code Access tab
	And I delete the Source Code Access user with the email address "info.services.qa@gmail.com"
	Then I should not be able to view the is-support-integration repository using the username "infoservicesqa" and the password "rejoicealways123"

Scenario: View source code access user after multiple project expirations
	New source code access users on multiple expired projects should not be able to gain access to the proper GitHub repository.

	Given I generate past static start and end dates
	And I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | | 
	And I create a product purchase for an account with the name "Dupzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I create a product purchase for an account with the name "Dupzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "PJDZENPJDZEN" should be part of the support site
	And the support organization with the code "DUPZENDUPZEN" should be part of the support site
	When I log into the support homepage as "${staticSupportAdminEmailAdress}" with password "${staticSupportAdminPassword}"
	And I go to the Project Details page 
	And I search for the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the Source Code Access tab
	And I add a Source Code Access user with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	Then the Source Code Access user panel for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And the Source Code Access user details for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And I accept the GitHub repository invitation using the username "infoservicesqa" and the password "rejoicealways123"
	And I go to the Project Details page 
	And I search for the project with the name "Dupzen - ${staticProjectName}"
	And I go to the project with the name "Dupzen - ${staticProjectName}"
	And I go to the Source Code Access tab
	And I add a Source Code Access user with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	Then the Source Code Access user panel for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And the Source Code Access user details for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	When I update a product purchase on koroneiki made by the account "Pjdzen - ${staticProjectName}" for the product "DXP Production" to have the following values
	| End Date                | ${staticPastEndDate}         | |
	| Original End Date       | ${staticPastOriginalEndDate} | |
	| Perpetual               | false                        | |
	| Start Date              | ${staticPastStartDate}       | |
	| Status                  | Approved                     | |
	And I update a product purchase on koroneiki made by the account "Pjdzen - ${staticProjectName}" for the product "Platinum Subscription" to have the following values
	| End Date                | ${staticPastEndDate}         | |
	| Original End Date       | ${staticPastOriginalEndDate} | |
	| Perpetual               | false                        | |
	| Start Date              | ${staticPastStartDate}       | |
	| Status                  | Approved                     | |
	And I update a product purchase on koroneiki made by the account "Dupzen - ${staticProjectName}" for the product "DXP Production" to have the following values
	| End Date                | ${staticPastEndDate}         | |
	| Original End Date       | ${staticPastOriginalEndDate} | |
	| Perpetual               | false                        | |
	| Start Date              | ${staticPastStartDate}       | |
	| Status                  | Approved                     | |
	And I update a product purchase on koroneiki made by the account "Dupzen - ${staticProjectName}" for the product "Platinum Subscription" to have the following values
	| End Date                | ${staticPastEndDate}         | |
	| Original End Date       | ${staticPastOriginalEndDate} | |
	| Perpetual               | false                        | |
	| Start Date              | ${staticPastStartDate}       | |
	| Status                  | Approved                     | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	# Necessary pause to allow synchronization to take place before the github repo is viewed again without permissions
	And I wait for "5" seconds
	Then I should not be able to view the is-support-integration repository using the username "infoservicesqa" and the password "rejoicealways123"

Scenario: View source code access user added and removed before project approval
	New source code access users added and removed from a closed project should not be able to gain access to the proper GitHub repository after project approval.

	Given I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	Then the support organization with the code "PJDZENPJDZEN" should be part of the support site
	When I update a koroneiki account with the name "Pjdzen - ${staticProjectName}" to have the following values
	| Status | Closed | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	And I log into the support homepage as "${staticSupportAdminEmailAdress}" with password "${staticSupportAdminPassword}"
	And I go to the Project Details page
	And I search for the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the Source Code Access tab
	And I add a Source Code Access user with the following values on a closed project
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	Then the Source Code Access user panel for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And the Source Code Access user details for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	When I delete the Source Code Access user with the email address "info.services.qa@gmail.com"
	And I update a koroneiki account with the name "Pjdzen - ${staticProjectName}" to have the following values
	| Status | Active | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	Then I should not be able to view the GitHub repository invitation using the username "infoservicesqa" and the password "rejoicealways123"
	And I should not be able to view the is-support-integration repository using the username "infoservicesqa" and the password "rejoicealways123"

Scenario: View source code access form errors
	Proper error messages should be visible when the form is filled out incorrectly.

	Given I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	Then the support organization with the code "PJDZENPJDZEN" should be part of the support site
	When I log into the support homepage as "${staticSupportAdminEmailAdress}" with password "${staticSupportAdminPassword}"
	And I go to the Project Details page 
	And I search for the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the Source Code Access tab
	And I click on the Source Code Access add user button
	And I populate the Source Code Access user form with the following values
	| Name            | |
	| Email Address   | |
	| GitHub Username | |
	And I click on the Source Code Access Form submit button
	Then I should be able to view the Source Code Access user form errors with the following values
	| Main Error                  | Please correct the following fields: Name, Email Address, GitHub Username |
	| Name Field Error            | This field is required.                                                   |
	| Email Address Field Error   | This field is required.                                                   |
	| GitHub Username Field Error | This field is required.                                                   |
	When I populate the Source Code Access user form with the following values
	| Name            | Info                |
	| Email Address   | info.services.qa    |
	| GitHub Username | infoservicesqa78592 |
	And I click on the Source Code Access Form submit button
	Then I should be able to view the Source Code Access user form errors with the following values
	| Main Error                  | Please correct the following fields: Email Address |
	| Email Address Field Error   | Invalid Format                                     |
	When I populate the Source Code Access user form with the following values
	| Name            | Info                   |
	| Email Address   | info.services.qa@gmail |
	| GitHub Username | infoservicesqa78592    |
	And I click on the Source Code Access Form submit button
	Then I should be able to view the Source Code Access user form errors with the following values
	| Main Error                  | Please correct the following fields: Email Address |
	| Email Address Field Error   | Invalid Format                                     |
	When I populate the Source Code Access user form with the following values
	| Name            | Info Services          |
	| Email Address   | info.services.qa@gmail |
	| GitHub Username | infoservicesqa78592    |
	And I click on the Source Code Access Form submit button
	Then I should be able to view the Source Code Access user form errors with the following values
	| Main Error                | Please correct the following fields: Email Address |
	| Email Address Field Error | Invalid Format                                     |
	When I populate the Source Code Access user form with the following values
	| Name            | Info Services              |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa.78592       |
	And I click on the Source Code Access Form submit button
	Then I should be able to view the Source Code Access user form errors with the following values
	| Main Error                  | Error: Please correct the following fields: GitHub Username |
	| GitHub Username Field Error | Incorrect Username                                          |
	When I populate the Source Code Access user form with the following values
	| Name            | Info Services              |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa78592        |
	And I click on the Source Code Access Form submit button
	Then I should be able to view the Source Code Access user form errors with the following values
	| Main Error | Please provide a valid GitHub user or collaborator. |

Scenario: Add duplicate source code access user
	New source code access users on an approved project should be able to gain access to the proper GitHub repository and prevent duplicate GitHub screennames on the same project from being added.

	Given I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I create a product purchase for an account with the name "Pjdzen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	Then the support organization with the code "PJDZENPJDZEN" should be part of the support site
	When I log into the support homepage as "${staticSupportAdminEmailAdress}" with password "${staticSupportAdminPassword}"
	And I go to the Project Details page 
	And I search for the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the Source Code Access tab
	And I add a Source Code Access user with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	Then the Source Code Access user panel for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And the Source Code Access user details for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	When I wait for "30" seconds
	And I accept the GitHub repository invitation using the username "infoservicesqa" and the password "rejoicealways123"
	Then I should be able to view the is-support-integration repository using the username "infoservicesqa" and the password "rejoicealways123"
	When I go to the Project Details page 
	And I search for the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the project with the name "Pjdzen - ${staticProjectName}"
	And I go to the Source Code Access tab
	And I click on the Source Code Access add user button
	And I populate the Source Code Access user form with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And I click on the Source Code Access Form submit button
	Then I should be able to view the Source Code Access user form errors with the following values
	| Main Error |  Please provide a unique GitHub username |
	When I populate the Source Code Access user form with the following values
	| Name            | Info Service QA           |
	| Email Address   | info.service.qa@gmail.com |
	| GitHub Username | infoservicesqa            |
	And I click on the Source Code Access Form submit button
	Then I should be able to view the Source Code Access user form errors with the following values
	| Main Error |  Please provide a unique GitHub username |

Scenario: Add source code access user on partner account
	New source code access users added on a partner account should be able to gain access to the proper GitHub repository.

	Given I create a product purchase for an account with the name "Zenpar - ${staticCompanyName}" and the product "Service Partnership" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Partner"
	And I synchronize the contact entitlement definition with the name "Partner"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "ZENPARZENPAR" should be part of the support site
	And the support user with the email address "pjd.partner1@lrpartner.com.broken" should be synced to the support site
	When I update the support password for user with the email address "pjd.partner1@lrpartner.com.broken" to "${staticSupportUserTestPassword}"
	And I assign the support user with the email address "pjd.partner1@lrpartner.com.broken" to the support group "Help Center"
	And I log out and log into the support homepage as "pjd.partner1@lrpartner.com.broken" with password "${staticSupportUserTestPassword}"
	And I go to the Project Details page
	And I go to the Source Code Access tab
	And I add a Source Code Access user with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	Then the Source Code Access user panel for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And the Source Code Access user details for a user with the email address "info.services.qa@gmail.com" should be visible with the following values
	| Name            | Info Services QA           |
	| Email Address   | info.services.qa@gmail.com |
	| GitHub Username | infoservicesqa             |
	And I accept the GitHub repository invitation using the username "infoservicesqa" and the password "rejoicealways123"
	Then I should be able to view the is-support-integration repository using the username "infoservicesqa" and the password "rejoicealways123"