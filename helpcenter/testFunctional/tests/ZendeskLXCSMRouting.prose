@testray.main.component.name = "Support QA"
@type = "zendesk.api"

Feature: LXC SM Routing
	Testing the routing of the LXC SM ticket to the LXC SM Engineering group

Setup:
	* I get all test passwords and admin emails

Teardown:
	* I teardown the tickets with ticketIds "${staticRequestIdList}" from zendesk.liferay.com
	* I teardown users with the following userExternalIds from zendesk.liferay.com
	| User1 |
	* I teardown organizations with the following organizationExternalIds from zendesk.liferay.com
	| 12345678 |
	* I wait for "3" seconds

Scenario: LXC SM No Region Routing
	End users in the US support region who create requests for the LXC SM product and components should have their requests routed to the LXC SM US group.

	Given I create an organization with the following values
	| Status           | Approved            |
	| Name             | Organization Name 1 |
	| Tags             | lxc_sm              |
	| Shared Tickets   | true                |
	| Shared Comments  | true                |
	| SLA              | Gold                |
	| Tier             | T4                  |
	| External ID      | 12345678            |
	And I create a user with the following values
	| Name             | Zendesk User1                    |
	| Tags             | osb_customer,osb_kb              |
	| Password         | ${staticZendeskAdminPassword}    |
	| Role             | End-user                         |
	| Access           | Organization                     |
	| Email            | zendesk.user1@zendesk.com.broken |
	| Phone Number     | +1 (555) 555-5555                |
	| Organization     | Organization Name 1              |
	| Language         | English                          |
	| Time Zone        | Pacific Time (US & Canada)       |
	| External ID      | User1                            |
	When I create several requests as a user with the email address "zendesk.user1@zendesk.com.broken" with the following product and component values
	| prd_lxc_sm | backup_recovery     |
	| prd_lxc_sm | connectivity        |
	| prd_lxc_sm | networking          |
	| prd_lxc_sm | system_availability |
	| prd_lxc_sm | vpn                 |
	Then the tickets with ticketIds "${staticRequestIdList}" should be assigned to the group "LXC - SM - Support"

Scenario: LXC SM No Region Routing for FLS Organization
	End users in the US support region with FLS support who create requests for the LXC SM product and components should have their requests routed to the LXC SM Support group.

	Given I create an organization with the following values
	| Status                     | Approved            |
	| Name                       | Organization Name 1 |
	| Tags                       | lxc_sm              |
	| Shared Tickets             | true                |
	| Shared Comments            | true                |
	| SLA                        | Gold                |
	| Tier                       | T4                  |
	| Partner First Line Support | true                |
	| External ID                | 12345678            |
	And I create a user with the following values
	| Name             | Zendesk User1                    |
	| Tags             | osb_customer,osb_kb              |
	| Password         | ${staticZendeskAdminPassword}    |
	| Role             | End-user                         |
	| Access           | Organization                     |
	| Email            | zendesk.user1@zendesk.com.broken |
	| Phone Number     | +1 (555) 555-5555                |
	| Organization     | Organization Name 1              |
	| Language         | English                          |
	| Time Zone        | Pacific Time (US & Canada)       |
	| External ID      | User1                            |
	When I create several requests as a user with the email address "zendesk.user1@zendesk.com.broken" with the following product and component values
	| prd_lxc_sm | backup_recovery     |
	| prd_lxc_sm | connectivity        |
	| prd_lxc_sm | networking          |
	| prd_lxc_sm | system_availability |
	| prd_lxc_sm | vpn                 |
	Then the tickets with ticketIds "${staticRequestIdList}" should be assigned to the group "LXC - SM - Support"

Scenario: LXC SM US Routing
	@ help.center.acceptance = "true"
	End users in the US support region who create requests for the LXC SM product and components should have their requests routed to the LXC SM US group.

	Given I create an organization with the following values
	| Status           | Approved            |
	| Name             | Organization Name 1 |
	| Tags             | lxc_sm              |
	| Shared Tickets   | true                |
	| Shared Comments  | true                |
	| SLA              | Gold                |
	| Tier             | T4                  |
	| Support Region   | US                  |
	| Support Language | English             |
	| External ID      | 12345678            |
	And I create a user with the following values
	| Name             | Zendesk User1                    |
	| Tags             | osb_customer,osb_kb              |
	| Password         | ${staticZendeskAdminPassword}    |
	| Role             | End-user                         |
	| Access           | Organization                     |
	| Email            | zendesk.user1@zendesk.com.broken |
	| Phone Number     | +1 (555) 555-5555                |
	| Organization     | Organization Name 1              |
	| Language         | English                          |
	| Time Zone        | Pacific Time (US & Canada)       | 
	| External ID      | User1                            |
	When I create several requests as a user with the email address "zendesk.user1@zendesk.com.broken" with the following product and component values
	| prd_lxc_sm | backup_recovery     |
	| prd_lxc_sm | connectivity        |
	| prd_lxc_sm | networking          |
	| prd_lxc_sm | system_availability |
	| prd_lxc_sm | vpn                 |
	Then the tickets with ticketIds "${staticRequestIdList}" should be assigned to the group "LXC - SM - US"

Scenario: LXC SM US Routing for FLS Organization
	End users in the US support region with FLS support who create requests for the LXC SM product and components should have their requests routed to the LXC SM Support group.

	Given I create an organization with the following values
	| Status                     | Approved            |
	| Name                       | Organization Name 1 |
	| Tags                       | lxc_sm              |
	| Shared Tickets             | true                |
	| Shared Comments            | true                |
	| SLA                        | Gold                |
	| Tier                       | T4                  |
	| Support Region             | US                  |
	| Support Language           | English             |
	| Partner First Line Support | true                |
	| External ID                | 12345678            |
	And I create a user with the following values
	| Name             | Zendesk User1                    |
	| Tags             | osb_customer,osb_kb              |
	| Password         | ${staticZendeskAdminPassword}    |
	| Role             | End-user                         |
	| Access           | Organization                     |
	| Email            | zendesk.user1@zendesk.com.broken |
	| Phone Number     | +1 (555) 555-5555                |
	| Organization     | Organization Name 1              |
	| Language         | English                          |
	| Time Zone        | Pacific Time (US & Canada)       |
	| External ID      | User1                            |
	When I create several requests as a user with the email address "zendesk.user1@zendesk.com.broken" with the following product and component values
	| prd_lxc_sm | backup_recovery     |
	| prd_lxc_sm | connectivity        |
	| prd_lxc_sm | networking          |
	| prd_lxc_sm | system_availability |
	| prd_lxc_sm | vpn                 |
	Then the tickets with ticketIds "${staticRequestIdList}" should be assigned to the group "LXC - SM - US"

Scenario: LXC SM HU Routing
	End users in the HU support region who create requests for the LXC SM product and components should have their requests routed to the LXC SM HU group.

	Given I create an organization with the following values
	| Status           | Approved            |
	| Name             | Organization Name 1 |
	| Tags             | lxc_sm              |
	| Shared Tickets   | true                |
	| Shared Comments  | true                |
	| SLA              | Gold                |
	| Tier             | T4                  |
	| Support Region   | Hungary             |
	| Support Language | English             |
	| External ID      | 12345678            |
	And I create a user with the following values
	| Name             | Zendesk User1                    |
	| Tags             | osb_customer,osb_kb              |
	| Password         | ${staticZendeskAdminPassword}    |
	| Role             | End-user                         |
	| Access           | Organization                     |
	| Email            | zendesk.user1@zendesk.com.broken |
	| Phone Number     | +1 (555) 555-5555                |
	| Organization     | Organization Name 1              |
	| Language         | English                          |
	| Time Zone        | Budapest                         |
	| External ID      | User1                            |
	When I create several requests as a user with the email address "zendesk.user1@zendesk.com.broken" with the following product and component values
	| prd_lxc_sm | backup_recovery     |
	| prd_lxc_sm | connectivity        |
	| prd_lxc_sm | networking          |
	| prd_lxc_sm | system_availability |
	| prd_lxc_sm | vpn                 |
	Then the tickets with ticketIds "${staticRequestIdList}" should be assigned to the group "LXC - SM - HU"

Scenario: LXC SM JP Routing
	End users in the JP support region who create requests for the LXC SM product and components should have their requests routed to the LXC SM JP group.

	Given I create an organization with the following values
	| Status           | Approved            |
	| Name             | Organization Name 1 |
	| Tags             | lxc_sm              |
	| Shared Tickets   | true                |
	| Shared Comments  | true                |
	| SLA              | Gold                |
	| Tier             | T4                  |
	| Support Region   | Japan               |
	| Support Language | Japanese            |
	| External ID      | 12345678            |
	And I create a user with the following values
	| Name             | Zendesk User1                    |
	| Tags             | osb_customer,osb_kb              |
	| Password         | ${staticZendeskAdminPassword}    |
	| Role             | End-user                         |
	| Access           | Organization                     |
	| Email            | zendesk.user1@zendesk.com.broken |
	| Phone Number     | +1 (555) 555-5555                |
	| Organization     | Organization Name 1              |
	| Language         | Japanese                         |
	| Time Zone        | Tokyo                            |
	| External ID      | User1                            |
	When I create several requests as a user with the email address "zendesk.user1@zendesk.com.broken" with the following product and component values
	| prd_lxc_sm | backup_recovery     |
	| prd_lxc_sm | connectivity        |
	| prd_lxc_sm | networking          |
	| prd_lxc_sm | system_availability |
	| prd_lxc_sm | vpn                 |
	Then the tickets with ticketIds "${staticRequestIdList}" should be assigned to either the group "LXC - SM - JP" or "LXC - SM - US" or "LXC - SM - HU"

Scenario: LXC SM AU Routing
	End users in the AU support region who create requests for the LXC SM product and components should have their requests routed to the LXC SM JP/AU group.

	Given I create an organization with the following values
	| Status           | Approved            |
	| Name             | Organization Name 1 |
	| Tags             | lxc_sm              |
	| Shared Tickets   | true                |
	| Shared Comments  | true                |
	| SLA              | Gold                |
	| Tier             | T4                  |
	| Support Region   | Australia           |
	| Support Language | English             |
	| External ID      | 12345678            |
	And I create a user with the following values
	| Name             | Zendesk User1                    |
	| Tags             | osb_customer,osb_kb              |
	| Password         | ${staticZendeskAdminPassword}    |
	| Role             | End-user                         |
	| Access           | Organization                     |
	| Email            | zendesk.user1@zendesk.com.broken |
	| Phone Number     | +1 (555) 555-5555                |
	| Organization     | Organization Name 1              |
	| Language         | English                          |
	| Time Zone        | Sydney                           |
	| External ID      | User1                            |
	When I create several requests as a user with the email address "zendesk.user1@zendesk.com.broken" with the following product and component values
	| prd_lxc_sm | backup_recovery     |
	| prd_lxc_sm | connectivity        |
	| prd_lxc_sm | networking          |
	| prd_lxc_sm | system_availability |
	| prd_lxc_sm | vpn                 |
	Then the tickets with ticketIds "${staticRequestIdList}" should be assigned to either the group "LXC - SM - AU/NZ" or "LXC - SM - US" or "LXC - SM - HU"

Scenario: LXC SM IN Routing
	End users in the IN support region who create requests for the LXC SM product and components should have their requests routed to the LXC SM IN group.

	Given I create an organization with the following values
	| Status           | Approved            |
	| Name             | Organization Name 1 |
	| Tags             | lxc_sm              |
	| Shared Tickets   | true                |
	| Shared Comments  | true                |
	| SLA              | Gold                |
	| Tier             | T4                  |
	| Support Region   | India               |
	| Support Language | English             |
	| External ID      | 12345678            |
	And I create a user with the following values
	| Name             | Zendesk User1                    |
	| Tags             | osb_customer,osb_kb              |
	| Password         | ${staticZendeskAdminPassword}    |
	| Role             | End-user                         |
	| Access           | Organization                     |
	| Email            | zendesk.user1@zendesk.com.broken |
	| Phone Number     | +1 (555) 555-5555                |
	| Organization     | Organization Name 1              |
	| Language         | English                          |
	| Time Zone        | Mumbai                           |
	| External ID      | User1                            |
	When I create several requests as a user with the email address "zendesk.user1@zendesk.com.broken" with the following product and component values
	| prd_lxc_sm | backup_recovery     |
	| prd_lxc_sm | connectivity        |
	| prd_lxc_sm | networking          |
	| prd_lxc_sm | system_availability |
	| prd_lxc_sm | vpn                 |
	Then the tickets with ticketIds "${staticRequestIdList}" should be assigned to the group "LXC - SM - IN"

Scenario: LXC SM ES Routing
	End users in the ES support region who create requests for the LXC SM product and components should have their requests routed to the LXC SM ES group.

	Given I create an organization with the following values
	| Status           | Approved            |
	| Name             | Organization Name 1 |
	| Tags             | lxc_sm              |
	| Shared Tickets   | true                |
	| Shared Comments  | true                |
	| SLA              | Gold                |
	| Tier             | T4                  |
	| Support Region   | Spain               |
	| Support Language | Spanish             |
	| External ID      | 12345678            |
	And I create a user with the following values
	| Name             | Zendesk User1                    |
	| Tags             | osb_customer,osb_kb              |
	| Password         | ${staticZendeskAdminPassword}    |
	| Role             | End-user                         |
	| Access           | Organization                     |
	| Email            | zendesk.user1@zendesk.com.broken |
	| Phone Number     | +1 (555) 555-5555                |
	| Organization     | Organization Name 1              |
	| Language         | Spanish                          |
	| Time Zone        | Madrid                           |
	| External ID      | User1                            |
	When I create several requests as a user with the email address "zendesk.user1@zendesk.com.broken" with the following product and component values
	| prd_lxc_sm | backup_recovery     |
	| prd_lxc_sm | connectivity        |
	| prd_lxc_sm | networking          |
	| prd_lxc_sm | system_availability |
	| prd_lxc_sm | vpn                 |
	Then the tickets with ticketIds "${staticRequestIdList}" should be assigned to the group "LXC - SM - ES"

Scenario: LXC SM BR Routing
	End users in the BR support region who create requests for the LXC SM product and components should have their requests routed to the LXC SM BR group.

	Given I create an organization with the following values
	| Status           | Approved            |
	| Name             | Organization Name 1 |
	| Tags             | lxc_sm              |
	| Shared Tickets   | true                |
	| Shared Comments  | true                |
	| SLA              | Gold                |
	| Tier             | T4                  |
	| Support Region   | Brazil              |
	| Support Language | Portuguese          |
	| External ID      | 12345678            |
	And I create a user with the following values
	| Name             | Zendesk User1                    |
	| Tags             | osb_customer,osb_kb              |
	| Password         | ${staticZendeskAdminPassword}    |
	| Role             | End-user                         |
	| Access           | Organization                     |
	| Email            | zendesk.user1@zendesk.com.broken |
	| Phone Number     | +1 (555) 555-5555                |
	| Organization     | Organization Name 1              |
	| Language         | Portuguese                       |
	| Time Zone        | Brasilia                         |
	| External ID      | User1                            |
	When I create several requests as a user with the email address "zendesk.user1@zendesk.com.broken" with the following product and component values
	| prd_lxc_sm | backup_recovery     |
	| prd_lxc_sm | connectivity        |
	| prd_lxc_sm | networking          |
	| prd_lxc_sm | system_availability |
	| prd_lxc_sm | vpn                 |
	Then the tickets with ticketIds "${staticRequestIdList}" should be assigned to the group "LXC - SM - BR"

Scenario: LXC SM CN Routing
	End users in the CN support region who create requests for the LXC SM product and components should have their requests routed to the LXC SM CN group.

	Given I create an organization with the following values
	| Status           | Approved            |
	| Name             | Organization Name 1 |
	| Tags             | lxc_sm              |
	| Shared Tickets   | true                |
	| Shared Comments  | true                |
	| SLA              | Gold                |
	| Tier             | T4                  |
	| Support Region   | China               |
	| Support Language | Chinese             |
	| External ID      | 12345678            |
	And I create a user with the following values
	| Name             | Zendesk User1                    |
	| Tags             | osb_customer,osb_kb              |
	| Password         | ${staticZendeskAdminPassword}    |
	| Role             | End-user                         |
	| Access           | Organization                     |
	| Email            | zendesk.user1@zendesk.com.broken |
	| Phone Number     | +1 (555) 555-5555                |
	| Organization     | Organization Name 1              |
	| Language         | Portuguese                       |
	| Time Zone        | Brasilia                         |
	| External ID      | User1                            |
	When I create several requests as a user with the email address "zendesk.user1@zendesk.com.broken" with the following product and component values
	| prd_lxc_sm | backup_recovery     |
	| prd_lxc_sm | connectivity        |
	| prd_lxc_sm | networking          |
	| prd_lxc_sm | system_availability |
	| prd_lxc_sm | vpn                 |
	Then the tickets with ticketIds "${staticRequestIdList}" should be assigned to the group "LXC - SM - CN"