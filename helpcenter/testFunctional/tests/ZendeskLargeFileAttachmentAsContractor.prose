@testray.main.component.name = "Support QA"
@type = "zendesk.frontend"

Feature: Zendesk Large File Attachment As Contractor
	Testing the large file uploader as contractor in Zendesk.

Setup:
	* I get all test passwords and admin emails

Teardown:
	* I get the ID of the default user belonging to the organization "Lfazen - ${staticProjectName}" from zendesk.liferay.com
	* I teardown tickets requested by the users with the following email addresses from zendesk.liferay.com
	| lfa.customer1@lrcustomer.com.broken |
	* I tear down the projects with the following project codes
	| LFAZENLFAZEN |
	* I tear down the accounts on koroneiki with the following names
	| Lfazen - ${staticProjectName} |
	* I tear down the accounts on koroneiki with the following "Code" values
	| LFAZENLFAZEN |
	* I synchronize the account entitlement definition with the name "Gold Subscription"
	* I tear down the users with the following email addresses from web.liferay.com
	| lfa.customer1@lrcustomer.com.broken |
	* I tear down the users with the following email addresses from customer.liferay.com
	| lfa.customer1@lrcustomer.com.broken   |
	| lfa.contractor1@gs.liferay.com.broken |
	* I tear down the users with the following email addresses from koroneiki
	| lfa.customer1@lrcustomer.com.broken |
	* I tear down the contacts on koroneiki with the following email addresses
	| lfa.customer1@lrcustomer.com.broken |
	* I tear down the users with the following email addresses from okta
	| lfa.customer1@lrcustomer.com.broken |
	* I obtain the IDs of the following user email addresses from zendesk.liferay.com
	| lfa.customer1@lrcustomer.com.broken   |
	| lfa.contractor1@gs.liferay.com.broken |
	* I teardown users with the following userIds "${staticUserIdList}" from zendesk.liferay.com
	* I obtain the external IDs of the following organizations from zendesk.liferay.com if still present
	| Lfazen - ${staticProjectName} |
	* I teardown organizations with the following organizationExternalIds "${staticOrganizationExternalIdList}" from zendesk.liferay.com
	* I teardown the user with userId "${staticDefaultUserId}" from zendesk.liferay.com

Scenario: Upload large file
	Contractors should be able to upload large attachments to a ticket using the large file uploader

	Given I generate a provisioning project name
	And I get all test passwords and admin emails
	And I create a user with the following parameters
	| Screen Name   | lfa.customer1                       |
	| Email Address | lfa.customer1@lrcustomer.com.broken |
	| First Name    | LFA                                 |
	| Last Name     | Customer1                           |
	And I create a support user with the following parameters
	| Screen Name   | lfa.contractor1                       |
	| Email Address | lfa.contractor1@gs.liferay.com.broken |
	| First Name    | LFA                                   |
	| Last Name     | Contractor1                           |
	And I assign the support user with the email address "lfa.contractor1@gs.liferay.com.broken" to the support organization "Data Access US"
	And I create an account on koroneiki with the following values
	| Code        | LFAZENLFAZEN                  | |
	| Data Region | United States                 | |
	| Language    | English                       | |
	| Name        | Lfazen - ${staticProjectName} | |
	| Region      | United States                 | |
	| Status      | Active                        | |
	| Tier        | T1                            | |
	And I assign the contact with the email address "lfa.customer1@lrcustomer.com.broken" on the account "Lfazen - ${staticProjectName}" to the following roles
	| Support Requester |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Lfazen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Lfazen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	And I synchronize the contact entitlement definition with the name "Customer"
	And I synchronize the contact entitlement definition with the name "Customer - Portal"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the support organization with the code "LFAZENLFAZEN" should be part of the support site
	And the organization "Lfazen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the support user with the email address "lfa.customer1@lrcustomer.com.broken" should be synced to the support site
	And the support user with the email address "lfa.contractor1@gs.liferay.com.broken" should be synced to the support site
	When I assign the support user with the email address "lfa.contractor1@gs.liferay.com.broken" to the support organization "Liferay Contractor"
	And I update the support password for user with the email address "lfa.customer1@lrcustomer.com.broken" to "${staticSupportUserTestPassword}"
	And I update the support password for user with the email address "lfa.contractor1@gs.liferay.com.broken" to "${staticSupportUserTestPassword}"
	Then the user with the email address "lfa.customer1@lrcustomer.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "lfa.contractor1@gs.liferay.com.broken" should be present on zendesk.liferay.com
	And I update the user with the email address "lfa.customer1@lrcustomer.com.broken" to use the password "${staticZendeskAdminPassword}"
	And I update the user with the email address "lfa.contractor1@gs.liferay.com.broken" to use the password "${staticZendeskAdminPassword}"
	And I update the user with the email address "lfa.contractor1@gs.liferay.com.broken" to have the role "Agent"
#	And I update the user with the email address "lfa.contractor1@gs.liferay.com.broken" to have the custom role "Light agent"
#	And I assign the user with the email address "lfa.contractor1@gs.liferay.com.broken" to the group "Light Agents"
	And I log out and log into the support homepage as "lfa.customer1@lrcustomer.com.broken" with password "${staticSupportUserTestPassword}"
	And I log into zendesk as "lfa.customer1@lrcustomer.com.broken" with password "${staticZendeskAdminPassword}"
	And I create a request with the following values
	| Product       | Liferay Portal                          | 6.2                                   |
	| Component     | Activation Key & Project Administration |                                       |
	| System Status | Production                              | Functioning with limited capabilities |
	| Subject       | Request Subject                         |                                       |
	| Description   | Request Description                     |                                       |
	| Attachments   | Document_1.txt                          |                                       |
	Then I should be able to view a request with the following details
	| Subject       | Request Subject                                    |
	| Status        | OPEN                                               |
	| Requester     | LFA Customer1                                      |
	| Priority      | Normal                                             |
	| Product       | Liferay Portal > 6.2                               |
	| Sub-Status    | —                                                  |
	| Component     | Activation Key & Project Administration            |
	| System Status | Production > Functioning with limited capabilities |
	| Attachments   | Document_1.txt                                     |
	And I log out and log into the support homepage as "lfa.contractor1@gs.liferay.com.broken" with password "${staticSupportUserTestPassword}"
	And I log out and log into zendesk as "lfa.contractor1@gs.liferay.com.broken" with password "${staticZendeskAdminPassword}"
	# Necessary pause for tags,ticket and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "20" seconds
	And I go to the request with the ID "${staticRequestId}"
	And I dismiss the ticket interface tour
	And I upload a file using the large file uploader with the following parameters via the ticket page
	| Attachment       | Document_2.txt      |
	| Comment          | Large File Comment1 |
	| Data Region      | United States       |
	| No Personal Data | false               |
	And I go to the request with the ID "${staticRequestId}"
	Then the ticket with the ticketId "${staticRequestId}" should have the comment containing the text "Large File Comment1"
	And the ticket with the ticketId "${staticRequestId}" should have the comment containing the text "Document_2.txt"