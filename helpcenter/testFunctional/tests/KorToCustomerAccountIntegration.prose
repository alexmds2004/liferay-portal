@testray.main.component.name = "Support QA"
@type = "customer.integration"

Feature:  Koroneiki to Customer Account Integration
	Testing to assure that koroneiki account information transfers properly to the account details page on customer.

Setup:
	* I get all test passwords and admin emails
	* I generate static start and end dates
	* I generate past static start and end dates
	* I generate future static start and end dates
	* I generate a provisioning company name
	* I generate a provisioning project name
	* I create a user with the following parameters
	| Screen Name   | cstm.customer1                       |
	| Email Address | cstm.customer1@lrcustomer.com.broken |
	| First Name    | CSTM                                 |
	| Last Name     | Customer1                            |
	* I create a user with the following parameters
	| Screen Name   | cstm.customer2                       |
	| Email Address | cstm.customer2@lrcustomer.com.broken |
	| First Name    | CSTM                                 |
	| Last Name     | Customer2                            |
	* I create an account on koroneiki with the following values
	| Code   | KORCSTKORCST                  | |
	| Name   | Korcst - ${staticProjectName} | |
	| Region | United States                 | |
	| Status | Active                        | |
	| Tier   | T1                            | |
	* I assign the contact with the email address "cstm.customer1@lrcustomer.com.broken" on the account "Korcst - ${staticProjectName}" to the following roles
	| Support Requester |
	* I assign the contact with the email address "cstm.customer2@lrcustomer.com.broken" on the account "Korcst - ${staticProjectName}" to the following roles
	| Support User |

Teardown:
	* I tear down the teams on koroneiki with the following names
	| CSTM Customer |
	* I tear down the projects with the following project codes
	| KORCSTKORCST |
	| KORCT2KORCT2 |
	* I tear down the accounts on koroneiki with the following names
	| Korcst - ${staticProjectName} |
	* I tear down the accounts on koroneiki with the following "Code" values
	| KORCSTKORCST |
	| KORCT2KORCT2 |
	* I tear down the users with the following email addresses from web.liferay.com
	| cstm.customer1@lrcustomer.com.broken |
	| cstm.customer2@lrcustomer.com.broken |
	* I tear down the users with the following email addresses from customer.liferay.com
	| cstm.customer1@lrcustomer.com.broken |
	| cstm.customer2@lrcustomer.com.broken |
	* I tear down the users with the following email addresses from koroneiki
	| cstm.customer1@lrcustomer.com.broken |
	| cstm.customer2@lrcustomer.com.broken |
	* I tear down the contacts on koroneiki with the following email addresses
	| cstm.customer1@lrcustomer.com.broken |
	| cstm.customer2@lrcustomer.com.broken |
	* I tear down the users with the following email addresses from okta
	| cstm.customer1@lrcustomer.com.broken |
	| cstm.customer2@lrcustomer.com.broken |

Scenario: View DXP Production account on Customer account page
	Testing the ability to create a DXP Production account in Koroneiki and viewing the account in Customer accounts page

	Given I create a product purchase for an account with the name "Korcst - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I log into the customer homepage as "${staticCustomerAdminEmailAddress}" with password "${staticCustomerAdminPassword}"
	Then I should be able to view the following project "Korcst - ${staticProjectName}" values in the customer project details page
	| Name           | Korcst - ${staticProjectName} |
	| Code           | KORCSTKORCST                  |
	| State          | ACTIVE                        |
	| Support Region | United States                 |
	| SLA            | -                             |
	| Support Seats  | 0 / 0 Filled                  |
	| Tier           | T1                            |
	| FLS Partner    | -                             |

Scenario: View Portal Production account on Customer account page
	Testing the ability to create a Portal Production account in Koroneiki and viewing the account in Customer accounts page

	Given I create a product purchase for an account with the name "Korcst - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I log into the customer homepage as "${staticCustomerAdminEmailAddress}" with password "${staticCustomerAdminPassword}"
	Then I should be able to view the following project "Korcst - ${staticProjectName}" values in the customer project details page
	| Name           | Korcst - ${staticProjectName} |
	| Code           | KORCSTKORCST                  |
	| State          | ACTIVE                        |
	| Support Region | United States                 |
	| SLA            | -                             |
	| Support Seats  | 0 / 0 Filled                  |
	| Tier           | T1                            |
	| FLS Partner    | -                             |

Scenario: View DXP Production account with Enterprise Search on Customer account page
	Testing the ability to create a DXP Production account with Enterprise Search in Koroneiki and viewing the account in Customer accounts page

	Given I create a product purchase for an account with the name "Korcst - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I create a product purchase for an account with the name "Korcst - ${staticProjectName}" and the product "Enterprise Search - Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I log into the customer homepage as "${staticCustomerAdminEmailAddress}" with password "${staticCustomerAdminPassword}"
	Then I should be able to view the following project "Korcst - ${staticProjectName}" values in the customer project details page
	| Name           | Korcst - ${staticProjectName} |
	| Code           | KORCSTKORCST                  |
	| State          | ACTIVE                        |
	| Support Region | United States                 |
	| SLA            | -                             |
	| Support Seats  | 0 / 0 Filled                  |
	| Tier           | T1                            |
	| FLS Partner    | -                             |

Scenario: View DXP Production account with Commerce on Customer account page
	Testing the ability to create a DXP Production account with Commerce in Koroneiki and viewing the account in Customer accounts page

	Given I create a product purchase for an account with the name "Korcst - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I create a product purchase for an account with the name "Korcst - ${staticProjectName}" and the product "Commerce Subscription Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I log into the customer homepage as "${staticCustomerAdminEmailAddress}" with password "${staticCustomerAdminPassword}"
	Then I should be able to view the following project "Korcst - ${staticProjectName}" values in the customer project details page
	| Name           | Korcst - ${staticProjectName} |
	| Code           | KORCSTKORCST                  |
	| State          | ACTIVE                        |
	| Support Region | United States                 |
	| SLA            | -                             |
	| Support Seats  | 0 / 0 Filled                  |
	| Tier           | T1                            |
	| FLS Partner    | -                             |

Scenario: View Platinum account on Customer account page
	Testing the ability to create a Platinum account in Koroneiki and viewing the account in Customer accounts page

	And I create a product purchase for an account with the name "Korcst - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I log into the customer homepage as "${staticCustomerAdminEmailAddress}" with password "${staticCustomerAdminPassword}"

Scenario: View Limited account on Customer account page
	Testing the ability to create a Limited account in Koroneiki and viewing the account in Customer accounts page

	And I create a product purchase for an account with the name "Korcst - ${staticProjectName}" and the product "Limited Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I log into the customer homepage as "${staticCustomerAdminEmailAddress}" with password "${staticCustomerAdminPassword}"

Scenario: View T1 account on Customer account page
	Testing the ability to create a T1 account in Koroneiki and viewing the account in Customer accounts page

	And I create an account on koroneiki with the following values
	| Code   | KORCT2KORCT2                  | |
	| Name   | Korcp3 - ${staticProjectName} | |
	| Region | United States                 | |
	| Status | Active                        | |
	| Tier   | T1                            | |
	And I log into the customer homepage as "${staticCustomerAdminEmailAddress}" with password "${staticCustomerAdminPassword}"

Scenario: View T2 account on Customer account page
	Testing the ability to create a T2 account in Koroneiki and viewing the account in Customer accounts page

	And I create an account on koroneiki with the following values
	| Code   | KORCT2KORCT2                  | |
	| Name   | Korcp3 - ${staticProjectName} | |
	| Region | United States                 | |
	| Status | Active                        | |
	| Tier   | T2                            | |
	And I log into the customer homepage as "${staticCustomerAdminEmailAddress}" with password "${staticCustomerAdminPassword}"

Scenario: View T3 account on Customer account page
	Testing the ability to create a T3 account in Koroneiki and viewing the account in Customer accounts page

	And I create an account on koroneiki with the following values
	| Code   | KORCT2KORCT2                  | |
	| Name   | Korcp3 - ${staticProjectName} | |
	| Region | United States                 | |
	| Status | Active                        | |
	| Tier   | T3                            | |
	And I log into the customer homepage as "${staticCustomerAdminEmailAddress}" with password "${staticCustomerAdminPassword}"

Scenario: View T4 account on Customer account page
	Testing the ability to create a T4 account in Koroneiki and viewing the account in Customer accounts page

	And I create an account on koroneiki with the following values
	| Code   | KORCT2KORCT2                  | |
	| Name   | Korcp3 - ${staticProjectName} | |
	| Region | United States                 | |
	| Status | Active                        | |
	| Tier   | T4                            | |
	And I log into the customer homepage as "${staticCustomerAdminEmailAddress}" with password "${staticCustomerAdminPassword}"

Scenario: View US Support Region account on Customer account page
	Testing the ability to create a US Support Region account in Koroneiki and viewing the account in Customer accounts page

	And I log into the customer homepage as "${staticCustomerAdminEmailAddress}" with password "${staticCustomerAdminPassword}"

Scenario: View Australia Support Region account on Customer account page
	Testing the ability to create a Australia Support Region account in Koroneiki and viewing the account in Customer accounts page

	And I create an account on koroneiki with the following values
	| Code   | KORCT2KORCT2                  | |
	| Name   | Korcp3 - ${staticProjectName} | |
	| Region | Australia                     | |
	| Status | Active                        | |
	| Tier   | T1                            | |
	And I log into the customer homepage as "${staticCustomerAdminEmailAddress}" with password "${staticCustomerAdminPassword}"

Scenario: View Brazil Support Region account on Customer account page
	Testing the ability to create a Brazil Support Region account in Koroneiki and viewing the account in Customer accounts page

	And I create an account on koroneiki with the following values
	| Code   | KORCT2KORCT2                  | |
	| Name   | Korcp3 - ${staticProjectName} | |
	| Region | Brazil                        | |
	| Status | Active                        | |
	| Tier   | T1                            | |
	And I log into the customer homepage as "${staticCustomerAdminEmailAddress}" with password "${staticCustomerAdminPassword}"

Scenario: View China Support Region account on Customer account page
	Testing the ability to create a China Support Region account in Koroneiki and viewing the account in Customer accounts page

	And I create an account on koroneiki with the following values
	| Code   | KORCT2KORCT2                  | |
	| Name   | Korcp3 - ${staticProjectName} | |
	| Region | China                         | |
	| Status | Active                        | |
	| Tier   | T1                            | |
	And I log into the customer homepage as "${staticCustomerAdminEmailAddress}" with password "${staticCustomerAdminPassword}"

Scenario: View Spain Support Region account on Customer account page
	Testing the ability to create a Spain Support Region account in Koroneiki and viewing the account in Customer accounts page

	And I create an account on koroneiki with the following values
	| Code   | KORCT2KORCT2                  | |
	| Name   | Korcp3 - ${staticProjectName} | |
	| Region | Spain                         | |
	| Status | Active                        | |
	| Tier   | T1                            | |
	And I log into the customer homepage as "${staticCustomerAdminEmailAddress}" with password "${staticCustomerAdminPassword}"

Scenario: View Hungary Support Region account on Customer account page
	Testing the ability to create a Hungary Support Region account in Koroneiki and viewing the account in Customer accounts page

	And I create an account on koroneiki with the following values
	| Code   | KORCT2KORCT2                  | |
	| Name   | Korcp3 - ${staticProjectName} | |
	| Region | Hungary                       | |
	| Status | Active                        | |
	| Tier   | T1                            | |
	And I log into the customer homepage as "${staticCustomerAdminEmailAddress}" with password "${staticCustomerAdminPassword}"

Scenario: View India Support Region account on Customer account page
	Testing the ability to create a India Support Region account in Koroneiki and viewing the account in Customer accounts page

	Given I create an account on koroneiki with the following values
	| Code   | KORCT2KORCT2                  | |
	| Name   | Korcp3 - ${staticProjectName} | |
	| Region | India                         | |
	| Status | Active                        | |
	| Tier   | T1                            | |
	And I log into the customer homepage as "${staticCustomerAdminEmailAddress}" with password "${staticCustomerAdminPassword}"

Scenario: View Active account on Customer account page
	Testing the ability to create a Active account in Koroneiki and viewing the account in Customer accounts page

	Given I create a product purchase for an account with the name "Korcst - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I log into the customer homepage as "${staticCustomerAdminEmailAddress}" with password "${staticCustomerAdminPassword}"

Scenario: View Future account on Customer account page
	Testing the ability to create a Future account in Koroneiki and viewing the account in Customer accounts page

	Given I create a product purchase for an account with the name "Korcst - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticFutureEndDate}         | |
	| Original End Date       | ${staticFutureOriginalEndDate} | |
	| Perpetual               | false                          | |
	| Quantity                | 1                              | |
	| Start Date              | ${staticFutureStartDate}       | |
	| Status                  | Approved                       | |
	And I log into the customer homepage as "${staticCustomerAdminEmailAddress}" with password "${staticCustomerAdminPassword}"

Scenario: View Expired account on Customer account page
	Testing the ability to create a Expired account in Koroneiki and viewing the account in Customer accounts page

	Given I create a product purchase for an account with the name "Korcst - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticPastEndDate}         | |
	| Original End Date       | ${staticPastOriginalEndDate} | |
	| Perpetual               | false                        | |
	| Quantity                | 1                            | |
	| Start Date              | ${staticPastStartDate}       | |
	| Status                  | Approved                     | |
	And I log into the customer homepage as "${staticCustomerAdminEmailAddress}" with password "${staticCustomerAdminPassword}"

Scenario: View Canceled account on Customer account page
	Testing the ability to create a Canceled account in Koroneiki and viewing the account in Customer accounts page

	Given I create a product purchase for an account with the name "Korcst - ${staticProjectName}" and the product "DXP Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Cancelled                | |
	And I log into the provisioning homepage as "${staticCustomerAdminEmailAddress}" with password "${staticCustomerAdminPassword}"