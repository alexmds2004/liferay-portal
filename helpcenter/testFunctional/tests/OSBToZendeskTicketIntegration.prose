#@testray.main.component.name = "Support QA"
#@type = "zendesk.integration"

Feature: OSB to Zendesk Ticket Integration
	Testing ticket sync updates in Zendesk caused by changes in OSB.

Setup:
	* I get all test passwords and admin emails
	* I generate a provisioning project name
	* I create a user with the following parameters
	| Screen Name   | osb.customer1                       |
	| Email Address | osb.customer1@lrcustomer.com.broken |
	| First Name    | OSB                                 |
	| Last Name     | Customer1                           |
	* I create a user with the following parameters
	| Screen Name   | osb.customer2                       |
	| Email Address | osb.customer2@lrcustomer.com.broken |
	| First Name    | OSB                                 |
	| Last Name     | Customer2                           |
	* I create an account on koroneiki with the following values
	| Code     | OSBZENOSBZEN                  | |
	| Language | English                       | |
	| Name     | Osbzen - ${staticProjectName} | |
	| Region   | United States                 | |
	| Status   | Active                        | |
	| Tier     | T1                            | |
	* I assign the contact with the email address "osb.customer1@lrcustomer.com.broken" on the account "Osbzen - ${staticProjectName}" to the following roles
	| Support Requester |

Teardown:
	* I get the ID of the default user belonging to the organization "Osbzen - ${staticProjectName}" from zendesk.liferay.com
	* I teardown the tickets with ticketIds "${staticRequestIdList}" from zendesk.liferay.com
	* I tear down the projects with the following project codes
	| OSBZENOSBZEN |
	* I tear down the accounts on koroneiki with the following names
	| Osbzen - ${staticProjectName} |
	* I tear down the accounts on koroneiki with the following "Code" values
	| OSBZENOSBZEN |
	* I synchronize the account entitlement definition with the name "Gold Subscription"
	* I tear down the users with the following email addresses from web.liferay.com
	| osb.customer1@lrcustomer.com.broken |
	| osb.customer2@lrcustomer.com.broken |
	* I tear down the users with the following email addresses from customer.liferay.com
	| osb.customer1@lrcustomer.com.broken |
	| osb.customer2@lrcustomer.com.broken |
	* I tear down the users with the following email addresses from koroneiki
	| osb.customer1@lrcustomer.com.broken |
	| osb.customer2@lrcustomer.com.broken |
	* I tear down the contacts on koroneiki with the following email addresses
	| osb.customer1@lrcustomer.com.broken |
	| osb.customer2@lrcustomer.com.broken |
	* I tear down the users with the following email addresses from okta
	| osb.customer1@lrcustomer.com.broken |
	| osb.customer2@lrcustomer.com.broken |
	* I obtain the IDs of the following user email addresses from zendesk.liferay.com
	| osb.customer1@lrcustomer.com.broken |
	| osb.customer2@lrcustomer.com.broken |
	* I teardown users with the following userIds "${staticUserIdList}" from zendesk.liferay.com
	* I obtain the external IDs of the following organizations from zendesk.liferay.com if still present
	| Osbzen - ${staticProjectName} |
	* I teardown organizations with the following organizationExternalIds "${staticOrganizationExternalIdList}" from zendesk.liferay.com
	* I teardown the user with userId "${staticDefaultUserId}" from zendesk.liferay.com

Scenario: View ticket after project offering expiration
	@ help.center.acceptance = "true"
	Zendesk tickets should be moved to closed status when its respective project is expired

	Given I assign the contact with the email address "osb.customer2@lrcustomer.com.broken" on the account "Osbzen - ${staticProjectName}" to the following roles
	| Support Requester |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer1@lrcustomer.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer2@lrcustomer.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer1@lrcustomer.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.customer2@lrcustomer.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	When I create a request as a user with the email address "osb.customer1@lrcustomer.com.broken" with the following values
	| Product           | prd_liferay_portal_6_2                           |
	| Component         | activation_key_project_administration            |
	| System Status     | production_functioning_with_limited_capabilities |
	| Subject           | Request Subject                                  |
	| Description       | Request Description                              |
	| Organization Name | Osbzen - ${staticProjectName}                    |
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the ticket with ticketId "${staticRequestIdList}" should have the status "new"
	And the ticket with ticketId "${staticRequestIdList}" should have the requester with the email address "osb.customer1@lrcustomer.com.broken"
	And I generate past static start and end dates
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "Portal Production" to have the following values
	| End Date                | ${staticPastEndDate}         | |
	| Original End Date       | ${staticPastOriginalEndDate} | |
	| Perpetual               | false                        | |
	| Start Date              | ${staticPastStartDate}       | |
	| Status                  | Approved                     | |
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "Gold Subscription" to have the following values
	| End Date                | ${staticPastEndDate}         | |
	| Original End Date       | ${staticPastOriginalEndDate} | |
	| Perpetual               | false                        | |
	| Start Date              | ${staticPastStartDate}       | |
	| Status                  | Approved                     | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the ticket with ticketId "${staticRequestIdList}" should have the status "solved"
	And the ticket with ticketId "${staticRequestIdList}" should have the default user from the organization with the name "Osbzen - ${staticProjectName}" as the requester
#	When I update the user with the email address "osb.customer1@lrcustomer.com.broken" to use the password "${staticZendeskAdminPassword}"
#	And I log into zendesk as "osb.customer1@lrcustomer.com.broken" with password "${staticZendeskAdminPassword}"
#	And I go to My activities
#	Then I should not see a request with the subject "Request Subject" in the index

Scenario: View ticket after project offering expiration with closed watcher
	Zendesk tickets should be moved to closed status when its respective project is expired with a closed watcher

	Given I assign the contact with the email address "osb.customer2@lrcustomer.com.broken" on the account "Osbzen - ${staticProjectName}" to the following roles
	| Support Closed Watcher |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer1@lrcustomer.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer2@lrcustomer.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer1@lrcustomer.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.customer2@lrcustomer.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And I create a request as a user with the email address "osb.customer1@lrcustomer.com.broken" with the following values
	| Product           | prd_liferay_portal_6_2                           |
	| Component         | activation_key_project_administration            |
	| System Status     | production_functioning_with_limited_capabilities |
	| Subject           | Request Subject                                  |
	| Description       | Request Description                              |
	| Organization Name | Osbzen - ${staticProjectName}                    |
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the ticket with ticketId "${staticRequestIdList}" should have the status "new"
	And the ticket with ticketId "${staticRequestIdList}" should have the requester with the email address "osb.customer1@lrcustomer.com.broken"
	And I generate past static start and end dates
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "Portal Production" to have the following values
	| End Date                | ${staticPastEndDate}         | |
	| Original End Date       | ${staticPastOriginalEndDate} | |
	| Perpetual               | false                        | |
	| Start Date              | ${staticPastStartDate}       | |
	| Status                  | Approved                     | |
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "Gold Subscription" to have the following values
	| End Date                | ${staticPastEndDate}         | |
	| Original End Date       | ${staticPastOriginalEndDate} | |
	| Perpetual               | false                        | |
	| Start Date              | ${staticPastStartDate}       | |
	| Status                  | Approved                     | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the ticket with ticketId "${staticRequestIdList}" should have the status "solved"
	And the ticket with ticketId "${staticRequestIdList}" should have the default user from the organization with the name "Osbzen - ${staticProjectName}" as the requester
#	When I update the user with the email address "osb.customer1@lrcustomer.com.broken" to use the password "${staticZendeskAdminPassword}"
#	And I log into zendesk as "osb.customer1@lrcustomer.com.broken" with password "${staticZendeskAdminPassword}"
#	And I go to My activities
#	Then I should not see a request with the subject "Request Subject" in the index

Scenario: View ticket after project offering deactivation
	@ help.center.acceptance = "true"
	Zendesk tickets should be moved to closed status when its respective project is expired and deactivated

	Given I assign the contact with the email address "osb.customer2@lrcustomer.com.broken" on the account "Osbzen - ${staticProjectName}" to the following roles
	| Support Requester |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer1@lrcustomer.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer2@lrcustomer.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer1@lrcustomer.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.customer2@lrcustomer.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And I create a request as a user with the email address "osb.customer1@lrcustomer.com.broken" with the following values
	| Product           | prd_liferay_portal_6_2                           |
	| Component         | activation_key_project_administration            |
	| System Status     | production_functioning_with_limited_capabilities |
	| Subject           | Request Subject                                  |
	| Description       | Request Description                              |
	| Organization Name | Osbzen - ${staticProjectName}                    |
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the ticket with ticketId "${staticRequestIdList}" should have the status "new"
	And the ticket with ticketId "${staticRequestIdList}" should have the requester with the email address "osb.customer1@lrcustomer.com.broken"
	And I generate future static start and end dates
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "Portal Production" to have the following values
	| End Date                | ${staticFutureEndDate}         | |
	| Original End Date       | ${staticFutureOriginalEndDate} | |
	| Perpetual               | false                          | |
	| Start Date              | ${staticFutureStartDate}       | |
	| Status                  | Approved                       | |
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "Gold Subscription" to have the following values
	| End Date                | ${staticFutureEndDate}         | |
	| Original End Date       | ${staticFutureOriginalEndDate} | |
	| Perpetual               | false                          | |
	| Start Date              | ${staticFutureStartDate}       | |
	| Status                  | Approved                       | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the ticket with ticketId "${staticRequestIdList}" should have the status "new"
	And the ticket with ticketId "${staticRequestIdList}" should have the requester with the email address "osb.customer1@lrcustomer.com.broken"
#	When I update the user with the email address "osb.customer1@lrcustomer.com.broken" to use the password "${staticZendeskAdminPassword}"
#	And I log into zendesk as "osb.customer1@lrcustomer.com.broken" with password "${staticZendeskAdminPassword}"
#	And I go to My activities
#	Then I should not see a request with the subject "Request Subject" in the index

Scenario: View ticket after project offering deactivation with closed watcher
	Zendesk tickets should be moved to closed status when its respective project is expired and deactivated with closer watcher 

	Given I assign the contact with the email address "osb.customer2@lrcustomer.com.broken" on the account "Osbzen - ${staticProjectName}" to the following roles
	| Support Closed Watcher |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer1@lrcustomer.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer2@lrcustomer.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer1@lrcustomer.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.customer2@lrcustomer.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And I create a request as a user with the email address "osb.customer1@lrcustomer.com.broken" with the following values
	| Product           | prd_liferay_portal_6_2                           |
	| Component         | activation_key_project_administration            |
	| System Status     | production_functioning_with_limited_capabilities |
	| Subject           | Request Subject                                  |
	| Description       | Request Description                              |
	| Organization Name | Osbzen - ${staticProjectName}                    |
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the ticket with ticketId "${staticRequestIdList}" should have the status "new"
	And the ticket with ticketId "${staticRequestIdList}" should have the requester with the email address "osb.customer1@lrcustomer.com.broken"
	And I generate future static start and end dates
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "Portal Production" to have the following values
	| End Date                | ${staticFutureEndDate}         | |
	| Original End Date       | ${staticFutureOriginalEndDate} | |
	| Perpetual               | false                          | |
	| Start Date              | ${staticFutureStartDate}       | |
	| Status                  | Approved                       | |
	And I update a product purchase on koroneiki made by the account "Osbzen - ${staticProjectName}" for the product "Gold Subscription" to have the following values
	| End Date                | ${staticFutureEndDate}         | |
	| Original End Date       | ${staticFutureOriginalEndDate} | |
	| Perpetual               | false                          | |
	| Start Date              | ${staticFutureStartDate}       | |
	| Status                  | Approved                       | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the ticket with ticketId "${staticRequestIdList}" should have the status "new"
	And the ticket with ticketId "${staticRequestIdList}" should have the requester with the email address "osb.customer1@lrcustomer.com.broken"
#	When I update the user with the email address "osb.customer1@lrcustomer.com.broken" to use the password "${staticZendeskAdminPassword}"
#	And I log into zendesk as "osb.customer1@lrcustomer.com.broken" with password "${staticZendeskAdminPassword}"
#	And I go to My activities
#	Then I should not see a request with the subject "Request Subject" in the index

Scenario: View ticket after requester removal on organization with multiple customers
	@ help.center.acceptance = "true"
	Zendesk users should not be removed from an OSB org if they have open tickets and are the last remaining customer on the OSB org.

	Given I assign the contact with the email address "osb.customer2@lrcustomer.com.broken" on the account "Osbzen - ${staticProjectName}" to the following roles
	| Support Requester |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer1@lrcustomer.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer2@lrcustomer.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer1@lrcustomer.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.customer2@lrcustomer.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And I create a request as a user with the email address "osb.customer1@lrcustomer.com.broken" with the following values
	| Product           | prd_liferay_portal_6_2                           |
	| Component         | activation_key_project_administration            |
	| System Status     | production_functioning_with_limited_capabilities |
	| Subject           | Request Subject                                  |
	| Description       | Request Description                              |
	| Organization Name | Osbzen - ${staticProjectName}                    |
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the ticket with ticketId "${staticRequestIdList}" should have the status "new"
	And the ticket with ticketId "${staticRequestIdList}" should have the requester with the email address "osb.customer1@lrcustomer.com.broken"
	And I remove customer contacts with the following email addresses from the account "Osbzen - ${staticProjectName}"
	| osb.customer1@lrcustomer.com.broken |
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the ticket with ticketId "${staticRequestIdList}" should have the status "new"
	And the ticket with ticketId "${staticRequestIdList}" should have the requester with the email address "osb.customer2@lrcustomer.com.broken"
	And I remove customer contacts with the following email addresses from the account "Osbzen - ${staticProjectName}"
	| osb.customer2@lrcustomer.com.broken |
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the ticket with ticketId "${staticRequestIdList}" should have the status "new"
	And the ticket with ticketId "${staticRequestIdList}" should have the default user from the organization with the name "Osbzen - ${staticProjectName}" as the requester

Scenario: View ticket after requester removal on organization with one customer
	Zendesk users should not be removed from an OSB org if they have open tickets and are the only customer on the OSB org.

	Given I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer1@lrcustomer.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer1@lrcustomer.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And I create a request as a user with the email address "osb.customer1@lrcustomer.com.broken" with the following values
	| Product           | prd_liferay_portal_6_2                           |
	| Component         | activation_key_project_administration            |
	| System Status     | production_functioning_with_limited_capabilities |
	| Subject           | Request Subject                                  |
	| Description       | Request Description                              |
	| Organization Name | Osbzen - ${staticProjectName}                    |
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the ticket with ticketId "${staticRequestIdList}" should have the status "new"
	And the ticket with ticketId "${staticRequestIdList}" should have the requester with the email address "osb.customer1@lrcustomer.com.broken"
	And I remove customer contacts with the following email addresses from the account "Osbzen - ${staticProjectName}"
	| osb.customer1@lrcustomer.com.broken |
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the ticket with ticketId "${staticRequestIdList}" should have the status "new"
	And the ticket with ticketId "${staticRequestIdList}" should have the default user from the organization with the name "Osbzen - ${staticProjectName}" as the requester

Scenario: View ticket after requester removal on organization with one developer and one watcher
	Zendesk users should not be removed from an OSB org if they have open tickets and are the last remaining developer customer on the OSB org.

	Given I assign the contact with the email address "osb.customer2@lrcustomer.com.broken" on the account "Osbzen - ${staticProjectName}" to the following roles
	| Support User  |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer1@lrcustomer.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer2@lrcustomer.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer1@lrcustomer.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.customer2@lrcustomer.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And I create a request as a user with the email address "osb.customer1@lrcustomer.com.broken" with the following values
	| Product           | prd_liferay_portal_6_2                           |
	| Component         | activation_key_project_administration            |
	| System Status     | production_functioning_with_limited_capabilities |
	| Subject           | Request Subject                                  |
	| Description       | Request Description                              |
	| Organization Name | Osbzen - ${staticProjectName}                    |
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the ticket with ticketId "${staticRequestIdList}" should have the status "new"
	And the ticket with ticketId "${staticRequestIdList}" should have the requester with the email address "osb.customer1@lrcustomer.com.broken"
	And I remove customer contacts with the following email addresses from the account "Osbzen - ${staticProjectName}"
	| osb.customer1@lrcustomer.com.broken |
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the ticket with ticketId "${staticRequestIdList}" should have the status "new"
	And the ticket with ticketId "${staticRequestIdList}" should have the default user from the organization with the name "Osbzen - ${staticProjectName}" as the requester

Scenario: View ticket after requester role removal on organization with multiple customers
	Zendesk user roles should not be removed from an OSB org if they have open tickets and are the last remaining customer on the OSB org.

	Given I assign the contact with the email address "osb.customer2@lrcustomer.com.broken" on the account "Osbzen - ${staticProjectName}" to the following roles
	| Support Requester |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer1@lrcustomer.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer2@lrcustomer.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer1@lrcustomer.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.customer2@lrcustomer.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And I create a request as a user with the email address "osb.customer1@lrcustomer.com.broken" with the following values
	| Product           | prd_liferay_portal_6_2                           |
	| Component         | activation_key_project_administration            |
	| System Status     | production_functioning_with_limited_capabilities |
	| Subject           | Request Subject                                  |
	| Description       | Request Description                              |
	| Organization Name | Osbzen - ${staticProjectName}                    |
	Then the ticket with ticketId "${staticRequestIdList}" should have the status "new"
	And the ticket with ticketId "${staticRequestIdList}" should have the requester with the email address "osb.customer1@lrcustomer.com.broken"
	And I remove the contact with the email address "osb.customer1@lrcustomer.com.broken" on the account "Osbzen - ${staticProjectName}" from the following roles	
	| Support Requester |
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the ticket with ticketId "${staticRequestIdList}" should have the status "new"
	And the ticket with ticketId "${staticRequestIdList}" should have the requester with the email address "osb.customer2@lrcustomer.com.broken"
	And I remove the contact with the email address "osb.customer2@lrcustomer.com.broken" on the account "Osbzen - ${staticProjectName}" from the following roles	
	| Support Requester |
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the ticket with ticketId "${staticRequestIdList}" should have the status "new"
	And the ticket with ticketId "${staticRequestIdList}" should have the default user from the organization with the name "Osbzen - ${staticProjectName}" as the requester

Scenario: View ticket after requester role removal on organization with one developer and one watcher
	Zendesk user roles should not be removed from an OSB org if they have open tickets and are the last remaining developer customer on the OSB org.

	Given I assign the contact with the email address "osb.customer2@lrcustomer.com.broken" on the account "Osbzen - ${staticProjectName}" to the following roles
	| Support User  |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer1@lrcustomer.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer2@lrcustomer.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer1@lrcustomer.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.customer2@lrcustomer.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And I create a request as a user with the email address "osb.customer1@lrcustomer.com.broken" with the following values
	| Product           | prd_liferay_portal_6_2                           |
	| Component         | activation_key_project_administration            |
	| System Status     | production_functioning_with_limited_capabilities |
	| Subject           | Request Subject                                  |
	| Description       | Request Description                              |
	| Organization Name | Osbzen - ${staticProjectName}                    |
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the ticket with ticketId "${staticRequestIdList}" should have the status "new"
	And the ticket with ticketId "${staticRequestIdList}" should have the requester with the email address "osb.customer1@lrcustomer.com.broken"
	And I remove the contact with the email address "osb.customer1@lrcustomer.com.broken" on the account "Osbzen - ${staticProjectName}" from the following roles	
	| Support Requester |
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the ticket with ticketId "${staticRequestIdList}" should have the status "new"
	And the ticket with ticketId "${staticRequestIdList}" should have the default user from the organization with the name "Osbzen - ${staticProjectName}" as the requester

Scenario: View ticket after requester removal on provisioning account with multiple customers
	Zendesk users should not be removed from a provisioning account if they have open tickets and are the last remaining customer on the OSB org.

	Given I assign the contact with the email address "osb.customer2@lrcustomer.com.broken" on the account "Osbzen - ${staticProjectName}" to the following roles
	| Support Requester |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer1@lrcustomer.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer2@lrcustomer.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer1@lrcustomer.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.customer2@lrcustomer.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And I create a request as a user with the email address "osb.customer1@lrcustomer.com.broken" with the following values
	| Product           | prd_liferay_portal_6_2                           |
	| Component         | activation_key_project_administration            |
	| System Status     | production_functioning_with_limited_capabilities |
	| Subject           | Request Subject                                  |
	| Description       | Request Description                              |
	| Organization Name | Osbzen - ${staticProjectName}                    |
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the ticket with ticketId "${staticRequestIdList}" should have the status "new"
	And the ticket with ticketId "${staticRequestIdList}" should have the requester with the email address "osb.customer1@lrcustomer.com.broken"
	And I log into the provisioning homepage as "${staticProvisioningAdminEmailAddress}" with password "${staticProvisioningAdminPassword}"
	And I go to the account index page
	And I search the accounts page using the keyword "Osbzen"
	And I click on the account with the name "Osbzen - ${staticProjectName}" on the provisioning index
	And I click on the "Contacts" tab
	And I click on the "Unassign" button in the dropdown menu for the contact with the email address "osb.customer1@lrcustomer.com.broken"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the ticket with ticketId "${staticRequestIdList}" should have the status "new"
	And the ticket with ticketId "${staticRequestIdList}" should have the requester with the email address "osb.customer2@lrcustomer.com.broken"
	And I click on the "Unassign" button in the dropdown menu for the contact with the email address "osb.customer2@lrcustomer.com.broken"
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then I should be able to view the error message for open zendesk tickets
	And the ticket with ticketId "${staticRequestIdList}" should have the status "new"
	And the ticket with ticketId "${staticRequestIdList}" should have the requester with the email address "osb.customer2@lrcustomer.com.broken"

Scenario: View ticket after requester removal on provisioning account with one customer
	Zendesk users should not be removed from a provisioning account if they have open tickets and are the only customer on the OSB org.

	Given I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer1@lrcustomer.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer1@lrcustomer.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And I create a request as a user with the email address "osb.customer1@lrcustomer.com.broken" with the following values
	| Product           | prd_liferay_portal_6_2                           |
	| Component         | activation_key_project_administration            |
	| System Status     | production_functioning_with_limited_capabilities |
	| Subject           | Request Subject                                  |
	| Description       | Request Description                              |
	| Organization Name | Osbzen - ${staticProjectName}                    |
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the ticket with ticketId "${staticRequestIdList}" should have the status "new"
	And the ticket with ticketId "${staticRequestIdList}" should have the requester with the email address "osb.customer1@lrcustomer.com.broken"
	And I log into the provisioning homepage as "${staticProvisioningAdminEmailAddress}" with password "${staticProvisioningAdminPassword}"
	And I go to the account index page
	And I search the accounts page using the keyword "Osbzen"
	And I click on the account with the name "Osbzen - ${staticProjectName}" on the provisioning index
	And I click on the "Contacts" tab
	And I click on the "Unassign" button in the dropdown menu for the contact with the email address "osb.customer1@lrcustomer.com.broken"
	Then I should be able to view the error message for open zendesk tickets
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	And the ticket with ticketId "${staticRequestIdList}" should have the status "new"
	And the ticket with ticketId "${staticRequestIdList}" should have the requester with the email address "osb.customer1@lrcustomer.com.broken"

Scenario: View ticket after requester removal on provisioning account with one developer and one watcher
	Zendesk users should not be removed from a provisioning account if they have open tickets and are the last remaining developer customer on the OSB org.

	Given I assign the contact with the email address "osb.customer2@lrcustomer.com.broken" on the account "Osbzen - ${staticProjectName}" to the following roles
	| Support User  |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer1@lrcustomer.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer2@lrcustomer.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer1@lrcustomer.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.customer2@lrcustomer.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And I create a request as a user with the email address "osb.customer1@lrcustomer.com.broken" with the following values
	| Product           | prd_liferay_portal_6_2                           |
	| Component         | activation_key_project_administration            |
	| System Status     | production_functioning_with_limited_capabilities |
	| Subject           | Request Subject                                  |
	| Description       | Request Description                              |
	| Organization Name | Osbzen - ${staticProjectName}                    |
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the ticket with ticketId "${staticRequestIdList}" should have the status "new"
	And the ticket with ticketId "${staticRequestIdList}" should have the requester with the email address "osb.customer1@lrcustomer.com.broken"
	And I log into the provisioning homepage as "${staticProvisioningAdminEmailAddress}" with password "${staticProvisioningAdminPassword}"
	And I go to the account index page
	And I search the accounts page using the keyword "Osbzen"
	And I click on the account with the name "Osbzen - ${staticProjectName}" on the provisioning index
	And I click on the "Contacts" tab
	And I click on the "Unassign" button in the dropdown menu for the contact with the email address "osb.customer1@lrcustomer.com.broken"
	Then I should be able to view the error message for open zendesk tickets
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	And the ticket with ticketId "${staticRequestIdList}" should have the status "new"
	And the ticket with ticketId "${staticRequestIdList}" should have the requester with the email address "osb.customer1@lrcustomer.com.broken"

Scenario: View ticket after requester role removal on provisioning account with multiple customers
	Zendesk users should not be removed from a provisioning account role if they have open tickets and are the last remaining customer on the OSB org.

	Given I assign the contact with the email address "osb.customer2@lrcustomer.com.broken" on the account "Osbzen - ${staticProjectName}" to the following roles
	| Support Requester |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer1@lrcustomer.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer2@lrcustomer.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer1@lrcustomer.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.customer2@lrcustomer.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And I create a request as a user with the email address "osb.customer1@lrcustomer.com.broken" with the following values
	| Product           | prd_liferay_portal_6_2                           |
	| Component         | activation_key_project_administration            |
	| System Status     | production_functioning_with_limited_capabilities |
	| Subject           | Request Subject                                  |
	| Description       | Request Description                              |
	| Organization Name | Osbzen - ${staticProjectName}                    |
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the ticket with ticketId "${staticRequestIdList}" should have the status "new"
	And the ticket with ticketId "${staticRequestIdList}" should have the requester with the email address "osb.customer1@lrcustomer.com.broken"
	And I log into the provisioning homepage as "${staticProvisioningAdminEmailAddress}" with password "${staticProvisioningAdminPassword}"
	And I go to the account index page
	And I search the accounts page using the keyword "Osbzen"
	And I click on the account with the name "Osbzen - ${staticProjectName}" on the provisioning index
	And I click on the "Contacts" tab
	And I click on the "Edit" button in the dropdown menu for the contact with the email address "osb.customer1@lrcustomer.com.broken"
	And I populate and save the assign contact form with the following values
	| Unassign Role | Support Requester |
	| Assign Role   | Support User      |
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the ticket with ticketId "${staticRequestIdList}" should have the status "new"
	And the ticket with ticketId "${staticRequestIdList}" should have the requester with the email address "osb.customer2@lrcustomer.com.broken"
	And I click on the "Edit" button in the dropdown menu for the contact with the email address "osb.customer2@lrcustomer.com.broken"
	And I populate and save the assign contact form with the following values
	| Unassign Role | Support Requester |
	| Assign Role   | Support User      |
	Then I should be able to view the error message for open zendesk tickets
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	And the ticket with ticketId "${staticRequestIdList}" should have the status "new"
	And the ticket with ticketId "${staticRequestIdList}" should have the requester with the email address "osb.customer2@lrcustomer.com.broken"

Scenario: View ticket after requester role removal on provisioning account with one developer and one watcher
	Zendesk users should not be removed from a provisioning account role if they have open tickets and are the last remaining developer customer on the OSB org.

	Given I assign the contact with the email address "osb.customer2@lrcustomer.com.broken" on the account "Osbzen - ${staticProjectName}" to the following roles
	| Support User  |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer1@lrcustomer.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer2@lrcustomer.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer1@lrcustomer.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And the user with the email address "osb.customer2@lrcustomer.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And I create a request as a user with the email address "osb.customer1@lrcustomer.com.broken" with the following values
	| Product           | prd_liferay_portal_6_2                           |
	| Component         | activation_key_project_administration            |
	| System Status     | production_functioning_with_limited_capabilities |
	| Subject           | Request Subject                                  |
	| Description       | Request Description                              |
	| Organization Name | Osbzen - ${staticProjectName}                    |
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the ticket with ticketId "${staticRequestIdList}" should have the status "new"
	And the ticket with ticketId "${staticRequestIdList}" should have the requester with the email address "osb.customer1@lrcustomer.com.broken"
	And I log into the provisioning homepage as "${staticProvisioningAdminEmailAddress}" with password "${staticProvisioningAdminPassword}"
	And I go to the account index page
	And I search the accounts page using the keyword "Osbzen"
	And I click on the account with the name "Osbzen - ${staticProjectName}" on the provisioning index
	And I click on the "Contacts" tab
	And I click on the "Edit" button in the dropdown menu for the contact with the email address "osb.customer1@lrcustomer.com.broken"
	And I populate and save the assign contact form with the following values
	| Unassign Role | Support Requester |
	| Assign Role   | Support User      |
	Then I should be able to view the error message for open zendesk tickets
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	And the ticket with ticketId "${staticRequestIdList}" should have the status "new"
	And the ticket with ticketId "${staticRequestIdList}" should have the requester with the email address "osb.customer1@lrcustomer.com.broken"

Scenario: View ticket creation by admin user
	Zendesk tickets should be able to be created by admin users

	Given I remove the contact with the email address "osb.customer1@lrcustomer.com.broken" on the account "Osbzen - ${staticProjectName}" from the following roles
	| Support Requester |
	And I assign the contact with the email address "osb.customer1@lrcustomer.com.broken" on the account "Osbzen - ${staticProjectName}" to the following roles
	| Support Administrator |
	And I generate static start and end dates
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Portal Production" on koroneiki with the following values
	| End Date                | ${staticEndDate}         |   |
	| Original End Date       | ${staticOriginalEndDate} |   |
	| Perpetual               | false                    |   |
	| Product Field and Value | sizing                   | 1 |
	| Product Field and Value | licenses                 | 1 |
	| Product Field and Value | version                  | 6 |
	| Quantity                | 1                        |   |
	| Start Date              | ${staticStartDate}       |   |
	| Status                  | Approved                 |   |
	And I create a product purchase for an account with the name "Osbzen - ${staticProjectName}" and the product "Gold Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I synchronize the account entitlement definition with the name "Gold Subscription"
	Then the support organization with the code "OSBZENOSBZEN" should be part of the support site
	And the organization "Osbzen - ${staticProjectName}" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer1@lrcustomer.com.broken" should be present on zendesk.liferay.com
	And the user with the email address "osb.customer1@lrcustomer.com.broken" should be in the organization "Osbzen - ${staticProjectName}"
	And I create a request as a user with the email address "osb.customer1@lrcustomer.com.broken" with the following values
	| Product           | prd_liferay_portal_6_2                           |
	| Component         | activation_key_project_administration            |
	| System Status     | production_functioning_with_limited_capabilities |
	| Subject           | Request Subject                                  |
	| Description       | Request Description                              |
	| Organization Name | Osbzen - ${staticProjectName}                    |
	# Necessary pause for tags and zendesk org sync. Analyze if can be taken out or reduced once account index improves
	And I wait for "1" minutes
	Then the ticket with ticketId "${staticRequestIdList}" should have the status "new"
	And the ticket with ticketId "${staticRequestIdList}" should have the requester with the email address "osb.customer1@lrcustomer.com.broken"