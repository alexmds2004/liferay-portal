definition {

	property testray.main.component.name = "Support QA";
	property type = "sites";

	setUp {
		Sites.getLinks();
	}

	test CustomerSite {
        property help.center.smoke = "true";

		Sites.viewSitePageLink(pageLink = ${customerURL});
	}

	test IssuesZenSite {
        property help.center.smoke = "true";

		Sites.viewSitePageLink(pageLink = ${issuesZenURL});
	}

	test KoroneikiSite {
        property help.center.smoke = "true";

		Sites.viewSitePageLink(pageLink = ${koroneikiURL});
	}

	test OktaSite {
        property help.center.smoke = "true";

		Sites.viewSitePageLink(pageLink = ${oktaURL});
	}

	test ProvisioningSite {
        property help.center.smoke = "true";

		Sites.viewSitePageLink(pageLink = ${provisioningURL});
	}

	test RabbitMQSite {
        property help.center.smoke = "true";

		Sites.viewSitePageLink(pageLink = ${rabbitmqURL});
	}

	test ZendeskSite {
        property help.center.smoke = "true";

		Sites.viewSitePageLink(pageLink = ${zendeskURL});
	}

}