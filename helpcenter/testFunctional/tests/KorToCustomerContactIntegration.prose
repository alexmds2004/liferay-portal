#@testray.main.component.name = "Support QA"
#@type = "customer.integration"

Feature:  Koroneiki to Customer Contact Integration
	Testing to assure that koroneiki contact information transfers properly to customer.

Setup:
	* I get all test passwords and admin emails
	* I generate static start and end dates
	* I generate past static start and end dates
	* I generate future static start and end dates
	* I generate a provisioning company name
	* I generate a provisioning project name
	* I create a user with the following parameters
	| Screen Name   | guicstm                       |
	| Email Address | guicstm@lrcustomer.com.broken |
	| First Name    | GUI                           |
	| Last Name     | CSTM                          |
	* I create a user with the following parameters
	| Screen Name   | cstm.customer2                       |
	| Email Address | cstm.customer2@lrcustomer.com.broken |
	| First Name    | CSTM                                 |
	| Last Name     | Customer2                            |
	* I create an account on koroneiki with the following values
	| Code   | GUIZENGUIZEN                  | |
	| Name   | Guizen - ${staticProjectName} | |
	| Region | United States                 | |
	| Status | Active                        | |
	| Tier   | T1                            | |

Teardown:
	* I tear down the teams on koroneiki with the following names
	| CSTM Customer |
	* I tear down the product consumption list on koroneiki made by the account "Korcst - ${staticProjectName}" for the following products
	| DXP Production      |
	| Service Partnership |
	* I tear down the product consumptions on koroneiki made by the account "Korcst - ${staticProjectName}" for the following products
	| DXP Production      |
	| Service Partnership |
	* I tear down the projects with the following project codes
	| KORCSTKORCST |
	| KORCT2KORCT2 |
	* I tear down the accounts on koroneiki with the following names
	| Korcst - ${staticProjectName} |
	* I tear down the accounts on koroneiki with the following "Code" values
	| KORCSTKORCST |
	| KORCT2KORCT2 |
	* I tear down the users with the following email addresses from web.liferay.com
	| cstm.customer1@lrcustomer.com.broken |
	| cstm.customer2@2rcustomer.com.broken |
	* I tear down the users with the following email addresses from customer.liferay.com
	| cstm.customer1@lrcustomer.com.broken |
	| cstm.customer2@2rcustomer.com.broken |
	* I tear down the users with the following email addresses from koroneiki
	| cstm.customer1@lrcustomer.com.broken |
	| cstm.customer2@2rcustomer.com.broken |
	* I tear down the contacts on koroneiki with the following email addresses
	| cstm.customer1@lrcustomer.com.broken |
	| cstm.customer2@2rcustomer.com.broken |
	* I tear down the users with the following email addresses from okta
	| cstm.customer1@lrcustomer.com.broken |
	| cstm.customer2@2rcustomer.com.broken |

Scenario: View Support Administrator on Customer
	Testing the ability to create and assign a Support Administrator in Koroneiki and viewing the contact in Customer accounts page

	Given I create a product purchase for an account with the name "Guizen - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I assign the contact with the email address "guicstm@lrcustomer.com.broken" on the account "Guizen - ${staticProjectName}" to the following roles
	| Support Administrator |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	And I log into the provisioning homepage as "${staticProvisioningAdminEmailAddress}" with password "${staticProvisioningAdminPassword}"
	And I go to the account index page
	And I search the accounts page using the keyword "Guizen"
	And I click on the account with the name "Guizen - ${staticProjectName}" on the provisioning index
	And I click on the Sync To Customer Portal button on the account page
	And I log into the customer homepage as "${staticCustomerAdminEmailAddress}" with password "${staticCustomerAdminPassword}"
	And I search the customer projects page using the keyword "Korcst"
	And I click on the project with the name "Korcst - ${staticProjectName}" on the customer project details page
	Then I should be able to view a support level purchase on the customer subscription page with the following values
	| Name                   | Platinum                                                      |
	| State                  | CURRENT                                                       |
	| Support Level Duration | ${staticMmDdYyyyStartDate} - ${staticMmDdYyyyOriginalEndDate} |
	And I click on the "Team Members" tab at customer portal
	Then I should be able to view the customer team member "CSTM Customer1" and the following values 
	| Name          | CSTM customer1                       |
	| Email Address | cstm.customer1@lrcustomer.com.broken |
	| Role          | Requester                            |
	| Status        | Invited                              |
	And I should be able to view the customer team member "CSTM Customer2" and the following values
	| Name          | CSTM customer2                       |
	| Email Address | cstm.customer2@lrcustomer.com.broken |
	| Role          | Requester                            |
	| Status        | Invited                              |
	
Scenario: View Support Requester on customer
	Testing the ability to create and assign a Support Requester in Koroneiki and viewing the contact in Customer accounts page

	Given I create a product purchase for an account with the name "Korcst - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I assign the contact with the email address "cstm.customer1@lrcustomer.com.broken" on the account "Korcst - ${staticProjectName}" to the following roles
	| Support Requester |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	And I log into the provisioning homepage as "${staticProvisioningAdminEmailAddress}" with password "${staticProvisioningAdminPassword}"
	And I go to the account index page
	And I search the accounts page using the keyword "Korcst"
	And I click on the account with the name "Korcst - ${staticProjectName}" on the provisioning index
	And I click on the Sync To Customer Portal button on the account page
	And I log into the customer homepage as "${staticCustomerAdminEmailAddress}" with password "${staticCustomerAdminPassword}"
	And I search the customer projects page using the keyword "Korcst"
	And I click on the project with the name "Korcst - ${staticProjectName}" on the customer project details page
	Then I should be able to view a support level purchase on the customer subscription page with the following values
	| Name                   | Platinum                                                      |
	| State                  | CURRENT                                                       |
	| Support Level Duration | ${staticMmDdYyyyStartDate} - ${staticMmDdYyyyOriginalEndDate} |
	And I click on the "Team Members" tab at customer portal
	Then I should be able to view the customer team member "CSTM Customer1" and the following values
	| Name          | CSTM customer1                       |
	| Email Address | cstm.customer1@lrcustomer.com.broken |
	| Role          | Support Requester                    |
	| User Status   | VERIFIED                             |

Scenario: View Support User on customer
	Testing the ability to create and assign a Support User in Koroneiki and viewing the contact in Customer accounts page

	Given I create a product purchase for an account with the name "Korcst - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I assign the contact with the email address "cstm.customer1@lrcustomer.com.broken" on the account "Korcst - ${staticProjectName}" to the following roles
	| Support Requester |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	And I log into the provisioning homepage as "${staticProvisioningAdminEmailAddress}" with password "${staticProvisioningAdminPassword}"
	And I go to the account index page
	And I search the accounts page using the keyword "Korcst"
	And I click on the account with the name "Korcst - ${staticProjectName}" on the provisioning index
	And I click on the Sync To Customer Portal button on the account page
	And I log into the customer homepage as "${staticCustomerAdminEmailAddress}" with password "${staticCustomerAdminPassword}"
	And I search the customer projects page using the keyword "Korcst"
	And I click on the project with the name "Korcst - ${staticProjectName}" on the customer project details page
	Then I should be able to view a support level purchase on the customer subscription page with the following values
	| Name                   | Platinum                                                      |
	| State                  | CURRENT                                                       |
	| Support Level Duration | ${staticMmDdYyyyStartDate} - ${staticMmDdYyyyOriginalEndDate} |
	And I click on the "Team Members" tab at customer portal
	Then I should be able to view the customer team member "CSTM Customer1" and the following values
	| Name          | CSTM customer1                       |
	| Email Address | cstm.customer1@lrcustomer.com.broken |
	| Role          | Support Requester                    |
	| User Status   | VERIFIED                             |

Scenario: View Partner Member on customer
	Testing the ability to create and assign a Partner Member in Koroneiki and viewing the contact in Customer accounts page

	Given I create a product purchase for an account with the name "Korcst - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I create a product purchase for an account with the name "Korcst - ${staticProjectName}" and the product "Service Partnership" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I assign the contact with the email address "cstm.customer1@lrcustomer.com.broken" on the account "Korcst - ${staticProjectName}" to the following roles
	| Partner Member |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	And I synchronize the account entitlement definition with the name "Partner"
	And I log into the provisioning homepage as "${staticProvisioningAdminEmailAddress}" with password "${staticProvisioningAdminPassword}"
	And I go to the account index page
	And I search the accounts page using the keyword "Korcst"
	And I click on the account with the name "Korcst - ${staticProjectName}" on the provisioning index
	And I click on the Sync To Customer Portal button on the account page
	And I log into the customer homepage as "${staticCustomerAdminEmailAddress}" with password "${staticCustomerAdminPassword}"
	And I search the customer projects page using the keyword "Korcst"
	And I click on the project with the name "Korcst - ${staticProjectName}" on the customer project details page
	Then I should be able to view a support level purchase on the customer subscription page with the following values
	| Name                   | Platinum                                                      |
	| Status                 | CURRENT                                                       |
	| Support Level Duration | ${staticMmDdYyyyStartDate} - ${staticMmDdYyyyOriginalEndDate} |
	And I should be able to view a "Service Partnership" subscription purchase on the customer project overview page with the following values
	| Type                   | Partnership                                                   |
	| Name                   | Service Partnership                                           |
	| Date                   | ${staticMmDdYyyyStartDate} - ${staticMmDdYyyyOriginalEndDate} |
	| Instance Size          | 0                                                             |
	| State                  | Active                                                        |
	And I click on the "Team Members" tab at customer portal
	Then I should be able to view the customer team member "CSTM Customer1" and the following values 
	| Name          | CSTM Customer1                       |
	| Email         | cstm.customer1@lrcustomer.com.broken |
	| Support Seat  | false                                |
	| Role          | Partner Member                       |
	| Status        | Invited                              |

Scenario: View Partner Manager on customer
	Testing the ability to create and assign a Partner Manager in Koroneiki and viewing the contact in Customer accounts page

	Given I create a product purchase for an account with the name "Korcst - ${staticProjectName}" and the product "Platinum Subscription" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I create a product purchase for an account with the name "Korcst - ${staticProjectName}" and the product "Service Partnership" on koroneiki with the following values
	| End Date                | ${staticEndDate}         | |
	| Original End Date       | ${staticOriginalEndDate} | |
	| Perpetual               | false                    | |
	| Quantity                | 1                        | |
	| Start Date              | ${staticStartDate}       | |
	| Status                  | Approved                 | |
	And I assign the contact with the email address "cstm.customer2@lrcustomer.com.broken" on the account "Korcst - ${staticProjectName}" to the following roles
	| Partner Manager |
	And I synchronize the account entitlement definition with the name "Platinum Subscription"
	And I synchronize the account entitlement definition with the name "Partner"
	And I log into the provisioning homepage as "${staticProvisioningAdminEmailAddress}" with password "${staticProvisioningAdminPassword}"
	And I go to the account index page
	And I search the accounts page using the keyword "Korcst"
	And I click on the account with the name "Korcst - ${staticProjectName}" on the provisioning index
	And I click on the Sync To Customer Portal button on the account page
	And I log into the customer homepage as "${staticCustomerAdminEmailAddress}" with password "${staticCustomerAdminPassword}"
	And I search the customer projects page using the keyword "Korcst"
	And I click on the project with the name "Korcst - ${staticProjectName}" on the customer project details page
	Then I should be able to view a support level purchase on the customer subscription page with the following values
	| Name                   | Platinum                                                      |
	| State                  | CURRENT                                                       |
	| Support Level Duration | ${staticMmDdYyyyStartDate} - ${staticMmDdYyyyOriginalEndDate} |
	And I should be able to view a "Service Partnership" subscription purchase on the customer project overview page with the following values
	| Type                   | Partnership                                                   |
	| Name                   | Service Partnership                                           |
	| Instance Size          | ${staticMmDdYyyyStartDate} - ${staticMmDdYyyyOriginalEndDate} |
	| Status                 | Active                                                        |
	And I click on the "Team Members" tab at customer portal
	Then I should be able to view the customer team member "CSTM Customer1" and the following values 
	| Name          | CSTM Customer1                       |
	| Email         | cstm.customer1@lrcustomer.com.broken |
	| Role          | Partner Member                       |
	| Status        | Invited                              |