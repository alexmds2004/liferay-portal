definition {

	var apiToken = PropsUtil.get("koroneiki.non.permission.api.token");
	var koroneikiURL = PropsUtil.get("koroneiki.liferay.url");
	var roleName = PropsUtil.get("koroneiki.non.permission.role.name");

	@prose = "I attempt to create an external link for a product purchase made by account ${accountName} for the product ${productName} with the following values as a non permissioned user ${table}"
	macro _createProductPurchaseExternalLinkAttempt {
		var productPurchaseKey = KoroneikiAccountAPI._getAccountProductPurchaseKeyViaAccountAndProductName(
			accountName = ${accountName},
			productName = ${productName});
		var rawTable = TableUtil.newTable(${table});

		for (var row : table ${rawTable}) {
			var field = "${row[0]}";
			var value = "${row[1]}";

			if (${field} == "Domain") {
				var domain = ${value};
			}
			else if (${field} == "Entity Id") {
				var entityId = ${value};
			}
			else if (${field} == "Entity Name") {
				var entityName = ${value};
			}
			else {
				fail("No such external link field ${field}");
			}
		}

		if (!(isSet(domain))) {
			var domain = "";
		}

		if (!(isSet(entityId))) {
			var entityId = "";
		}

		if (!(isSet(entityName))) {
			var entityName = "";
		}

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/product-purchases/${productPurchaseKey}/external-links --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
				-d
				{
					"domain": "${domain}",
					"entityId": "${entityId}",
					"entityName": "${entityName}"
				}
		''';

		com.liferay.poshi.runner.util.JSONCurlUtil.post(${curl});
	}

	@prose = "I delete a product purchase on koroneiki made by the account ${accountName} for the product ${productName} as a permissioned user"
	macro _deleteProductPurchase {
		var productPurchaseKey = KoroneikiAccountAPI._getAccountProductPurchaseKeyViaAccountAndProductName(
			accountName = ${accountName},
			productName = ${productName});

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/product-purchases/${productPurchaseKey} --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';

		com.liferay.poshi.runner.util.JSONCurlUtil.delete(${curl});
	}

	@prose = "I attempt to delete a product purchase on koroneiki made by the account ${accountName} for the product ${productName} as a non permissioned user"
	macro _deleteProductPurchaseAttempt {
		var productPurchaseKey = KoroneikiAccountAPI._getAccountProductPurchaseKeyViaAccountAndProductName(
			accountName = ${accountName},
			productName = ${productName});

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/product-purchases/${productPurchaseKey} --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';

		com.liferay.poshi.runner.util.JSONCurlUtil.delete(${curl});
	}

	@prose = "I attempt to delete an external link with the entity id ${entityId} on a product purchase made by account ${accountName} for the product ${productName} as a non permissioned user"
	macro _deleteProductPurchaseExternalLinkAttempt {
		var externalLinkKey = KoroneikiProductPurchaseAPI._getProductPurchaseExternalLinkKey(
			accountName = ${accountName},
			entityId = ${entityId},
			productName = ${productName});

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/external-links/${externalLinkKey} --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';

		com.liferay.poshi.runner.util.JSONCurlUtil.delete(${curl});
	}

	@prose = "I set the permissions for the product purchase made by the account ${accountName} for the product ${productName} to have the following values for a non permissioned user as a permissioned user ${table}"
	macro _setProductPurchasePermissions {
		var productPurchaseKey = KoroneikiAccountAPI._getAccountProductPurchaseKeyViaAccountAndProductName(
			accountName = ${accountName},
			productName = ${productName});
		var rawTable = TableUtil.newTable(${table});

		for (var row : table ${rawTable}) {
			var field = "${row[0]}";
			var value = "${row[1]}";

			if (${field} == "Delete") {
				var value = StringUtil.lowerCase(${value});

				var delete = ${value};
			}
			else if (${field} == "Permissions") {
				var value = StringUtil.lowerCase(${value});

				var permissions = ${value};
			}
			else if (${field} == "Update") {
				var value = StringUtil.lowerCase(${value});

				var update = ${value};
			}
			else if (${field} == "View") {
				var value = StringUtil.lowerCase(${value});

				var view = ${value};
			}
			else {
				fail("No such permission field ${field}");
			}
		}

		if (!(isSet(delete))) {
			var delete = "false";
		}

		if (!(isSet(permissions))) {
			var permissions = "false";
		}

		if (!(isSet(update))) {
			var update = "false";
		}

		if (!(isSet(view))) {
			var view = "false";
		}

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/product-purchases/${productPurchaseKey}/product-purchase-permissions --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
				-d
				{
					"delete": "${delete}",
					"permissions": "${permissions}",
					"roleNames": [
						"${roleName}"
					],
					"update": "${update}",
					"view": "${view}"
				}
		''';

		com.liferay.poshi.runner.util.JSONCurlUtil.put(${curl});
	}

	@prose = "I attempt to set the permissions for the product purchase made by the account ${accountName} for the product ${productName} to have the following values for a non permissioned user as a non permissioned user ${table}"
	macro _setProductPurchasePermissionsAttempt {
		var productPurchaseKey = KoroneikiAccountAPI._getAccountProductPurchaseKeyViaAccountAndProductName(
			accountName = ${accountName},
			productName = ${productName});
		var rawTable = TableUtil.newTable(${table});

		for (var row : table ${rawTable}) {
			var field = "${row[0]}";
			var value = "${row[1]}";

			if (${field} == "Delete") {
				var value = StringUtil.lowerCase(${value});

				var delete = ${value};
			}
			else if (${field} == "Permissions") {
				var value = StringUtil.lowerCase(${value});

				var permissions = ${value};
			}
			else if (${field} == "Update") {
				var value = StringUtil.lowerCase(${value});

				var update = ${value};
			}
			else if (${field} == "View") {
				var value = StringUtil.lowerCase(${value});

				var view = ${value};
			}
			else {
				fail("No such permission field ${field}");
			}
		}

		if (!(isSet(delete))) {
			var delete = "false";
		}

		if (!(isSet(permissions))) {
			var permissions = "false";
		}

		if (!(isSet(update))) {
			var update = "false";
		}

		if (!(isSet(view))) {
			var view = "false";
		}

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/product-purchases/${productPurchaseKey}/product-purchase-permissions --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
				-d
				{
					"delete": "${delete}",
					"permissions": "${permissions}",
					"roleNames": [
						"${roleName}"
					],
					"update": "${update}",
					"view": "${view}"
				}
		''';

		com.liferay.poshi.runner.util.JSONCurlUtil.put(${curl});
	}

	@prose = "I unset the following permissions for the product purchase made by the account ${accountName} for the product ${productName} for a permissioned user as a permissioned user ${table}"
	macro _unsetProductPurchasePermissions {
		var productPurchaseKey = KoroneikiAccountAPI._getAccountProductPurchaseKeyViaAccountAndProductName(
			accountName = ${accountName},
			productName = ${productName});
		var rawTable = TableUtil.newTable(${table});

		for (var row : table ${rawTable}) {
			var field = "${row[0]}";
			var value = "${row[1]}";

			if (${field} == "Delete") {
				var value = StringUtil.lowerCase(${value});

				var delete = ${value};
			}
			else if (${field} == "Permissions") {
				var value = StringUtil.lowerCase(${value});

				var permissions = ${value};
			}
			else if (${field} == "Update") {
				var value = StringUtil.lowerCase(${value});

				var update = ${value};
			}
			else if (${field} == "View") {
				var value = StringUtil.lowerCase(${value});

				var view = ${value};
			}
			else {
				fail("No such permission field ${field}");
			}
		}

		if (!(isSet(delete))) {
			var delete = "false";
		}

		if (!(isSet(permissions))) {
			var permissions = "false";
		}

		if (!(isSet(update))) {
			var update = "false";
		}

		if (!(isSet(view))) {
			var view = "false";
		}

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/product-purchases/${productPurchaseKey}/product-purchase-permissions --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
				-d
				{
					"delete": "${delete}",
					"permissions": "${permissions}",
					"roleNames": [
						"${roleName}"
					],
					"update": "${update}",
					"view": "${view}"
				}
		''';

		com.liferay.poshi.runner.util.JSONCurlUtil.delete(${curl});
	}

	@prose = "I attempt to unset the following permissions for the product purchase made by the account ${accountName} for the product ${productName} for a permissioned user as a non permissioned user ${table}"
	macro _unsetProductPurchasePermissionsAttempt {
		var productPurchaseKey = KoroneikiAccountAPI._getAccountProductPurchaseKeyViaAccountAndProductName(
			accountName = ${accountName},
			productName = ${productName});
		var rawTable = TableUtil.newTable(${table});

		for (var row : table ${rawTable}) {
			var field = "${row[0]}";
			var value = "${row[1]}";

			if (${field} == "Delete") {
				var value = StringUtil.lowerCase(${value});

				var delete = ${value};
			}
			else if (${field} == "Permissions") {
				var value = StringUtil.lowerCase(${value});

				var permissions = ${value};
			}
			else if (${field} == "Update") {
				var value = StringUtil.lowerCase(${value});

				var update = ${value};
			}
			else if (${field} == "View") {
				var value = StringUtil.lowerCase(${value});

				var view = ${value};
			}
			else {
				fail("No such permission field ${field}");
			}
		}

		if (!(isSet(delete))) {
			var delete = "false";
		}

		if (!(isSet(permissions))) {
			var permissions = "false";
		}

		if (!(isSet(update))) {
			var update = "false";
		}

		if (!(isSet(view))) {
			var view = "false";
		}

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/product-purchases/${productPurchaseKey}/product-purchase-permissions --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
				-d
				{
					"delete": "${delete}",
					"permissions": "${permissions}",
					"roleNames": [
						"${roleName}"
					],
					"update": "${update}",
					"view": "${view}"
				}
		''';

		com.liferay.poshi.runner.util.JSONCurlUtil.delete(${curl});
	}

	@prose = "I update a product purchase on koroneiki made by the account ${accountName} for the product ${productName} to have the following values as a permissioned user ${table}"
	macro _updateProductPurchase {
		var productPurchaseKey = KoroneikiAccountAPI._getAccountProductPurchaseKeyViaAccountAndProductName(
			accountName = ${accountName},
			productName = ${productName});
		var properties = "";
		var rawTable = TableUtil.newTable(${table});

		for (var row : table ${rawTable}) {
			var field = "${row[0]}";
			var value1 = "${row[1]}";
			var value2 = "${row[2]}";

			if (${field} == "End Date") {
				var endDate = ${value1};
			}
			else if (${field} == "Original End Date") {
				var originalEndDate = ${value1};
			}
			else if (${field} == "Perpetual") {
				var value1 = StringUtil.lowerCase(${value1});

				var value1 = StringUtil.replace(${value1}, " ", "-");

				var perpetual = ${value1};
			}
			else if (${field} == "Product Name") {
				var productKey = KoroneikiProductAPI._getProductKeyViaProductName(productName = ${value1});
			}
			else if (${field} == "Product Field and Value") {
				if (${properties} == "") {
					var properties = "\"${value1}\": \"${value2}\"";
				}
				else {
					var properties = "${properties},\"${value1}\": \"${value2}\"";
				}
			}
			else if (${field} == "Quantity") {
				var quantity = ${value1};
			}
			else if (${field} == "Start Date") {
				var startDate = ${value1};
			}
			else if (${field} == "Status") {
				var status = ${value1};
			}
			else {
				fail("No such product purchase field ${field}");
			}
		}

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/product-purchases/${productPurchaseKey} --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';

		if (!(isSet(endDate))) {
			var productPurchaseObject = JSONCurlUtil.get(${curl}, "$");

			if (contains(${productPurchaseObject}, "endDate=")) {
				var value = JSONCurlUtil.get(${curl}, "$.endDate");

				var endDate = ${value};
			}
			else {
				var endDate = "";
			}
		}

		if (!(isSet(originalEndDate))) {
			var productPurchaseObject = JSONCurlUtil.get(${curl}, "$");

			if (contains(${productPurchaseObject}, "originalEndDate=")) {
				var value = JSONCurlUtil.get(${curl}, "$.originalEndDate");

				var originalEndDate = ${value};
			}
			else {
				var originalEndDate = "";
			}
		}

		if (!(isSet(perpetual))) {
			var productPurchaseObject = JSONCurlUtil.get(${curl}, "$");

			if (contains(${productPurchaseObject}, "perpetual=")) {
				var value = JSONCurlUtil.get(${curl}, "$.perpetual");

				var perpetual = ${value};
			}
			else {
				var perpetual = "true";
			}
		}

		if (!(isSet(productKey))) {
			var productPurchaseObject = JSONCurlUtil.get(${curl}, "$");

			if (contains(${productPurchaseObject}, "productKey=")) {
				var value = JSONCurlUtil.get(${curl}, "$.productKey");

				var productKey = ${value};
			}
			else {
				var productKey = "";
			}
		}

		if (!(isSet(properties))) {
			var productPurchaseObject = JSONCurlUtil.get(${curl}, "$");

			if (contains(${productPurchaseObject}, "properties=")) {
				var value = JSONCurlUtil.get(${curl}, "$.properties");

				var properties = ${value};
			}
			else {
				var properties = "";
			}
		}

		if (!(isSet(quantity))) {
			var productPurchaseObject = JSONCurlUtil.get(${curl}, "$");

			if (contains(${productPurchaseObject}, "quantity=")) {
				var value = JSONCurlUtil.get(${curl}, "$.quantity");

				var quantity = ${value};
			}
			else {
				var quantity = 1;
			}
		}

		if (!(isSet(startDate))) {
			var productPurchaseObject = JSONCurlUtil.get(${curl}, "$");

			if (contains(${productPurchaseObject}, "startDate=")) {
				var value = JSONCurlUtil.get(${curl}, "$.startDate");

				var startDate = ${value};
			}
			else {
				var startDate = "";
			}
		}

		if (!(isSet(status))) {
			var productPurchaseObject = JSONCurlUtil.get(${curl}, "$");

			if (contains(${productPurchaseObject}, "status=")) {
				var value = JSONCurlUtil.get(${curl}, "$.status");

				var status = ${value};
			}
			else {
				var status = "";
			}
		}

		if (${properties} == "") {
			var curl = '''
				${koroneikiURL}/o/koroneiki-rest/v1.0/product-purchases/${productPurchaseKey} --tlsv1.2
					--header "API_Token: ${apiToken}"
					-H "accept: application/json"
					-H "Content-Type: application/json"
					-d
					{
						"endDate": "${endDate}",
						"originalEndDate": "${originalEndDate}",
						"perpetual": ${perpetual},
						"productKey": "${productKey}",
						"quantity": ${quantity},
						"startDate": "${startDate}",
						"status": "${status}"
					}
			''';
		}
		else {
			var curl = '''
				${koroneikiURL}/o/koroneiki-rest/v1.0/product-purchases/${productPurchaseKey} --tlsv1.2
					--header "API_Token: ${apiToken}"
					-H "accept: application/json"
					-H "Content-Type: application/json"
					-d
					{
						"endDate": "${endDate}",
						"originalEndDate": "${originalEndDate}",
						"perpetual": ${perpetual},
						"productKey": "${productKey}",
						"properties": {
							${properties}
						},
						"quantity": ${quantity},
						"startDate": "${startDate}",
						"status": "${status}"
					}
			''';
		}

		com.liferay.poshi.runner.util.JSONCurlUtil.put(${curl});
	}

	@prose = "I attempt to update a product purchase on koroneiki made by the account ${accountName} for the product ${productName} to have the following values as a non permissioned user ${table}"
	macro _updateProductPurchaseAttempt {
		var productPurchaseKey = KoroneikiAccountAPI._getAccountProductPurchaseKeyViaAccountAndProductName(
			accountName = ${accountName},
			productName = ${productName});
		var properties = "";
		var rawTable = TableUtil.newTable(${table});

		for (var row : table ${rawTable}) {
			var field = "${row[0]}";
			var value1 = "${row[1]}";
			var value2 = "${row[2]}";

			if (${field} == "End Date") {
				var endDate = ${value1};
			}
			else if (${field} == "Original End Date") {
				var originalEndDate = ${value1};
			}
			else if (${field} == "Perpetual") {
				var value1 = StringUtil.lowerCase(${value1});

				var value1 = StringUtil.replace(${value1}, " ", "-");

				var perpetual = ${value1};
			}
			else if (${field} == "Product Name") {
				var productKey = KoroneikiProductAPI._getProductKeyViaProductName(productName = ${value1});
			}
			else if (${field} == "Product Field and Value") {
				if (${properties} == "") {
					var properties = "\"${value1}\": \"${value2}\"";
				}
				else {
					var properties = "${properties},\"${value1}\": \"${value2}\"";
				}
			}
			else if (${field} == "Quantity") {
				var quantity = ${value1};
			}
			else if (${field} == "Start Date") {
				var startDate = ${value1};
			}
			else if (${field} == "Status") {
				var status = ${value1};
			}
			else {
				fail("No such product purchase field ${field}");
			}
		}

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/product-purchases/${productPurchaseKey} --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';

		if (!(isSet(endDate))) {
			var productPurchaseObject = JSONCurlUtil.get(${curl}, "$");

			if (contains(${productPurchaseObject}, "endDate=")) {
				var value = JSONCurlUtil.get(${curl}, "$.endDate");

				var endDate = ${value};
			}
			else {
				var endDate = "";
			}
		}

		if (!(isSet(originalEndDate))) {
			var productPurchaseObject = JSONCurlUtil.get(${curl}, "$");

			if (contains(${productPurchaseObject}, "originalEndDate=")) {
				var value = JSONCurlUtil.get(${curl}, "$.originalEndDate");

				var originalEndDate = ${value};
			}
			else {
				var originalEndDate = "";
			}
		}

		if (!(isSet(perpetual))) {
			var productPurchaseObject = JSONCurlUtil.get(${curl}, "$");

			if (contains(${productPurchaseObject}, "perpetual=")) {
				var value = JSONCurlUtil.get(${curl}, "$.perpetual");

				var perpetual = ${value};
			}
			else {
				var perpetual = "true";
			}
		}

		if (!(isSet(productKey))) {
			var productPurchaseObject = JSONCurlUtil.get(${curl}, "$");

			if (contains(${productPurchaseObject}, "productKey=")) {
				var value = JSONCurlUtil.get(${curl}, "$.productKey");

				var productKey = ${value};
			}
			else {
				var productKey = "";
			}
		}

		if (!(isSet(properties))) {
			var productPurchaseObject = JSONCurlUtil.get(${curl}, "$");

			if (contains(${productPurchaseObject}, "properties=")) {
				var value = JSONCurlUtil.get(${curl}, "$.properties");

				var properties = ${value};
			}
			else {
				var properties = "";
			}
		}

		if (!(isSet(quantity))) {
			var productPurchaseObject = JSONCurlUtil.get(${curl}, "$");

			if (contains(${productPurchaseObject}, "quantity=")) {
				var value = JSONCurlUtil.get(${curl}, "$.quantity");

				var quantity = ${value};
			}
			else {
				var quantity = 1;
			}
		}

		if (!(isSet(startDate))) {
			var productPurchaseObject = JSONCurlUtil.get(${curl}, "$");

			if (contains(${productPurchaseObject}, "startDate=")) {
				var value = JSONCurlUtil.get(${curl}, "$.startDate");

				var startDate = ${value};
			}
			else {
				var startDate = "";
			}
		}

		if (!(isSet(status))) {
			var productPurchaseObject = JSONCurlUtil.get(${curl}, "$");

			if (contains(${productPurchaseObject}, "status=")) {
				var value = JSONCurlUtil.get(${curl}, "$.status");

				var status = ${value};
			}
			else {
				var status = "";
			}
		}

		if (${properties} == "") {
			var curl = '''
				${koroneikiURL}/o/koroneiki-rest/v1.0/product-purchases/${productPurchaseKey} --tlsv1.2
					--header "API_Token: ${apiToken}"
					-H "accept: application/json"
					-H "Content-Type: application/json"
					-d
					{
						"endDate": "${endDate}",
						"originalEndDate": "${originalEndDate}",
						"perpetual": ${perpetual},
						"productKey": "${productKey}",
						"quantity": ${quantity},
						"startDate": "${startDate}",
						"status": "${status}"
					}
			''';
		}
		else {
			var curl = '''
				${koroneikiURL}/o/koroneiki-rest/v1.0/product-purchases/${productPurchaseKey} --tlsv1.2
					--header "API_Token: ${apiToken}"
					-H "accept: application/json"
					-H "Content-Type: application/json"
					-d
					{
						"endDate": "${endDate}",
						"originalEndDate": "${originalEndDate}",
						"perpetual": ${perpetual},
						"productKey": "${productKey}",
						"properties": {
							${properties}
						},
						"quantity": ${quantity},
						"startDate": "${startDate}",
						"status": "${status}"
					}
			''';
		}

		com.liferay.poshi.runner.util.JSONCurlUtil.put(${curl});
	}

	@prose = "I should not be able to view a product purchase on koroneiki made by the account ${accountName} for the product ${productName} as a non permissioned user"
	macro _viewNoProductPurchase {
		var accountKey = KoroneikiAccountAPI._getAccountKeyViaAccountName(accountName = ${accountName});
		var productKey = KoroneikiProductAPI._getProductKeyViaProductName(productName = ${productName});
		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/accounts/${accountKey}/product-purchases --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';

		var allAccountProductPurchases = JSONCurlUtil.get(${curl}, "$");

		if (contains(${allAccountProductPurchases}, ${productKey})) {
			fail("The product purchase made by ${accountName} for the product ${productName} is visible to non permissioned users");
		}

		var productPurchaseKey = KoroneikiAccountAPI._getAccountProductPurchaseKeyViaAccountAndProductName(
			accountName = ${accountName},
			productName = ${productName});

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/product-purchases/${productPurchaseKey} --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';

		var accountProductPurchase = JSONCurlUtil.get(${curl}, "$");

		if (contains(${accountProductPurchase}, ${productKey})) {
			fail("The product purchase made by ${accountName} for the product ${productName} is visible to non permissioned users");
		}
	}

	@prose = "the external link with the entity id ${entityId} on the product purchase made by account ${accountName} for the product ${productName} should not be visible to non permissioned users"
	macro _viewNoProductPurchaseExternalLink {
		var productPurchaseKey = KoroneikiAccountAPI._getAccountProductPurchaseKeyViaAccountAndProductName(
			accountName = ${accountName},
			productName = ${productName});

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/product-purchases/${productPurchaseKey}/external-links --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';

		var allProductPurchaseExternalLinks = JSONCurlUtil.get(${curl}, "$");

		if (contains(${allProductPurchaseExternalLinks}, ${entityId})) {
			fail("The external link with the entity id ${entityId} on the product purchase made by the account ${accountName} for the product ${productName} is visible to non permissioned users");
		}

		var externalLinkKey = KoroneikiProductPurchaseAPI._getProductPurchaseExternalLinkKey(
			accountName = ${accountName},
			entityId = ${entityId},
			productName = ${productName});

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/external-links/${externalLinkKey} --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';

		var productPurchaseExternalLink = JSONCurlUtil.get(${curl}, "$");

		if (contains(${productPurchaseExternalLink}, ${entityId})) {
			fail("The external link with the entity id ${entityId} on the product purchase made by the account ${accountName} for the product ${productName} is visible to non permissioned users");
		}
	}

	@prose = "I should not be able to view a product purchase on koroneiki made by the account ${accountName} for the product ${productName} using the external link domain ${domain} and entity name ${entityName} and entity id ${entityId} as a non permissioned user"
	macro _viewNoProductPurchaseViaExternalLink {
		var accountKey = KoroneikiAccountAPI._getAccountKeyViaAccountName(accountName = ${accountName});
		var productKey = KoroneikiProductAPI._getProductKeyViaProductName(productName = ${productName});
		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/product-purchases/by-external-link/${domain}/${entityName}/${entityId} --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';

		var accountProductPurchase = JSONCurlUtil.get(${curl}, "$");

		if (contains(${accountProductPurchase}, ${accountKey}) || contains(${accountProductPurchase}, ${productKey})) {
			fail("The product purchase made by ${accountName} for the product ${productName} is visible to non permissioned users");
		}
	}

	@prose = "I should be able to view a product purchase on koroneiki made by the account ${accountName} for the product ${productName} with the following values as a permissioned user ${table}"
	macro _viewProductPurchase {
		var productPurchaseKey = KoroneikiAccountAPI._getAccountProductPurchaseKeyViaAccountAndProductName(
			accountName = ${accountName},
			productName = ${productName});

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/product-purchases/${productPurchaseKey} --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';
		var rawTable = TableUtil.newTable(${table});

		for (var row : table ${rawTable}) {
			var field = "${row[0]}";
			var value1 = "${row[1]}";
			var value2 = "${row[2]}";

			if (${field} == "End Date") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.endDate");
			}
			else if (${field} == "Original End Date") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.originalEndDate");
			}
			else if (${field} == "Perpetual") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.perpetual");
			}
			else if (${field} == "Product Field and Value") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.properties.${value1}");
			}
			else if (${field} == "Quantity") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.quantity");
			}
			else if (${field} == "Start Date") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.startDate");
			}
			else if (${field} == "Status") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.status");
			}
			else {
				fail("No such product purchase field ${field}");
			}

			if (${field} == "Product Field and Value") {
				echo("Expected property field ${value1} value: ${value2}");

				echo("Actual property field ${value1} value: ${valueFromAPI}");

				if (!(contains(${value2}, ${valueFromAPI}))) {
					fail("Actual property field ${value1} value ${valueFromAPI} does not match expected property field ${value1} value ${value2}");
				}
			}
			else {
				echo("Expected ${field} Value: ${value1}");

				echo("Actual ${field} Value: ${valueFromAPI}");

				if (!(contains(${value1}, ${valueFromAPI}))) {
					fail("Actual ${field} value ${valueFromAPI} does not match expected ${field} value ${value1}");
				}
			}
		}
	}

}