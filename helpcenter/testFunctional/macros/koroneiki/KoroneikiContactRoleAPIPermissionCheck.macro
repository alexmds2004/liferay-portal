definition {

	var apiToken = PropsUtil.get("koroneiki.non.permission.api.token");
	var koroneikiURL = PropsUtil.get("koroneiki.liferay.url");
	var roleName = PropsUtil.get("koroneiki.non.permission.role.name");

	@prose = "I attempt to create a contact role on koroneiki with the following values as a non permissioned user ${table}"
	macro _createContactRoleAttempt {
		var rawTable = TableUtil.newTable(${table});

		for (var row : table ${rawTable}) {
			var field = "${row[0]}";
			var value = "${row[1]}";

			if (${field} == "Description") {
				var description = ${value};
			}
			else if (${field} == "Name") {
				var name = ${value};
			}
			else if (${field} == "Type") {
				var type = ${value};
			}
			else {
				fail("No such contact role field ${field}");
			}
		}

		if (!(isSet(description))) {
			var description = "";
		}

		if (!(isSet(name))) {
			var name = "";
		}

		if (!(isSet(type))) {
			var type = "";
		}

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/contact-roles --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
				-d
				{
					"description": "${description}",
					"name": "${name}",
					"type": "${type}"
				}
		''';

		com.liferay.poshi.runner.util.JSONCurlUtil.post(${curl});
	}

	@prose = "I delete a contact role on koroneiki with the name ${contactRoleName} as a permissioned user"
	macro _deleteContactRole {
		var contactRoleKey = KoroneikiContactRoleAPI._getContactRoleKeyViaContactRoleName(contactRoleName = ${contactRoleName});

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/contact-roles/${contactRoleKey} --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';

		com.liferay.poshi.runner.util.JSONCurlUtil.delete(${curl});
	}

	@prose = "I attempt to delete a contact role on koroneiki with the name ${contactRoleName} as a non permissioned user"
	macro _deleteContactRoleAttempt {
		var contactRoleKey = KoroneikiContactRoleAPI._getContactRoleKeyViaContactRoleName(contactRoleName = ${contactRoleName});

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/contact-roles/${contactRoleKey} --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';

		com.liferay.poshi.runner.util.JSONCurlUtil.delete(${curl});
	}

	@prose = "I set the permissions for the contact role ${contactRoleName} to have the following values for a non permissioned user as a permissioned user ${table}"
	macro _setContactRolePermissions {
		var contactRoleKey = KoroneikiContactRoleAPI._getContactRoleKeyViaContactRoleName(contactRoleName = ${contactRoleName});
		var rawTable = TableUtil.newTable(${table});

		for (var row : table ${rawTable}) {
			var field = "${row[0]}";
			var value = "${row[1]}";

			if (${field} == "Assign Contact") {
				var value = StringUtil.lowerCase(${value});

				var assignContact = ${value};
			}
			else if (${field} == "Delete") {
				var value = StringUtil.lowerCase(${value});

				var delete = ${value};
			}
			else if (${field} == "Permissions") {
				var value = StringUtil.lowerCase(${value});

				var permissions = ${value};
			}
			else if (${field} == "Update") {
				var value = StringUtil.lowerCase(${value});

				var update = ${value};
			}
			else if (${field} == "View") {
				var value = StringUtil.lowerCase(${value});

				var view = ${value};
			}
			else {
				fail("No such permission field ${field}");
			}
		}

		if (!(isSet(assignContact))) {
			var assignContact = "false";
		}

		if (!(isSet(delete))) {
			var delete = "false";
		}

		if (!(isSet(permissions))) {
			var permissions = "false";
		}

		if (!(isSet(update))) {
			var update = "false";
		}

		if (!(isSet(view))) {
			var view = "false";
		}

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/contact-roles/${contactRoleKey}/contact-role-permissions --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
				-d
				{
					"assignContact": "${assignContact}",
					"delete": "${delete}",
					"permissions": "${permissions}",
					"roleNames": [
						"${roleName}"
					],
					"update": "${update}",
					"view": "${view}"
				}
		''';

		com.liferay.poshi.runner.util.JSONCurlUtil.put(${curl});
	}

	@prose = "I attempt to set the permissions for the contact role ${contactRoleName} to have the following values for a non permissioned user as a non permissioned user ${table}"
	macro _setContactRolePermissionsAttempt {
		var contactRoleKey = KoroneikiContactRoleAPI._getContactRoleKeyViaContactRoleName(contactRoleName = ${contactRoleName});
		var rawTable = TableUtil.newTable(${table});

		for (var row : table ${rawTable}) {
			var field = "${row[0]}";
			var value = "${row[1]}";

			if (${field} == "Assign Contact") {
				var value = StringUtil.lowerCase(${value});

				var assignContact = ${value};
			}
			else if (${field} == "Delete") {
				var value = StringUtil.lowerCase(${value});

				var delete = ${value};
			}
			else if (${field} == "Permissions") {
				var value = StringUtil.lowerCase(${value});

				var permissions = ${value};
			}
			else if (${field} == "Update") {
				var value = StringUtil.lowerCase(${value});

				var update = ${value};
			}
			else if (${field} == "View") {
				var value = StringUtil.lowerCase(${value});

				var view = ${value};
			}
			else {
				fail("No such permission field ${field}");
			}
		}

		if (!(isSet(assignContact))) {
			var assignContact = "false";
		}

		if (!(isSet(delete))) {
			var delete = "false";
		}

		if (!(isSet(permissions))) {
			var permissions = "false";
		}

		if (!(isSet(update))) {
			var update = "false";
		}

		if (!(isSet(view))) {
			var view = "false";
		}

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/contact-roles/${contactRoleKey}/contact-role-permissions --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
				-d
				{
					"assignContact": "${assignContact}",
					"delete": "${delete}",
					"permissions": "${permissions}",
					"roleNames": [
						"${roleName}"
					],
					"update": "${update}",
					"view": "${view}"
				}
		''';

		com.liferay.poshi.runner.util.JSONCurlUtil.put(${curl});
	}

	@prose = "I unset the following permissions for the contact role ${contactRoleName} for a permissioned user as a permissioned user ${table}"
	macro _unsetContactRolePermissions {
		var contactRoleKey = KoroneikiContactRoleAPI._getContactRoleKeyViaContactRoleName(contactRoleName = ${contactRoleName});
		var rawTable = TableUtil.newTable(${table});

		for (var row : table ${rawTable}) {
			var field = "${row[0]}";
			var value = "${row[1]}";

			if (${field} == "Assign Contact") {
				var value = StringUtil.lowerCase(${value});

				var assignContact = ${value};
			}
			else if (${field} == "Delete") {
				var value = StringUtil.lowerCase(${value});

				var delete = ${value};
			}
			else if (${field} == "Permissions") {
				var value = StringUtil.lowerCase(${value});

				var permissions = ${value};
			}
			else if (${field} == "Update") {
				var value = StringUtil.lowerCase(${value});

				var update = ${value};
			}
			else if (${field} == "View") {
				var value = StringUtil.lowerCase(${value});

				var view = ${value};
			}
			else {
				fail("No such permission field ${field}");
			}
		}

		if (!(isSet(assignContact))) {
			var assignContact = "false";
		}

		if (!(isSet(delete))) {
			var delete = "false";
		}

		if (!(isSet(permissions))) {
			var permissions = "false";
		}

		if (!(isSet(update))) {
			var update = "false";
		}

		if (!(isSet(view))) {
			var view = "false";
		}

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/contact-roles/${contactRoleKey}/contact-role-permissions --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
				-d
				{
					"assignContact": "${assignContact}",
					"delete": "${delete}",
					"permissions": "${permissions}",
					"roleNames": [
						"${roleName}"
					],
					"update": "${update}",
					"view": "${view}"
				}
		''';

		com.liferay.poshi.runner.util.JSONCurlUtil.delete(${curl});
	}

	@prose = "I attempt to unset the following permissions for the contact role ${contactRoleName} for a permissioned user as a non permissioned user ${table}"
	macro _unsetContactRolePermissionsAttempt {
		var contactRoleKey = KoroneikiContactRoleAPI._getContactRoleKeyViaContactRoleName(contactRoleName = ${contactRoleName});
		var rawTable = TableUtil.newTable(${table});

		for (var row : table ${rawTable}) {
			var field = "${row[0]}";
			var value = "${row[1]}";

			if (${field} == "Assign Contact") {
				var value = StringUtil.lowerCase(${value});

				var assignContact = ${value};
			}
			else if (${field} == "Delete") {
				var value = StringUtil.lowerCase(${value});

				var delete = ${value};
			}
			else if (${field} == "Permissions") {
				var value = StringUtil.lowerCase(${value});

				var permissions = ${value};
			}
			else if (${field} == "Update") {
				var value = StringUtil.lowerCase(${value});

				var update = ${value};
			}
			else if (${field} == "View") {
				var value = StringUtil.lowerCase(${value});

				var view = ${value};
			}
			else {
				fail("No such permission field ${field}");
			}
		}

		if (!(isSet(assignContact))) {
			var assignContact = "false";
		}

		if (!(isSet(delete))) {
			var delete = "false";
		}

		if (!(isSet(permissions))) {
			var permissions = "false";
		}

		if (!(isSet(update))) {
			var update = "false";
		}

		if (!(isSet(view))) {
			var view = "false";
		}

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/contact-roles/${contactRoleKey}/contact-role-permissions --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
				-d
				{
					"assignContact": "${assignContact}",
					"delete": "${delete}",
					"permissions": "${permissions}",
					"roleNames": [
						"${roleName}"
					],
					"update": "${update}",
					"view": "${view}"
				}
		''';

		com.liferay.poshi.runner.util.JSONCurlUtil.delete(${curl});
	}

	@prose = "I update a koroneiki contact role with the name ${contactRoleName} to have the following values as a permissioned user ${table}"
	macro _updateContactRole {
		var contactRoleKey = KoroneikiContactRoleAPI._getContactRoleKeyViaContactRoleName(contactRoleName = ${contactRoleName});
		var rawTable = TableUtil.newTable(${table});

		for (var row : table ${rawTable}) {
			var field = "${row[0]}";
			var value = "${row[1]}";

			if (${field} == "Description") {
				var description = ${value};
			}
			else if (${field} == "Name") {
				var name = ${value};
			}
			else if (${field} == "Type") {
				var type = ${value};
			}
			else {
				fail("No such contact role field ${field}");
			}
		}

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/contact-roles/${contactRoleKey} --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';

		if (!(isSet(description))) {
			var contactRoleObject = JSONCurlUtil.get(${curl}, "$");

			if (contains(${contactRoleObject}, "description=")) {
				var value = JSONCurlUtil.get(${curl}, "$.description");

				var description = ${value};
			}
			else {
				var description = "";
			}
		}

		if (!(isSet(name))) {
			var contactRoleObject = JSONCurlUtil.get(${curl}, "$");

			if (contains(${contactRoleObject}, "name=")) {
				var value = JSONCurlUtil.get(${curl}, "$.name");

				var name = ${value};
			}
			else {
				var name = "";
			}
		}

		if (!(isSet(type))) {
			var contactRoleObject = JSONCurlUtil.get(${curl}, "$");

			if (contains(${contactRoleObject}, "type=")) {
				var value = JSONCurlUtil.get(${curl}, "$.type");

				var type = ${value};
			}
			else {
				var type = "";
			}
		}

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/contact-roles/${contactRoleKey} --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
				-d
				{
					"description": "${description}",
					"name": "${name}",
					"type": "${type}"
				}
		''';

		com.liferay.poshi.runner.util.JSONCurlUtil.put(${curl});
	}

	@prose = "I attempt to update a koroneiki contact role with the name ${contactRoleName} to have the following values as a non permissioned user ${table}"
	macro _updateContactRoleAttempt {
		var contactRoleKey = KoroneikiContactRoleAPI._getContactRoleKeyViaContactRoleName(contactRoleName = ${contactRoleName});
		var rawTable = TableUtil.newTable(${table});

		for (var row : table ${rawTable}) {
			var field = "${row[0]}";
			var value = "${row[1]}";

			if (${field} == "Description") {
				var description = ${value};
			}
			else if (${field} == "Name") {
				var name = ${value};
			}
			else if (${field} == "Type") {
				var type = ${value};
			}
			else {
				fail("No such contact role field ${field}");
			}
		}

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/contact-roles/${contactRoleKey} --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';

		if (!(isSet(description))) {
			var contactRoleObject = JSONCurlUtil.get(${curl}, "$");

			if (contains(${contactRoleObject}, "description=")) {
				var value = JSONCurlUtil.get(${curl}, "$.description");

				var description = ${value};
			}
			else {
				var description = "";
			}
		}

		if (!(isSet(name))) {
			var contactRoleObject = JSONCurlUtil.get(${curl}, "$");

			if (contains(${contactRoleObject}, "name=")) {
				var value = JSONCurlUtil.get(${curl}, "$.name");

				var name = ${value};
			}
			else {
				var name = "";
			}
		}

		if (!(isSet(type))) {
			var contactRoleObject = JSONCurlUtil.get(${curl}, "$");

			if (contains(${contactRoleObject}, "type=")) {
				var value = JSONCurlUtil.get(${curl}, "$.type");

				var type = ${value};
			}
			else {
				var type = "";
			}
		}

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/contact-roles/${contactRoleKey} --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
				-d
				{
					"description": "${description}",
					"name": "${name}",
					"type": "${type}"
				}
		''';

		com.liferay.poshi.runner.util.JSONCurlUtil.put(${curl});
	}

	@prose = "I should be able to view a koroneiki contact role with the name ${contactRoleName} and the following values as a permissioned user ${table}"
	macro _viewContactRole {
		var contactRoleKey = KoroneikiContactRoleAPI._getContactRoleKeyViaContactRoleName(contactRoleName = ${contactRoleName});

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/contact-roles/${contactRoleKey} --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';
		var rawTable = TableUtil.newTable(${table});

		for (var row : table ${rawTable}) {
			var field = "${row[0]}";
			var value = "${row[1]}";

			if (${field} == "Description") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.description");
			}
			else if (${field} == "Name") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.name");
			}
			else if (${field} == "Type") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.type");
			}
			else {
				fail("No such contact role field ${field}");
			}

			echo("Expected ${field} Value: ${value}");

			echo("Actual ${field} Value: ${valueFromAPI}");

			if (!(contains(${value}, ${valueFromAPI}))) {
				fail("Actual ${field} value ${valueFromAPI} does not match expected ${field} value ${value}");
			}
		}
	}

	@prose = "I should not be able to view a koroneiki contact role with the name ${contactRoleName} as a non permissioned user"
	macro _viewNoContactRole {
		if (contains(${contactRoleName}, " ")) {
			var keyword = StringUtil.replace(${contactRoleName}, " ", "+");
		}
		else {
			var keyword = ${contactRoleName};
		}

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/contact-roles?filter="name%20eq%20'${keyword}'" --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';

		var allContactRoles = JSONCurlUtil.get(${curl}, "$");

		if (contains(${allContactRoles}, ${contactRoleName})) {
			fail("The contact role with the name ${contactRoleName} is visible to a non permissioned user");
		}

		var contactRoleKey = KoroneikiContactRoleAPI._getContactRoleKeyViaContactRoleName(contactRoleName = ${contactRoleName});

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/contact-roles/${contactRoleKey} --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';

		var contactRole = JSONCurlUtil.get(${curl}, "$");

		if (contains(${contactRole}, ${contactRoleName})) {
			fail("The contact role with the name ${contactRoleName} is visible to a non permissioned user");
		}
	}

	@prose = "I should not be able to view an audit entry for a change to the contact role ${contactRoleName} field ${fieldName} from the old value ${oldValue} to the new value ${newValue} as a non permissioned user"
	macro _viewNoContactRoleAuditEntry {
		var contactRoleKey = KoroneikiContactRoleAPI._getContactRoleKeyViaContactRoleName(contactRoleName = ${contactRoleName});

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/contact-roles/${contactRoleKey}/audit-entries --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';

		var contactRoleAuditEntries = JSONCurlUtil.get(${curl}, "$");

		if (contains(${contactRoleAuditEntries}, ${fieldName}) || contains(${contactRoleAuditEntries}, ${oldValue}) || contains(${contactRoleAuditEntries}, ${newValue})) {
			fail("The contact role audit entry for a change to the contact role ${contactRoleName} field ${fieldName} from the old value ${oldValue} to the new value ${newValue} is visible to a non permissioned user");
		}

		var auditEntryKey = KoroneikiContactRoleAPI._getContactRoleAuditEntryKey(
			contactRoleName = ${contactRoleName},
			fieldName = ${fieldName},
			newValue = ${newValue},
			oldValue = ${oldValue});

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/audit-entries/${auditEntryKey} --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';

		var contactRoleAuditEntry = JSONCurlUtil.get(${curl}, "$");

		if (contains(${contactRoleAuditEntry}, ${fieldName}) || contains(${contactRoleAuditEntry}, ${oldValue}) || contains(${contactRoleAuditEntry}, ${newValue})) {
			fail("The contact role audit entry for a change to the contact role ${contactRoleName} field ${fieldName} from the old value ${oldValue} to the new value ${newValue} is visible to a non permissioned user");
		}
	}

}