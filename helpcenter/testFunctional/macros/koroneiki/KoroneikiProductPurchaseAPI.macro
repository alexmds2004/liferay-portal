definition {

	var apiToken = PropsUtil.get("koroneiki.api.token");
	var koroneikiAdminEmail = PropsUtil.get("koroneiki.admin.email.address");
	var koroneikiAdminPassword = PropsUtil.get("koroneiki.admin.password");
	var koroneikiURL = PropsUtil.get("koroneiki.liferay.url");
	var roleName = PropsUtil.get("koroneiki.non.permission.role.name");

	@prose = "I create an external link for a product purchase made by account ${accountName} for the product ${productName} with the following values ${table}"
	macro _createProductPurchaseExternalLink {
		var productPurchaseKey = KoroneikiAccountAPI._getAccountProductPurchaseKeyViaAccountAndProductName(
			accountName = ${accountName},
			productName = ${productName});
		var rawTable = TableUtil.newTable(${table});

		for (var row : table ${rawTable}) {
			var field = "${row[0]}";
			var value = "${row[1]}";

			if (${field} == "Domain") {
				var domain = ${value};
			}
			else if (${field} == "Entity Id") {
				var entityId = ${value};
			}
			else if (${field} == "Entity Name") {
				var entityName = ${value};
			}
			else {
				fail("No such external link field ${field}");
			}
		}

		if (!(isSet(domain))) {
			var domain = "";
		}

		if (!(isSet(entityId))) {
			var entityId = "";
		}

		if (!(isSet(entityName))) {
			var entityName = "";
		}

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/product-purchases/${productPurchaseKey}/external-links --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
				-d
				{
					"domain": "${domain}",
					"entityId": "${entityId}",
					"entityName": "${entityName}"
				}
		''';

		com.liferay.poshi.runner.util.JSONCurlUtil.post(${curl});
	}

	@prose = "I create a nested field for the product purchase external link with the following values ${table}"
	macro _createProductPurchaseExternalLinkNestedField {
		var rawTable = TableUtil.newTable(${table});

		for (var row : table ${rawTable}) {
			var field = "${row[0]}";
			var value = "${row[1]}";

			if (${field} == "Domain") {
				var domain = ${value};
			}
			else if (${field} == "Entity Id") {
				var entityId = ${value};
			}
			else if (${field} == "Entity Name") {
				var entityName = ${value};
			}
			else if (${field} == "URL") {
				var url = ${value};
			}
			else {
				fail("No such external link field ${field}");
			}
		}

		if (!(isSet(domain))) {
			var domain = "";
		}

		if (!(isSet(entityId))) {
			var entityId = "";
		}

		if (!(isSet(entityName))) {
			var entityName = "";
		}

		if (!(isSet(url))) {
			var url = "";
		}

		static var staticProductPurchaseExternalLinksNestedField = "{\"domain\":\"${domain}\",\"entityId\":\"${entityId}\",\"entityName\":\"${entityName}\",\"url\":\"${url}\"}";

		return ${staticProductPurchaseExternalLinksNestedField};
	}

	@prose = "I create a nested field for the product purchase for the product ${productName} with the following values ${table}"
	macro _createProductPurchaseNestedField {
		var productKey = KoroneikiProductAPI._getProductKeyViaProductName(productName = ${productName});
		var properties = "";
		var rawTable = TableUtil.newTable(${table});

		for (var row : table ${rawTable}) {
			var field = "${row[0]}";
			var value1 = "${row[1]}";
			var value2 = "${row[2]}";

			if (${field} == "End Date") {
				var endDate = ${value1};
			}
			else if (${field} == "Original End Date") {
				var originalEndDate = ${value1};
			}
			else if (${field} == "External Link Nested Field") {
				var externalLinksNestedField = ${value1};
			}
			else if (${field} == "Perpetual") {
				var value1 = StringUtil.lowerCase(${value1});

				var value1 = StringUtil.replace(${value1}, " ", "-");

				var perpetual = ${value1};
			}
			else if (${field} == "Product Field and Value") {
				if (${properties} == "") {
					var properties = "\"${value1}\":\"${value2}\"";
				}
				else {
					var properties = "${properties},\"${value1}\":\"${value2}\"";
				}
			}
			else if (${field} == "Quantity") {
				var quantity = ${value1};
			}
			else if (${field} == "Start Date") {
				var startDate = ${value1};
			}
			else if (${field} == "Status") {
				var status = ${value1};
			}
			else {
				fail("No such product purchase field ${field}");
			}
		}

		if (!(isSet(endDate))) {
			var endDate = "";
		}

		if (!(isSet(externalLinksNestedField))) {
			var externalLinksNestedField = "";
		}

		if (!(isSet(originalEndDate))) {
			var originalEndDate = "";
		}

		if (!(isSet(perpetual))) {
			var perpetual = "true";
		}

		if (!(isSet(quantity))) {
			var quantity = 1;
		}

		if (!(isSet(startDate))) {
			var startDate = "";
		}

		if (!(isSet(status))) {
			var status = "";
		}

		if (${properties} == "") {
			static var staticProductPurchasesNestedField = "{\"endDate\":\"${endDate}\",\"externalLinks\":[${externalLinksNestedField}],\"originalEndDate\":\"${originalEndDate}\",\"perpetual\":${perpetual},\"productKey\":\"${productKey}\",\"quantity\":${quantity},\"startDate\":\"${startDate}\",\"status\":\"${status}\"}";
		}
		else {
			static var staticProductPurchasesNestedField = "{\"endDate\":\"${endDate}\",\"externalLinks\":[${externalLinksNestedField}],\"originalEndDate\":\"${originalEndDate}\",\"perpetual\":${perpetual},\"productKey\":\"${productKey}\",\"properties\":{${properties}},\"quantity\":${quantity},\"startDate\":\"${startDate}\",\"status\":\"${status}\"}";
		}

		return ${staticProductPurchasesNestedField};
	}

	@prose = "I create a nested field for the product purchase for a non existent product ${productName} and the following values ${table}"
	macro _createProductPurchaseNestedFieldWithNonexistentProduct {
		var properties = "";
		var rawTable = TableUtil.newTable(${table});

		for (var row : table ${rawTable}) {
			var field = "${row[0]}";
			var value1 = "${row[1]}";
			var value2 = "${row[2]}";

			if (${field} == "End Date") {
				var endDate = ${value1};
			}
			else if (${field} == "Original End Date") {
				var originalEndDate = ${value1};
			}
			else if (${field} == "External Link Nested Field") {
				var externalLinksNestedField = ${value1};
			}
			else if (${field} == "Perpetual") {
				var value1 = StringUtil.lowerCase(${value1});

				var value1 = StringUtil.replace(${value1}, " ", "-");

				var perpetual = ${value1};
			}
			else if (${field} == "Product Field and Value") {
				if (${properties} == "") {
					var properties = "\"${value1}\":\"${value2}\"";
				}
				else {
					var properties = "${properties},\"${value1}\":\"${value2}\"";
				}
			}
			else if (${field} == "Quantity") {
				var quantity = ${value1};
			}
			else if (${field} == "Start Date") {
				var startDate = ${value1};
			}
			else if (${field} == "Status") {
				var status = ${value1};
			}
			else {
				fail("No such product purchase field ${field}");
			}
		}

		if (!(isSet(endDate))) {
			var endDate = "";
		}

		if (!(isSet(externalLinksNestedField))) {
			var externalLinksNestedField = "";
		}

		if (!(isSet(originalEndDate))) {
			var originalEndDate = "";
		}

		if (!(isSet(perpetual))) {
			var perpetual = "true";
		}

		if (!(isSet(quantity))) {
			var quantity = 1;
		}

		if (!(isSet(startDate))) {
			var startDate = "";
		}

		if (!(isSet(status))) {
			var status = "";
		}

		if (${properties} == "") {
			static var staticProductPurchasesNestedField = "{\"endDate\":\"${endDate}\",\"externalLinks\":[${externalLinksNestedField}],\"originalEndDate\":\"${originalEndDate}\",\"perpetual\":${perpetual},\"product\":{\"name\":\"${productName}\"},\"quantity\":${quantity},\"startDate\":\"${startDate}\",\"status\":\"${status}\"}";
		}
		else {
			static var staticProductPurchasesNestedField = "{\"endDate\":\"${endDate}\",\"externalLinks\":[${externalLinksNestedField}],\"originalEndDate\":\"${originalEndDate}\",\"perpetual\":${perpetual},\"product\":{\"name\":\"${productName}\"},\"properties\":{${properties}},\"quantity\":${quantity},\"startDate\":\"${startDate}\",\"status\":\"${status}\"}";
		}

		return ${staticProductPurchasesNestedField};
	}

	@prose = "I delete a product purchase on koroneiki made by the account ${accountName} for the product ${productName}"
	macro _deleteProductPurchase {
		var productPurchaseKey = KoroneikiAccountAPI._getAccountProductPurchaseKeyViaAccountAndProductName(
			accountName = ${accountName},
			productName = ${productName});

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/product-purchases/${productPurchaseKey} --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';

		com.liferay.poshi.runner.util.JSONCurlUtil.delete(${curl});
	}

	@prose = "I delete a product purchase on koroneiki made by the account ${accountName} for the product ${productName} executed by ${agentName}"
	macro _deleteProductPurchaseExecutedByAgent {
		var productPurchaseKey = KoroneikiAccountAPI._getAccountProductPurchaseKeyViaAccountAndProductName(
			accountName = ${accountName},
			productName = ${productName});
		var agentName = StringUtil.replace(${agentName}, " ", "%20");

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/product-purchases/${productPurchaseKey}?agentName=${agentName}\&agentUID=a1b2c3d4-e5f6-g7h8-i9j1-0k11l12m13n1 --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';

		com.liferay.poshi.runner.util.JSONCurlUtil.delete(${curl});
	}

	@prose = "I delete an external link with the entity id ${entityId} on a product purchase made by account ${accountName} for the product ${productName}"
	macro _deleteProductPurchaseExternalLink {
		var externalLinkKey = KoroneikiProductPurchaseAPI._getProductPurchaseExternalLinkKey(
			accountName = ${accountName},
			entityId = ${entityId},
			productName = ${productName});

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/external-links/${externalLinkKey} --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';

		com.liferay.poshi.runner.util.JSONCurlUtil.delete(${curl});
	}

	@prose = "I delete a product purchase on koroneiki made by the account ${accountName} for the product ${productName} with the ${fieldName} ${fieldValue}"
	macro _deleteProductPurchaseWithFieldValue {
		var productPurchaseKey = KoroneikiAccountAPI._getAccountProductPurchaseKeyViaAccountAndProductNameAndFieldValue(
			accountName = ${accountName},
			fieldName = ${fieldName},
			fieldValue = ${fieldValue},
			productName = ${productName});

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/product-purchases/${productPurchaseKey} --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';

		com.liferay.poshi.runner.util.JSONCurlUtil.delete(${curl});
	}

	@prose = "I generate the product purchase ID value for the product purchase made by ${accountName} for ${productName}"
	macro _generateProductPurchaseId {
		var productPurchaseKey = KoroneikiAccountAPI._getAccountProductPurchaseKeyViaAccountAndProductName(
			accountName = ${accountName},
			productName = ${productName});

		var productPurchaseId = StringUtil.replace(${productPurchaseKey}, "KOR-", "");

		static var staticProductPurchaseId = ${productPurchaseId};

		return ${staticProductPurchaseId};
	}

	@prose = "I generate the product purchase ID value for the product purchase made by ${accountName} for ${productName} with the ${fieldName} ${fieldValue}"
	macro _generateProductPurchaseIdWithFieldValue {
		var productPurchaseKey = KoroneikiAccountAPI._getAccountProductPurchaseKeyViaAccountAndProductNameAndFieldValue(
			accountName = ${accountName},
			fieldName = ${fieldName},
			fieldValue = ${fieldValue},
			productName = ${productName});

		var productPurchaseId = StringUtil.replace(${productPurchaseKey}, "KOR-", "");

		static var staticProductPurchaseId = ${productPurchaseId};

		return ${staticProductPurchaseId};
	}

	@prose = "I generate the product purchase key value for the product purchase made by ${accountName} for ${productName}"
	macro _generateProductPurchaseKey {
		var productPurchaseKey = KoroneikiAccountAPI._getAccountProductPurchaseKeyViaAccountAndProductName(
			accountName = ${accountName},
			productName = ${productName});

		static var staticProductPurchaseKey = ${productPurchaseKey};

		return ${staticProductPurchaseKey};
	}

	@prose = "I generate the product purchase key value for the product purchase made by ${accountName} for ${productName} with the ${fieldName} ${fieldValue}"
	macro _generateProductPurchaseKeyWithFieldValue {
		var productPurchaseKey = KoroneikiAccountAPI._getAccountProductPurchaseKeyViaAccountAndProductNameAndFieldValue(
			accountName = ${accountName},
			fieldName = ${fieldName},
			fieldValue = ${fieldValue},
			productName = ${productName});

		static var staticProductPurchaseKey = ${productPurchaseKey};

		return ${staticProductPurchaseKey};
	}

	macro _getFullMonthName {
		if (${month} == "Jan") {
			var fullMonth = "January";
		}
		else if (${month} == "Feb") {
			var fullMonth = "February";
		}
		else if (${month} == "Mar") {
			var fullMonth = "March";
		}
		else if (${month} == "Apr") {
			var fullMonth = "April";
		}
		else if (${month} == "May") {
			var fullMonth = "May";
		}
		else if (${month} == "Jun") {
			var fullMonth = "June";
		}
		else if (${month} == "Jul") {
			var fullMonth = "July";
		}
		else if (${month} == "Aug") {
			var fullMonth = "August";
		}
		else if (${month} == "Sep") {
			var fullMonth = "September";
		}
		else if (${month} == "Oct") {
			var fullMonth = "October";
		}
		else if (${month} == "Nov") {
			var fullMonth = "November";
		}
		else if (${month} == "Dec") {
			var fullMonth = "December";
		}
		else {
			fail("No such abbreviated month ${month}");
		}

		return ${fullMonth};
	}

	macro _getProductPurchaseExternalLinkKey {
		var productPurchaseKey = KoroneikiAccountAPI._getAccountProductPurchaseKeyViaAccountAndProductName(
			accountName = ${accountName},
			productName = ${productName});

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/product-purchases/${productPurchaseKey}/external-links --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';

		var externalLinkKey = JSONCurlUtil.get(${curl}, "$.items[?(@.entityId=='${entityId}')].key");

		var externalLinkKey = StringUtil.replace(${externalLinkKey}, "[", "");

		var externalLinkKey = StringUtil.replace(${externalLinkKey}, "]", "");

		var externalLinkKey = StringUtil.replace(${externalLinkKey}, "\"", "");

		echo("External link key value for ${entityId} on product purchase on ${accountName} for ${productName}: ${externalLinkKey}");

		return ${externalLinkKey};
	}

	@prose = "I rename the first product purchase external link nested field variable"
	macro _renameFirstProductPurchaseExternalLinkNestedFieldVariable {
		static var staticProductPurchaseExternalLinksNestedField1 = ${staticProductPurchaseExternalLinksNestedField};

		return ${staticProductPurchaseExternalLinksNestedField1};
	}

	@prose = "I rename the first product purchase id variable"
	macro _renameFirstProductPurchaseIdVariable {
		static var staticProductPurchaseId1 = ${staticProductPurchaseId};

		return ${staticProductPurchaseId1};
	}

	@prose = "I rename the first product purchase key variable"
	macro _renameFirstProductPurchaseKeyVariable {
		static var staticProductPurchaseKey1 = ${staticProductPurchaseKey};

		return ${staticProductPurchaseKey1};
	}

	@prose = "I rename the second product purchase external link nested field variable"
	macro _renameSecondProductPurchaseExternalLinkNestedFieldVariable {
		static var staticProductPurchaseExternalLinksNestedField2 = ${staticProductPurchaseExternalLinksNestedField};

		return ${staticProductPurchaseExternalLinksNestedField2};
	}

	@prose = "I rename the second product purchase id variable"
	macro _renameSecondProductPurchaseIdVariable {
		static var staticProductPurchaseId2 = ${staticProductPurchaseId};

		return ${staticProductPurchaseId2};
	}

	@prose = "I rename the second product purchase key variable"
	macro _renameSecondProductPurchaseKeyVariable {
		static var staticProductPurchaseKey2 = ${staticProductPurchaseKey};

		return ${staticProductPurchaseKey2};
	}

	@prose = "I rename the third product purchase external link nested field variable"
	macro _renameThirdProductPurchaseExternalLinkNestedFieldVariable {
		static var staticProductPurchaseExternalLinksNestedField3 = ${staticProductPurchaseExternalLinksNestedField};

		return ${staticProductPurchaseExternalLinksNestedField3};
	}

	@prose = "I rename the third product purchase id variable"
	macro _renameThirdProductPurchaseIdVariable {
		static var staticProductPurchaseId3 = ${staticProductPurchaseId};

		return ${staticProductPurchaseId3};
	}

	@prose = "I rename the third product purchase key variable"
	macro _renameThirdProductPurchaseKeyVariable {
		static var staticProductPurchaseKey3 = ${staticProductPurchaseKey};

		return ${staticProductPurchaseKey3};
	}

	@prose = "I set the permissions for the product purchase made by the account ${accountName} for the product ${productName} to have the following values for a non permissioned user ${table}"
	macro _setProductPurchasePermissions {
		var productPurchaseKey = KoroneikiAccountAPI._getAccountProductPurchaseKeyViaAccountAndProductName(
			accountName = ${accountName},
			productName = ${productName});
		var rawTable = TableUtil.newTable(${table});

		for (var row : table ${rawTable}) {
			var field = "${row[0]}";
			var value = "${row[1]}";

			if (${field} == "Delete") {
				var value = StringUtil.lowerCase(${value});

				var delete = ${value};
			}
			else if (${field} == "Permissions") {
				var value = StringUtil.lowerCase(${value});

				var permissions = ${value};
			}
			else if (${field} == "Update") {
				var value = StringUtil.lowerCase(${value});

				var update = ${value};
			}
			else if (${field} == "View") {
				var value = StringUtil.lowerCase(${value});

				var view = ${value};
			}
			else {
				fail("No such permission field ${field}");
			}
		}

		if (!(isSet(delete))) {
			var delete = "false";
		}

		if (!(isSet(permissions))) {
			var permissions = "false";
		}

		if (!(isSet(update))) {
			var update = "false";
		}

		if (!(isSet(view))) {
			var view = "false";
		}

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/product-purchases/${productPurchaseKey}/product-purchase-permissions --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
				-d
				{
					"delete": "${delete}",
					"permissions": "${permissions}",
					"roleNames": [
						"${roleName}"
					],
					"update": "${update}",
					"view": "${view}"
				}
		''';

		com.liferay.poshi.runner.util.JSONCurlUtil.put(${curl});
	}

	@prose = "I set the project details non abbreviated date method to true"
	macro _setProjectDetailsNonAbbreviatedDateMethod {
		static var staticProjectDetailsNonAbbreviatedDateMethod = "true";

		return ${staticProjectDetailsNonAbbreviatedDateMethod};
	}

	@prose = "I tear down the external links for the product purchase made by account ${accountName} for the product ${productName} with the following entity id values ${table}"
	macro _tearDownProductPurchaseExternalLinks {
		var rawTable = TableUtil.newTable(${table});

		for (var row : table ${rawTable}) {
			var entityId = "${row[0]}";

			if (contains(${accountName}, " ")) {
				var keyword = StringUtil.replace(${accountName}, " ", "+");
			}
			else {
				var keyword = ${accountName};
			}

			var curl = '''
				${koroneikiURL}/o/koroneiki-rest/v1.0/accounts?filter="name%20eq%20'${keyword}'" --tlsv1.2
					--header "API_Token: ${apiToken}"
					-H "accept: application/json"
					-H "Content-Type: application/json"
			''';

			var allAccounts = JSONCurlUtil.get(${curl}, "$");

			if (contains(${allAccounts}, ${accountName})) {
				var accountKey = KoroneikiAccountAPI._getAccountKeyViaAccountName(accountName = ${accountName});

				var curl = '''
					${koroneikiURL}/o/koroneiki-rest/v1.0/accounts/${accountKey}/product-purchases --tlsv1.2
						--header "API_Token: ${apiToken}"
						-H "accept: application/json"
						-H "Content-Type: application/json"
				''';

				var allAccountProductPurchases = JSONCurlUtil.get(${curl}, "$");

				if (contains(${allAccountProductPurchases}, ${productName})) {
					var productPurchaseKey = KoroneikiAccountAPI._getAccountProductPurchaseKeyViaAccountAndProductName(
						accountName = ${accountName},
						productName = ${productName});

					var curl = '''
						${koroneikiURL}/o/koroneiki-rest/v1.0/product-purchases/${productPurchaseKey}/external-links --tlsv1.2
							--header "API_Token: ${apiToken}"
							-H "accept: application/json"
							-H "Content-Type: application/json"
					''';

					var allAccountProductPurchaseExternalLinks = JSONCurlUtil.get(${curl}, "$");

					if (contains(${allAccountProductPurchaseExternalLinks}, ${entityId})) {
						KoroneikiProductPurchaseAPI._deleteProductPurchaseExternalLink(
							accountName = ${accountName},
							entityId = ${entityId},
							productName = ${productName});

						echo("Entity ID of external link deleted for product purchase made by ${accountName} for ${productName}: ${entityId}");
					}
				}
			}
		}
	}

	@prose = "I tear down the permissions for the product purchase made by the account ${accountName} for the product ${productName} for a non permissioned user"
	macro _tearDownProductPurchasePermissions {
		if (contains(${accountName}, " ")) {
			var keyword = StringUtil.replace(${accountName}, " ", "+");
		}
		else {
			var keyword = ${accountName};
		}

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/accounts?filter="name%20eq%20'${keyword}'" --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';

		var allAccounts = JSONCurlUtil.get(${curl}, "$");

		if (contains(${allAccounts}, ${accountName})) {
			var accountKey = KoroneikiAccountAPI._getAccountKeyViaAccountName(accountName = ${accountName});

			var curl = '''
				${koroneikiURL}/o/koroneiki-rest/v1.0/accounts/${accountKey}/product-purchases --tlsv1.2
					--header "API_Token: ${apiToken}"
					-H "accept: application/json"
					-H "Content-Type: application/json"
			''';

			var allAccountProductPurchases = JSONCurlUtil.get(${curl}, "$");

			if (contains(${allAccountProductPurchases}, ${productName})) {
				var productPurchaseKey = KoroneikiAccountAPI._getAccountProductPurchaseKeyViaAccountAndProductName(
					accountName = ${accountName},
					productName = ${productName});

				var curl = '''
					${koroneikiURL}/o/koroneiki-rest/v1.0/product-purchases/${productPurchaseKey}/product-purchase-permissions --tlsv1.2
						--header "API_Token: ${apiToken}"
						-H "accept: application/json"
						-H "Content-Type: application/json"
						-d
						{
							"delete": "true",
							"permissions": "true",
							"roleNames": [
								"${roleName}"
							],
							"update": "true",
							"view": "true"
						}
				''';

				com.liferay.poshi.runner.util.JSONCurlUtil.delete(${curl});
			}
		}
	}

	@prose = "I tear down the product purchases on koroneiki made by the account ${accountName} for the following products ${table}"
	macro _tearDownProductPurchases {
		if (contains(${accountName}, " ")) {
			var keyword = StringUtil.replace(${accountName}, " ", "+");
		}
		else {
			var keyword = ${accountName};
		}

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/accounts?filter="name%20eq%20'${keyword}'" --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';

		var allAccounts = JSONCurlUtil.get(${curl}, "$");

		if (contains(${allAccounts}, ${accountName})) {
			var accountKey = KoroneikiAccountAPI._getAccountKeyViaAccountName(accountName = ${accountName});
			var rawTable = TableUtil.newTable(${table});

			for (var row : table ${rawTable}) {
				var productName = "${row[0]}";

				if (contains(${productName}, " ")) {
					var keyword = StringUtil.replace(${productName}, " ", "+");
				}
				else {
					var keyword = ${productName};
				}

				var curl = '''
					${koroneikiURL}/o/koroneiki-rest/v1.0/products?filter="name%20eq%20'${keyword}'" --tlsv1.2
						--header "API_Token: ${apiToken}"
						-H "accept: application/json"
						-H "Content-Type: application/json"
				''';

				var allProducts = JSONCurlUtil.get(${curl}, "$");

				if (contains(${allProducts}, ${productName})) {
					var productKey = KoroneikiProductAPI._getProductKeyViaProductName(productName = ${productName});
					var curl = '''
						${koroneikiURL}/o/koroneiki-rest/v1.0/accounts/${accountKey}/product-purchases --tlsv1.2
							--header "API_Token: ${apiToken}"
							-H "accept: application/json"
							-H "Content-Type: application/json"
					''';

					var allAccountProductPurchases = JSONCurlUtil.get(${curl}, "$");

					if (contains(${allAccountProductPurchases}, ${productKey})) {
						KoroneikiProductPurchaseAPI._deleteProductPurchase(
							accountName = ${accountName},
							productName = ${productName});

						echo("Product Purchase deleted: ${productName} made by ${accountName}");
					}
				}
			}
		}
	}

	@prose = "I tear down the product purchases on koroneiki made by the account ${accountName} for the following products with their respective field values ${table}"
	macro _tearDownProductPurchasesWithFieldValues {
		if (contains(${accountName}, " ")) {
			var keyword = StringUtil.replace(${accountName}, " ", "+");
		}
		else {
			var keyword = ${accountName};
		}

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/accounts?filter="name%20eq%20'${keyword}'" --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';

		var allAccounts = JSONCurlUtil.get(${curl}, "$");

		if (contains(${allAccounts}, ${accountName})) {
			var accountKey = KoroneikiAccountAPI._getAccountKeyViaAccountName(accountName = ${accountName});
			var rawTable = TableUtil.newTable(${table});

			for (var row : table ${rawTable}) {
				var productName = "${row[0]}";
				var fieldName = "${row[1]}";
				var fieldValue = "${row[2]}";

				if (contains(${productName}, " ")) {
					var keyword = StringUtil.replace(${productName}, " ", "+");
				}
				else {
					var keyword = ${productName};
				}

				var curl = '''
					${koroneikiURL}/o/koroneiki-rest/v1.0/products?filter="name%20eq%20'${keyword}'" --tlsv1.2
						--header "API_Token: ${apiToken}"
						-H "accept: application/json"
						-H "Content-Type: application/json"
				''';

				var allProducts = JSONCurlUtil.get(${curl}, "$");

				if (contains(${allProducts}, ${productName})) {
					var productKey = KoroneikiProductAPI._getProductKeyViaProductName(productName = ${productName});
					var curl = '''
						${koroneikiURL}/o/koroneiki-rest/v1.0/accounts/${accountKey}/product-purchases --tlsv1.2
							--header "API_Token: ${apiToken}"
							-H "accept: application/json"
							-H "Content-Type: application/json"
					''';

					var allAccountProductPurchases = JSONCurlUtil.get(${curl}, "$");

					if (contains(${allAccountProductPurchases}, ${productKey}) && contains(${allAccountProductPurchases}, ${fieldValue})) {
						KoroneikiProductPurchaseAPI._deleteProductPurchaseWithFieldValue(
							accountName = ${accountName},
							fieldName = ${fieldName},
							fieldValue = ${fieldValue},
							productName = ${productName});

						echo("Product Purchase deleted: ${productName} made by ${accountName}");
					}
				}
			}
		}
	}

	@prose = "I unset the following permissions for the product purchase made by the account ${accountName} for the product ${productName} for a permissioned user ${table}"
	macro _unsetProductPurchasePermissions {
		var productPurchaseKey = KoroneikiAccountAPI._getAccountProductPurchaseKeyViaAccountAndProductName(
			accountName = ${accountName},
			productName = ${productName});
		var rawTable = TableUtil.newTable(${table});

		for (var row : table ${rawTable}) {
			var field = "${row[0]}";
			var value = "${row[1]}";

			if (${field} == "Delete") {
				var value = StringUtil.lowerCase(${value});

				var delete = ${value};
			}
			else if (${field} == "Permissions") {
				var value = StringUtil.lowerCase(${value});

				var permissions = ${value};
			}
			else if (${field} == "Update") {
				var value = StringUtil.lowerCase(${value});

				var update = ${value};
			}
			else if (${field} == "View") {
				var value = StringUtil.lowerCase(${value});

				var view = ${value};
			}
			else {
				fail("No such permission field ${field}");
			}
		}

		if (!(isSet(delete))) {
			var delete = "false";
		}

		if (!(isSet(permissions))) {
			var permissions = "false";
		}

		if (!(isSet(update))) {
			var update = "false";
		}

		if (!(isSet(view))) {
			var view = "false";
		}

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/product-purchases/${productPurchaseKey}/product-purchase-permissions --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
				-d
				{
					"delete": "${delete}",
					"permissions": "${permissions}",
					"roleNames": [
						"${roleName}"
					],
					"update": "${update}",
					"view": "${view}"
				}
		''';

		com.liferay.poshi.runner.util.JSONCurlUtil.delete(${curl});
	}

	@prose = "I set the project details non abbreviated date method to false"
	macro _unsetProjectDetailsNonAbbreviatedDateMethod {
		static var staticProjectDetailsNonAbbreviatedDateMethod = "false";

		return ${staticProjectDetailsNonAbbreviatedDateMethod};
	}

	@prose = "I update a product purchase on koroneiki made by the account ${accountName} for the product ${productName} to have the following values ${table}"
	macro _updateProductPurchase {
		var productPurchaseKey = KoroneikiAccountAPI._getAccountProductPurchaseKeyViaAccountAndProductName(
			accountName = ${accountName},
			productName = ${productName});
		var properties = "";
		var rawTable = TableUtil.newTable(${table});

		for (var row : table ${rawTable}) {
			var field = "${row[0]}";
			var value1 = "${row[1]}";
			var value2 = "${row[2]}";

			if (${field} == "Agent Name") {
				var value1 = StringUtil.replace(${value1}, " ", "%20");

				var agentName = ${value1};
			}
			else if (${field} == "End Date") {
				var endDate = ${value1};
			}
			else if (${field} == "Original End Date") {
				var originalEndDate = ${value1};
			}
			else if (${field} == "Perpetual") {
				var value1 = StringUtil.lowerCase(${value1});

				var value1 = StringUtil.replace(${value1}, " ", "-");

				var perpetual = ${value1};
			}
			else if (${field} == "Product Name") {
				var productKey = KoroneikiProductAPI._getProductKeyViaProductName(productName = ${value1});
			}
			else if (${field} == "Product Field and Value") {
				if (${properties} == "") {
					var properties = "\"${value1}\":\"${value2}\"";
				}
				else {
					var properties = "${properties},\"${value1}\":\"${value2}\"";
				}
			}
			else if (${field} == "Quantity") {
				var quantity = ${value1};
			}
			else if (${field} == "Start Date") {
				var startDate = ${value1};
			}
			else if (${field} == "Status") {
				var status = ${value1};
			}
			else {
				fail("No such product purchase field ${field}");
			}
		}

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/product-purchases/${productPurchaseKey} --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';

		if (!(isSet(agentName))) {
			var agentName = "";
		}

		if (!(isSet(endDate))) {
			var productPurchaseObject = JSONCurlUtil.get(${curl}, "$");

			if (contains(${productPurchaseObject}, "endDate=")) {
				var value = JSONCurlUtil.get(${curl}, "$.endDate");

				var endDate = ${value};
			}
			else {
				var endDate = "";
			}
		}

		if (!(isSet(originalEndDate))) {
			var productPurchaseObject = JSONCurlUtil.get(${curl}, "$");

			if (contains(${productPurchaseObject}, "originalEndDate=")) {
				var value = JSONCurlUtil.get(${curl}, "$.originalEndDate");

				var originalEndDate = ${value};
			}
			else {
				var originalEndDate = "";
			}
		}

		if (!(isSet(perpetual))) {
			var productPurchaseObject = JSONCurlUtil.get(${curl}, "$");

			if (contains(${productPurchaseObject}, "perpetual=")) {
				var value = JSONCurlUtil.get(${curl}, "$.perpetual");

				var perpetual = ${value};
			}
			else {
				var perpetual = "true";
			}
		}

		if (!(isSet(productKey))) {
			var productPurchaseObject = JSONCurlUtil.get(${curl}, "$");

			if (contains(${productPurchaseObject}, "productKey=")) {
				var value = JSONCurlUtil.get(${curl}, "$.productKey");

				var productKey = ${value};
			}
			else {
				var productKey = "";
			}
		}

		if (!(isSet(properties))) {
			var productPurchaseObject = JSONCurlUtil.get(${curl}, "$");

			if (contains(${productPurchaseObject}, "properties=")) {
				var value = JSONCurlUtil.get(${curl}, "$.properties");

				var properties = ${value};
			}
			else {
				var properties = "";
			}
		}

		if (!(isSet(quantity))) {
			var productPurchaseObject = JSONCurlUtil.get(${curl}, "$");

			if (contains(${productPurchaseObject}, "quantity=")) {
				var value = JSONCurlUtil.get(${curl}, "$.quantity");

				var quantity = ${value};
			}
			else {
				var quantity = 1;
			}
		}

		if (!(isSet(startDate))) {
			var productPurchaseObject = JSONCurlUtil.get(${curl}, "$");

			if (contains(${productPurchaseObject}, "startDate=")) {
				var value = JSONCurlUtil.get(${curl}, "$.startDate");

				var startDate = ${value};
			}
			else {
				var startDate = "";
			}
		}

		if (!(isSet(status))) {
			var productPurchaseObject = JSONCurlUtil.get(${curl}, "$");

			if (contains(${productPurchaseObject}, "status=")) {
				var value = JSONCurlUtil.get(${curl}, "$.status");

				var status = ${value};
			}
			else {
				var status = "";
			}
		}

		if (${properties} == "") {
			if (${agentName} == "") {
				var curl = '''
					${koroneikiURL}/o/koroneiki-rest/v1.0/product-purchases/${productPurchaseKey} --tlsv1.2
						--header "API_Token: ${apiToken}"
						-H "accept: application/json"
						-H "Content-Type: application/json"
						-d
						{
							"endDate": "${endDate}",
							"originalEndDate": "${originalEndDate}",
							"perpetual": ${perpetual},
							"productKey": "${productKey}",
							"quantity": ${quantity},
							"startDate": "${startDate}",
							"status": "${status}"
						}
				''';
			}
			else {
				var curl = '''
					${koroneikiURL}/o/koroneiki-rest/v1.0/product-purchases/${productPurchaseKey}?agentName=${agentName}\&agentUID=a1b2c3d4-e5f6-g7h8-i9j1-0k11l12m13n1 --tlsv1.2
						--header "API_Token: ${apiToken}"
						-H "accept: application/json"
						-H "Content-Type: application/json"
						-d
						{
							"endDate": "${endDate}",
							"originalEndDate": "${originalEndDate}",
							"perpetual": ${perpetual},
							"productKey": "${productKey}",
							"quantity": ${quantity},
							"startDate": "${startDate}",
							"status": "${status}"
						}
				''';
			}
		}
		else {
			if (${agentName} == "") {
				var curl = '''
					${koroneikiURL}/o/koroneiki-rest/v1.0/product-purchases/${productPurchaseKey} --tlsv1.2
						--header "API_Token: ${apiToken}"
						-H "accept: application/json"
						-H "Content-Type: application/json"
						-d
						{
							"endDate": "${endDate}",
							"originalEndDate": "${originalEndDate}",
							"perpetual": ${perpetual},
							"productKey": "${productKey}",
							"properties": {
								${properties}
							},
							"quantity": ${quantity},
							"startDate": "${startDate}",
							"status": "${status}"
						}
				''';
			}
			else {
				var curl = '''
					${koroneikiURL}/o/koroneiki-rest/v1.0/product-purchases/${productPurchaseKey}?agentName=${agentName}\&agentUID=a1b2c3d4-e5f6-g7h8-i9j1-0k11l12m13n1 --tlsv1.2
						--header "API_Token: ${apiToken}"
						-H "accept: application/json"
						-H "Content-Type: application/json"
						-d
						{
							"endDate": "${endDate}",
							"originalEndDate": "${originalEndDate}",
							"perpetual": ${perpetual},
							"productKey": "${productKey}",
							"properties": {
								${properties}
							},
							"quantity": ${quantity},
							"startDate": "${startDate}",
							"status": "${status}"
						}
				''';
			}
		}

		com.liferay.poshi.runner.util.JSONCurlUtil.put(${curl});
	}

	@prose = "I should not be able to view a product purchase on koroneiki made by the account ${accountName} for the product ${productName}"
	macro _viewNoProductPurchase {
		var accountKey = KoroneikiAccountAPI._getAccountKeyViaAccountName(accountName = ${accountName});
		var productKey = KoroneikiProductAPI._getProductKeyViaProductName(productName = ${productName});
		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/accounts/${accountKey}/product-purchases --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';

		var allAccountProductPurchases = JSONCurlUtil.get(${curl}, "$");

		if (contains(${allAccountProductPurchases}, ${productKey})) {
			fail("The product purchase made by ${accountName} for the product ${productName} is still present on koroneiki");
		}
	}

	@prose = "the external link with the entity id ${entityId} should not be present on the product purchase made by account ${accountName} for the product ${productName}"
	macro _viewNoProductPurchaseExternalLink {
		var productPurchaseKey = KoroneikiAccountAPI._getAccountProductPurchaseKeyViaAccountAndProductName(
			accountName = ${accountName},
			productName = ${productName});

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/product-purchases/${productPurchaseKey}/external-links --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';

		var allProductPurchaseExternalLinks = JSONCurlUtil.get(${curl}, "$");

		if (contains(${allProductPurchaseExternalLinks}, ${entityId})) {
			fail("The external link with the entity id ${entityId} is on the product purchase made by the account ${accountName} for the product ${productName}");
		}
	}

	@prose = "I should not be able to view a product purchase on koroneiki made by the account ${accountName} for the product ${productName} via nested field"
	macro _viewNoProductPurchaseViaNestedField {
		var accountKey = KoroneikiAccountAPI._getAccountKeyViaAccountName(accountName = ${accountName});
		var productKey = KoroneikiProductAPI._getProductKeyViaProductName(productName = ${productName});
		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/accounts/${accountKey}?nestedFields=productPurchases --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';

		var allAccountFields = JSONCurlUtil.get(${curl}, "$");

		if (contains(${allAccountFields}, "productPurchases")) {
			var allAccountProductPurchases = JSONCurlUtil.get(${curl}, "$.productPurchases");

			if (contains(${allAccountProductPurchases}, ${productKey})) {
				fail("The product purchase made by ${accountName} for the product ${productName} is still present on koroneiki");
			}
		}
		else {
			echo("No product purchases present on ${accountName} and therefore there is no product purchases nested field visible");
		}
	}

	@prose = "I should be able to view a product purchase on koroneiki made by the account ${accountName} for the product ${productName} with the following values ${table}"
	macro _viewProductPurchase {
		var productPurchaseKey = KoroneikiAccountAPI._getAccountProductPurchaseKeyViaAccountAndProductName(
			accountName = ${accountName},
			productName = ${productName});

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/product-purchases/${productPurchaseKey}?nestedFields=productConsumptions --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';
		var rawTable = TableUtil.newTable(${table});

		for (var row : table ${rawTable}) {
			var field = "${row[0]}";
			var value1 = "${row[1]}";
			var value2 = "${row[2]}";

			if (${field} == "End Date") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.endDate");
			}
			else if (${field} == "External Link Nested Field") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.externalLinks");
			}
			else if (${field} == "Original End Date") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.originalEndDate");
			}
			else if (${field} == "Perpetual") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.perpetual");
			}
			else if (${field} == "Product Consumption Nested Field") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.product.name");
			}
			else if (${field} == "Product Field and Value") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.properties.${value1}");
			}
			else if (${field} == "Quantity") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.quantity");
			}
			else if (${field} == "Start Date") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.startDate");
			}
			else if (${field} == "Status") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.status");
			}
			else {
				fail("No such product purchase field ${field}");
			}

			if (${field} == "Product Field and Value") {
				echo("Expected property field ${value1} value: ${value2}");

				echo("Actual property field ${value1} value: ${valueFromAPI}");

				if (!(contains(${value2}, ${valueFromAPI}))) {
					fail("Actual property field ${value1} value ${valueFromAPI} does not match expected property field ${value1} value ${value2}");
				}
			}
			else if (contains(${field}, "Nested Field")) {
				echo("Expected Partial ${field} Value: ${value1}");

				echo("Actual ${field} Value: ${valueFromAPI}");

				if (!(contains(${valueFromAPI}, ${value1}))) {
					fail("Actual ${field} value ${valueFromAPI} does not contain the expected ${field} value ${value1}");
				}
			}
			else {
				echo("Expected ${field} Value: ${value1}");

				echo("Actual ${field} Value: ${valueFromAPI}");

				if (!(contains(${value1}, ${valueFromAPI}))) {
					fail("Actual ${field} value ${valueFromAPI} does not match expected ${field} value ${value1}");
				}
			}
		}
	}

	@prose = "the external link with the entity id ${entityId} should be present on the product purchase made by account ${accountName} for the product ${productName} with the following values ${table}"
	macro _viewProductPurchaseExternalLink {
		var productPurchaseKey = KoroneikiAccountAPI._getAccountProductPurchaseKeyViaAccountAndProductName(
			accountName = ${accountName},
			productName = ${productName});

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/product-purchases/${productPurchaseKey}/external-links --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';
		var rawTable = TableUtil.newTable(${table});

		for (var row : table ${rawTable}) {
			var field = "${row[0]}";
			var value = "${row[1]}";

			if (${field} == "Domain") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.items[?(@.entityId=='${entityId}')].domain");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "[", "");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "]", "");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "\"", "");
			}
			else if (${field} == "Entity Id") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.items[?(@.entityId=='${entityId}')].entityId");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "[", "");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "]", "");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "\"", "");
			}
			else if (${field} == "Entity Name") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.items[?(@.entityId=='${entityId}')].entityName");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "[", "");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "]", "");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "\"", "");
			}
			else if (${field} == "URL") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.items[?(@.entityId=='${entityId}')].url");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "[", "");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "]", "");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "\"", "");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "\/", "/");
			}
			else {
				fail("No such external link field ${field}");
			}

			echo("Expected ${field} Value: ${value}");

			echo("Actual ${field} Value: ${valueFromAPI}");

			if (!(contains(${value}, ${valueFromAPI}))) {
				fail("Actual ${field} value ${valueFromAPI} does not match expected ${field} value ${value}");
			}
		}
	}

	@prose = "the external link with the entity id ${entityId} should be present on the product purchase made by account ${accountName} for the product ${productName} via the external link key with the following values ${table}"
	macro _viewProductPurchaseExternalLinkViaExternalLinkKey {
		var externalLinkKey = KoroneikiProductPurchaseAPI._getProductPurchaseExternalLinkKey(
			accountName = ${accountName},
			entityId = ${entityId},
			productName = ${productName});

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/external-links/${externalLinkKey} --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';
		var rawTable = TableUtil.newTable(${table});

		for (var row : table ${rawTable}) {
			var field = "${row[0]}";
			var value = "${row[1]}";

			if (${field} == "Domain") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.domain");
			}
			else if (${field} == "Entity Id") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.entityId");
			}
			else if (${field} == "Entity Name") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.entityName");
			}
			else if (${field} == "URL") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.url");
			}
			else {
				fail("No such external link field ${field}");
			}

			echo("Expected ${field} Value: ${value}");

			echo("Actual ${field} Value: ${valueFromAPI}");

			if (!(contains(${value}, ${valueFromAPI}))) {
				fail("Actual ${field} value ${valueFromAPI} does not match expected ${field} value ${value}");
			}
		}
	}

	@prose = "I should be able to view a product purchase on koroneiki made by the account ${accountName} for the product ${productName} with the following values using the external link domain ${domain} and entity name ${entityName} and entity id ${entityId} ${table}"
	macro _viewProductPurchaseViaExternalLink {
		var accountKey = KoroneikiAccountAPI._getAccountKeyViaAccountName(accountName = ${accountName});
		var productKey = KoroneikiProductAPI._getProductKeyViaProductName(productName = ${productName});
		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/product-purchases/by-external-link/${domain}/${entityName}/${entityId} --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';
		var rawTable = TableUtil.newTable(${table});

		for (var row : table ${rawTable}) {
			var field = "${row[0]}";
			var value1 = "${row[1]}";
			var value2 = "${row[2]}";

			if (${field} == "End Date") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.items[?(@.accountKey=='${accountKey}' && @.productKey=='${productKey}')].endDate");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "[", "");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "]", "");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "\"", "");
			}
			else if (${field} == "Original End Date") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.items[?(@.accountKey=='${accountKey}' && @.productKey=='${productKey}')].originalEndDate");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "[", "");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "]", "");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "\"", "");
			}
			else if (${field} == "Perpetual") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.items[?(@.accountKey=='${accountKey}' && @.productKey=='${productKey}')].perpetual");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "[", "");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "]", "");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "\"", "");
			}
			else if (${field} == "Product Field and Value") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.items[?(@.accountKey=='${accountKey}' && @.productKey=='${productKey}')].properties.${value1}");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "[", "");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "]", "");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "\"", "");
			}
			else if (${field} == "Quantity") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.items[?(@.accountKey=='${accountKey}' && @.productKey=='${productKey}')].quantity");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "[", "");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "]", "");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "\"", "");
			}
			else if (${field} == "Start Date") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.items[?(@.accountKey=='${accountKey}' && @.productKey=='${productKey}')].startDate");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "[", "");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "]", "");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "\"", "");
			}
			else if (${field} == "Status") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.items[?(@.accountKey=='${accountKey}' && @.productKey=='${productKey}')].status");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "[", "");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "]", "");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "\"", "");
			}
			else {
				fail("No such product purchase field ${field}");
			}

			if (${field} == "Product Field and Value") {
				echo("Expected property field ${value1} value: ${value2}");

				echo("Actual property field ${value1} value: ${valueFromAPI}");

				if (!(contains(${value2}, ${valueFromAPI}))) {
					fail("Actual property field ${value1} value ${valueFromAPI} does not match expected property field ${value1} value ${value2}");
				}
			}
			else {
				echo("Expected ${field} Value: ${value1}");

				echo("Actual ${field} Value: ${valueFromAPI}");

				if (!(contains(${value1}, ${valueFromAPI}))) {
					fail("Actual ${field} value ${valueFromAPI} does not match expected ${field} value ${value1}");
				}
			}
		}
	}

	@prose = "I should be able to view a product purchase on koroneiki made by the account ${accountName} for the product ${productName} via nested field with the following values ${table}"
	macro _viewProductPurchaseViaNestedField {
		var accountKey = KoroneikiAccountAPI._getAccountKeyViaAccountName(accountName = ${accountName});
		var productKey = KoroneikiProductAPI._getProductKeyViaProductName(productName = ${productName});
		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/accounts/${accountKey}?nestedFields=productPurchases --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';
		var rawTable = TableUtil.newTable(${table});

		for (var row : table ${rawTable}) {
			var field = "${row[0]}";
			var value1 = "${row[1]}";
			var value2 = "${row[2]}";

			if (${field} == "End Date") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.productPurchases[?(@.productKey=='${productKey}')].endDate");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "[", "");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "]", "");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "\"", "");
			}
			else if (${field} == "Original End Date") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.productPurchases[?(@.productKey=='${productKey}')].originalEndDate");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "[", "");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "]", "");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "\"", "");
			}
			else if (${field} == "Perpetual") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.productPurchases[?(@.productKey=='${productKey}')].perpetual");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "[", "");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "]", "");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "\"", "");
			}
			else if (${field} == "Product Field and Value") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.productPurchases[?(@.productKey=='${productKey}')].properties.${value1}");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "[", "");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "]", "");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "\"", "");
			}
			else if (${field} == "Quantity") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.productPurchases[?(@.productKey=='${productKey}')].quantity");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "[", "");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "]", "");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "\"", "");
			}
			else if (${field} == "Start Date") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.productPurchases[?(@.productKey=='${productKey}')].startDate");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "[", "");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "]", "");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "\"", "");
			}
			else if (${field} == "Status") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.productPurchases[?(@.productKey=='${productKey}')].status");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "[", "");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "]", "");

				var valueFromAPI = StringUtil.replace(${valueFromAPI}, "\"", "");
			}
			else {
				fail("No such product purchase field ${field}");
			}

			if (${field} == "Product Field and Value") {
				echo("Expected property field ${value1} value: ${value2}");

				echo("Actual property field ${value1} value: ${valueFromAPI}");

				if (!(contains(${value2}, ${valueFromAPI}))) {
					fail("Actual property field ${value1} value ${valueFromAPI} does not match expected property field ${value1} value ${value2}");
				}
			}
			else {
				echo("Expected ${field} Value: ${value1}");

				echo("Actual ${field} Value: ${valueFromAPI}");

				if (!(contains(${value1}, ${valueFromAPI}))) {
					fail("Actual ${field} value ${valueFromAPI} does not match expected ${field} value ${value1}");
				}
			}
		}
	}

	@prose = "I should be able to view a product purchase on koroneiki made by the account ${accountName} for the product ${productName} with the ${fieldName} ${fieldValue} and the following values ${table}"
	macro _viewProductPurchaseWithFieldValue {
		var productPurchaseKey = KoroneikiAccountAPI._getAccountProductPurchaseKeyViaAccountAndProductNameAndFieldValue(
			accountName = ${accountName},
			fieldName = ${fieldName},
			fieldValue = ${fieldValue},
			productName = ${productName});

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/product-purchases/${productPurchaseKey} --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';
		var rawTable = TableUtil.newTable(${table});

		for (var row : table ${rawTable}) {
			var field = "${row[0]}";
			var value1 = "${row[1]}";
			var value2 = "${row[2]}";

			if (${field} == "End Date") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.endDate");
			}
			else if (${field} == "External Link Nested Field") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.externalLinks");
			}
			else if (${field} == "Original End Date") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.originalEndDate");
			}
			else if (${field} == "Perpetual") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.perpetual");
			}
			else if (${field} == "Product Field and Value") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.properties.${value1}");
			}
			else if (${field} == "Quantity") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.quantity");
			}
			else if (${field} == "Start Date") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.startDate");
			}
			else if (${field} == "Status") {
				var valueFromAPI = JSONCurlUtil.get(${curl}, "$.status");
			}
			else {
				fail("No such product purchase field ${field}");
			}

			if (${field} == "Product Field and Value") {
				echo("Expected property field ${value1} value: ${value2}");

				echo("Actual property field ${value1} value: ${valueFromAPI}");

				if (!(contains(${value2}, ${valueFromAPI}))) {
					fail("Actual property field ${value1} value ${valueFromAPI} does not match expected property field ${value1} value ${value2}");
				}
			}
			else if (contains(${field}, "Nested Field")) {
				echo("Expected Partial ${field} Value: ${value1}");

				echo("Actual ${field} Value: ${valueFromAPI}");

				if (!(contains(${valueFromAPI}, ${value1}))) {
					fail("Actual ${field} value ${valueFromAPI} does not contain the expected ${field} value ${value1}");
				}
			}
			else {
				echo("Expected ${field} Value: ${value1}");

				echo("Actual ${field} Value: ${valueFromAPI}");

				if (!(contains(${value1}, ${valueFromAPI}))) {
					fail("Actual ${field} value ${valueFromAPI} does not match expected ${field} value ${value1}");
				}
			}
		}
	}

	@prose = "I generate alternative static infinite end dates"
	macro generateAlternativeStaticInfiniteEndDates {
		var infiniteEndDay = Date.generateDay(dayDifference = 36524);
		var infiniteEndMonth = Date.generateMonth(dayDifference = 36524);
		var infiniteEndYear = DateUtil.getYear(36524);

		var staticInfiniteEndDate1 = "${infiniteEndYear}-${infiniteEndMonth}-${infiniteEndDay}T00:00:00Z";
		var infiniteEndDay = Date.generateDay(dayDifference = 36525);
		var infiniteEndMonth = Date.generateMonth(dayDifference = 36525);
		var infiniteEndYear = DateUtil.getYear(36525);

		var staticInfiniteEndDate2 = "${infiniteEndYear}-${infiniteEndMonth}-${infiniteEndDay}T00:00:00Z";

		static var staticInfiniteEndDates = "${staticInfiniteEndDate1},${staticInfiniteEndDate2}";

		return ${staticInfiniteEndDates};

		echo("Generated Alternative Infinite End Dates: ${staticInfiniteEndDates}");
	}

	@prose = "I generate static end dates"
	macro generateStaticEndDates {
		var endDay = Date.generateDay(dayDifference = 365);
		var endMonth = Date.generateMonth(dayDifference = 365);
		var endYear = DateUtil.getYear(365);

		var staticEndDate1 = "${endYear}-${endMonth}-${endDay}T00:00:00Z";
		var endDay = Date.generateDay(dayDifference = 366);
		var endMonth = Date.generateMonth(dayDifference = 366);
		var endYear = DateUtil.getYear(366);

		var staticEndDate2 = "${endYear}-${endMonth}-${endDay}T00:00:00Z";

		static var staticEndDates = "${staticEndDate1},${staticEndDate2}";

		return ${staticEndDates};

		echo("Generated Static End Dates: ${staticEndDates}");
	}

	@prose = "I generate static end dates for perpetual subscription licenses"
	macro generateStaticEndDatesForPepetualSubscriptionLicenses {
		var endDay = Date.generateDay(dayDifference = 395);
		var endMonth = Date.generateMonth(dayDifference = 395);
		var endYear = DateUtil.getYear(395);

		var staticEndDate1 = "${endYear}-${endMonth}-${endDay}T00:00:00Z";
		var endDay = Date.generateDay(dayDifference = 396);
		var endMonth = Date.generateMonth(dayDifference = 396);
		var endYear = DateUtil.getYear(396);

		var staticEndDate2 = "${endYear}-${endMonth}-${endDay}T00:00:00Z";

		static var staticEndDates = "${staticEndDate1},${staticEndDate2}";

		return ${staticEndDates};

		echo("Generated End Dates For Perpetual Subscription Licenses: ${staticEndDates}");
	}

	@prose = "I generate future static full start and end dates"
	macro generateStaticFullFutureStartAndEndDate {
		var startDay = Date.generateDay(dayDifference = 30);
		var startMonth = Date.generateFullMonth(dayDifference = 30);
		var startYear = DateUtil.getYear(30);
		var originalEndDay = Date.generateDay(dayDifference = 395);
		var originalEndMonth = Date.generateFullMonth(dayDifference = 395);
		var originalEndYear = DateUtil.getYear(395);
		var endDay = Date.generateDay(dayDifference = 425);
		var endMonth = Date.generateFullMonth(dayDifference = 425);
		var endYear = DateUtil.getYear(425);
		static var staticFullFutureStartDate = "${startMonth} ${startDay}, ${startYear}";
		static var staticFullFutureOriginalEndDate = "${originalEndMonth} ${originalEndDay}, ${originalEndYear}";
		static var staticFullFutureEndDate = "${endMonth} ${endDay}, ${endYear}";

		return ${staticFullFutureStartDate};

		return ${staticFullFutureOriginalEndDate};

		return ${staticFullFutureEndDate};

		echo("Generated Future Full Start Date: ${staticFullFutureStartDate}");

		echo("Generated Future Full Original End Date: ${staticFullFutureOriginalEndDate}");

		echo("Generated Future Full End Date: ${staticFullFutureEndDate}");
	}

	@prose = "I generate future static full non abbreviated start and end dates"
	macro generateStaticFullNonAbbreviatedFutureStartAndEndDate {
		if (${staticProjectDetailsNonAbbreviatedDateMethod} != "true") {
			var startDay = Date.generateDay(dayDifference = 30);
		}
		else {
			var startDay = DateUtil.getDate(30);
		}

		var startMonth = Date.generateFullNonAbbreviatedMonth(dayDifference = 30);
		var startYear = DateUtil.getYear(30);

		if (${staticProjectDetailsNonAbbreviatedDateMethod} != "true") {
			var originalEndDay = Date.generateDay(dayDifference = 395);
		}
		else {
			var originalEndDay = DateUtil.getDate(395);
		}

		var originalEndMonth = Date.generateFullNonAbbreviatedMonth(dayDifference = 395);
		var originalEndYear = DateUtil.getYear(395);

		if (${staticProjectDetailsNonAbbreviatedDateMethod} != "true") {
			var endDay = Date.generateDay(dayDifference = 425);
		}
		else {
			var endDay = DateUtil.getDate(425);
		}

		var endMonth = Date.generateFullNonAbbreviatedMonth(dayDifference = 425);
		var endYear = DateUtil.getYear(425);
		static var staticFullNonAbbreviatedFutureStartDate = "${startMonth} ${startDay}, ${startYear}";
		static var staticFullNonAbbreviatedFutureOriginalEndDate = "${originalEndMonth} ${originalEndDay}, ${originalEndYear}";
		static var staticFullNonAbbreviatedFutureEndDate = "${endMonth} ${endDay}, ${endYear}";

		return ${staticFullNonAbbreviatedFutureStartDate};

		return ${staticFullNonAbbreviatedFutureOriginalEndDate};

		return ${staticFullNonAbbreviatedFutureEndDate};

		echo("Generated Future Full Non Abbreviated Start Date: ${staticFullNonAbbreviatedFutureStartDate}");

		echo("Generated Future Full Non Abbreviated Original End Date: ${staticFullNonAbbreviatedFutureOriginalEndDate}");

		echo("Generated Future Full Non Abbreviated End Date: ${staticFullNonAbbreviatedFutureEndDate}");
	}

	@prose = "I generate past static full non abbreviated start and end dates"
	macro generateStaticFullNonAbbreviatedPastStartAndEndDate {
		if (${staticProjectDetailsNonAbbreviatedDateMethod} != "true") {
			var startDay = Date.generateDay(dayDifference = "-425");
		}
		else {
			var startDay = DateUtil.getDate("-425");
		}

		var startMonth = Date.generateFullNonAbbreviatedMonth(dayDifference = "-425");
		var startYear = DateUtil.getYear("-425");

		if (${staticProjectDetailsNonAbbreviatedDateMethod} != "true") {
			var originalEndDay = Date.generateDay(dayDifference = "-60");
		}
		else {
			var originalEndDay = DateUtil.getDate("-60");
		}

		var originalEndMonth = Date.generateFullNonAbbreviatedMonth(dayDifference = "-60");
		var originalEndYear = DateUtil.getYear("-60");

		if (${staticProjectDetailsNonAbbreviatedDateMethod} != "true") {
			var endDay = Date.generateDay(dayDifference = "-30");
		}
		else {
			var endDay = DateUtil.getDate("-30");
		}

		var endMonth = Date.generateFullNonAbbreviatedMonth(dayDifference = "-30");
		var endYear = DateUtil.getYear("-30");
		static var staticFullNonAbbreviatedPastStartDate = "${startMonth} ${startDay}, ${startYear}";
		static var staticFullNonAbbreviatedPastOriginalEndDate = "${originalEndMonth} ${originalEndDay}, ${originalEndYear}";
		static var staticFullNonAbbreviatedPastEndDate = "${endMonth} ${endDay}, ${endYear}";

		return ${staticFullNonAbbreviatedPastStartDate};

		return ${staticFullNonAbbreviatedPastOriginalEndDate};

		return ${staticFullNonAbbreviatedPastEndDate};

		echo("Generated Past Full Non Abbreviated Start Date: ${staticFullNonAbbreviatedPastStartDate}");

		echo("Generated Past Full Non Abbreviated Original Date: ${staticFullNonAbbreviatedPastOriginalEndDate}");

		echo("Generated Past Full Non Abbreviated End Date: ${staticFullNonAbbreviatedPastEndDate}");
	}

	@prose = "I generate static full non abbreviated start and end dates"
	macro generateStaticFullNonAbbreviatedStartAndEndDate {
		if (${staticProjectDetailsNonAbbreviatedDateMethod} != "true") {
			var startDay = Date.generateDay(dayDifference = 0);
		}
		else {
			var startDay = DateUtil.getDate(0);
		}

		var startMonth = Date.generateFullNonAbbreviatedMonth(dayDifference = 0);
		var startYear = DateUtil.getYear(0);

		if (${staticProjectDetailsNonAbbreviatedDateMethod} != "true") {
			var originalEndDay = Date.generateDay(dayDifference = 335);
		}
		else {
			var originalEndDay = DateUtil.getDate(335);
		}

		var originalEndMonth = Date.generateFullNonAbbreviatedMonth(dayDifference = 335);
		var originalEndYear = DateUtil.getYear(335);

		if (${staticProjectDetailsNonAbbreviatedDateMethod} != "true") {
			var endDay = Date.generateDay(dayDifference = 365);
		}
		else {
			var endDay = DateUtil.getDate(365);
		}

		var endMonth = Date.generateFullNonAbbreviatedMonth(dayDifference = 365);
		var endYear = DateUtil.getYear(365);
		static var staticFullNonAbbreviatedStartDate = "${startMonth} ${startDay}, ${startYear}";
		static var staticFullNonAbbreviatedOriginalEndDate = "${originalEndMonth} ${originalEndDay}, ${originalEndYear}";
		static var staticFullNonAbbreviatedEndDate = "${endMonth} ${endDay}, ${endYear}";

		return ${staticFullNonAbbreviatedStartDate};

		return ${staticFullNonAbbreviatedOriginalEndDate};

		return ${staticFullNonAbbreviatedEndDate};

		echo("Generated Full Non Abbreviated Start Date: ${staticFullNonAbbreviatedStartDate}");

		echo("Generated Full Non Abbreviated Original End Date: ${staticFullNonAbbreviatedOriginalEndDate}");

		echo("Generated Full Non Abbreviated End Date: ${staticFullNonAbbreviatedEndDate}");
	}

	@prose = "I generate past static full start and end dates"
	macro generateStaticFullPastStartAndEndDate {
		var startDay = Date.generateDay(dayDifference = "-425");
		var startMonth = Date.generateFullMonth(dayDifference = "-425");
		var startYear = DateUtil.getYear("-425");
		var originalEndDay = Date.generateDay(dayDifference = "-60");
		var originalEndMonth = Date.generateFullMonth(dayDifference = "-60");
		var originalEndYear = DateUtil.getYear("-60");
		var endDay = Date.generateDay(dayDifference = "-30");
		var endMonth = Date.generateFullMonth(dayDifference = "-30");
		var endYear = DateUtil.getYear("-30");
		static var staticFullPastStartDate = "${startMonth} ${startDay}, ${startYear}";
		static var staticFullPastOriginalEndDate = "${originalEndMonth} ${originalEndDay}, ${originalEndYear}";
		static var staticFullPastEndDate = "${endMonth} ${endDay}, ${endYear}";

		return ${staticFullPastStartDate};

		return ${staticFullPastOriginalEndDate};

		return ${staticFullPastEndDate};

		echo("Generate Past Full Start Date: ${staticFullPastStartDate}");

		echo("Generate Past Full Original End Date: ${staticFullPastOriginalEndDate}");

		echo("Generate Past Full End Date: ${staticFullPastEndDate}");
	}

	@prose = "I generate static full start and end dates"
	macro generateStaticFullStartAndEndDate {
		var startDay = Date.generateDay(dayDifference = 0);
		var startMonth = Date.generateFullMonth(dayDifference = 0);
		var startYear = DateUtil.getYear(0);
		var originalEndDay = Date.generateDay(dayDifference = 335);
		var originalEndMonth = Date.generateFullMonth(dayDifference = 335);
		var originalEndYear = DateUtil.getYear(335);
		var endDay = Date.generateDay(dayDifference = 365);
		var endMonth = Date.generateFullMonth(dayDifference = 365);
		var endYear = DateUtil.getYear(365);
		static var staticFullStartDate = "${startMonth} ${startDay}, ${startYear}";
		static var staticFullOriginalEndDate = "${originalEndMonth} ${originalEndDay}, ${originalEndYear}";
		static var staticFullEndDate = "${endMonth} ${endDay}, ${endYear}";

		return ${staticFullStartDate};

		return ${staticFullOriginalEndDate};

		return ${staticFullEndDate};

		echo("Generate Full Start Date: ${staticFullStartDate}");

		echo("Generate Full Original End Date: ${staticFullOriginalEndDate}");

		echo("Generate Full End Date: ${staticFullEndDate}");
	}

	@prose = "I generate future static start and end dates"
	macro generateStaticFutureStartAndEndDate {
		var startDay = Date.generateDay(dayDifference = 30);
		var startMonth = Date.generateMonth(dayDifference = 30);
		var startYear = DateUtil.getYear(30);
		var originalEndDay = Date.generateDay(dayDifference = 395);
		var originalEndMonth = Date.generateMonth(dayDifference = 395);
		var originalEndYear = DateUtil.getYear(395);
		var endDay = Date.generateDay(dayDifference = 425);
		var endMonth = Date.generateMonth(dayDifference = 425);
		var endYear = DateUtil.getYear(425);
		static var staticFutureStartDate = "${startYear}-${startMonth}-${startDay}T00:00:00Z";
		static var staticFutureOriginalEndDate = "${originalEndYear}-${originalEndMonth}-${originalEndDay}T00:00:00Z";
		static var staticFutureEndDate = "${endYear}-${endMonth}-${endDay}T00:00:00Z";

		return ${staticFutureStartDate};

		return ${staticFutureOriginalEndDate};

		return ${staticFutureEndDate};

		echo("Generate Future Start Date: ${staticFutureStartDate}");

		echo("Generate Future Original End Date: ${staticFutureOriginalEndDate}");

		echo("Generate Future End Date: ${staticFutureEndDate}");
	}

	@prose = "I generate static future start dates"
	macro generateStaticFutureStartDates {
		var startDay = Date.generateDay(dayDifference = 30);
		var startMonth = Date.generateMonth(dayDifference = 30);
		var startYear = DateUtil.getYear(30);

		var staticStartDate1 = "${startYear}-${startMonth}-${startDay}T00:00:00Z";
		var startDay = Date.generateDay(dayDifference = 31);
		var startMonth = Date.generateMonth(dayDifference = 31);
		var startYear = DateUtil.getYear(31);

		var staticStartDate2 = "${startYear}-${startMonth}-${startDay}T00:00:00Z";

		static var staticFutureStartDates = "${staticStartDate1},${staticStartDate2}";

		return ${staticFutureStartDates};

		echo("Generate Future Start Dates: ${staticFutureStartDates}");
	}

	@prose = "I generate static infinite end dates"
	macro generateStaticInfiniteEndDates {
		var infiniteEndDay = Date.generateDay(dayDifference = 36500);
		var infiniteEndMonth = Date.generateMonth(dayDifference = 36500);
		var infiniteEndYear = DateUtil.getYear(36500);

		var staticInfiniteEndDate1 = "${infiniteEndYear}-${infiniteEndMonth}-${infiniteEndDay}T00:00:00Z";
		var infiniteEndDay = Date.generateDay(dayDifference = 36501);
		var infiniteEndMonth = Date.generateMonth(dayDifference = 36501);
		var infiniteEndYear = DateUtil.getYear(36501);

		var staticInfiniteEndDate2 = "${infiniteEndYear}-${infiniteEndMonth}-${infiniteEndDay}T00:00:00Z";

		static var staticInfiniteEndDates = "${staticInfiniteEndDate1},${staticInfiniteEndDate2}";

		return ${staticInfiniteEndDates};

		echo("Generate Infinite End Dates: ${staticInfiniteEndDates}");
	}

	@prose = "I generate static infinite end dates for perpetual subscription licenses"
	macro generateStaticInfiniteEndDatesForPerpetualSubscriptionLicenses {
		var infiniteEndDay = Date.generateDay(dayDifference = 36500);
		var infiniteEndMonth = Date.generateMonth(dayDifference = 36500);
		var infiniteEndYear = DateUtil.getYear(36500);

		var staticInfiniteEndDate1 = "${infiniteEndYear}-${infiniteEndMonth}-${infiniteEndDay}T00:00:00Z";
		var infiniteEndDay = Date.generateDay(dayDifference = 36501);
		var infiniteEndMonth = Date.generateMonth(dayDifference = 36501);
		var infiniteEndYear = DateUtil.getYear(36501);

		var staticInfiniteEndDate2 = "${infiniteEndYear}-${infiniteEndMonth}-${infiniteEndDay}T00:00:00Z";

		static var staticInfiniteEndDates = "${staticInfiniteEndDate1},${staticInfiniteEndDate2}";

		return ${staticInfiniteEndDates};

		echo("Generate Infinite End Dates For Perpetual Subscription Licenses: ${staticInfiniteEndDates}");
	}

	@prose = "I generate future static mmddyyyy start and end dates"
	macro generateStaticMmDdYyyyFutureStartAndEndDate {
		var startDay = Date.generateDay(dayDifference = 30);
		var startMonth = Date.generateMonth(dayDifference = 30);
		var startYear = DateUtil.getYear(30);
		var originalEndDay = Date.generateDay(dayDifference = 395);
		var originalEndMonth = Date.generateMonth(dayDifference = 395);
		var originalEndYear = DateUtil.getYear(395);
		var endDay = Date.generateDay(dayDifference = 425);
		var endMonth = Date.generateMonth(dayDifference = 425);
		var endYear = DateUtil.getYear(425);
		static var staticMmDdYyyyFutureStartDate = "${startMonth}/${startDay}/${startYear}";
		static var staticMmDdYyyyFutureOriginalEndDate = "${originalEndMonth}/${originalEndDay}/${originalEndYear}";
		static var staticMmDdYyyyFutureEndDate = "${endMonth}/${endDay}/${endYear}";

		return ${staticMmDdYyyyFutureStartDate};

		return ${staticMmDdYyyyFutureOriginalEndDate};

		return ${staticMmDdYyyyFutureEndDate};

		echo("Generate Future mm/dd/yyyy Start Date: ${staticMmDdYyyyFutureStartDate}");

		echo("Generate Future mm/dd/yyyy Original End Date: ${staticMmDdYyyyFutureOriginalEndDate}");

		echo("Generate Future mm/dd/yyyy End Date: ${staticMmDdYyyyFutureEndDate}");
	}

	@prose = "I generate past static mmddyyyy start and end dates"
	macro generateStaticMmDdYyyyPastStartAndEndDate {
		var startDay = Date.generateDay(dayDifference = "-425");
		var startMonth = Date.generateMonth(dayDifference = "-425");
		var startYear = DateUtil.getYear("-425");
		var originalEndDay = Date.generateDay(dayDifference = "-60");
		var originalEndMonth = Date.generateMonth(dayDifference = "-60");
		var originalEndYear = DateUtil.getYear("-60");
		var endDay = Date.generateDay(dayDifference = "-30");
		var endMonth = Date.generateMonth(dayDifference = "-30");
		var endYear = DateUtil.getYear("-30");
		static var staticMmDdYyyyPastStartDate = "${startMonth}/${startDay}/${startYear}";
		static var staticMmDdYyyyPastOriginalEndDate = "${originalEndMonth}/${originalEndDay}/${originalEndYear}";
		static var staticMmDdYyyyPastEndDate = "${endMonth}/${endDay}/${endYear}";

		return ${staticMmDdYyyyPastStartDate};

		return ${staticMmDdYyyyPastOriginalEndDate};

		return ${staticMmDdYyyyPastEndDate};

		echo("Generate Past mm/dd/yyyy Start Date: ${staticMmDdYyyyPastStartDate}");

		echo("Generate Past mm/dd/yyyy Original End Date: ${staticMmDdYyyyPastOriginalEndDate}");

		echo("Generate Past mm/dd/yyyy End Date: ${staticMmDdYyyyPastEndDate}");
	}

	@prose = "I generate static mmddyyyy start and end dates"
	macro generateStaticMmDdYyyyStartAndEndDate {
		var startDay = Date.generateDay(dayDifference = 0);
		var startMonth = Date.generateMonth(dayDifference = 0);
		var startYear = DateUtil.getYear(0);
		var originalEndDay = Date.generateDay(dayDifference = 335);
		var originalEndMonth = Date.generateMonth(dayDifference = 335);
		var originalEndYear = DateUtil.getYear(335);
		var endDay = Date.generateDay(dayDifference = 365);
		var endMonth = Date.generateMonth(dayDifference = 365);
		var endYear = DateUtil.getYear(365);
		static var staticMmDdYyyyStartDate = "${startMonth}/${startDay}/${startYear}";
		static var staticMmDdYyyyOriginalEndDate = "${originalEndMonth}/${originalEndDay}/${originalEndYear}";
		static var staticMmDdYyyyEndDate = "${endMonth}/${endDay}/${endYear}";

		return ${staticMmDdYyyyStartDate};

		return ${staticMmDdYyyyOriginalEndDate};

		return ${staticMmDdYyyyEndDate};

		echo("Generate mm/dd/yyyy Start Date: ${staticMmDdYyyyStartDate}");

		echo("Generate mm/dd/yyyy Original End Date: ${staticMmDdYyyyOriginalEndDate}");

		echo("Generate mm/dd/yyyy End Date: ${staticMmDdYyyyEndDate}");
	}

	@prose = "I generate past static start and end dates"
	macro generateStaticPastStartAndEndDate {
		var startDay = Date.generateDay(dayDifference = "-425");
		var startMonth = Date.generateMonth(dayDifference = "-425");
		var startYear = DateUtil.getYear("-425");
		var originalEndDay = Date.generateDay(dayDifference = "-60");
		var originalEndMonth = Date.generateMonth(dayDifference = "-60");
		var originalEndYear = DateUtil.getYear("-60");
		var endDay = Date.generateDay(dayDifference = "-30");
		var endMonth = Date.generateMonth(dayDifference = "-30");
		var endYear = DateUtil.getYear("-30");
		static var staticPastStartDate = "${startYear}-${startMonth}-${startDay}T00:00:00Z";
		static var staticPastOriginalEndDate = "${originalEndYear}-${originalEndMonth}-${originalEndDay}T00:00:00Z";
		static var staticPastEndDate = "${endYear}-${endMonth}-${endDay}T00:00:00Z";

		return ${staticPastStartDate};

		return ${staticPastOriginalEndDate};

		return ${staticPastEndDate};

		echo("Generate Past Start Date: ${staticPastStartDate}");

		echo("Generate Past Original End Date: ${staticPastOriginalEndDate}");

		echo("Generate Past End Date: ${staticPastEndDate}");
	}

	@prose = "I generate static start and end dates"
	macro generateStaticStartAndEndDate {
		var startDay = Date.generateDay(dayDifference = 0);
		var startMonth = Date.generateMonth(dayDifference = 0);
		var startYear = DateUtil.getYear(0);
		var originalEndDay = Date.generateDay(dayDifference = 335);
		var originalEndMonth = Date.generateMonth(dayDifference = 335);
		var originalEndYear = DateUtil.getYear(335);
		var endDay = Date.generateDay(dayDifference = 365);
		var endMonth = Date.generateMonth(dayDifference = 365);
		var endYear = DateUtil.getYear(365);
		static var staticStartDate = "${startYear}-${startMonth}-${startDay}T00:00:00Z";
		static var staticOriginalEndDate = "${originalEndYear}-${originalEndMonth}-${originalEndDay}T00:00:00Z";
		static var staticEndDate = "${endYear}-${endMonth}-${endDay}T00:00:00Z";

		return ${staticStartDate};

		return ${staticOriginalEndDate};

		return ${staticEndDate};

		echo("Generate Start Date: ${staticStartDate}");

		echo("Generate Original End Date: ${staticOriginalEndDate}");

		echo("Generate End Date: ${staticEndDate}");
	}

	@prose = "I generate static start dates"
	macro generateStaticStartDates {
		var startDay = Date.generateDay(dayDifference = 0);
		var startMonth = Date.generateMonth(dayDifference = 0);
		var startYear = DateUtil.getYear(0);

		var staticStartDate1 = "${startYear}-${startMonth}-${startDay}T00:00:00Z";
		var startDay = Date.generateDay(dayDifference = 1);
		var startMonth = Date.generateMonth(dayDifference = 1);
		var startYear = DateUtil.getYear(1);

		var staticStartDate2 = "${startYear}-${startMonth}-${startDay}T00:00:00Z";

		static var staticStartDates = "${staticStartDate1},${staticStartDate2}";

		return ${staticStartDates};

		echo("Generate Start Dates: ${staticStartDates}");
	}

	@prose = "I generate future static yyyymmdd start and end dates"
	macro generateStaticYyyyMmDdFutureStartAndEndDate {
		var startDay = Date.generateDay(dayDifference = 30);
		var startMonth = Date.generateMonth(dayDifference = 30);
		var startYear = DateUtil.getYear(30);
		var originalEndDay = Date.generateDay(dayDifference = 395);
		var originalEndMonth = Date.generateMonth(dayDifference = 395);
		var originalEndYear = DateUtil.getYear(395);
		var endDay = Date.generateDay(dayDifference = 425);
		var endMonth = Date.generateMonth(dayDifference = 425);
		var endYear = DateUtil.getYear(425);
		static var staticYyyyMmDdFutureStartDate = "${startYear}-${startMonth}-${startDay}";
		static var staticYyyyMmDdFutureOriginalEndDate = "${originalEndYear}-${originalEndMonth}-${originalEndDay}";
		static var staticYyyyMmDdFutureEndDate = "${endYear}-${endMonth}-${endDay}";

		return ${staticYyyyMmDdFutureStartDate};

		return ${staticYyyyMmDdFutureOriginalEndDate};

		return ${staticYyyyMmDdFutureEndDate};

		echo("Generate Future yyyy-mm-dd Start Date: ${staticYyyyMmDdFutureStartDate}");

		echo("Generate Future yyyy-mm-dd Original End Date: ${staticYyyyMmDdFutureOriginalEndDate}");

		echo("Generate Future yyyy-mm-dd End Date: ${staticYyyyMmDdFutureEndDate}");
	}

	@prose = "I generate infinite static yyyymmdd end date"
	macro generateStaticYyyyMmDdInfiniteStartAndEndDate {
		var endDay = Date.generateDay(dayDifference = 36500);
		var endMonth = Date.generateMonth(dayDifference = 36500);
		var endYear = DateUtil.getYear(36500);

		static var staticYyyyMmDdInfiniteEndDate = "${endYear}-${endMonth}-${endDay}";

		return ${staticYyyyMmDdInfiniteEndDate};

		echo("Generate Infinite yyyy-mm-dd End Date: ${staticYyyyMmDdInfiniteEndDate}");
	}

	@prose = "I generate past static yyyymmdd start and end dates"
	macro generateStaticYyyyMmDdPastStartAndEndDate {
		var startDay = Date.generateDay(dayDifference = "-425");
		var startMonth = Date.generateMonth(dayDifference = "-425");
		var startYear = DateUtil.getYear("-425");
		var originalEndDay = Date.generateDay(dayDifference = "-60");
		var originalEndMonth = Date.generateMonth(dayDifference = "-60");
		var originalEndYear = DateUtil.getYear("-60");
		var endDay = Date.generateDay(dayDifference = "-30");
		var endMonth = Date.generateMonth(dayDifference = "-30");
		var endYear = DateUtil.getYear("-30");
		static var staticYyyyMmDdPastStartDate = "${startYear}-${startMonth}-${startDay}";
		static var staticYyyyMmDdPastOriginalEndDate = "${originalEndYear}-${originalEndMonth}-${originalEndDay}";
		static var staticYyyyMmDdPastEndDate = "${endYear}-${endMonth}-${endDay}";

		return ${staticYyyyMmDdPastStartDate};

		return ${staticYyyyMmDdPastOriginalEndDate};

		return ${staticYyyyMmDdPastEndDate};

		echo("Generate Past yyyy-mm-dd Start Date: ${staticYyyyMmDdPastStartDate}");

		echo("Generate Past yyyy-mm-dd Original End Date: ${staticYyyyMmDdPastOriginalEndDate}");

		echo("Generate Past yyyy-mm-dd End Date: ${staticYyyyMmDdPastEndDate}");
	}

	@prose = "I generate static yyyymmdd start and end dates"
	macro generateStaticYyyyMmDdStartAndEndDate {
		var startDay = Date.generateDay(dayDifference = 0);
		var startMonth = Date.generateMonth(dayDifference = 0);
		var startYear = DateUtil.getYear(0);
		var originalEndDay = Date.generateDay(dayDifference = 335);
		var originalEndMonth = Date.generateMonth(dayDifference = 335);
		var originalEndYear = DateUtil.getYear(335);
		var endDay = Date.generateDay(dayDifference = 365);
		var endMonth = Date.generateMonth(dayDifference = 365);
		var endYear = DateUtil.getYear(365);
		static var staticYyyyMmDdStartDate = "${startYear}-${startMonth}-${startDay}";
		static var staticYyyyMmDdOriginalEndDate = "${originalEndYear}-${originalEndMonth}-${originalEndDay}";
		static var staticYyyyMmDdEndDate = "${endYear}-${endMonth}-${endDay}";

		return ${staticYyyyMmDdStartDate};

		return ${staticYyyyMmDdOriginalEndDate};

		return ${staticYyyyMmDdEndDate};

		echo("Generate yyyy-mm-dd Start Date: ${staticYyyyMmDdStartDate}");

		echo("Generate yyyy-mm-dd Original End Date: ${staticYyyyMmDdOriginalEndDate}");

		echo("Generate yyyy-mm-dd End Date: ${staticYyyyMmDdEndDate}");
	}

	@prose = "the following product purchase json fields and their respective quantities should be present ${table}"
	macro getCurrentKoroneikiProductPurchase {
		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/product-purchases --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
		''';

		var currentKoroneikiProductPurchase = JSONCurlUtil.get(${curl}, "$.items[0]");

		JSONAPI._checkJSONFields(
			currentJson = ${currentKoroneikiProductPurchase},
			table = ${table});
	}

}