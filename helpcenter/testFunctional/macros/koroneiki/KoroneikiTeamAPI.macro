definition {

	var apiToken = PropsUtil.get("koroneiki.api.token");
	var koroneikiAdminEmail = PropsUtil.get("koroneiki.admin.email.address");
	var koroneikiAdminPassword = PropsUtil.get("koroneiki.admin.password");
	var koroneikiURL = PropsUtil.get("koroneiki.liferay.url");
	var roleName = PropsUtil.get("koroneiki.non.permission.role.name");

	@prose = "I assign the contact with the email address ${contactEmailAddress} on the team ${teamName} to the following roles ${table}"
	macro _assignContactRolesToContact {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");
		var  rawTable = TableUtil.newTable(${table});

		for (var row : table "${rawTable}") {
			var contactRoleName = "${row[0]}";

			var contactRoleKey = KoroneikiContactRoleAPI._getContactRoleKeyViaContactRoleName(contactRoleName = "${contactRoleName}");

			var curl = '''
				${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey}/contacts/by-email-address/${contactEmailAddress}/roles?contactRoleKeys=${contactRoleKey} --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
				-H "Content-Length: 0"
				''';

			com.liferay.poshi.runner.util.JSONCurlUtil.put("${curl}");
		}
	}

	@prose = "I assign the contact with the email address ${contactEmailAddress} on the team ${teamName} to the following roles executed by ${agentName} ${table}"
	macro _assignContactRolesToContactExecutedByAgent {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");
		var agentName = StringUtil.replace("${agentName}", " ", "%20");
		var  rawTable = TableUtil.newTable(${table});

		for (var row : table "${rawTable}") {
			var contactRoleName = "${row[0]}";

			var contactRoleKey = KoroneikiContactRoleAPI._getContactRoleKeyViaContactRoleName(contactRoleName = "${contactRoleName}");

			var curl = '''
				${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey}/contacts/by-email-address/${contactEmailAddress}/roles?agentName=${agentName}\&agentUID=a1b2c3d4-e5f6-g7h8-i9j1-0k11l12m13n1\&contactRoleKeys=${contactRoleKey} --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
				-H "Content-Length: 0"
				''';

			com.liferay.poshi.runner.util.JSONCurlUtil.put("${curl}");
		}
	}

	@prose = "I assign the contact with the email address ${contactEmailAddress} on the team ${teamName} to the following roles via uuid ${table}"
	macro _assignContactRolesToContactViaUuid {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");
		var contactUuid = KoroneikiContactAPI._getContactUuidViaContactEmailAddress(contactEmailAddress = "${contactEmailAddress}");
		var  rawTable = TableUtil.newTable(${table});

		for (var row : table "${rawTable}") {
			var contactRoleName = "${row[0]}";

			var contactRoleKey = KoroneikiContactRoleAPI._getContactRoleKeyViaContactRoleName(contactRoleName = "${contactRoleName}");

			var curl = '''
				${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey}/contacts/by-uuid/${contactUuid}/roles?contactRoleKeys=${contactRoleKey} --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
				-H "Content-Length: 0"
				''';

			com.liferay.poshi.runner.util.JSONCurlUtil.put("${curl}");
		}
	}

	@prose = "I assign contacts with the following email addresses to the team ${teamName} ${table}"
	macro _assignContacts {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");
		var  rawTable = TableUtil.newTable(${table});

		for (var row : table "${rawTable}") {
			var contactEmailAddress = "${row[0]}";

			var curl = '''
				${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey}/contacts/by-email-address?emailAddresses=${contactEmailAddress} --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
				-H "Content-Length: 0"
				''';

			com.liferay.poshi.runner.util.JSONCurlUtil.put("${curl}");
		}
	}

	@prose = "I assign contacts with the following email addresses to the team ${teamName} executed by ${agentName} ${table}"
	macro _assignContactsExecutedByAgent {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");
		var agentName = StringUtil.replace("${agentName}", " ", "%20");
		var  rawTable = TableUtil.newTable(${table});

		for (var row : table "${rawTable}") {
			var contactEmailAddress = "${row[0]}";

			var curl = '''
				${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey}/contacts/by-email-address?agentName=${agentName}\&agentUID=a1b2c3d4-e5f6-g7h8-i9j1-0k11l12m13n1\&emailAddresses=${contactEmailAddress} --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
				-H "Content-Length: 0"
				''';

			com.liferay.poshi.runner.util.JSONCurlUtil.put("${curl}");
		}
	}

	@prose = "I assign contacts with the following email addresses to the team ${teamName} via uuid ${table}"
	macro _assignContactsViaUuid {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");
		var  rawTable = TableUtil.newTable(${table});

		for (var row : table "${rawTable}") {
			var contactEmailAddress = "${row[0]}";

			var contactUuid = KoroneikiContactAPI._getContactUuidViaContactEmailAddress(contactEmailAddress = "${contactEmailAddress}");

			var curl = '''
				${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey}/contacts/by-uuid?contactUuids=${contactUuid} --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
				-H "Content-Length: 0"
				''';

			com.liferay.poshi.runner.util.JSONCurlUtil.put("${curl}");
		}
	}

	@prose = "I create an external link on a team ${teamName} with the following values ${table}"
	macro _createTeamExternalLink {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");
		var  rawTable = TableUtil.newTable(${table});

		for (var row : table "${rawTable}") {
			var field = "${row[0]}";
			var value = "${row[1]}";

			if ("${field}" == "Agent Name") {
				var value = StringUtil.replace("${value}", " ", "%20");

				var agentName = "${value}";
			}
			else if ("${field}" == "Domain") {
				var domain = "${value}";
			}
			else if ("${field}" == "Entity Id") {
				var entityId = "${value}";
			}
			else if ("${field}" == "Entity Name") {
				var entityName = "${value}";
			}
			else {
				fail("No such external link field ${field}");
			}
		}

		if (!(isSet(agentName))) {
			var agentName = "";
		}

		if (!(isSet(domain))) {
			var domain = "";
		}

		if (!(isSet(entityId))) {
			var entityId = "";
		}

		if (!(isSet(entityName))) {
			var entityName = "";
		}

		if ("${agentName}" == "") {
			var curl = '''
				${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey}/external-links --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
				-d
				{
					"domain": "${domain}",
					"entityId": "${entityId}",
					"entityName": "${entityName}"
				}
				''';
		}
		else {
			var curl = '''
				${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey}/external-links?agentName=${agentName}\&agentUID=a1b2c3d4-e5f6-g7h8-i9j1-0k11l12m13n1 --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
				-d
				{
					"domain": "${domain}",
					"entityId": "${entityId}",
					"entityName": "${entityName}"
				}
				''';
		}

		com.liferay.poshi.runner.util.JSONCurlUtil.post("${curl}");
	}

	@prose = "I create a nested field for the team external link with the following values ${table}"
	macro _createTeamExternalLinkNestedField {
		var  rawTable = TableUtil.newTable(${table});

		for (var row : table "${rawTable}") {
			var field = "${row[0]}";
			var value = "${row[1]}";

			if ("${field}" == "Domain") {
				var domain = "${value}";
			}
			else if ("${field}" == "Entity Id") {
				var entityId = "${value}";
			}
			else if ("${field}" == "Entity Name") {
				var entityName = "${value}";
			}
			else if ("${field}" == "URL") {
				var url = "${value}";
			}
			else {
				fail("No such external link field ${field}");
			}
		}

		if (!(isSet(domain))) {
			var domain = "";
		}

		if (!(isSet(entityId))) {
			var entityId = "";
		}

		if (!(isSet(entityName))) {
			var entityName = "";
		}

		if (!(isSet(url))) {
			var url = "";
		}

		static var staticTeamExternalLinksNestedField = "{&quot;domain&quot;:&quot;${domain}&quot;,&quot;entityId&quot;:&quot;${entityId}&quot;,&quot;entityName&quot;:&quot;${entityName}&quot;,&quot;url&quot;:&quot;${url}&quot;}";

		return "${staticTeamExternalLinksNestedField}";
	}

	@prose = "I delete a koroneiki team with the name ${teamName}"
	macro _deleteTeam {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey} --tlsv1.2
			--header "API_Token: ${apiToken}"
			-H "accept: application/json"
			-H "Content-Type: application/json"
			''';

		com.liferay.poshi.runner.util.JSONCurlUtil.delete("${curl}");
	}

	@prose = "I attempt to delete a koroneiki team with the name ${teamName}"
	macro _deleteTeamAttempt {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey} --tlsv1.2
			--header "API_Token: ${apiToken}"
			-H "accept: application/json"
			-H "Content-Type: application/json"
			''';

		com.liferay.poshi.runner.util.JSONCurlUtil.delete("${curl}");
	}

	@prose = "I delete a koroneiki team with the name ${teamName} executed by ${agentName}"
	macro _deleteTeamExecutedByAgent {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");
		var agentName = StringUtil.replace("${agentName}", " ", "%20");

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey}?agentName=${agentName}\&agentUID=a1b2c3d4-e5f6-g7h8-i9j1-0k11l12m13n1 --tlsv1.2
			--header "API_Token: ${apiToken}"
			-H "accept: application/json"
			-H "Content-Type: application/json"
			''';

		com.liferay.poshi.runner.util.JSONCurlUtil.delete("${curl}");
	}

	@prose = "I delete an external link with the entity id ${entityId} on the team ${teamName}"
	macro _deleteTeamExternalLink {
		var externalLinkKey = KoroneikiTeamAPI._getTeamExternalLinkKey(
			entityId = "${entityId}",
			teamName = "${teamName}");

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/external-links/${externalLinkKey} --tlsv1.2
			--header "API_Token: ${apiToken}"
			-H "accept: application/json"
			-H "Content-Type: application/json"
			''';

		com.liferay.poshi.runner.util.JSONCurlUtil.delete("${curl}");
	}

	@prose = "I delete an external link with the entity id ${entityId} on the team ${teamName} executed by ${agentName}"
	macro _deleteTeamExternalLinkExecutedByAgent {
		var externalLinkKey = KoroneikiTeamAPI._getTeamExternalLinkKey(
			entityId = "${entityId}",
			teamName = "${teamName}");
		var agentName = StringUtil.replace("${agentName}", " ", "%20");

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/external-links/${externalLinkKey}?agentName=${agentName}\&agentUID=a1b2c3d4-e5f6-g7h8-i9j1-0k11l12m13n1 --tlsv1.2
			--header "API_Token: ${apiToken}"
			-H "accept: application/json"
			-H "Content-Type: application/json"
			''';

		com.liferay.poshi.runner.util.JSONCurlUtil.delete("${curl}");
	}

	macro _getTeamAuditEntryKey {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");
		var field = StringUtil.lowerCaseFirstLetter("${field}");

		var field = StringUtil.replace("${field}", " ", "");
		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey}/audit-entries?pageSize=100 --tlsv1.2
			--header "API_Token: ${apiToken}"
			-H "accept: application/json"
			-H "Content-Type: application/json"
			''';

		if (("${oldValue}" != "") && ("${newValue}" != "")) {
			var auditEntryKey = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.oldValue=='${oldValue}' && @.newValue=='${newValue}')].key");
		}
		else if ("${newValue}" != "") {
			var auditEntryKey = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.newValue=='${newValue}')].key");
		}
		else {
			var auditEntryKey = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.oldValue=='${oldValue}')].key");
		}

		var auditEntryKey = StringUtil.replace("${auditEntryKey}", "[", "");

		var auditEntryKey = StringUtil.replace("${auditEntryKey}", "]", "");

		var auditEntryKey = StringUtil.replace("${auditEntryKey}", "\"", "");

		echo("Audit entry key value: ${auditEntryKey}");

		return "${auditEntryKey}";
	}

	macro _getTeamAuditEntryKeyRegardingAdditionalEntities {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey}/audit-entries?pageSize=100 --tlsv1.2
			--header "API_Token: ${apiToken}"
			-H "accept: application/json"
			-H "Content-Type: application/json"
			''';

		if (("${oldValue}" != "") && ("${newValue}" != "")) {
			var auditEntryKey = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.description=='${description}' && @.oldValue=='${oldValue}' && @.newValue=='${newValue}')].key");
		}
		else if ("${newValue}" != "") {
			var auditEntryKey = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.description=='${description}' && @.newValue=='${newValue}')].key");
		}
		else {
			var auditEntryKey = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.description=='${description}' && @.oldValue=='${oldValue}')].key");
		}

		var auditEntryKey = StringUtil.replace("${auditEntryKey}", "[", "");

		var auditEntryKey = StringUtil.replace("${auditEntryKey}", "]", "");

		var auditEntryKey = StringUtil.replace("${auditEntryKey}", "\"", "");

		echo("Audit entry key value: ${auditEntryKey}");

		return "${auditEntryKey}";
	}

	macro _getTeamExternalLinkKey {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey}/external-links --tlsv1.2
			--header "API_Token: ${apiToken}"
			-H "accept: application/json"
			-H "Content-Type: application/json"
			''';

		var externalLinkKey = JSONCurlUtil.get("${curl}", "$.items[?(@.entityId=='${entityId}')].key");

		var externalLinkKey = StringUtil.replace("${externalLinkKey}", "[", "");

		var externalLinkKey = StringUtil.replace("${externalLinkKey}", "]", "");

		var externalLinkKey = StringUtil.replace("${externalLinkKey}", "\"", "");

		echo("External link key value for ${entityId} for team ${teamName}: ${externalLinkKey}");

		return "${externalLinkKey}";
	}

	macro _getTeamKeyViaTeamName {
		if (contains("${teamName}", " ")) {
			var keyword = StringUtil.replace("${teamName}", " ", "+");
		}
		else {
			var keyword = "${teamName}";
		}

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/teams?filter="name%20eq%20'${keyword}'" --tlsv1.2
			--header "API_Token: ${apiToken}"
			-H "accept: application/json"
			-H "Content-Type: application/json"
			''';

		var teamKey = JSONCurlUtil.get("${curl}", "$.items[?(@.name=='${teamName}')].key");

		var teamKey = StringUtil.replace("${teamKey}", "[", "");

		var teamKey = StringUtil.replace("${teamKey}", "]", "");

		var teamKey = StringUtil.replace("${teamKey}", "\"", "");

		echo("Team key value for ${teamName}: ${teamKey}");

		return "${teamKey}";
	}

	@prose = "I remove the contact with the email address ${contactEmailAddress} on the team ${teamName} from the following roles ${table}"
	macro _removeContactRolesFromContact {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");
		var  rawTable = TableUtil.newTable(${table});

		for (var row : table "${rawTable}") {
			var contactRoleName = "${row[0]}";

			var contactRoleKey = KoroneikiContactRoleAPI._getContactRoleKeyViaContactRoleName(contactRoleName = "${contactRoleName}");

			var curl = '''
				${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey}/contacts/by-email-address/${contactEmailAddress}/roles?contactRoleKeys=${contactRoleKey} --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
				-H "Content-Length: 0"
				''';

			com.liferay.poshi.runner.util.JSONCurlUtil.delete("${curl}");
		}
	}

	@prose = "I remove the contact with the email address ${contactEmailAddress} on the team ${teamName} from the following roles executed by ${agentName} ${table}"
	macro _removeContactRolesFromContactExecutedByAgent {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");
		var agentName = StringUtil.replace("${agentName}", " ", "%20");
		var  rawTable = TableUtil.newTable(${table});

		for (var row : table "${rawTable}") {
			var contactRoleName = "${row[0]}";

			var contactRoleKey = KoroneikiContactRoleAPI._getContactRoleKeyViaContactRoleName(contactRoleName = "${contactRoleName}");

			var curl = '''
				${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey}/contacts/by-email-address/${contactEmailAddress}/roles?agentName=${agentName}\&agentUID=a1b2c3d4-e5f6-g7h8-i9j1-0k11l12m13n1\&contactRoleKeys=${contactRoleKey} --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
				-H "Content-Length: 0"
				''';

			com.liferay.poshi.runner.util.JSONCurlUtil.delete("${curl}");
		}
	}

	@prose = "I remove the contact with the email address ${contactEmailAddress} on the team ${teamName} from the following roles via uuid ${table}"
	macro _removeContactRolesFromContactViaUuid {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");
		var contactUuid = KoroneikiContactAPI._getContactUuidViaContactEmailAddress(contactEmailAddress = "${contactEmailAddress}");
		var  rawTable = TableUtil.newTable(${table});

		for (var row : table "${rawTable}") {
			var contactRoleName = "${row[0]}";

			var contactRoleKey = KoroneikiContactRoleAPI._getContactRoleKeyViaContactRoleName(contactRoleName = "${contactRoleName}");

			var curl = '''
				${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey}/contacts/by-uuid/${contactUuid}/roles?contactRoleKeys=${contactRoleKey} --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
				-H "Content-Length: 0"
				''';

			com.liferay.poshi.runner.util.JSONCurlUtil.delete("${curl}");
		}
	}

	@prose = "I remove contacts with the following email addresses from the team ${teamName} ${table}"
	macro _removeContacts {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");
		var  rawTable = TableUtil.newTable(${table});

		for (var row : table "${rawTable}") {
			var contactEmailAddress = "${row[0]}";

			var curl = '''
				${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey}/contacts/by-email-address?emailAddresses=${contactEmailAddress} --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
				-H "Content-Length: 0"
				''';

			com.liferay.poshi.runner.util.JSONCurlUtil.delete("${curl}");
		}
	}

	@prose = "I remove contacts with the following email addresses from the team ${teamName} executed by ${agentName} ${table}"
	macro _removeContactsExecutedByAgent {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");
		var agentName = StringUtil.replace("${agentName}", " ", "%20");
		var  rawTable = TableUtil.newTable(${table});

		for (var row : table "${rawTable}") {
			var contactEmailAddress = "${row[0]}";

			var curl = '''
				${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey}/contacts/by-email-address?agentName=${agentName}\&agentUID=a1b2c3d4-e5f6-g7h8-i9j1-0k11l12m13n1\&emailAddresses=${contactEmailAddress} --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
				-H "Content-Length: 0"
				''';

			com.liferay.poshi.runner.util.JSONCurlUtil.delete("${curl}");
		}
	}

	@prose = "I remove contacts with the following email addresses from the team ${teamName} via uuid ${table}"
	macro _removeContactsViaUuid {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");
		var  rawTable = TableUtil.newTable(${table});

		for (var row : table "${rawTable}") {
			var contactEmailAddress = "${row[0]}";

			var contactUuid = KoroneikiContactAPI._getContactUuidViaContactEmailAddress(contactEmailAddress = "${contactEmailAddress}");

			var curl = '''
				${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey}/contacts/by-uuid?contactUuids=${contactUuid} --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
				-H "Content-Length: 0"
				''';

			com.liferay.poshi.runner.util.JSONCurlUtil.delete("${curl}");
		}
	}

	@prose = "I set the permissions for a team with the name ${teamName} to have the following values for a non permissioned user ${table}"
	macro _setTeamPermissions {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");
		var  rawTable = TableUtil.newTable(${table});

		for (var row : table "${rawTable}") {
			var field = "${row[0]}";
			var value = "${row[1]}";

			if ("${field}" == "Assign Contact") {
				var value = StringUtil.lowerCase("${value}");

				var assignContact = "${value}";
			}
			else if ("${field}" == "Delete") {
				var value = StringUtil.lowerCase("${value}");

				var delete = "${value}";
			}
			else if ("${field}" == "Permissions") {
				var value = StringUtil.lowerCase("${value}");

				var permissions = "${value}";
			}
			else if ("${field}" == "Update") {
				var value = StringUtil.lowerCase("${value}");

				var update = "${value}";
			}
			else if ("${field}" == "View") {
				var value = StringUtil.lowerCase("${value}");

				var view = "${value}";
			}
			else {
				fail("No such permission field ${field}");
			}
		}

		if (!(isSet(assignContact))) {
			var assignContact = "false";
		}

		if (!(isSet(delete))) {
			var delete = "false";
		}

		if (!(isSet(permissions))) {
			var permissions = "false";
		}

		if (!(isSet(update))) {
			var update = "false";
		}

		if (!(isSet(view))) {
			var view = "false";
		}

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey}/team-permissions --tlsv1.2
			--header "API_Token: ${apiToken}"
			-H "accept: application/json"
			-H "Content-Type: application/json"
			-d
			{
				"assignContact": "${assignContact}",
				"delete": "${delete}",
				"permissions": "${permissions}",
				"roleNames": [
					"${roleName}"
				],
				"update": "${update}",
				"view": "${view}"
			}
			''';

		com.liferay.poshi.runner.util.JSONCurlUtil.put("${curl}");
	}

	@prose = "I tear down the external links for the team ${teamName} on koroneiki with the following entity id values ${table}"
	macro _tearDownTeamExternalLinks {
		var  rawTable = TableUtil.newTable(${table});

		for (var row : table "${rawTable}") {
			var entityId = "${row[0]}";

			if (contains("${teamName}", " ")) {
				var keyword = StringUtil.replace("${teamName}", " ", "+");
			}
			else {
				var keyword = "${teamName}";
			}

			var curl = '''
				${koroneikiURL}/o/koroneiki-rest/v1.0/teams?filter="name%20eq%20'${keyword}'" --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
				''';

			var allTeams = JSONCurlUtil.get("${curl}", "$");

			if (contains("${allTeams}", "${teamName}")) {
				var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");

				var curl = '''
					${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey}/external-links --tlsv1.2
					--header "API_Token: ${apiToken}"
					-H "accept: application/json"
					-H "Content-Type: application/json"
					''';

				var allTeamExternalLinks = JSONCurlUtil.get("${curl}", "$");

				if (contains("${allTeamExternalLinks}", "${entityId}")) {
					KoroneikiTeamAPI._deleteTeamExternalLink(
						entityId = "${entityId}",
						teamName = "${teamName}");

					echo("Entity ID of external link for ${teamName} deleted: ${entityId}");
				}
			}
		}
	}

	@prose = "I tear down the permissions for a koroneiki team with the name ${teamName} set for a non permissioned user"
	macro _tearDownTeamPermissions {
		if (contains("${teamName}", " ")) {
			var keyword = StringUtil.replace("${teamName}", " ", "+");
		}
		else {
			var keyword = "${teamName}";
		}

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/teams?filter="name%20eq%20'${keyword}'" --tlsv1.2
			--header "API_Token: ${apiToken}"
			-H "accept: application/json"
			-H "Content-Type: application/json"
			''';

		var allTeams = JSONCurlUtil.get("${curl}", "$");

		if (contains("${allTeams}", "${teamName}")) {
			var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");

			var curl = '''
				${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey}/team-permissions --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
				-d
				{
					"assignContact": "true",
					"delete": "true",
					"permissions": "true",
					"roleNames": [
						"${roleName}"
					],
					"update": "true",
					"view": "true"
				}
				''';

			com.liferay.poshi.runner.util.JSONCurlUtil.delete("${curl}");
		}
	}

	@prose = "I tear down the teams on koroneiki with the following names ${table}"
	macro _tearDownTeams {
		var  rawTable = TableUtil.newTable(${table});

		for (var row : table "${rawTable}") {
			var teamName = "${row[0]}";

			if (contains("${teamName}", " ")) {
				var keyword = StringUtil.replace("${teamName}", " ", "+");
			}
			else {
				var keyword = "${teamName}";
			}

			var curl = '''
				${koroneikiURL}/o/koroneiki-rest/v1.0/teams?filter="name%20eq%20'${keyword}'" --tlsv1.2
				--header "API_Token: ${apiToken}"
				-H "accept: application/json"
				-H "Content-Type: application/json"
				''';

			var allTeams = JSONCurlUtil.get("${curl}", "$");

			if (contains("${allTeams}", "${teamName}")) {
				KoroneikiTeamAPI._deleteTeam(teamName = "${teamName}");

				echo("Name of team deleted: ${teamName}");
			}
		}
	}

	@prose = "I unset the following permissions for a team with the name ${teamName} for a permissioned user ${table}"
	macro _unsetTeamPermissions {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");
		var  rawTable = TableUtil.newTable(${table});

		for (var row : table "${rawTable}") {
			var field = "${row[0]}";
			var value = "${row[1]}";

			if ("${field}" == "Assign Contact") {
				var value = StringUtil.lowerCase("${value}");

				var assignContact = "${value}";
			}
			else if ("${field}" == "Delete") {
				var value = StringUtil.lowerCase("${value}");

				var delete = "${value}";
			}
			else if ("${field}" == "Permissions") {
				var value = StringUtil.lowerCase("${value}");

				var permissions = "${value}";
			}
			else if ("${field}" == "Update") {
				var value = StringUtil.lowerCase("${value}");

				var update = "${value}";
			}
			else if ("${field}" == "View") {
				var value = StringUtil.lowerCase("${value}");

				var view = "${value}";
			}
			else {
				fail("No such permission field ${field}");
			}
		}

		if (!(isSet(assignContact))) {
			var assignContact = "false";
		}

		if (!(isSet(delete))) {
			var delete = "false";
		}

		if (!(isSet(permissions))) {
			var permissions = "false";
		}

		if (!(isSet(update))) {
			var update = "false";
		}

		if (!(isSet(view))) {
			var view = "false";
		}

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey}/team-permissions --tlsv1.2
			--header "API_Token: ${apiToken}"
			-H "accept: application/json"
			-H "Content-Type: application/json"
			-d
			{
				"assignContact": "${assignContact}",
				"delete": "${delete}",
				"permissions": "${permissions}",
				"roleNames": [
					"${roleName}"
				],
				"update": "${update}",
				"view": "${view}"
			}
			''';

		com.liferay.poshi.runner.util.JSONCurlUtil.delete("${curl}");
	}

	@prose = "the contact with the email address ${contactEmailAddress} should be assigned the team ${teamName}"
	macro _viewContact {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey}/contacts --tlsv1.2
			--header "API_Token: ${apiToken}"
			-H "accept: application/json"
			-H "Content-Type: application/json"
			''';

		var contactEmailAddressFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.emailAddress=='${contactEmailAddress}')].emailAddress");

		var contactEmailAddressFromAPI = StringUtil.replace("${contactEmailAddressFromAPI}", "[", "");

		var contactEmailAddressFromAPI = StringUtil.replace("${contactEmailAddressFromAPI}", "]", "");

		var contactEmailAddressFromAPI = StringUtil.replace("${contactEmailAddressFromAPI}", "\"", "");

		echo("Expected contact email address value: ${contactEmailAddress}");

		echo("Actual contact email address value: ${contactEmailAddressFromAPI}");

		if ("${contactEmailAddressFromAPI}" != "${contactEmailAddress}") {
			fail("Actual contact email address value ${contactEmailAddressFromAPI} does not match expected contact email address value ${contactEmailAddress}");
		}
	}

	@prose = "the contact with the email address ${contactEmailAddress} assigned the team ${teamName} should have the contact role ${contactRoleName}"
	macro _viewContactRole {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey}/contacts/by-email-address/${contactEmailAddress}/roles --tlsv1.2
			--header "API_Token: ${apiToken}"
			-H "accept: application/json"
			-H "Content-Type: application/json"
			''';

		var contactRoleNameFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.name=='${contactRoleName}')].name");

		var contactRoleNameFromAPI = StringUtil.replace("${contactRoleNameFromAPI}", "[", "");

		var contactRoleNameFromAPI = StringUtil.replace("${contactRoleNameFromAPI}", "]", "");

		var contactRoleNameFromAPI = StringUtil.replace("${contactRoleNameFromAPI}", "\"", "");

		echo("Expected contact role name value: ${contactRoleName}");

		echo("Actual contact role name value: ${contactRoleNameFromAPI}");

		if ("${contactRoleNameFromAPI}" != "${contactRoleName}") {
			fail("Actual contact role name value ${contactRoleNameFromAPI} does not match expected contact role name value ${contactRoleName}");
		}
	}

	@prose = "the contact with the email address ${contactEmailAddress} assigned the team ${teamName} should have the contact role ${contactRoleName} visible via uuid"
	macro _viewContactRoleViaContactUuid {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");
		var contactUuid = KoroneikiContactAPI._getContactUuidViaContactEmailAddress(contactEmailAddress = "${contactEmailAddress}");

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey}/contacts/by-uuid/${contactUuid}/roles --tlsv1.2
			--header "API_Token: ${apiToken}"
			-H "accept: application/json"
			-H "Content-Type: application/json"
			''';

		var contactRoleNameFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.name=='${contactRoleName}')].name");

		var contactRoleNameFromAPI = StringUtil.replace("${contactRoleNameFromAPI}", "[", "");

		var contactRoleNameFromAPI = StringUtil.replace("${contactRoleNameFromAPI}", "]", "");

		var contactRoleNameFromAPI = StringUtil.replace("${contactRoleNameFromAPI}", "\"", "");

		echo("Expected contact role name value: ${contactRoleName}");

		echo("Actual contact role name value: ${contactRoleNameFromAPI}");

		if ("${contactRoleNameFromAPI}" != "${contactRoleName}") {
			fail("Actual contact role name value ${contactRoleNameFromAPI} does not match expected contact role name value ${contactRoleName}");
		}
	}

	@prose = "the contact with the email address ${contactEmailAddress} should be visible via nested field on the team ${teamName}"
	macro _viewContactViaNestedField {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey}?nestedFields=contacts --tlsv1.2
			--header "API_Token: ${apiToken}"
			-H "accept: application/json"
			-H "Content-Type: application/json"
			''';

		var contactEmailAddressFromAPI = JSONCurlUtil.get("${curl}", "$.contacts[?(@.emailAddress=='${contactEmailAddress}')].emailAddress");

		var contactEmailAddressFromAPI = StringUtil.replace("${contactEmailAddressFromAPI}", "[", "");

		var contactEmailAddressFromAPI = StringUtil.replace("${contactEmailAddressFromAPI}", "]", "");

		var contactEmailAddressFromAPI = StringUtil.replace("${contactEmailAddressFromAPI}", "\"", "");

		echo("Expected contact email address value: ${contactEmailAddress}");

		echo("Actual contact email address value: ${contactEmailAddressFromAPI}");

		if ("${contactEmailAddressFromAPI}" != "${contactEmailAddress}") {
			fail("Actual contact email address value ${contactEmailAddressFromAPI} does not match expected contact email address value ${contactEmailAddress}");
		}
	}

	@prose = "the contact with the email address ${contactEmailAddress} should not be assigned the team ${teamName}"
	macro _viewNoContact {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey}/contacts --tlsv1.2
			--header "API_Token: ${apiToken}"
			-H "accept: application/json"
			-H "Content-Type: application/json"
			''';

		var allTeamContacts = JSONCurlUtil.get("${curl}", "$");

		if (contains("${allTeamContacts}", "${contactEmailAddress}")) {
			fail("The team with the name ${teamName} contains the contact ${contactEmailAddress}");
		}
	}

	@prose = "the contact with the email address ${contactEmailAddress} assigned the team ${teamName} should not have the contact role ${contactRoleName}"
	macro _viewNoContactRole {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey}/contacts/by-email-address/${contactEmailAddress}/roles --tlsv1.2
			--header "API_Token: ${apiToken}"
			-H "accept: application/json"
			-H "Content-Type: application/json"
			''';

		var allTeamContactContactRoles = JSONCurlUtil.get("${curl}", "$");

		if (contains("${allTeamContactContactRoles}", "${contactRoleName}")) {
			fail("The contact with the email address ${contactEmailAddress} on the team with the name ${teamName} contains the contact role ${contactRoleName}");
		}
	}

	@prose = "the contact with the email address ${contactEmailAddress} assigned the team ${teamName} should not have the contact role ${contactRoleName} visible via uuid"
	macro _viewNoContactRoleViaContactUuid {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");
		var contactUuid = KoroneikiContactAPI._getContactUuidViaContactEmailAddress(contactEmailAddress = "${contactEmailAddress}");

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey}/contacts/by-uuid/${contactUuid}/roles --tlsv1.2
			--header "API_Token: ${apiToken}"
			-H "accept: application/json"
			-H "Content-Type: application/json"
			''';

		var allTeamContactContactRoles = JSONCurlUtil.get("${curl}", "$");

		if (contains("${allTeamContactContactRoles}", "${contactRoleName}")) {
			fail("The contact with the email address ${contactEmailAddress} on the team with the name ${teamName} contains the contact role ${contactRoleName}");
		}
	}

	@prose = "the contact with the email address ${contactEmailAddress} should not be visible via nested field on the team ${teamName}"
	macro _viewNoContactViaNestedField {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey}?nestedFields=contacts --tlsv1.2
			--header "API_Token: ${apiToken}"
			-H "accept: application/json"
			-H "Content-Type: application/json"
			''';

		var allTeamFields = JSONCurlUtil.get("${curl}", "$");

		if (contains("${allTeamFields}", "contacts")) {
			var allTeamContacts = JSONCurlUtil.get("${curl}", "$.contacts");

			if (contains("${allTeamContacts}", "${contactEmailAddress}")) {
				fail("The team with the name ${teamName} contains the contact ${contactEmailAddress}");
			}
		}
		else {
			echo("No contacts present on ${teamName} and therefore there is no contacts nested field visible");
		}
	}

	@prose = "I should not be able to view a koroneiki team with the name ${teamName}"
	macro _viewNoTeam {
		if (contains("${teamName}", " ")) {
			var keyword = StringUtil.replace("${teamName}", " ", "+");
		}
		else {
			var keyword = "${teamName}";
		}

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/teams?filter="name%20eq%20'${keyword}'" --tlsv1.2
			--header "API_Token: ${apiToken}"
			-H "accept: application/json"
			-H "Content-Type: application/json"
			''';

		var allTeams = JSONCurlUtil.get("${curl}", "$");

		if (contains("${allTeams}", "${teamName}")) {
			fail("The team with the name ${teamName} is still present in Koroneiki");
		}
	}

	@prose = "I should not be able to view an assigned account with the name ${accountName} for the team ${teamName}"
	macro _viewNoTeamAssignedAccount {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey}/assigned-accounts --tlsv1.2
			--header "API_Token: ${apiToken}"
			-H "accept: application/json"
			-H "Content-Type: application/json"
			''';

		var allTeamAccounts = JSONCurlUtil.get("${curl}", "$");

		if (contains("${allTeamAccounts}", "${accountName}")) {
			fail("The account with the name ${accountName} is still present on the Koroneiki team ${teamName}");
		}
	}

	@prose = "the external link with the entity id ${entityId} should not be present on the team ${teamName}"
	macro _viewNoTeamExternalLink {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey}/external-links --tlsv1.2
			--header "API_Token: ${apiToken}"
			-H "accept: application/json"
			-H "Content-Type: application/json"
			''';

		var allTeamExternalLinks = JSONCurlUtil.get("${curl}", "$");

		if (contains("${allTeamExternalLinks}", "${entityId}")) {
			fail("The external link with the entity id ${entityId} is on the team ${teamName}");
		}
	}

	@prose = "I should be able to view a koroneiki team with the name ${teamName}"
	macro _viewTeam {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey} --tlsv1.2
			--header "API_Token: ${apiToken}"
			-H "accept: application/json"
			-H "Content-Type: application/json"
			''';

		var teamNameFromAPI = JSONCurlUtil.get("${curl}", "$.name");

		echo("Expected Name Value: ${teamName}");

		echo("Actual Name Value: ${teamNameFromAPI}");

		if (!(contains("${teamName}", "${teamNameFromAPI}"))) {
			fail("Actual team name value ${teamNameFromAPI} does not match expected team name value ${teamName}");
		}
	}

	@prose = "I should be able to view an assigned account with the name ${accountName} for the team ${teamName} and the following values ${table}"
	macro _viewTeamAssignedAccount {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey}/assigned-accounts --tlsv1.2
			--header "API_Token: ${apiToken}"
			-H "accept: application/json"
			-H "Content-Type: application/json"
			''';
		var  rawTable = TableUtil.newTable(${table});

		for (var row : table "${rawTable}") {
			var field = "${row[0]}";
			var value = "${row[1]}";

			if ("${field}" == "Code") {
				var valueFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.name=='${accountName}')].code");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "[", "");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "]", "");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "\"", "");
			}
			else if ("${field}" == "Contact Email Address") {
				var valueFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.name=='${accountName}')].contactEmailAddress");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "[", "");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "]", "");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "\"", "");
			}
			else if ("${field}" == "Data Region") {
				var valueFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.name=='${accountName}')].dataRegion");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "[", "");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "]", "");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "\"", "");
			}
			else if ("${field}" == "Description") {
				var valueFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.name=='${accountName}')].description");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "[", "");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "]", "");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "\"", "");
			}
			else if ("${field}" == "Fax Number") {
				var valueFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.name=='${accountName}')].faxNumber");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "[", "");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "]", "");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "\"", "");
			}
			else if ("${field}" == "Name") {
				var valueFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.name=='${accountName}')].name");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "[", "");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "]", "");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "\"", "");
			}
			else if ("${field}" == "Phone Number") {
				var valueFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.name=='${accountName}')].phoneNumber");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "[", "");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "]", "");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "\"", "");
			}
			else if ("${field}" == "Profile Email Address") {
				var valueFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.name=='${accountName}')].profileEmailAddress");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "[", "");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "]", "");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "\"", "");
			}
			else if ("${field}" == "Region") {
				var valueFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.name=='${accountName}')].region");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "[", "");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "]", "");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "\"", "");
			}
			else if ("${field}" == "Status") {
				var valueFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.name=='${accountName}')].status");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "[", "");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "]", "");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "\"", "");
			}
			else if ("${field}" == "Tier") {
				var valueFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.name=='${accountName}')].tier");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "[", "");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "]", "");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "\"", "");
			}
			else if ("${field}" == "Website") {
				var valueFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.name=='${accountName}')].website");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "[", "");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "]", "");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "\"", "");
			}
			else {
				fail("No such account field ${field}");
			}

			echo("Expected ${field} Value: ${value}");

			echo("Actual ${field} Value: ${valueFromAPI}");

			if (!(contains("${value}", "${valueFromAPI}"))) {
				fail("Actual ${field} value ${valueFromAPI} does not match expected ${field} value ${value}");
			}
		}
	}

	@prose = "I should be able to view an audit entry for the summary ${summary} related to the team ${teamName} field ${field} from the old value ${oldValue} to the new value ${newValue} executed by ${agentName}"
	macro _viewTeamAuditEntry {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");
		var field = StringUtil.lowerCaseFirstLetter("${field}");

		var field = StringUtil.replace("${field}", " ", "");
		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey}/audit-entries?pageSize=100 --tlsv1.2
			--header "API_Token: ${apiToken}"
			-H "accept: application/json"
			-H "Content-Type: application/json"
			''';

		if (("${oldValue}" != "") && ("${newValue}" != "")) {
			var summaryFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.oldValue=='${oldValue}' && @.newValue=='${newValue}')].summary");
		}
		else if ("${newValue}" != "") {
			var summaryFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.newValue=='${newValue}')].summary");
		}
		else {
			var summaryFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.oldValue=='${oldValue}')].summary");
		}

		var summaryFromAPI = StringUtil.replace("${summaryFromAPI}", "[", "");

		var summaryFromAPI = StringUtil.replace("${summaryFromAPI}", "]", "");

		var summaryFromAPI = StringUtil.replace("${summaryFromAPI}", "\"", "");

		echo("Expected Action Value: ${summary}");

		echo("Actual Action Value: ${summaryFromAPI}");

		if ("${summary}" != "${summaryFromAPI}") {
			fail("Actual summary value ${summaryFromAPI} does not match expected summary value ${summary}");
		}

		if (("${oldValue}" != "") && ("${newValue}" != "")) {
			var fieldFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.oldValue=='${oldValue}' && @.newValue=='${newValue}')].field");
		}
		else if ("${newValue}" != "") {
			var fieldFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.newValue=='${newValue}')].field");
		}
		else {
			var fieldFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.oldValue=='${oldValue}')].field");
		}

		var fieldFromAPI = StringUtil.replace("${fieldFromAPI}", "[", "");

		var fieldFromAPI = StringUtil.replace("${fieldFromAPI}", "]", "");

		var fieldFromAPI = StringUtil.replace("${fieldFromAPI}", "\"", "");

		echo("Expected Field Value: ${field}");

		echo("Actual Field Value: ${fieldFromAPI}");

		if ("${field}" != "${fieldFromAPI}") {
			fail("Actual field value ${fieldFromAPI} does not match expected field value ${field}");
		}

		if ("${oldValue}" != "") {
			if ("${newValue}" != "") {
				var oldValueFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.oldValue=='${oldValue}' && @.newValue=='${newValue}')].oldValue");
			}
			else {
				var oldValueFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.oldValue=='${oldValue}')].oldValue");
			}

			var oldValueFromAPI = StringUtil.replace("${oldValueFromAPI}", "[", "");

			var oldValueFromAPI = StringUtil.replace("${oldValueFromAPI}", "]", "");

			var oldValueFromAPI = StringUtil.replace("${oldValueFromAPI}", "\"", "");

			echo("Expected Old Value: ${oldValue}");

			echo("Actual Old Value: ${oldValueFromAPI}");

			if ("${oldValue}" != "${oldValueFromAPI}") {
				fail("Actual old value ${oldValueFromAPI} does not match expected old value ${oldValue}");
			}
		}

		if ("${newValue}" != "") {
			if ("${oldValue}" != "") {
				var newValueFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.oldValue=='${oldValue}' && @.newValue=='${newValue}')].newValue");
			}
			else {
				var newValueFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.newValue=='${newValue}')].newValue");
			}

			var newValueFromAPI = StringUtil.replace("${newValueFromAPI}", "[", "");

			var newValueFromAPI = StringUtil.replace("${newValueFromAPI}", "]", "");

			var newValueFromAPI = StringUtil.replace("${newValueFromAPI}", "\"", "");

			echo("Expected New Value: ${newValue}");

			echo("Actual New Value: ${newValueFromAPI}");

			if ("${newValue}" != "${newValueFromAPI}") {
				fail("Actual new value ${newValueFromAPI} does not match expected new value ${newValue}");
			}
		}

		if (("${oldValue}" != "") && ("${newValue}" != "")) {
			var agentNameFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.oldValue=='${oldValue}' && @.newValue=='${newValue}')].agentName");
		}
		else if ("${newValue}" != "") {
			var agentNameFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.newValue=='${newValue}')].agentName");
		}
		else {
			var agentNameFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.oldValue=='${oldValue}')].agentName");
		}

		var agentNameFromAPI = StringUtil.replace("${agentNameFromAPI}", "[", "");

		var agentNameFromAPI = StringUtil.replace("${agentNameFromAPI}", "]", "");

		var agentNameFromAPI = StringUtil.replace("${agentNameFromAPI}", "\"", "");

		echo("Expected Agent: ${agentName}");

		echo("Actual Agent: ${agentNameFromAPI}");

		if ("${agentName}" != "${agentNameFromAPI}") {
			fail("Actual agent ${agentNameFromAPI} does not match expected agent ${agentName}");
		}

		if (("${oldValue}" != "") && ("${newValue}" != "")) {
			var auditEntry = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.oldValue=='${oldValue}' && @.newValue=='${newValue}')]");
		}
		else if ("${newValue}" != "") {
			var auditEntry = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.newValue=='${newValue}')]");
		}
		else {
			var auditEntry = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.oldValue=='${oldValue}')]");
		}

		if (("${agentName}" != "Koroneiki QA Service Producer") && ("${agentName}" != "Auto")) {
			if (("${oldValue}" != "") && ("${newValue}" != "")) {
				var agentUIDFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.oldValue=='${oldValue}' && @.newValue=='${newValue}')].agentUID");
			}
			else if ("${newValue}" != "") {
				var agentUIDFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.newValue=='${newValue}')].agentUID");
			}
			else {
				var agentUIDFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.oldValue=='${oldValue}')].agentUID");
			}

			var agentUIDFromAPI = StringUtil.replace("${agentUIDFromAPI}", "[", "");

			var agentUIDFromAPI = StringUtil.replace("${agentUIDFromAPI}", "]", "");

			var agentUIDFromAPI = StringUtil.replace("${agentUIDFromAPI}", "\"", "");

			echo("Expected Agent UID: a1b2c3d4-e5f6-g7h8-i9j1-0k11l12m13n1");

			echo("Actual Agent UID: ${agentUIDFromAPI}");

			if ("a1b2c3d4-e5f6-g7h8-i9j1-0k11l12m13n1" != "${agentUIDFromAPI}") {
				fail("Actual agent UID ${agentUIDFromAPI} does not match expected agent UID a1b2c3d4-e5f6-g7h8-i9j1-0k11l12m13n1");
			}
		}
	}

	@prose = "I should be able to view an audit entry for the summary ${summary} related to the team with the name ${teamName} field ${field} from the old value ${oldValue} to the new value ${newValue} regarding ${description} executed by ${agentName}"
	macro _viewTeamAuditEntryRegardingAdditionalEntities {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey}/audit-entries?pageSize=100 --tlsv1.2
			--header "API_Token: ${apiToken}"
			-H "accept: application/json"
			-H "Content-Type: application/json"
			''';

		if (("${oldValue}" != "") && ("${newValue}" != "")) {
			var summaryFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.description=='${description}' && @.oldValue=='${oldValue}' && @.newValue=='${newValue}')].summary");
		}
		else if ("${newValue}" != "") {
			var summaryFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.description=='${description}' && @.newValue=='${newValue}')].summary");
		}
		else {
			var summaryFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.description=='${description}' && @.oldValue=='${oldValue}')].summary");
		}

		var summaryFromAPI = StringUtil.replace("${summaryFromAPI}", "[", "");

		var summaryFromAPI = StringUtil.replace("${summaryFromAPI}", "]", "");

		var summaryFromAPI = StringUtil.replace("${summaryFromAPI}", "\"", "");

		echo("Expected Action Value: ${summary}");

		echo("Actual Action Value: ${summaryFromAPI}");

		if ("${summary}" != "${summaryFromAPI}") {
			fail("Actual summary value ${summaryFromAPI} does not match expected summary value ${summary}");
		}

		if (("${oldValue}" != "") && ("${newValue}" != "")) {
			var descriptionFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.description=='${description}' && @.oldValue=='${oldValue}' && @.newValue=='${newValue}')].description");
		}
		else if ("${newValue}" != "") {
			var descriptionFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.description=='${description}' && @.newValue=='${newValue}')].description");
		}
		else {
			var descriptionFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.description=='${description}' && @.oldValue=='${oldValue}')].description");
		}

		var descriptionFromAPI = StringUtil.replace("${descriptionFromAPI}", "[", "");

		var descriptionFromAPI = StringUtil.replace("${descriptionFromAPI}", "]", "");

		var descriptionFromAPI = StringUtil.replace("${descriptionFromAPI}", "\"", "");

		echo("Expected Description Value: ${description}");

		echo("Actual Description Value: ${descriptionFromAPI}");

		if ("${description}" != "${descriptionFromAPI}") {
			fail("Actual description value ${descriptionFromAPI} does not match expected description value ${description}");
		}

		if (("${oldValue}" != "") && ("${newValue}" != "")) {
			var fieldFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.description=='${description}' && @.oldValue=='${oldValue}' && @.newValue=='${newValue}')].field");
		}
		else if ("${newValue}" != "") {
			var fieldFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.description=='${description}' && @.newValue=='${newValue}')].field");
		}
		else {
			var fieldFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.description=='${description}' && @.oldValue=='${oldValue}')].field");
		}

		var fieldFromAPI = StringUtil.replace("${fieldFromAPI}", "[", "");

		var fieldFromAPI = StringUtil.replace("${fieldFromAPI}", "]", "");

		var fieldFromAPI = StringUtil.replace("${fieldFromAPI}", "\"", "");

		echo("Expected Field Value: ${field}");

		echo("Actual Field Value: ${fieldFromAPI}");

		if ("${field}" != "${fieldFromAPI}") {
			fail("Actual field value ${fieldFromAPI} does not match expected field value ${field}");
		}

		if ("${oldValue}" != "") {
			if ("${newValue}" != "") {
				var oldValueFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.description=='${description}' && @.oldValue=='${oldValue}' && @.newValue=='${newValue}')].oldValue");
			}
			else {
				var oldValueFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.description=='${description}' && @.oldValue=='${oldValue}')].oldValue");
			}

			var oldValueFromAPI = StringUtil.replace("${oldValueFromAPI}", "[", "");

			var oldValueFromAPI = StringUtil.replace("${oldValueFromAPI}", "]", "");

			var oldValueFromAPI = StringUtil.replace("${oldValueFromAPI}", "\"", "");

			echo("Expected Old Value: ${oldValue}");

			echo("Actual Old Value: ${oldValueFromAPI}");

			if ("${oldValue}" != "${oldValueFromAPI}") {
				fail("Actual old value ${oldValueFromAPI} does not match expected old value ${oldValue}");
			}
		}

		if ("${newValue}" != "") {
			if ("${oldValue}" != "") {
				var newValueFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.description=='${description}' && @.oldValue=='${oldValue}' && @.newValue=='${newValue}')].newValue");
			}
			else {
				var newValueFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.description=='${description}' && @.newValue=='${newValue}')].newValue");
			}

			var newValueFromAPI = StringUtil.replace("${newValueFromAPI}", "[", "");

			var newValueFromAPI = StringUtil.replace("${newValueFromAPI}", "]", "");

			var newValueFromAPI = StringUtil.replace("${newValueFromAPI}", "\"", "");

			echo("Expected New Value: ${newValue}");

			echo("Actual New Value: ${newValueFromAPI}");

			if ("${newValue}" != "${newValueFromAPI}") {
				fail("Actual new value ${newValueFromAPI} does not match expected new value ${newValue}");
			}
		}

		if (("${oldValue}" != "") && ("${newValue}" != "")) {
			var agentNameFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.description=='${description}' && @.oldValue=='${oldValue}' && @.newValue=='${newValue}')].agentName");
		}
		else if ("${newValue}" != "") {
			var agentNameFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.description=='${description}' && @.newValue=='${newValue}')].agentName");
		}
		else {
			var agentNameFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.description=='${description}' && @.oldValue=='${oldValue}')].agentName");
		}

		var agentNameFromAPI = StringUtil.replace("${agentNameFromAPI}", "[", "");

		var agentNameFromAPI = StringUtil.replace("${agentNameFromAPI}", "]", "");

		var agentNameFromAPI = StringUtil.replace("${agentNameFromAPI}", "\"", "");

		echo("Expected Agent: ${agentName}");

		echo("Actual Agent: ${agentNameFromAPI}");

		if ("${agentName}" != "${agentNameFromAPI}") {
			fail("Actual agent ${agentNameFromAPI} does not match expected agent ${agentName}");
		}

		if (("${oldValue}" != "") && ("${newValue}" != "")) {
			var auditEntry = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.description=='${description}' && @.oldValue=='${oldValue}' && @.newValue=='${newValue}')]");
		}
		else if ("${newValue}" != "") {
			var auditEntry = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.description=='${description}' && @.newValue=='${newValue}')]");
		}
		else {
			var auditEntry = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.description=='${description}' && @.oldValue=='${oldValue}')]");
		}

		if (("${agentName}" != "Koroneiki QA Service Producer") && ("${agentName}" != "Auto")) {
			if (("${oldValue}" != "") && ("${newValue}" != "")) {
				var agentUIDFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.description=='${description}' && @.oldValue=='${oldValue}' && @.newValue=='${newValue}')].agentUID");
			}
			else if ("${newValue}" != "") {
				var agentUIDFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.description=='${description}' && @.newValue=='${newValue}')].agentUID");
			}
			else {
				var agentUIDFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.summary=='${summary}' && @.field=='${field}' && @.description=='${description}' && @.oldValue=='${oldValue}')].agentUID");
			}

			var agentUIDFromAPI = StringUtil.replace("${agentUIDFromAPI}", "[", "");

			var agentUIDFromAPI = StringUtil.replace("${agentUIDFromAPI}", "]", "");

			var agentUIDFromAPI = StringUtil.replace("${agentUIDFromAPI}", "\"", "");

			echo("Expected Agent UID: a1b2c3d4-e5f6-g7h8-i9j1-0k11l12m13n1");

			echo("Actual Agent UID: ${agentUIDFromAPI}");

			if ("a1b2c3d4-e5f6-g7h8-i9j1-0k11l12m13n1" != "${agentUIDFromAPI}") {
				fail("Actual agent UID ${agentUIDFromAPI} does not match expected agent UID a1b2c3d4-e5f6-g7h8-i9j1-0k11l12m13n1");
			}
		}
	}

	@prose = "I should be able to view an audit entry for the summary ${summary} related to the team with the name ${teamName} field ${field} from the old value ${oldValue} to the new value ${newValue} regarding ${description} executed by ${agentName} via the audit entry key"
	macro _viewTeamAuditEntryRegardingAdditionalEntitiesViaAuditEntryKey {
		var auditEntryKey = KoroneikiTeamAPI._getTeamAuditEntryKeyRegardingAdditionalEntities(
			description = "${description}",
			field = "${field}",
			newValue = "${newValue}",
			oldValue = "${oldValue}",
			summary = "${summary}",
			teamName = "${teamName}");

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/audit-entries/${auditEntryKey} --tlsv1.2
			--header "API_Token: ${apiToken}"
			-H "accept: application/json"
			-H "Content-Type: application/json"
			''';

		var summaryFromAPI = JSONCurlUtil.get("${curl}", "$.summary");

		echo("Expected Action Value: ${summary}");

		echo("Actual Action Value: ${summaryFromAPI}");

		if ("${summary}" != "${summaryFromAPI}") {
			fail("Actual summary value ${summaryFromAPI} does not match expected summary value ${summary}");
		}

		var descriptionFromAPI = JSONCurlUtil.get("${curl}", "$.description");

		echo("Expected Description Value: ${description}");

		echo("Actual Description Value: ${descriptionFromAPI}");

		if ("${description}" != "${descriptionFromAPI}") {
			fail("Actual description value ${descriptionFromAPI} does not match expected description value ${description}");
		}

		var fieldFromAPI = JSONCurlUtil.get("${curl}", "$.field");

		echo("Expected Field Value: ${field}");

		echo("Actual Field Value: ${fieldFromAPI}");

		if ("${field}" != "${fieldFromAPI}") {
			fail("Actual field value ${fieldFromAPI} does not match expected field value ${field}");
		}

		if ("${oldValue}" != "") {
			var oldValueFromAPI = JSONCurlUtil.get("${curl}", "$.oldValue");

			echo("Expected Old Value: ${oldValue}");

			echo("Actual Old Value: ${oldValueFromAPI}");

			if ("${oldValue}" != "${oldValueFromAPI}") {
				fail("Actual old value ${oldValueFromAPI} does not match expected old value ${oldValue}");
			}
		}

		if ("${newValue}" != "") {
			var newValueFromAPI = JSONCurlUtil.get("${curl}", "$.newValue");

			echo("Expected New Value: ${newValue}");

			echo("Actual New Value: ${newValueFromAPI}");

			if ("${newValue}" != "${newValueFromAPI}") {
				fail("Actual new value ${newValueFromAPI} does not match expected new value ${newValue}");
			}
		}

		var agentNameFromAPI = JSONCurlUtil.get("${curl}", "$.agentName");

		echo("Expected Agent: ${agentName}");

		echo("Actual Agent: ${agentNameFromAPI}");

		if ("${agentName}" != "${agentNameFromAPI}") {
			fail("Actual agent ${agentNameFromAPI} does not match expected agent ${agentName}");
		}

		var auditEntry = JSONCurlUtil.get("${curl}");

		if (("${agentName}" != "Koroneiki QA Service Producer") && ("${agentName}" != "Auto")) {
			var agentUIDFromAPI = JSONCurlUtil.get("${curl}", "$.agentUID");

			echo("Expected Agent UID: a1b2c3d4-e5f6-g7h8-i9j1-0k11l12m13n1");

			echo("Actual Agent UID: ${agentUIDFromAPI}");

			if ("a1b2c3d4-e5f6-g7h8-i9j1-0k11l12m13n1" != "${agentUIDFromAPI}") {
				fail("Actual agent UID ${agentUIDFromAPI} does not match expected agent UID a1b2c3d4-e5f6-g7h8-i9j1-0k11l12m13n1");
			}
		}
	}

	@prose = "I should be able to view an audit entry for the summary ${summary} related to the team ${teamName} field ${field} from the old value ${oldValue} to the new value ${newValue} executed by ${agentName} via the audit entry key"
	macro _viewTeamAuditEntryViaAuditEntryKey {
		var auditEntryKey = KoroneikiTeamAPI._getTeamAuditEntryKey(
			field = "${field}",
			newValue = "${newValue}",
			oldValue = "${oldValue}",
			summary = "${summary}",
			teamName = "${teamName}");
		var field = StringUtil.lowerCaseFirstLetter("${field}");

		var field = StringUtil.replace("${field}", " ", "");
		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/audit-entries/${auditEntryKey} --tlsv1.2
			--header "API_Token: ${apiToken}"
			-H "accept: application/json"
			-H "Content-Type: application/json"
			''';

		var summaryFromAPI = JSONCurlUtil.get("${curl}", "$.summary");

		echo("Expected Action Value: ${summary}");

		echo("Actual Action Value: ${summaryFromAPI}");

		if ("${summary}" != "${summaryFromAPI}") {
			fail("Actual summary value ${summaryFromAPI} does not match expected summary value ${summary}");
		}

		var fieldFromAPI = JSONCurlUtil.get("${curl}", "$.field");

		echo("Expected Field Value: ${field}");

		echo("Actual Field Value: ${fieldFromAPI}");

		if ("${field}" != "${fieldFromAPI}") {
			fail("Actual field value ${fieldFromAPI} does not match expected field value ${field}");
		}

		if ("${oldValue}" != "") {
			var oldValueFromAPI = JSONCurlUtil.get("${curl}", "$.oldValue");

			echo("Expected Old Value: ${oldValue}");

			echo("Actual Old Value: ${oldValueFromAPI}");

			if ("${oldValue}" != "${oldValueFromAPI}") {
				fail("Actual old value ${oldValueFromAPI} does not match expected old value ${oldValue}");
			}
		}

		if ("${newValue}" != "") {
			var newValueFromAPI = JSONCurlUtil.get("${curl}", "$.newValue");

			echo("Expected New Value: ${newValue}");

			echo("Actual New Value: ${newValueFromAPI}");

			if ("${newValue}" != "${newValueFromAPI}") {
				fail("Actual new value ${newValueFromAPI} does not match expected new value ${newValue}");
			}
		}

		var agentNameFromAPI = JSONCurlUtil.get("${curl}", "$.agentName");

		echo("Expected Agent: ${agentName}");

		echo("Actual Agent: ${agentNameFromAPI}");

		if ("${agentName}" != "${agentNameFromAPI}") {
			fail("Actual agent ${agentNameFromAPI} does not match expected agent ${agentName}");
		}

		var auditEntry = JSONCurlUtil.get("${curl}");

		if (("${agentName}" != "Koroneiki QA Service Producer") && ("${agentName}" != "Auto")) {
			var agentUIDFromAPI = JSONCurlUtil.get("${curl}", "$.agentUID");

			echo("Expected Agent UID: a1b2c3d4-e5f6-g7h8-i9j1-0k11l12m13n1");

			echo("Actual Agent UID: ${agentUIDFromAPI}");

			if ("a1b2c3d4-e5f6-g7h8-i9j1-0k11l12m13n1" != "${agentUIDFromAPI}") {
				fail("Actual agent UID ${agentUIDFromAPI} does not match expected agent UID a1b2c3d4-e5f6-g7h8-i9j1-0k11l12m13n1");
			}
		}
	}

	@prose = "the external link with the entity id ${entityId} should be present on the team ${teamName} with the following values ${table}"
	macro _viewTeamExternalLink {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey}/external-links --tlsv1.2
			--header "API_Token: ${apiToken}"
			-H "accept: application/json"
			-H "Content-Type: application/json"
			''';
		var  rawTable = TableUtil.newTable(${table});

		for (var row : table "${rawTable}") {
			var field = "${row[0]}";
			var value = "${row[1]}";

			if ("${field}" == "Domain") {
				var valueFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.entityId=='${entityId}')].domain");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "[", "");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "]", "");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "\"", "");
			}
			else if ("${field}" == "Entity Id") {
				var valueFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.entityId=='${entityId}')].entityId");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "[", "");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "]", "");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "\"", "");
			}
			else if ("${field}" == "Entity Name") {
				var valueFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.entityId=='${entityId}')].entityName");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "[", "");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "]", "");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "\"", "");
			}
			else if ("${field}" == "URL") {
				var valueFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.entityId=='${entityId}')].url");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "[", "");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "]", "");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "\"", "");

				var valueFromAPI = StringUtil.replace("${valueFromAPI}", "\/", "/");
			}
			else {
				fail("No such external link field ${field}");
			}

			echo("Expected ${field} Value: ${value}");

			echo("Actual ${field} Value: ${valueFromAPI}");

			if (!(contains("${value}", "${valueFromAPI}"))) {
				fail("Actual ${field} value ${valueFromAPI} does not match expected ${field} value ${value}");
			}
		}
	}

	@prose = "the external link with the entity id ${entityId} should be present on the team ${teamName} via the external link key with the following values ${table}"
	macro _viewTeamExternalLinkViaExternalLinkKey {
		var externalLinkKey = KoroneikiTeamAPI._getTeamExternalLinkKey(
			entityId = "${entityId}",
			teamName = "${teamName}");

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/external-links/${externalLinkKey} --tlsv1.2
			--header "API_Token: ${apiToken}"
			-H "accept: application/json"
			-H "Content-Type: application/json"
			''';
		var  rawTable = TableUtil.newTable(${table});

		for (var row : table "${rawTable}") {
			var field = "${row[0]}";
			var value = "${row[1]}";

			if ("${field}" == "Domain") {
				var valueFromAPI = JSONCurlUtil.get("${curl}", "$.domain");
			}
			else if ("${field}" == "Entity Id") {
				var valueFromAPI = JSONCurlUtil.get("${curl}", "$.entityId");
			}
			else if ("${field}" == "Entity Name") {
				var valueFromAPI = JSONCurlUtil.get("${curl}", "$.entityName");
			}
			else if ("${field}" == "URL") {
				var valueFromAPI = JSONCurlUtil.get("${curl}", "$.url");
			}
			else {
				fail("No such external link field ${field}");
			}

			echo("Expected ${field} Value: ${value}");

			echo("Actual ${field} Value: ${valueFromAPI}");

			if (!(contains("${value}", "${valueFromAPI}"))) {
				fail("Actual ${field} value ${valueFromAPI} does not match expected ${field} value ${value}");
			}
		}
	}

	@prose = "I should be able to view a koroneiki team with the name ${teamName} using the external link domain ${domain} and entity name ${entityName} and entity id ${entityId}"
	macro _viewTeamViaExternalLink {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");
		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/teams/by-external-link/${domain}/${entityName}/${entityId} --tlsv1.2
			--header "API_Token: ${apiToken}"
			-H "accept: application/json"
			-H "Content-Type: application/json"
			''';

		var teamNameFromAPI = JSONCurlUtil.get("${curl}", "$.items[?(@.name=='${teamName}')].name");

		var teamNameFromAPI = StringUtil.replace("${teamNameFromAPI}", "[", "");

		var teamNameFromAPI = StringUtil.replace("${teamNameFromAPI}", "]", "");

		var teamNameFromAPI = StringUtil.replace("${teamNameFromAPI}", "\"", "");

		echo("Expected Name Value: ${teamName}");

		echo("Actual Name Value: ${teamNameFromAPI}");

		if (!(contains("${teamName}", "${teamNameFromAPI}"))) {
			fail("Actual team name value ${teamNameFromAPI} does not match expected team name value ${teamName}");
		}
	}

	@prose = "I should be able to view a koroneiki team with the name ${teamName} with the external link nested field partial value ${teamExternalLinkNestedFieldPartialValue}"
	macro _viewTeamWithExternalLink {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey} --tlsv1.2
			--header "API_Token: ${apiToken}"
			-H "accept: application/json"
			-H "Content-Type: application/json"
			''';

		var teamNameFromAPI = JSONCurlUtil.get("${curl}", "$.name");

		echo("Expected Name Value: ${teamName}");

		echo("Actual Name Value: ${teamNameFromAPI}");

		if (!(contains("${teamName}", "${teamNameFromAPI}"))) {
			fail("Actual team name value ${teamNameFromAPI} does not match expected team name value ${teamName}");
		}

		var teamExternalLinkNestedFieldValueFromAPI = JSONCurlUtil.get("${curl}", "$.externalLinks");

		echo("Expected External Link Nested Field Partial Value: ${teamExternalLinkNestedFieldPartialValue}");

		echo("Actual External Link Nested Field Value: ${teamExternalLinkNestedFieldValueFromAPI}");

		if (!(contains("${teamExternalLinkNestedFieldValueFromAPI}", "${teamExternalLinkNestedFieldPartialValue}"))) {
			fail("Actual team external link nested field value ${teamExternalLinkNestedFieldValueFromAPI} does not contain the expected team external link nested field partial value ${teamExternalLinkNestedFieldPartialValue}");
		}
	}

	@prose = "I update a team with the name ${teamName} to have the new name ${teamNameEdit}"
	macro updateTeam {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey} --tlsv1.2
			--header "API_Token: ${apiToken}"
			-H "accept: application/json"
			-H "Content-Type: application/json"
			-d
			{
				"name": "${teamNameEdit}"
			}
			''';

		com.liferay.poshi.runner.util.JSONCurlUtil.put("${curl}");
	}

	@prose = "I attempt to update a team with the name ${teamName} to have the new name ${teamNameEdit}"
	macro updateTeamAttempt {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey} --tlsv1.2
			--header "API_Token: ${apiToken}"
			-H "accept: application/json"
			-H "Content-Type: application/json"
			-d
			{
				"name": "${teamNameEdit}"
			}
			''';

		com.liferay.poshi.runner.util.JSONCurlUtil.put("${curl}");
	}

	@prose = "I update a team with the name ${teamName} to have the new name ${teamNameEdit} executed by ${agentName}"
	macro updateTeamExecutedByAgent {
		var teamKey = KoroneikiTeamAPI._getTeamKeyViaTeamName(teamName = "${teamName}");
		var agentName = StringUtil.replace("${agentName}", " ", "%20");

		var curl = '''
			${koroneikiURL}/o/koroneiki-rest/v1.0/teams/${teamKey}?agentName=${agentName}\&agentUID=a1b2c3d4-e5f6-g7h8-i9j1-0k11l12m13n1 --tlsv1.2
			--header "API_Token: ${apiToken}"
			-H "accept: application/json"
			-H "Content-Type: application/json"
			-d
			{
				"name": "${teamNameEdit}"
			}
			''';

		com.liferay.poshi.runner.util.JSONCurlUtil.put("${curl}");
	}

}