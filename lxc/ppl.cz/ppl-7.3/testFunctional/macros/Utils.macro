definition {

	@summary = "Get directory to upload/import files"
	macro getFilePath {
		var dependenciesDir = "testFunctional/dependencies";

		var dependencyFilePath = "${dependenciesDir}/${fileName}";

		return ${dependencyFilePath};
	}

	@summary = "Launching Home page"
	macro launchUatHomePage {
		var portal = PropsUtil.get("portal.url");

		Default.Open(locator1 = ${portal});

		Default.WaitForPageLoad();
	}

}