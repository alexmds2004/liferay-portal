definition {

	@summary = "Navigate to the selected page, which is opened in a separated tab, via index"
	macro goToPage {
		if (isSet(pageIndex)) {
			Default.Click(
				key_index = ${pageIndex},
				key_sectionName = ${sectionName},
				locator1 = "Navigator#SECTION_NAME");
		}

		if (isSet(pageIndexGroundServices)) {
			Default.Click(
				key_index = ${pageIndexGroundServices},
				locator1 = "Navigator#PAGE_LINK_INDEX");
		}

		if (isSet(linkIndex)) {
			Default.Click(
				key_index = ${linkIndex},
				locator1 = "Navigator#PAGE_LINK_INDEX");
		}
	}

}