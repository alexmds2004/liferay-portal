definition {

	@summary = "Performing login process"
	macro _signIn {
		if (${userRole} == "admin") {
			var userEmailAddress = PropsUtil.get("test.this.cp.test.account.modl.admin.login");
			var userPassword = PropsUtil.getEnvironmentVariable("MODL_CUSTOMER_USERS_PASSWORD");
		}

		Default.WaitForElementPresent(locator1 = "Portal.TextInput#EMAIL_ADDRESS");

		Default.Type(
			locator1 = "Portal.TextInput#EMAIL_ADDRESS",
			value1 = ${userEmailAddress});

		Default.Type(
			locator1 = "Portal.TextInput#PASSWORD",
			value1 = ${userPassword});

		Default.Click(locator1 = "Utils#SIGN_IN_BUTTON");

		Default.WaitForElementNotPresent(locator1 = "Utils#SIGN_IN_BUTTON");

		Default.WaitForVisible(locator1 = "HomePage#SEARCH_BAR");
	}

	@summary = "Log in to the website as admin user"
	macro loginAsAdminUser {
		Login._signIn(userRole = "admin");

		Portal.CustomerPortal.loginOktaToUpdateCP();
	}

}