definition {

	var liferayURL = PropsUtil.get("portal.url");

	macro login {
		Open(locator1 = "${liferayURL}/group/customer");

		Type(
			locator1 = "OSBAdminLogin#EMAIL_ADDRESS_FIELD",
			value1 = ${userEmailAddress});

		Type(
			locator1 = "OSBAdminLogin#PASSWORD_FIELD",
			value1 = ${userPassword});

		Click(locator1 = "OSBAdminLogin#SIGN_IN_BUTTON");
	}

	macro logout {
		Open(locator1 = "${liferayURL}/c/portal/logout");
	}

	macro logoutAndLogin {
		OSBAdminUser.logout();

		OSBAdminUser.login(
			userEmailAddress = ${userEmailAddress},
			userPassword = ${userPassword});
	}

}